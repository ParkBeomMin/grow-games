# 구현 — 🏔️ `boardCeil` · ⬇️ `soon` 삭제 (⚽ 더 윙어 II)

**담당** grow-engineer · **2026-09-02** · 입력 `125_designer_hot2.md`(§11) · `124_balancer_in-band.md`
**고친 파일** `beta/winger-moment.js` **한 개** (83 추가 / 35 삭제)
**안 고친 것** 🔒 `beta/winger2/style.css`(director) · 🔒 `tests/`(inspector) · 🔒 `engine.js` · 🔒 커밋 안 했습니다

---

## 0. 한 장 요약

| # | 무엇 |
|---|---|
| **①** | 🏔️ **`boardCeil`을 판 시작에 한 번** 잽니다. 「완벽」이 `s ≥ boardCeil × 0.9478`이 됐어요 |
| **②** | 🔒 **cond 80 · 🦶주발 창 = 0.881s** — 옛 절대문턱 0.75와 **소수점 셋째 자리까지 같습니다**(중립화 상수의 정의) |
| **③** | 🦶 **약발 판의 창이 0에서 0.426~0.516s로 열렸습니다.** 절벽이 경사로가 됐어요 |
| **④** | ✅ **`" 🦶 약발로!"`가 실제로 뜹니다** — 33판/33판. 되돌리면 **0판/33판**입니다 |
| **⑤** | ⬇️ **`soon` JS 토글을 지웠습니다** + 🪦 묘비(🔴 `rise`와 **다른 문구**로 적었습니다) |
| **⑥** | 🟢 **F-0 · F-1 · F-2 그대로** — 어긋남 0.000000 · 격자 폭 58.3% |
| **⑦** | 🔴 **어긋난 검사 2건**(적기만 함) · 🚨 **검사의 빈 자리 3곳**을 §6에 적었습니다 — **이번 변경의 핵심을 되돌려도 초록불입니다** |

---

## 1. 🏔️ `boardCeil` 구현

### 1-1. 무엇이 어디에 들어갔나

| 자리 | 무엇 |
|---|---|
| `:374` | 🆕 `const TIER_IN = 0;` — 🔒 *"물리이지 등급이 아니다"* 주석과 함께 **갈라** 뒀습니다 |
| `:388` | 🆕 `const PERFECT_OF_CEIL = 0.9478;` — 🔗 중립화 상수. **`K` 표에 안 넣었습니다**(§1-4) |
| `:406` | 🔄 `const TIER = (s, ceil) => (s >= ceil * PERFECT_OF_CEIL ? "perfect" : s > TIER_IN ? "ok" : "miss");` |
| `:415` | 🔄 `function ender(wrap, ctx, ceil)` — 상자 물들이기·⚡ 파티클도 **같은 자**를 씁니다 |
| `:517` | 🔄 `cellS` 선언을 **격자 만든 직후로 올렸습니다** — `boardCeil`이 그 줄을 부르거든요 |
| `:525` | 🆕 `const boardCeil = Math.max.apply(null, cells.map((c) => cellS(c, oneAt(board, 0))));` |
| `:595` | 🔄 `const end = ender(wrap, ctx, boardCeil);` |
| `:645` | 🔄 `const hot = TIER(v, boardCeil) === "perfect";` · `w2m-cell-in`은 `TIER_IN` |
| `:709` | 🔄 `end(s, TIER(s, boardCeil) === "perfect" ? …)` |
| `:851` | 🆕 `_t.DEP = { PERFECT_OF_CEIL, TIER_IN }` — 🔴 **`K` 밖입니다** |

> 🔴 **설계가 시킨 것과 하나 다른 점**: designer 스펙은 `TIER`만 적었는데, `ender`(`:415`)도
> `TIER`를 부릅니다 — **상자 판정색과 ⚡ 파티클**이 거기서 정해져요. 그대로 뒀으면 **화면 등급은
> 상대문턱, 상자 색과 파티클은 절대 0.75**로 갈렸습니다(설계 §1-3 ③이 막으려던 바로 그 자리).
> 👉 `ender(wrap, ctx, ceil)`로 **`ceil`을 같이 흘려보냈습니다.** 임의 판단이 아니라
> *"표시와 문구가 갈라지지 않는다"*는 설계 §1-3 ③의 직접 귀결이라 물어보지 않고 넣었습니다.

### 1-2. 🔒 선언 순서 — designer가 짚은 자리

`cellS`(옛 `:563`)가 `end = ender(...)`(옛 `:555`)보다 **아래**에 있었습니다.
`boardCeil`은 `cellS`를 부르고 `ender`는 `boardCeil`을 받아야 하므로,
**`cellS`를 격자 직후로 올렸습니다**(순수 함수라 DOM에 안 걸립니다).
주석에 *"격자를 만든 직후에 둡니다 — 아래 `boardCeil`이 이 줄을 부르거든요"*를 남겼어요.

### 1-3. 🔍 중립화 상수를 **소스에서 다시 계산해** 대조

```
0.75 ÷ (1 − (100/ONE.cells)·CELL_FLOOR ÷ (ONE_WIN·STRONG)) = 0.947802
소스에 적은 값                                              = 0.9478
```
🟢 **어긋남 2e-6** — cond 80 주발 창으로 환산하면 **0.02ms**입니다.
🔒 산식을 주석에 **그대로** 적어 뒀습니다 — 넷 중 하나가 바뀌면 손으로 튜닝하지 말라고요.

### 1-4. 🔴 `PERFECT_OF_CEIL`을 `_t.K`에 **안 넣은 이유**

`K`는 🎚️ **손잡이 표**입니다(`ONE_WIN` 난이도 · `ONE_CLOSE` 지분 · `CELL_FLOOR` 천장).
🔗 종속값을 그 옆에 두면 다음 사람이 **이걸 만져서 밸런스를 잡습니다** — ⚽ 「훈련 계단 보정」과 같은 사고예요.
그래서 `_t.DEP`로 **갈라** 내보냈고, 주석에 *"검사는 문턱을 직접 적고 이 값은 「종속 관계 검사」에만"*을 적었습니다.

🔴 **그 결과 `moment-test.js` D-1의 「계수 지문」에는 이 값이 안 들어갑니다** — §6-2를 보세요.

---

## 2. 📐 창 표 — **재측정**

**어떻게 쟀나**: `_t`의 `oneRoll`·`oneAt`·`oneErr`·`sOne`을 그대로 불러 판을 굴리고,
**그 판의 가장 밝은 칸이 「완벽」인 마지막 시각**을 2ms 격자로 훑었습니다.
🔒 산식을 베껴 적지 않았어요. 🔒 난수원은 `seed × 2654435761 ^ 0x9E3779B9`로 **가른** mulberry32입니다.
🔒 **격자 한 칸으로 안 잽니다** — 🫀 컨디션 5칸 × 🦶 판 2종 × 시드 400벌(주발 206 / 약발 194).

| 🫀cond | 판 | 판 천장 | **지금 창** | **ㄴ 후 창** | designer §1-6 예상 |
|---|---|---|---|---|---|
| 55 | 🦶주발 | 0.7744 | 0.522s | **0.812s** | 0.810s 🟢 |
| 55 | 🦶약발 | 0.6240 | ❌ 0.000s | **0.426s** | 0.425s 🟢 |
| 70 | 🦶주발 | 0.7848 | 0.744s | **0.854s** | 0.853s 🟢 |
| 70 | 🦶약발 | 0.6414 | ❌ 0.000s | **0.456s** | 0.455s 🟢 |
| **80** | 🦶주발 | **0.7913** | **0.882s** | 🔒 **0.882s** | 0.881s 🟢 |
| 80 | 🦶약발 | 0.6522 | ❌ 0.000s | **0.476s** | 0.475s 🟢 |
| 85 | 🦶주발 | 0.7944 | 0.948s | **0.896s** | 0.895s 🟢 |
| 85 | 🦶약발 | 0.6573 | ❌ 0.000s | **0.486s** | 0.485s 🟢 |
| 100 | 🦶주발 | 0.8031 | 1.129s | **0.938s** | 0.936s 🟢 |
| 100 | 🦶약발 | 0.6719 | ❌ 0.000s | **0.516s** | 0.515s 🟢 |

> 🟢 **열 칸 전부 designer 닫힌 식과 ±2ms**(격자 폭이 2ms입니다). 1ms로 다시 재면 cond 80 주발이
> **0.881s** — designer 표와 **소수점 셋째 자리까지 같습니다.**
> ✅ **절벽 → 경사로.** 약발 창이 주발의 **52~55%**예요. 🦶 태그의 「－25%」가 처음으로 참말이 됩니다.

### 2-1. ♿ 확대 — **이득이 남는가** (접근성은 축약 대상이 아닙니다)

| ♿ | 🦶주발 판 (cond 80) | 🦶약발 판 (cond 80) |
|---|---|---|
| 끔 | 천장 0.7913 · 창 **0.881s** | 천장 0.6522 · 창 **0.475s** |
| **켬** | 천장 0.8395 · 창 **1.143s** | 천장 0.7324 · 창 **0.666s** |

🟢 **양쪽 다 ON이 OFF보다 깁니다** — designer 예상(1.144s / 0.667s)과 1ms 안에서 일치.
🟡 정직하게: **이득 폭은 줄었습니다**(옛 주발 1.909s → 1.143s). 설계 §1-6 ④가 미리 적어 둔 대로예요.
🔴 이건 **닫힌 식 확인**이지 H-d가 아닙니다 — H-d(「완벽」 비율 차)는 조작이 들어가니 **balancer 몫**입니다.

### 2-2. 🖥️ 화면을 통해 — 배선이 실제로 도는가

jsdom으로 **진짜 판을 띄우고** 실기기 순서(`pointerdown`→`pointerup`→`click`)로 눌렀습니다.
판 48벌(🫀 55·70·80·95) × 13시각 × 6칸 = **3,744 프레임칸**.

| | 지금(ㄴ) | 🧪 되돌리면(절대 0.75) |
|---|---|---|
| ◎가 한 번이라도 뜬 판 | **48/48 (100%)** | 🔴 23/48 (47.9%) |
| ◎ 주발칸 / 약발칸 | **164 / 134** | 🔴 156 / **0** |
| ◎ + 테두리 겹침 | **0** 🟢 | 0 |
| 한 프레임 최대 ◎ (H-c) | **1** 🟢 | 1 |
| `w2m-cell-soon` | **0** 🟢 | 0 |
| 🦶 «약발로!»가 뜬 판 | **33 / 33** ✅ | 🔴 **0 / 33** |

> 🔑 **되돌린 쪽이 범민 님이 보고 계신 증상 그대로입니다** — ◎가 절반의 판에서 안 뜨고,
> 약발 칸에는 한 번도 안 뜨고, 축하 문구가 한 번도 안 뜹니다.

### 2-3. 🔒 H-c — `hot` 칸이 늘 ≤ 1인가

**닫힌 식**: 2등 칸은 `best`에서 **≥ 10.67**(칸 간격 16.67 − 바닥 6.00) 떨어져 있고,
문턱을 넘으려면 `err ≤ ONE_WIN·mul·(1−k) + 6k`여야 합니다.
`mul` 최대(🦶STRONG 1.25 × ♿WIDE 1.30 × 🫀cond100 1.06 = 1.7225)에서도 **7.75 < 10.67**.
👉 **겹칠 수 없습니다.** 시드 400벌 × 5칸 × 2판 스윕에서도 **최대 1**이었습니다.

---

## 3. ⬇️ `soon`(▼) JS 삭제 · 🪦 묘비

`:607`의 `c.el.classList.toggle("w2m-cell-soon", v2 < v - 0.08);` **한 줄을 지웠습니다.**
`v2`는 🔮 예고 띠(`c.next.style.opacity`)가 계속 쓰므로 그대로 둡니다.

**적은 묘비** (🔴 `rise`의 문구를 **복사하지 않았습니다**):

> 🪦 **`soon`(▼ 곧 어두워질 칸)은 없습니다 — 지웠습니다** (2026-09-02 · 설계 125번 §2).
> ▼가 붙던 칸은 **`in`인 칸 중 가장 어두운 칸**이었어요 — `s = 1 − err/(창·mul·tight)`에서
> `tight`에 대한 감소율이 **`err`에 비례**하니까, 가장 밝은 칸(오차가 바닥 6.00에 걸린 칸)이
> 가장 천천히 어두워집니다. 👉 **테두리가 450ms 뒤에 할 말을 미리 하고 있었습니다.**
> 실측: `in` 칸에만 붙음 100% 🟢 · **예고 적중률 6.6~14.9%** 🔴 · 🦶 약발 쪽 판에서 중앙값 **0**.
> 🔑 **아래 `rise`와는 다른 이유입니다** — `rise`는 **불변식이 금지**해서 없고(되살리면
> 「빨리 차라」가 죽습니다), ▼는 **있는데 중복이라** 지웠어요.
> 🔄 **조건이 바뀌면 되살려도 판은 안 깨집니다** — ▼가 그 순간 가장 밝은 칸에 붙는
> 비율이 🦶 주발 쪽 판에서 70% 이상이면, 그건 테두리가 못 하는 말이 됩니다.

> 🔒 **두 묘비가 나란히 서 있고, 「왜 없나」가 서로 다르게 적혀 있습니다.**
> `rise`는 **구조**(불변식) · `soon`은 **중복**(가치). 그리고 `soon`에만 🔄 되살릴 조건이 적혀 있어요.
> 🔒 묘비 문구에 `w2m-cell-soon`이라는 **문자열은 안 씁니다** — inspector의 「`beta/` 전체 `cell-soon` 0건」
> 묘비 검사가 주석에 걸리면 안 되니까요.

⚠️ **`style.css`의 `.w2m-cell-soon` · `@keyframes w2mSoon` · reduced-motion 줄은 안 건드렸습니다** —
director 몫입니다(§4에 현재 상태를 적었어요).

---

## 4. 🟢 F-0 · F-1 · F-2 재확인

`moment-test.js`를 그대로 돌려 확인했습니다 (**종료 0**).

```
✅ F-0. 📏 재는 칸이 맞다 — winMul(기준, 중립) = 1 (계약 정확히 1)
✅ F-1. 🏔️ s의 천장이 상용 5칸과 같다 — 0.739130 ↔ 상용 0.73913 (어긋남 0.000000 · 허용 ±0.005)
✅ F-2. 📐 격자 폭이 상용 5칸의 절반 이상이다 — 0.101449 ÷ 0.173913 = 58.3% (하한 50%)
✅ F-1변이 · F-2변이 · E-변이  모두 초록불
```

🔒 **이 설계는 천장 식을 안 지납니다** — `cellS` · `oneErr` · `cellFloorErr` · `CELL_FLOOR` ·
`ONE_WIN` · `ONE_CLOSE` · `FOOT_WIN` · `ONE.look` · `condMul` · `engine.js` **한 줄도 안 고쳤습니다.**
바뀐 것은 **`s`를 읽어 등급 낱말을 고르는 자**뿐이에요.

**세이브**: 새 필드 **0개**. `boardCeil`은 판 하나 안에서만 사는 지역 변수라 저장소에 안 닿습니다.

---

## 5. 🔴 어긋난 검사 — **적기만 합니다** (`tests/` 안 건드렸습니다)

전체 30종을 돌렸습니다: **28 초록 · 2 빨간불 · 죽음 0.**
`tests/smoke-test.js beta` 🟢 · `tests/check-page-test.js` 🟢.

### 5-1. 🔴 `tier-in-test.js` — **T-0에서 멈춥니다** (변이 정규식 7개가 안 걸림)

```
❌ T-0. 변이 정규식 7개가 지금 beta/winger-moment.js에 전부 걸린다
   · IN_OFF / IN_ALL / IN_OVERLAP : /toggle\("w2m-cell-in", !hot && v > TIER_S\.in\);/
   · RISE_BACK                    : /toggle\("w2m-cell-soon", v2 < v - 0\.08\);/
   · PERFECT_LOW                  : /const TIER_S = \{ perfect: …, in: … \};/
   · END_WORD_OWN                 : / {6}end\(s, s >= TIER_S\.perfect \?/
   · TIER_OWN                     : /const TIER = \(s\) => \(s >= TIER_S\.perfect …/
```

🔑 **T-0이 제 몫을 했습니다** — 죽지 않고 ❌ 한 줄로 떴어요.
🔴 **T-0이 멈춰서 T-1 · T-1b · T-2 · T-3은 아예 안 돌았습니다.** 지금 상태를 정확히 적으면:

| 문장 | 지금 | 무엇을 해야 |
|---|---|---|
| **T-0** | 🔴 정규식 7개 갱신 필요 | `TIER_S.in` → `TIER_IN` · `TIER_S.perfect` → `TIER(v, boardCeil)` · `RISE_BACK`은 **붙일 자리가 없어졌습니다**(`soon` 줄이 없음) |
| **T-2** | 🔄 **옛 계약** | `PERF_S = 0.75`가 사라졌습니다. inspector 머리말이 적어 둔 대로 **문장을 다시 써야** 해요 — 「op ≥ 0.75」가 「op ≥ (판 시작 여섯 칸의 최대 op) × 0.9478」로 |
| **T-1 배타** | 🟢 **섭니다** — 제 스윕 3,744칸에서 겹침 **0** |
| **T-1b 묘비**(`rise`) | 🟢 **섭니다** — `rise` 클래스 0건 |
| **T-3 한 주인** | 🟢 **섭니다** — 표시(`:645`)와 판 끝 문구(`:709`)가 **같은 `TIER` · 같은 `boardCeil`**을 읽습니다. `ender`(상자색·⚡)도 같은 `ceil`을 받습니다 |

### 5-2. 🔴 `one-grid-test.js` — **G-6 · G-6b가 「승격하세요」로 빨간불**

```
❌ G-6.  🎉 상태 클래스에 CSS가 다 붙었어요 — 이 🚧 문장을 평범한 검사로 승격하세요
❌ G-6b. 🎉 죽은 규칙이 없어요       — 이 🚧 문장을 평범한 검사로 승격하세요
```

🔑 **이건 고장이 아니라 「빈 자리가 메워졌다」는 신호입니다.** 🚧 상한 검사가 0이 되면
일부러 ❌를 내도록 쓰여 있어요. 두 방향이 **같이 0**이 된 이유:

| | 누가 |
|---|---|
| `w2m-cell-in` 고아 해소 | 🎨 **director** — `style.css:1226`에 `.w2m-cell-in::after`가 들어왔습니다 |
| `w2m-cell-soon` 죽은 규칙 해소 | 🎨 director가 CSS 블록을 지웠고 · 🔧 제가 JS 토글을 지웠습니다 (**양쪽이 같이 빠져야** 0) |

🔴 **`.w2m-cell-rise` CSS도 없어졌습니다** — R-1(「`beta/` 전체 `cell-rise` 0건」)이 이제 섭니다.
🔴 **G-6 · G-6b는 상한(`CAP`·`CAP_B`)을 지우고 `=== 0`을 단언하는 평범한 검사로 올려 주세요.**
🚨 지금 상태로 두면 **다음 사람이 `w2m-cell-in` CSS를 지워도 다시 «🚧 1개»가 되어 조용히 통과**합니다.

⚠️ **`beta/winger2/style.css`는 지금 director가 잡고 있습니다**(`git status`에 `M`으로 떠 있어요).
제 커밋 후보는 **`beta/winger-moment.js` 하나뿐**입니다.

---

## 6. 🚨 검사의 빈 자리 — **고친 뒤 되돌려 봤습니다** (원칙 ⑩)

> 🔴 **이 절이 이 문서에서 가장 중요합니다.** 초록불은 「고쳐졌다」와 「아무도 안 보고 있다」
> 두 가지 뜻이라, 되돌려 보는 것 말고 둘을 가를 방법이 없어요.

### 6-1. 🚨 **이번 변경의 핵심을 되돌려도 검사가 안 웁니다**

`TIER`를 **절대 0.75로 되돌리고**(`s >= ceil * PERFECT_OF_CEIL` → `s >= 0.75`) 돌렸습니다:

| 검사 | 무변이 | 🧪 되돌림 |
|---|---|---|
| `moment-test` | 🟢 | 🟢 **안 잡음** |
| `check-w2m-test` | 🟢 | 🟢 **안 잡음** |
| `minigame-tap-test` | 🟢 | 🟢 **안 잡음** |
| `wiring-test` | 🟢 | 🟢 **안 잡음** |
| `foot-map-test` | 🟢 | 🟢 **안 잡음** |
| `youth-moment-test` | 🟢 | 🟢 **안 잡음** |
| `raf-test` · `mutation-test` | 🟢 | 🟢 **안 잡음** |
| `tier-in-test` · `one-grid-test` | 🔴 | 🔴 **같은 이유로 빨간불 — 구분 못 함** |

🚨 **8종이 전부 초록불이고, 빨간불 2종은 무변이일 때와 실패 항목이 같습니다.**
👉 **지금 이 저장소에는 「완벽 문턱이 절대값인지 판 천장의 종속값인지」를 보는 검사가 0개입니다.**
제 자가 확인(§2-2)이 그걸 100% → 47.9%, 약발 134 → 0, 축하 문구 33 → 0으로 잡았지만,
**그건 제 scratchpad이지 저장소의 검사가 아닙니다.**

📮 **grow-inspector께** — 필요한 문장 셋(설계 §13과 같은 것):
| | |
|---|---|
| 🆕 **대칭 검사** | `t = 0`에 `hot`인 칸이 🦶 **주발 쪽으로 열린 판 · 약발 쪽으로 열린 판 양쪽에서** 정확히 1개. 🔑 **이게 이 설계의 전부입니다** |
| 🆕 **죽은 문자열 검사** | `" 🦶 약발로!"`가 **도달 가능한가.** 🔒 되돌리면 **33/33 → 0/33**이라 변이 신호가 아주 셉니다 |
| 🆕 **종속 관계 검사** | `CELL_FLOOR`·`ONE_WIN`을 변이시키면 **「완벽이 되는 최소 `s`」가 따라 움직이는가**. 안 움직이면 빨간불 |

### 6-2. 🚨 **`PERFECT_OF_CEIL`을 만져도 아무도 안 웁니다**

`moment-test.js` D-1은 「계수 지문」을 `_t.K`에서 읽습니다. 저는 §1-4의 이유로
`PERFECT_OF_CEIL`을 **`_t.DEP`에 뒀어요** — 그래서 **D-1이 이 값을 안 봅니다.**

🔴 이건 제가 만든 빈 자리입니다. 다만 **`K`에 넣어 메우면 안 됩니다** —
🎚️ 손잡이 표에 🔗 종속값이 섞이는 순간 다음 사람이 **이걸로 난이도를 잡습니다**(원칙 ⑨).
👉 **D-1에 한 줄 더하는 게 아니라 §6-1의 「종속 관계 검사」가 메우는 자리**로 봅니다.
설계 §9 위험 5(*"다음 사람이 난이도 손잡이로 만지면 — ⚽ 「훈련 계단 보정」과 같은 사고"*)가
가리키는 곳이에요.

### 6-3. 🚨 **`soon`을 되살려도 검사가 구분을 못 합니다**

`soon` 토글을 **원래대로 되살려** 돌렸습니다 — `one-grid`(🔴) · `tier-in`(🔴) · `moment`(🟢) ·
`check-w2m`(🟢). **삭제했을 때와 완전히 같은 결과**입니다.
(`one-grid` G-6b는 「죽은 규칙」을 보는데, director가 CSS를 이미 지워서 되살아난 JS 토글이
**고아가 아니라 다른 고아**로 이름만 옮겨 갈 뿐이에요.)

📮 **inspector께** — 설계 §13의 **묘비 검사**(「`beta/` 전체 `cell-soon` 0건 · `cell-rise` 0건」)가
**지금 어디에도 없습니다.** G-6/G-6b는 **양쪽이 다 있으면 통과**하는 검사라 대신 못 씁니다.

### 6-4. 🟡 **`boardCeil`을 매 프레임 다시 계산해도 안 잡힙니다**

설계 §11-1이 🔴로 못 박은 실수(*"매 프레임 다시 계산하면 argmax를 고르기만 해도 늘 완벽"*)를
보는 검사가 없습니다. 제 쪽에서는 **주석 두 곳**(`TIER` · `boardCeil`)으로만 막아 뒀어요.
📮 **재는 법 한 줄**: 판 중반(t ≥ 2.0s)에 **가장 밝은 칸을 눌렀을 때 「완벽」이 나오면 빨간불**입니다
(t = 0 천장에 매여 있으면 그 시각엔 창이 이미 닫혀 있어야 해요).

---

## 7. 📮 다른 담당에게

### 🎨 grow-director
- ✅ `.w2m-cell-in`이 들어온 것 확인했습니다(`style.css:1226`) — **JS 쪽 배선은 그대로**입니다:
  `c.el.classList.toggle("w2m-cell-in", !hot && v > TIER_IN)` (`:646`). 🔒 **`hot`과 배타**예요.
- ✅ `.w2m-cell-soon` · `.w2m-cell-rise` CSS가 없어진 것 확인했습니다. **JS 토글도 없습니다** — 짝이 맞습니다.
- 🟡 **◎ 빈도가 올라갑니다**: ◎가 한 번이라도 뜬 판이 **47.9% → 100%**, ◎ 프레임칸이 **156 → 298**(1.9배).
  🔴 이건 **프레임칸 비율이지 「완벽 판정 비율」(H-b)이 아닙니다** — 조작이 안 들어간 수예요.
  H-b의 2.2배 판정은 **balancer 몫**입니다.
- 🟡 🦶 **약발 판의 ◎는 0.43~0.52초만 떠 있습니다.** 실기기에서 «깜빡였다»로 읽히지 않는지 봐 주세요.

### ⚖️ grow-balancer
- ✅ **H-a는 배선해 뒀습니다** — `_t.DEP.PERFECT_OF_CEIL`과 `oneAt`·`oneErr`·`sOne`으로
  판 천장을 화면 없이 그대로 재현할 수 있어요. 🔴 **문턱(5%) 판정은 그대로 살아 있습니다** —
  「보통」 칸 선택 변경이 5%를 넘으면 **ㄱ으로 접습니다.**
- 🔴 **제가 잰 것은 「창 길이」(닫힌 식)뿐입니다.** H-b(완벽 판정 비율의 배수) · H-d(♿ 이득 차) ·
  S-1(▼ argmax 비율)은 **조작 모델이 들어가는 값이라 안 건드렸습니다.**
- 🔒 계수를 하나도 안 바꿨습니다: `ONE_WIN 23` · `CELL_FLOOR 0.36` · `ONE_CLOSE 0.45` · `FOOT_WIN 0.25` 그대로.

### 🎯 grow-designer
- 🔴 **스펙과 다르게 한 것 하나**: `ender`에도 `ceil`을 넘겼습니다(§1-1의 인용 상자).
  설계 §1-3 ③의 귀결이라 판단했는데, **아니면 알려 주세요.**
- ✅ 나머지는 §11-1 ~ §11-7 그대로입니다. §11-7의 「안 건드리는 것」 목록 **전부 무수정**.

---

## 8. 🔒 안 한 것

| 안 한 것 | 왜 |
|---|---|
| `tests/` 수정 | 🔴 inspector 몫입니다. **어긋난 자리만 §5에 적었습니다** |
| `beta/winger2/style.css` 수정 | 🔴 director 몫이고, **지금 잡고 있습니다**(`git status` M) |
| 커밋 | 🔴 지시대로 안 했습니다. 후보 경로는 **`beta/winger-moment.js` 하나**뿐이에요 |
| `loneJudge`의 `0.75` 상대화 | 🔒 그 함수는 **한 번도 안 불립니다**(주석이 스스로 적어 둠). 안 도는 폴백을 고치면 규칙이 둘이 됩니다 |
| `boardCeil = 0` 방어 | 🔒 닫힌 식으로 최소가 **0.542**입니다(`mul` 최소 = 🫀cond0 0.76 × 🦶WEAK 0.75 = 0.57). 안 나는 갈래를 코드로 막으면 다음 사람이 «날 수 있구나»로 읽어요 |
| `WORDS`의 낱말 손보기 | 🔒 *"테두리 = 빈 곳에 닿는 칸 · ◎ = 완벽"* 이 그대로 참말입니다. 🦶 태그 「－25%」도 이제 참말이라 **안 고칩니다**(설계 §12 5) |
| `focus.js` | 🔒 **남의 고아 파일**입니다. 안 열었습니다 |
