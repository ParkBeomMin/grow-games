# 실측 — ⚽ 더 윙어 II · 🏔️ `CELL_FLOOR` 확정 · 🧒 초2·초3이 필요한가

2026-09-02 · grow-balancer · 커밋 `dfeea4c` 기준
입력: `116_engineer_one-mini.md`(§7-1) · `117_designer_school-arc.md`(§9-1·§11) ·
`beta/winger-moment.js` · `beta/winger2/{engine,prospect,career,town,game}.js`(읽기만)

> 🔴 **`beta/`를 한 줄도 안 건드렸습니다.** `CELL_FLOOR`도 제가 안 씁니다 — 재고 적기만 했어요.
> 🔴 **커밋 안 했습니다.** ⚠️ inspector가 `tests/`를 돌고 있어 `scratchpad/bal-*`만 썼습니다.

---

## 0. 판정 요약

| | |
|---|---|
| **①** | ✅ **`CELL_FLOOR = 0.36`은 쓸 수 있습니다 — 🚨 단, `ONE_WIN = 23`과 「한 벌」입니다.** 둘 중 하나만 넣으면 무너져요 (§1-6) |
| **②** | 🔑 **engineer가 짚은 「능숙 초과분」은 진짜입니다.** 조작자 모델 **셋 전부**에서 **+0.064 ~ +0.067** — 모델에 안 흔들립니다 (§1-4) |
| **③** | ✅ **0.36 + 23이 그 초과분을 절반으로 줄입니다** — 셋 전부에서 **+0.065 → +0.026 ~ +0.030.** engineer의 *"+0.050 → ~+0.026"* 예측 **확인** (§1-5) |
| **④** | 🚨 **`CELL_FLOOR 0.36` 단독(`ONE_WIN` 22 유지)은 못 씁니다** — 「보통」이 **−0.0195 / −0.0249**로 밀립니다 (계약 ≤0.005의 **4~5배 밖**). 게다가 `s` 천장이 **상용 5칸 밑**으로 내려가요 (§1-6) |
| **⑤** | 🔴 **engineer 하네스와 제 첫 하네스가 「보통」에서 0.10 어긋났고, 원인을 찾았습니다** — **조작자가 「밝은 칸을 고르는가」와 「자리를 누르는가」**입니다. 둘은 **6칸에 대해 정반대 편향**을 가져요. 셋 다 재서 답을 냈습니다 (§1-3) |
| **⑥** | 📐 **모델이 필요 없는 자를 하나 세웠습니다** — `s 천장 = 1 − 칸폭·FLOOR ÷ WIN`. **`0.36 + 23`이 상용 5칸의 0.7391로 정확히 되돌아옵니다** (§1-2) |
| **⑦** | 🟡 **값을 치릅니다 — 격자 폭(조준이 성적을 가르는 폭)이 5칸의 58%로 줄어요.** 대안 `0.33 + 22.5`(72% · 초과분 +0.046)를 나란히 놓았습니다. 🔴 **무엇을 포기할지는 범민 님 판단입니다** (§1-7) |
| **⑧** | 🔴 **🧒 초2·초3 — 짓지 마세요.** 축의 폭은 「순수 3택」이 전부이고 **그건 초1 하나로 다 나옵니다.** 초3의 한계 효과는 **12칸 중 8칸에서 잡음에 묻힘**, 잡히는 칸에서도 **최대 1.1%** (§2-0·§2-3) |
| **⑨** | 🔑 **결정타 — `spread()`가 `indexOf`로 봐서 `⚽×1`과 `⚽×3`이 「완전히 같은 stats」입니다** (4,000/4,000 일치). 🔴 **같은 걸 또 고르면 값이 정확히 0**이고, 섞으면 **순수보다 나빠집니다** (§2-2·§2-4) |
| **⑩** | 🟢 **초1은 남깁니다** — 순수 3택이 **포지션별 최선을 하나씩** 갈라 주고 대조 대비 **4.4σ ~ 16.1σ**로 확실히 삽니다. 🔑 **「차이가 없다」가 아니라 「그 차이가 한 번에 다 들어 있다」**예요 (§2-6) |
| **⑪** | 🔴 **designer 계산 넷 중 하나가 뒤집혔습니다** — ♾️ 리롤이 **벌어지는 게 아니라 좁아집니다**(1회 +5.62 → 50회 +3.86). designer 자신의 뒤집기 조건(+2)에 접근 중이에요 (§2-2 A-3) |
| **⑫** | 🔴 **`CHILD_FOCUS` 축이 반쪽입니다** — 「뾰족한 끝」이 **대조(선택 없음)와 SD 0.09 차이.** 사람이 고를 수 있는 건 **「평평하게 만들기」 한 방향**뿐 (§2-2 A-4 · §2-9 ①) |

---

# 1. 🏔️ `CELL_FLOOR` — 시드 3벌 · 조작자 모델 3벌

## 1-1. 무엇을 쟀나

| | |
|---|---|
| **A (기준선)** | 🔒 **커밋 `082018b`의 `beta/winger-moment.js`를 git에서 그대로 꺼냈습니다** — 옛 5칸 · 고정 키퍼 · `ONE_WIN 23` · `CELL_FLOOR 0.30`. **상용에 나간 판**입니다 |
| **B (재는 것)** | 지금의 `beta/winger-moment.js` — 6칸 · 미끄러지는 키퍼. `CELL_FLOOR`·`ONE_WIN`만 정규식 변이 |
| **산식** | 🔒 **베껴 적지 않았습니다.** 두 소스를 통째로 `new Function`에 실어 `_t`(`oneRoll`·`oneAt`·`oneCellX`·`oneErr`·`sOne`·`winMul`·`K`)를 **그 파일에서** 꺼내 씁니다. `cellMul` 한 줄도 정규식으로 뜯어와 `new Function`으로 되살렸어요 |
| **표본** | 조합마다 **20,000판 × 시드 3벌** (91001 · 730331 · 44207) × **조작자 모델 3벌** |
| **난수원** | 🔒 **셋을 가릅니다** — 무대 `seed ^ 0x85EBCA6B` · 조작자 `^ 0x9E3779B9` · 상황(🫀 컨디션 60~94 · 🦶 왼발 ¼) `^ 0xC2B2AE35` |
| **짝지음** | 조작자 draw와 상황은 A·B가 **같은 배열**을 씁니다. 무대는 **판마다 독립 시드**라 소비량 차이(A 1~2개 · B 3개)에 안 흔들려요 |

### 🔬 하네스 검산 — 배선이 진짜 걸렸는지 먼저 봤습니다 (`bal-floor/check.js`)

| | 실측 |
|---|---|
| 🫀 `condMul`이 진짜 걸리나 | cond 60 → **0.9400** · 94 → **1.0420** — 엔진 직접 호출과 **일치** ✅ (안 걸리면 1로 떨어져 컨디션이 통째로 죽습니다) |
| 🦶 강/약 비 | **1.6667** ✅ |
| `cellMul` 뜯어오기 | 오른발 6칸 → `0.750 0.750 0.750 1.250 1.250 1.250` — **3 : 3 정확히** ✅ |
| 변이가 진짜 먹나 | `CELL_FLOOR` 0 / 0.30 / 0.36 → 바닥 err **0.000 / 5.000 / 6.000** ✅ |
| 안 걸리는 변이 | **던집니다** ✅ (무변이로 조용히 통과하지 않아요) |
| 무대 불변식 | 빈 곳이 넓어진 순간 **0/420,000** · 🧤가 가운데를 넘은 순간 **0/420,000** ✅ |
| 🔬 **감도** — 하네스가 정말 `CELL_FLOOR`를 보고 있나 (`mono.js`) | 「능숙」 짝지은 차이가 floor **0.00 → +0.229 · 0.15 → +0.194 · 0.30 → +0.077 · 0.36 → +0.030 · 0.45 → −0.041**. 🔑 **단조로 크게 움직입니다** — 방어가 겹쳐 값이 안 흔들리는 상태가 아니에요 ✅ |

## 1-2. 📐 **모델이 필요 없는 자** — 먼저 이걸 세웠습니다

🔑 오차 → `s` 지도의 **두 끝**은 상수만으로 나옵니다. 조작자 모델이 한 톨도 안 들어가요.

```
s 천장 (칸 중심을 정확히 맞췄을 때) = 1 − (칸폭 × CELL_FLOOR) ÷ ONE_WIN
s 바닥 (가장 나쁜 조준 = 칸 경계)   = 1 − (칸폭 ÷ 2)          ÷ ONE_WIN
격자 폭 = 천장 − 바닥 = 칸폭 × (0.5 − CELL_FLOOR) ÷ ONE_WIN   ← 🔑 조준이 성적을 가르는 폭
```

| 판 | 칸 | 칸폭 | FLOOR | 바닥 err | WIN | **s 천장** | s 바닥 | **격자 폭** |
|---|---|---|---|---|---|---|---|---|
| **A 옛 5칸 (상용)** | 5 | 20.00 | 0.30 | 6.00 | 23 | **0.7391** | 0.5652 | **0.1739** (100%) |
| B `0.30 / 22` (지금) | 6 | 16.67 | 0.30 | 5.00 | 22 | 0.7727 (+0.034) | 0.6212 | 0.1515 (87%) |
| B `0.36 / 22` 🚨 단독 | 6 | 16.67 | 0.36 | 6.00 | 22 | **0.7273 (−0.012)** 🔴 | 0.6212 | 0.1061 (61%) |
| ⭐ **B `0.36 / 23`** | 6 | 16.67 | 0.36 | 6.00 | 23 | **0.7391 (±0.0000)** ✅ | 0.6377 | 0.1014 (58%) |
| B `0.33 / 22.5` | 6 | 16.67 | 0.33 | 5.50 | 22.5 | 0.7556 (+0.017) | 0.6032 | 0.1259 (72%) |
| B `0.36 / 23.7` | 6 | 16.67 | 0.36 | 6.00 | 23.7 | 0.7468 (+0.008) | 0.6484 | 0.0984 (57%) |

> ★ **여기서 이미 답의 절반이 나옵니다.** engineer가 원한 것은 *"바닥을 6.0으로 되돌린다"*였는데,
> **바닥을 6.0으로 되돌려도 창(`ONE_WIN`)이 22면 천장은 상용 5칸보다 더 낮아집니다**(0.7273 < 0.7391).
> 🔑 **`0.36`이 5칸의 천장을 정확히 되돌리는 것은 `ONE_WIN`이 23으로 같이 돌아갈 때뿐이에요.**
> 그리고 그 값은 산수로 나옵니다 — `1 − 6/23 = 0.73913`. **시뮬레이션이 필요 없습니다.**

### 🦶🫀♿ 배수가 다 걸린 자리에서도 같은가 — ✅ **소수점 넷째 자리까지 같습니다** (`bal-floor/wide.js`)

`s` 천장을 **가장 후한 자리**(즉시 누름 · 🦶 주발 · 🫀 컨디션 94 · ♿ 확대)에서 소스를 그대로 굴려 잰 값:

| 판 | 기본 | ♿ 확대 + 🦶 주발 + 🫀 최상 |
|---|---|---|
| **A 옛 5칸 (0.30 / 23)** | **0.7997** | **0.8459** |
| 지금 (0.30 / 22) | 0.8255 🔴 | 0.8658 🔴 |
| ⭐ **확정 (0.36 / 23)** | **0.7997** ✅ | **0.8459** ✅ |

🔑 **모든 배수가 곱해진 뒤에도 상용 5칸과 정확히 같은 자리로 돌아옵니다** —
`CELL_FLOOR`가 `상수 × mul` 꼴 안에 들어 있어서 🦶·🫀·♿가 **같은 비율로** 걸리기 때문이에요.
🔒 **만점(1.0)은 여전히 못 굽습니다** — 격자가 지운 「손의 바닥」을 `CELL_FLOOR`가 계속 대신합니다.

## 1-3. 🔴 두 하네스가 어긋났고 — 원인을 찾았습니다

> engineer의 하네스(`scratchpad/eng-one/`)는 **세션이 끝나 사라졌습니다.** 확인했어요.
> 그래서 처음부터 다시 지었고, **「보통」 짝지은 차이가 +0.0012(engineer) ↔ +0.110(제 것)**으로 갈렸습니다.

원인은 **조작자가 손을 쓰는 방식**입니다. 화면이 `cellFrom`으로 좌표를 칸에 **내림**(`Math.floor(x / 칸폭)`)하니 두 가지 읽기가 다 성립해요 —

| 모델 | 무엇을 하나 | 6칸에 대한 편향 |
|---|---|---|
| **A 밝기 argmax** | 🔑 화면이 칸마다 밝기(= 그 칸의 `s`)를 그려 줍니다. **가장 밝은 칸**을 고름 | 🔴 **6칸에 유리** — 6칸은 「빈 곳 한가운데」가 칸 중심 가까이 앉아요(생오차 1.8~4.9 vs 5칸 4.1~6.0) |
| **B 자리를 누름** | 🔑 손가락은 칸을 고르는 게 아니라 **빈 곳을 겨눠 한 점**을 누름. 손떨림이 칸 경계를 넘으면 옆 칸 | 🔴 **6칸에 불리** — 칸이 좁아 같은 손떨림이 **더 자주** 옆 칸으로 샙니다 |
| **C 칸 골라 중앙** | 밝기로 칸을 고른 뒤 **그 칸 한가운데**를 겨눔 (A와 B 사이) | 중간 |

🔑 **engineer의 하네스는 모델 B 쪽으로 보입니다** (🔴 하네스가 사라져 직접 확인은 못 했습니다 — **재현으로 추정**합니다).
제 모델 B가 그들의 표를 거의 그대로 재현했어요 —

| | engineer (시드 1벌) | 제 모델 B (시드 3벌 × 20,000) |
|---|---|---|
| `0.30 / 22` 서툰 | +0.013 | **+0.0023** |
| `0.30 / 22` 보통 | +0.0012 | **+0.0153** |
| `0.30 / 22` 능숙 | +0.050 | **+0.0661** |
| `0.36 / 23` 보통 | −0.0132 | **−0.0024** |
| `0.36 / 23` 능숙 | +0.018 | **+0.0302** |

> 🔒 **조작자는 셋 다 「A(상용 5칸)에서 서툰 0.406 / 보통 0.572 / 능숙 0.677이 나오도록」 `lat`을 재적합해 세웠습니다.**
> engineer의 절대값을 **계약이 아니라 자리표**로만 쓴 거예요(원칙 ⑧) — 조작자를 상용 판 위의 같은 자리에 세우는 용도입니다.

## 1-4. ★ 모델에 안 흔들리는 것 — **「능숙 초과분」은 진짜입니다**

`0.30 / 22`(지금)에서 **「능숙」 짝지은 차이**:

| 모델 A | 모델 B | 모델 C | engineer |
|---|---|---|---|
| **+0.0667** | **+0.0661** | **+0.0638** | +0.050 |

> ★ **셋이 소수점 둘째 자리까지 같습니다.** 조작자를 어떻게 모델하든 **잘하는 사람이 6칸에서 더 법니다.**
> 🔑 engineer의 진단이 맞았습니다 — 그리고 **`ONE_WIN`으로는 못 없앤다**는 것도 맞아요:
> `0.30 / 23`으로 창을 넓히면 능숙이 오히려 **+0.077 ~ +0.078**로 커집니다(§1-6 표).

## 1-5. 📊 측정값 — 시드 3벌 × 조작자 3벌 × 20,000판

**「보통」짝지은 차이** (계약 ≤ 0.005)

| floor / win | 모델 A | **모델 B** (engineer 하네스와 같은 종류) | 모델 C |
|---|---|---|---|
| `0.30 / 22` (지금) | +0.1102 ❌ | **+0.0153** ❌ | +0.0113 ❌ |
| `0.30 / 23` (창만) | +0.1230 ❌ | +0.0309 ❌ | +0.0258 ❌ |
| `0.36 / 22` (바닥만) 🚨 | +0.0560 ❌ | **−0.0195** ❌ | −0.0249 ❌ |
| `0.36 / 22.5` | +0.0638 ❌ | −0.0108 ❌ | −0.0166 ❌ |
| ⭐ **`0.36 / 23`** | +0.0712 ❌ | **−0.0024 ✅** | −0.0087 ❌ |
| `0.36 / 23.5` | +0.0781 ❌ | +0.0057 ❌ | **−0.0011 ✅** |
| `0.36 / 24` | +0.0849 ❌ | +0.0135 ❌ | +0.0063 ❌ |
| `0.33 / 22.5` | +0.0918 ❌ | +0.0062 ❌ | **+0.0012 ✅** |

**「능숙」짝지은 차이** (없애려는 것)

| floor / win | 모델 A | 모델 B | 모델 C |
|---|---|---|---|
| `0.30 / 22` (지금) | +0.0667 | +0.0661 | +0.0638 |
| `0.30 / 23` (창만) | +0.0778 🔴 | +0.0773 🔴 | +0.0750 🔴 |
| `0.36 / 22` (바닥만) | +0.0154 | +0.0169 | +0.0129 |
| ⭐ **`0.36 / 23`** | **+0.0288** | **+0.0302** | **+0.0263** |
| `0.33 / 22.5` | +0.0473 | +0.0477 | +0.0447 |

**「서툰」** (한 축만 밀리지 않는지 확인용): `0.36 / 23`에서 **+0.0252 / +0.0015 / −0.0009** — 셋 다 얌전합니다.

### 🔬 문턱 근처는 **50,000판으로 다시** 쟀습니다 (재현됩니다)

| floor / win | 모델 B 보통 | 모델 C 보통 | 판정 |
|---|---|---|---|
| ⭐ `0.36 / 23` | **−0.0028** ✅ | −0.0089 ❌ | 🔒 **모델이 필요 없는 자에서 정확히 맞는 값**(천장 0.7391) |
| `0.36 / 23.25` | +0.0013 ✅ | −0.0050 ❌(딱 경계) | 🟡 둘 다 통과에 가깝지만 **여유가 0** |
| `0.36 / 23.5` | +0.0053 ❌ | −0.0013 ✅ | |

> 🔴 **23.25를 고르지 마세요.** 두 모델을 억지로 맞춘 값이고 **구조적 뜻이 없습니다** —
> 문턱 여유가 0이라 계수가 한 번만 더 움직여도 우연으로 빨간불이 됩니다.
> ⭐ **23**은 「상용 5칸의 `s` 천장을 정확히 되돌린다」는 **산수의 값**이에요. 그쪽을 씁니다.

> 🔒 **시드 3벌 사이의 산포는 「보통」에서 0.0003 ~ 0.003**입니다 (표의 시드별 값은 `bal-floor/final.js` 출력).
> ⚠️ **시드 하나로는 못 봅니다** — engineer가 스스로 그렇게 적었고, 맞습니다.

## 1-6. 🚨🚨 **한 벌입니다 — 따로 넣을 수 없습니다**

> 🔴 **이 절을 요약에서 줄이지 마세요.** ⚽ 더 윙어 II에서 `NPC_SPOT` 세 값을 *"하나를 내리는 것과 같다"*로
> 접었다가 곡선이 무너진 적이 있습니다. **여기가 정확히 같은 모양의 자리예요.**

| 무엇을 넣나 | 보통 (B / C) | 능숙 (B / C) | s 천장 | 판정 |
|---|---|---|---|---|
| 🔴 **`ONE_WIN = 23`만** (floor 0.30 유지) | +0.0309 / +0.0258 | **+0.0773 / +0.0750** | 0.7826 | ❌ **아무것도 안 고쳐집니다.** 능숙이 **오히려 커져요** |
| 🔴 **`CELL_FLOOR = 0.36`만** (win 22 유지) | **−0.0195 / −0.0249** | +0.0169 / +0.0129 | **0.7273** | ❌ 보통이 **계약의 4~5배 밖**으로 밀립니다. 천장이 **상용 5칸 밑** |
| ⭐ **둘 다** | **−0.0024 / −0.0087** | **+0.0302 / +0.0263** | **0.7391** | ✅ 보통이 계약 안(모델 B) · 능숙 초과분 **절반** · 천장 **상용과 정확히 같음** |

> ★ ***"`CELL_FLOOR` 하나를 0.30 → 0.36으로 올리는 것"*이 아닙니다.**
> `ONE_WIN`도 **22 → 23으로 되돌아갑니다.** 🔑 6칸으로 가면서 23 → 22로 내렸던 그 값이,
> `CELL_FLOOR`가 난이도 몫을 받으면서 **제자리로 돌아가는 것**이에요.
> 🔒 손잡이 셋의 축은 그대로 삽니다 — `ONE_WIN`은 여전히 난이도, `CELL_FLOOR`는 여전히 천장.
> **바뀐 건 「6칸이 만든 초과분을 어느 축이 갚느냐」**입니다.

## 1-7. 🟡 치르는 값 — **격자 폭이 5칸의 58%로 줄어듭니다**

`CELL_FLOOR`를 올리는 것은 **「어느 칸을 눌러도 비슷해지는」 쪽**입니다. 소스 주석이 스스로
경고한 자리예요 — *"격자가 버튼 하나가 됩니다"*. 실제로 두 축이 반대로 움직입니다:

| 짝 | 격자 폭 (5칸 대비) | 능숙 초과분 (3모델 평균) |
|---|---|---|
| `0.30 / 22` (지금) | 0.1515 (**87%**) | +0.066 |
| `0.33 / 22.5` | 0.1259 (**72%**) | +0.046 |
| ⭐ `0.36 / 23` | 0.1014 (**58%**) | +0.028 |
| `0.42 / 23` | 0.0580 (**33%**) | 모델 A에서 세 실력 전부 \|차이\| ≤ 0.020 — 🚨 **격자가 거의 버튼입니다** |

> 🔴 **어느 쪽을 포기할지는 범민 님 판단입니다.** 양쪽을 나란히 놓습니다 —
> **`0.36 / 23`** = 곡선이 상용 5칸에 가장 가깝다. 대신 **어느 칸을 누르는지가 5칸의 58%만큼만** 성적을 가름.
> **`0.33 / 22.5`** = 격자가 72% 살아 있다. 대신 능숙 초과분이 **+0.046** 남음(절반이 아니라 ⅓만 깎임).

## 1-8. 판정

> ## ✅ **통과 — 단, `CELL_FLOOR = 0.36`과 `ONE_WIN = 23`을 「한 벌」로 확정합니다.**
> 🚨 **`CELL_FLOOR = 0.36` 단독은 못 씁니다** — 「보통」이 −0.0195 / −0.0249로 밀립니다(계약 4~5배 밖).

근거 셋:
1. **능숙 초과분이 조작자 모델 셋에서 전부 +0.064~+0.067** — 모델 산물이 아니라 판의 성질입니다.
2. **`0.36 + 23`이 셋 전부에서 그 초과분을 +0.026~+0.030으로 절반 냅니다.** engineer의 예측(+0.026) 확인.
3. **`0.36 + 23`은 모델이 필요 없는 자에서도 맞습니다** — `s` 천장이 상용 5칸의 **0.7391로 정확히** 되돌아와요.

🟡 **못 채운 것 하나:** 모델 A(밝기 argmax)에서는 **어떤 짝도 「보통 ≤ 0.005」를 못 넘습니다** —
`0.30 / 22`(지금)조차 **+0.110**입니다. 🔑 그 모델에서 계약을 맞추려면 `CELL_FLOOR`를 **0.42까지** 올려야 하고,
그러면 **격자가 5칸의 33%짜리 버튼**이 됩니다. 🔴 **곧 「계약을 못 맞추는 것」이 아니라
「이 계약이 조작자 모델을 탄다」는 뜻이에요** — §1-9를 보세요.

## 1-9. 📮 designer·inspector에게 — 🔴 **계약을 다시 써야 합니다**

`E[s] 「보통」 짝지은 차이 ≤ 0.005`는 **원칙 ⑦의 두 줄을 못 채웁니다**:

| | |
|---|---|
| ① 무엇과 견주는가 | ✅ **옛 5칸 판(커밋 `082018b`)** — 이건 적혀 있습니다 |
| ② 격자의 어느 칸에서 재는가 | 🚨 **안 적혀 있습니다.** 「보통」이 어떤 조작자인지에 따라 같은 소스가 **+0.0012 ~ +0.110**을 냅니다 |

🔒 **제안 — 모델이 안 들어가는 자로 바꾸세요** (§1-2). 시뮬레이션 없이 상수만으로 검사할 수 있습니다:

```
① s 천장 = 1 − (100/cells)·CELL_FLOOR / ONE_WIN  가 0.7391 ± 0.010   (= 상용 5칸 1 − 6/23)
② 격자 폭 = (100/cells)·(0.5 − CELL_FLOOR) / ONE_WIN  가 0.10 이상    (🔒 0에 가까우면 격자가 버튼)
```

### 🔬 왜 「차이」조차 안전하지 않았나 — **두 곡선이 교차합니다** (`bal-floor/probe.js` · 20,000판)

잡음 0인 완전 조작자의 `s` 천장을 **누른 시각별로** 재면:

| 누른 시각 | 0.10s | 0.35s | 0.70s | 1.20s | 1.80s | 2.40s |
|---|---|---|---|---|---|---|
| **A 옛 5칸** | 0.7172 | 0.6845 | 0.6386 | 0.6002 | 0.5399 | 0.4469 |
| **B 6칸 `0.30/22`** | 0.7684 | 0.7568 | 0.7383 | 0.6863 | 0.5505 | 0.4301 |
| 차이 | +0.051 | +0.072 | **+0.100** | +0.086 | +0.011 | **−0.017** |
| (참고) 칸 중심까지의 생오차 A / B | 4.13 / 1.78 | 4.63 / 2.48 | 5.36 / 3.47 | 6.01 / 4.88 | 6.20 / 6.48 | 5.25 / 6.89 |

🔑 **t ≈ 1.9초에서 부호가 뒤집힙니다.** 6칸은 「빈 곳 한가운데」가 **칸 중심 가까이 앉아**
초반에 유리하고, 후반에는 반대예요. 🚨 **그래서 「짝지은 차이」가 조작자의 누르는 시각으로
−0.03 ~ +0.12까지 갈립니다** — 잡음이 아니라 **판의 구조**입니다.

🔴 **그리고 「E[s] 절대값」은 어떤 형태로도 계약에 넣지 마세요** — 조작자 모델을 고치는 순간
난이도를 모델 쪽으로 끌고 갑니다(CLAUDE.md 원칙 8). **차이도 여기서는 안전하지 않았습니다** —
A와 B의 `s(t)` 곡선이 t ≈ 1.9초에서 **교차**해서, 조작자가 언제 누르느냐로 부호까지 뒤집힙니다.

## 1-10. 🔒 확정 계수

| 상수 | 지금 | **확정** | 자리 |
|---|---|---|---|
| `CELL_FLOOR` | `0.30` | **`0.36`** | `beta/winger-moment.js:149` |
| `ONE_WIN` | `22` | **`23`** | `beta/winger-moment.js:199` |

> 🚨 **둘을 따로 넣을 수 없습니다.** 하나만 넣었을 때의 실측은 §1-6 표에 있습니다 —
> **`CELL_FLOOR`만 넣으면 「보통」이 −0.0195 · `ONE_WIN`만 넣으면 「능숙」이 +0.0773으로 오히려 커집니다.**
> 🔴 `ONE.cells = 6`은 **읽힘의 손잡이라 안 건드립니다**(범민 님이 정하신 값).
> 🔴 `ONE_CLOSE = 0.45`도 안 건드립니다 — 다른 축이에요.

📝 **소스 주석도 같이 고쳐 주세요** — `beta/winger-moment.js:146` 근처의
*"`CELL_FLOOR = 0.36`이면 … 「능숙」 초과분이 +0.046 → +0.010"* 줄은 **`ONE_WIN` 22를 전제로 한 값**입니다.
`ONE_WIN = 23`과 한 벌이라는 것과 §1-6 표를 적어 두세요. 그 줄만 읽고 하나만 넣으면 그게 사고입니다.

## 1-10b. 🗄️ 옛 세이브 (원칙 ⑥) — 🟢 **위험 없습니다**

`CELL_FLOOR`·`ONE_WIN`은 **저장되는 값이 아닙니다.** 새 필드도 안 생겨요.
`undefined`가 산식을 지나갈 수 있는 두 자리를 확인했습니다 —

| 자리 | 옛 세이브에서 | 실측 |
|---|---|---|
| `opts.condition`이 없음 | `condOf(undefined)` → `engine.condMul` | 🟢 `cond == null ? COND_REF` 가드가 있습니다 (`engine.js:216`) — NaN 안 납니다 |
| `grow-mech-ready`에 옛 열쇠(`w2-cut`·`w2-kp`·`w2-blk`) | `readSeen()` | 🟢 `Number(seen[key]) \|\| 0` — 없으면 0. 옛 열쇠는 그냥 남고 아무 일 안 해요 |

## 1-11. 📮 inspector에게 — **테스트로 굳힐 것** (①에서 나온 것만)

🔒 **전부 시뮬레이션 없이 상수만으로 검사됩니다.** `_t.K`에서 값을 읽어 오면 안 되고,
**문턱은 검사에 직접 적으세요**(상수를 바꿔도 검사가 따라가면 아무것도 안 잡힙니다).

| # | 계약 | 문턱 | 왜 |
|---|---|---|---|
| **F-1** | `s 천장 = 1 − (100/cells)·CELL_FLOOR / ONE_WIN` | **0.7391 ± 0.010** | 🔑 상용 5칸(`1 − 6/23`)의 자리. **모델이 안 들어갑니다** |
| **F-2** | `격자 폭 = (100/cells)·(0.5 − CELL_FLOOR) / ONE_WIN` | **≥ 0.10** | 🚨 0에 가까우면 **격자가 버튼 하나**가 됩니다 (소스 주석이 스스로 경고한 자리) |
| **F-3** | `CELL_FLOOR < 0.5` | — | `0.5`면 바닥 err = 칸폭÷2 = **최대 오차**라 여섯 칸의 `s`가 전부 같아집니다 |
| **F-4** | 🚨 **한 벌 검사** — `CELL_FLOOR`와 `ONE_WIN`을 **소스에서 함께 정규식으로 뜯어** F-1을 계산 | | 🔴 **하나만 바뀐 커밋이 F-1을 깨야 합니다.** 이게 §1-6을 지키는 유일한 자리예요 |
| **F-5** | 🧤 무대 불변식 — 빈 곳이 넓어진 순간 0 · 키퍼가 가운데를 넘은 순간 0 | 0/420,000 | 이미 제가 쟀습니다(§1-1). engineer 116번 §3-4와 같은 값 |

🔴 **변이 검증**: `CELL_FLOOR`만 0.36으로 · `ONE_WIN`만 23으로 — **각각 F-1이 빨간불**이어야 합니다.
둘 다 넣으면 초록불. 🔑 **하나만 넣었을 때 초록불이면 F-4가 아무것도 안 지키고 있는 겁니다.**

🟡 **그리고 옛 계약 하나를 지워 주세요** — `one-grid-test G-0a`가 **칸을 5로 단언**합니다(116번 §5-2).
설계가 뒤집혔으니 그건 이제 옛 계약이에요.

## 1-12. 🧰 재느라 만든 하네스 (①) — 🔴 **커밋 안 했습니다**

`/tmp/claude-0/-workspace-grow-games/79cf1e76-c844-41c3-8122-5fc7f1594e2e/scratchpad/`

| 파일 | 무엇 |
|---|---|
| `bal-old5.js` | 🔒 **커밋 `082018b`의 `beta/winger-moment.js`** — 기준선 A. `git show`로 꺼냈습니다 |
| `bal-floor/lib.js` | 로더(new Function · Math 주입 · `cellMul` 정규식 추출) · 난수원 3갈래 · **조작자 모델 A/B/C** |
| `bal-floor/check.js` | 🔬 **하네스 검산** — condMul·주발 비·변이·무대 불변식 (§1-1) |
| `bal-floor/probe.js` · `probe.txt` | 누른 시각별 `s` 천장과 생오차 — **두 곡선의 교차** (§1-9) |
| `bal-floor/anchors.js` | 📐 **모델이 필요 없는 자** — 천장·바닥·격자 폭 표 (§1-2) |
| `bal-floor/calib.js` · `calib.json` | 조작자를 **A(상용 5칸)의 0.406/0.572/0.677**에 맞춰 세우는 `lat` 이분 탐색 |
| `bal-floor/final.js` | ★ **본 측정** — 모델 3 × 설정 × 시드 3 × 20,000(문턱 근처 50,000) |
| `bal-floor/wide.js` | ♿🦶🫀 배수가 다 걸린 자리의 천장 (§1-2) |
| `bal-floor/sigma.js` · `grid2d.js` · `main.js` · `es.js` · `aim.js` | 감도 탐색 (잡음 σ · (FLOOR × WIN) 2차원 · 초기 탐색) |

---

# 2. 🧒 초2·초3이 필요한가 — designer가 「생사」라 부른 자리

## 2-0. 판정

> # 🔴 **짓지 마세요 — 🧒 초1 하나만 짓습니다.**
>
> 🔑 **축의 폭은 「순수 3택」이 전부이고, 그건 초1 하나로 다 나옵니다.**
> 초2·초3이 새로 더하는 「2+1」·「셋 다 다름」은 **그 폭 안쪽의 점**이에요.
> 🔴 **초3의 한계 효과는 12칸 중 8칸에서 잡음에 묻히고, 잡히는 칸에서도 최대 1.1%**입니다.

⚠️ **designer가 적은 폐기 조건(「세 조합의 골 차이 ±3% 안」)은 문자 그대로는 df에서 안 걸립니다**(5.0~5.9%).
🔑 **그런데 그 조건이 「틀린 것」을 재고 있습니다** — 재판에 서 있는 것은 *세 조합의 차이*가 아니라
***초2·초3이라는 화면 둘이 무엇을 더하는가***예요. 그걸 따로 쟀고, **거의 0입니다**(§2-3).

## 2-1. 무엇을 쟀나

| | |
|---|---|
| **🔴 구현이 아직 없습니다** | `CHILD_FOCUS`는 designer 설계만 있어요. engineer 스펙 **한 줄**을 정규식 **변이**로 얹어(`prospect.js`의 `const focus = YOUTH_FOCUS[marketId] \|\| [];` → `.concat(childFocus)`) **진짜 `rollShape`를 굴렸습니다.** 변이 적용 확인 완료 |
| **경로** | 🔒 `bootPage`로 페이지째 띄워 `career.js`의 `leagueRound` 형태 그대로 — 내 경기 `createMatch` · 나머지 `autoMatch` · 38라운드 · 6클럽 × 선발 11. **경로가 다른 시뮬레이터를 새로 짓지 않았습니다** |
| **격자** | 능력치 **70 / 110 / 150** × 포지션 **fw / wg / mf / df** = **12칸** (원칙 ⑥) |
| **조합** | ⚽×3 · ⚽⚽🛡️ · ⚽🛡️👀 · 🛡️×3 · 👀×3 · **대조(선택 없음 = 옛 세이브)** |
| **표본** | 1,200커리어 × 시드 3벌 = **n = 3,600 / 칸**. 부문상은 리그 66명 × 200시즌 × 3벌 |
| **짝지음** | 🔒 같은 커리어 인덱스에서 **난수 자리를 되돌려** 조합만 갈아 끼웠습니다 |

## 2-2. 🔬 designer의 세 계산 — 검산 결과

| # | designer가 적은 것 | 검산 | 판정 |
|---|---|---|---|
| **A-1** | `overall()`이 여섯 조합 전부 32.333 — *"실측이 아니라 `POOL` 고정의 따름정리"* | 40,000굴림 × 6조합 × 5시장 × 4포지션 — **총합이 194가 아닌 굴림 0건.** 조합 간 차 **7.1e-15** | ✅ **결론 맞습니다** |
| | *"반올림·clamp가 개입하지 않는다"* | 🟡 **`clamp(…, 18, 54)`는 실제로 걸립니다.** 총합이 194로 남는 건 clamp가 안 걸려서가 아니라 **뒤의 우수리 되돌리기 루프**가 되돌리기 때문이에요 | 🟡 **근거 정정** — 결론은 살고 이유가 틀립니다 |
| | 숫자 이름표 | 🟡 `overall()`은 `nowStats`(나이곡선)를 곱해 만 18세에 **22.633**을 냅니다. **32.333은 raw 6칸 평균**이에요 | 🟡 **표기 정정** (불변성은 맞음) |
| **A-2** | 유스 `blendOf`가 여섯 조합 전부 **40.0~40.4** | 실측 **40.04~40.67**. clamp 전 raw는 **32.68~38.36** — 전부 바닥 40 아래 | ✅ 맞습니다 (폭만 조금 넓음) |
| **A-3** | ♾️ 리롤이 편중을 **안 씻는다** (1회 +6.5 → 50회 **+8.1**, 벌어짐) | 1회 **+5.62** → 5회 +5.64 → 10회 +5.40 → 20회 +4.80 → 50회 **+3.86** | 🔴 **추세가 반대입니다** |
| | | 🔑 차이가 **안 닫히는 건 맞지만, 벌어지는 게 아니라 31% 좁아집니다.** designer §9가 *"리롤 후 차이가 +2 아래면 판단을 뒤집는다"*고 적었는데 **50회에서 +3.86으로 그쪽으로 가고 있어요.** `REROLL_MAX = Infinity`라 이 자리는 다시 봐야 합니다 | 🔴 **designer 재검토** |
| **A-4** | 셋 다 다르면 평평 · 하나에 몰면 뾰족 | 시작 stats 표준편차 ⚽×3 **6.64** > ⚽⚽🛡️ 6.19 > ⚽🛡️👀 **5.33** | ✅ 방향 맞음 |
| | | 🔴 **그런데 대조(선택 없음)가 6.55입니다** — ⚽×3(6.64)과 **0.09 차이.** 🔑 **뾰족한 쪽은 「아무것도 안 고른 것」과 사실상 같아요.** 사람이 고를 수 있는 건 **「평평하게 만들기」 한 방향뿐**입니다 | 🔴 **축이 반쪽** |

### 🔑 그리고 검산 중에 **결정적인 것 하나**가 나왔습니다

```
⚽×1 과 ⚽×3 은 완전히 같은 stats를 냅니다 — 4,000굴림 / 4,000 일치
```

`spread()`가 `focus`를 **`indexOf`로** 보기 때문이에요(designer §3-3이 스스로 적은 성질).
🔑 **그래서 「같은 걸 세 번 고르기」는 「한 번 고르기」에 정확히 0을 더합니다.**
초2가 새로 더하는 것은 **「2+1」 하나뿐**이고, 초3이 더하는 것은 **「셋 다 다름」 하나뿐**입니다.

## 2-3. ★ 본론 — **초2·초3의 한계 가치** (짝지은 차이 · 골/시즌)

> 🔒 **여기가 재판에 서 있는 값입니다.** 「세 조합의 차이」가 아니라 **「화면 하나가 더하는 양」**이에요.

| 포지션 | 능력치 | **초2**가 더하는 골 | % | **초3**이 더하는 골 | % | 초3이 잡히나 |
|---|---|---|---|---|---|---|
| fw | 70 / 110 / 150 | −0.317 / −0.357 / −0.199 | −2.6 / −1.9 / −0.8% | −0.005 / −0.024 / −0.009 | −0.04 / −0.13 / −0.04% | 🔴 **셋 다 묻힘** |
| wg | 70 / 110 / 150 | −0.259 / −0.284 / −0.180 | −2.6 / −1.8 / −0.8% | +0.028 / +0.033 / +0.024 | 0.28 / 0.20 / 0.11% | 🔴 **셋 다 묻힘** |
| mf | 70 / 110 / 150 | −0.174 / −0.210 / −0.130 | −2.6 / −1.8 / −0.8% | +0.053 / +0.132 / +0.096 | 0.78 / **1.12** / 0.60% | 🟡 110·150만 |
| df | 70 / 110 / 150 | +0.150 / +0.247 / +0.179 | 6.1 / **5.2** / 2.5% | −0.042 / −0.053 / −0.017 | −1.70 / −1.11 / −0.24% | 🟡 70·110만 |

> 🔴 **초3은 12칸 중 4칸에서만 3σ를 넘고, 넘는 칸에서도 최대 1.1%입니다.**
> 🔑 그리고 **초2가 더하는 것은 대부분 음수**예요 — 「2+1」은 순수 3택보다 **나쁜 선택지**입니다(§2-4).

## 2-4. 왜 그런가 — **폭을 안 늘립니다** (L = 110 · `blendOf`)

| | 대조 | ⚽×3 | 🛡️×3 | 👀×3 | ⚽⚽🛡️ | ⚽🛡️👀 |
|---|---|---|---|---|---|---|
| fw | 112.24 | ★ **115.40** | 110.27 | 112.14 | 113.42 | 113.31 |
| wg | 113.65 | ★ **114.94** | 111.63 | 113.99 | 112.99 | 113.32 |
| mf | 115.02 | 114.17 | 113.69 | ★ **115.42** | 112.90 | 113.36 |
| df | 112.46 | 110.48 | ★ **116.06** | 111.75 | 114.06 | 113.34 |

| | |
|---|---|
| ✅ | 🔑 **designer §3-2는 맞습니다** — 네 자리에 최선이 **하나씩**, 지배되는 선택지 없음 |
| 🔴 | **그 최선은 전부 「순수 3택」이고, 순수 3택은 🧒 초1 하나로 다 나옵니다** |
| 🔴 | **혼합 둘(⚽⚽🛡️ 113.42 · ⚽🛡️👀 113.31)은 순수의 폭(110.27 ~ 115.40) 안쪽**입니다. wg·df도 같아요 |
| 🔴 | mf만 ⚽⚽🛡️(112.90)가 순수 최저(113.69)보다 낮은데 — 🔑 그건 폭이 는 게 아니라 **더 나쁜 선택지 하나가 는 것**입니다 |
| 🔴 | 「평평한 끝」(⚽🛡️👀)은 네 포지션에서 **113.31 ~ 113.36으로 사실상 동일** — *아무것도 안 고른 것과 구별이 안 됩니다* |

## 2-5. 🔴 designer의 폐기 조건 — **문자 그대로는 df에서 안 걸립니다**

**세 조합의 골 최대차** (designer §9-1의 자)

| 포지션 | 70 | 110 | 150 |
|---|---|---|---|
| fw | 2.72% ✅ | 2.09% ✅ | 0.88% ✅ |
| wg | 2.59% ✅ | 1.77% ✅ | 0.85% ✅ |
| mf | 2.59% ✅ | 1.80% ✅ | 0.81% ✅ |
| **df** | **5.85%** 🔴 | **5.03%** 🔴 | 2.43% ✅ |

> 🟡 **다만 df의 5.9%는 「시즌 2.5골의 5.9% = 0.15골」**이고, df는 **득점왕 0% · 도움왕 ≤1.3%**로
> 부문상에 아예 안 닿습니다. **비율만 크고 사람이 느낄 양이 아닙니다.**
> 🔴 **그리고 이 자는 초2·초3이 아니라 「세 조합」을 재요.** 화면 둘의 값어치는 §2-3에 있습니다.

## 2-6. 🔒 검정력 — **「차이가 없다」인가, 「표본이 모자라 못 보는 것」인가**

> 🔑 **이게 이 실측에서 제일 중요한 구분입니다.**

**차이는 확실히 있습니다.** 대조 대비 최대 짝차가 **전 12칸에서 4.4σ ~ 16.1σ**(n = 3,600).
🔴 **표본 부족이 아니라 효과가 실제로 작은 것**이에요. 표본을 열 배로 늘려도 답은 안 바뀝니다.

🥇 **부문상** (리그 66명 · 200시즌 × 3벌): 최대 짝차는 fw@110 ⚽×3 **+3.00 ± 1.39%p**(24.8% → 27.8%)와
wg@150 ⚽×3 **+3.00 ± 1.20%p**. 세 조합만 보면 fw@110에서 **27.8 / 25.2 / 26.0 — 2.6%p 폭**입니다.
⚠️ 오차가 ±1.2~1.4%p라 3%p 짝차는 **약 2σ, 경계선**이에요 — **부문상을 계약으로 쓰려면 표본을 더 키워야 합니다.**

## 2-7. 📮 범민 님께 — 한 줄

> **초1 하나만 지으세요.** 여덟 살에 뭘 하고 놀았는지 **한 번** 고르는 것으로
> 이 설계가 줄 수 있는 것은 **전부** 나옵니다. 아홉 살·열 살에 또 묻는 화면 둘은
> **고를 것을 늘리지 않고**(같은 걸 또 고르면 값이 0, 섞으면 순수보다 나빠짐)
> **결과도 안 바꿉니다**(초3의 몫이 12칸 중 8칸에서 잡음에 묻힘).

🟡 **초1은 남깁니다** — 순수 3택이 **포지션별로 최선을 정확히 하나씩** 갈라 주고(`blendOf` ±2.5~3.6),
대조 대비 **4.4σ ~ 16.1σ**로 확실히 삽니다. *"그 선택지가 커서 능력치에 영향을 주는거지"* —
🔑 **영향은 있습니다. 다만 그 영향이 「한 번」에 다 들어 있어요.**

## 2-8. 🚨 정직하게 적는 한계 — **숨기지 않습니다**

| | |
|---|---|
| **①** | 🔴 **`liftTo`는 모델입니다.** 훈련이 덧셈(`game.js:1046`)이라 여섯 칸에 같은 값을 더해 능력치 칸을 만들었어요. **사람이 어느 칸을 훈련할지는 안 넣었습니다.** 편중 훈련을 하면 격차가 커질 수 있는데, 🔑 그건 **조작 축**이지 `CHILD_FOCUS` 축이 아닙니다. **절대값이 아니라 조합 간 차이만** 보세요 |
| **②** | 20시즌을 20번 굴리지 않고 **한 시즌 × 3,600표본**으로 냈습니다. 능력치 칸이 고정이라 통계적으로 동치이지만 **시즌 사이 이동·승격·나이곡선의 누적은 안 탔습니다** |
| **③** | 🚨 **능력치 150은 `STAT_CAP = 130`을 넘습니다.** 초월(각성) 없이는 도달 못 하고, **각성은 `S.stats[key]`를 30~60으로 덮어써서 모양을 지웁니다** — 즉 실제 게임에서 150 칸은 **`CHILD_FOCUS`가 이미 지워진 뒤**일 수 있어요. 🔴 **따로 재야 할 자리로 남깁니다** |
| **④** | `blendOf`의 raw 가중표(`BLEND`/`BLEND_W`)는 손으로 적은 뒤 **엔진 `E.blendOf`와 그 자리에서 대조**해 통과시켰습니다(clamp만 차이). **그 밖의 산식은 전부 페이지의 실제 함수**입니다 |
| **⑤** | `CHILD_FOCUS` 구현이 아직 없어 **engineer 스펙 한 줄을 변이로 얹어** 쟀습니다. 🔴 **구현이 그 한 줄과 다르면 이 표는 무효**입니다 — engineer는 `focus` 배열에 `concat`하는 형태를 지켜 주세요 |

## 2-9. 📮 designer에게 — **되돌려 보냅니다**

| | |
|---|---|
| 🔴 **①** | **`CHILD_FOCUS` 축이 반쪽입니다** — 「뾰족함 ↔ 평평함」이라고 적으셨는데, **뾰족한 끝이 대조(선택 없음)와 SD 0.09 차이**입니다. 사람이 실제로 고를 수 있는 것은 **「평평하게 만들기」 한 방향**이고, 그 끝은 네 포지션에서 `blendOf`가 거의 같아요. 🔑 **「고르면 평평해진다」는 축은 손잡이가 아니라 페널티로 읽힙니다** |
| 🔴 **②** | **§5-3의 리롤 추세가 반대입니다** — 벌어지는 게 아니라 **1회 +5.62 → 50회 +3.86으로 좁아집니다.** §9의 *"리롤 후 차이가 +2 아래면 판단을 뒤집는다"*가 **살아 있는 조건**이 됐어요 |
| 🟡 **③** | **§5-2의 「32.333」은 `overall()`이 아니라 raw 6칸 평균**입니다(`overall()`은 나이곡선이 곱해져 만 18세에 22.633). **불변성은 맞고 이름표만 틀렸어요** — 다음 사람이 `overall()`을 32.333으로 단언하는 검사를 쓰면 그건 빨간불입니다 |
| 🟡 **④** | **§5-2의 「clamp가 개입하지 않는다」는 부정확합니다** — clamp는 걸리고, **우수리 되돌리기 루프**가 194를 지킵니다. 🔒 **그 루프가 잠금장치예요.** 거기를 건드리면 `POOL`이 깨집니다 |
| 🔒 **⑤** | **폐기 조건의 자를 바꾸세요** — 「세 조합의 차이」가 아니라 **「화면 하나의 한계 가치」**입니다. 이번엔 제가 둘 다 쟀지만, 다음 설계에서는 처음부터 뒤쪽으로 적어 주세요 |

## 2-10. 📮 inspector에게 — ②에서 나온 것

🔴 **초2·초3을 안 짓기로 하면 아래 셋은 「짓지 않은 것」을 지키는 검사입니다.**

| # | 계약 | 왜 |
|---|---|---|
| **C-1** | `CHILD_FOCUS` 세 선택지의 **합집합 = 6칸 · 겹침 = 0** (소스에서 정규식 추출) | 겹치는 순간 §2-2 A-1의 중립이 깨집니다 |
| **C-2** | 3택 × 5시장 × 4포지션에서 **`sum(stats) === POOL`** (`POOL`도 소스에서 추출) | 🔒 **우수리 되돌리기 루프가 잠금장치**입니다 (§2-9 ④) |
| **C-3** | 🚨 **`focus`가 `indexOf`로 읽히는가** — 같은 키를 두 번 넣어도 결과 stats가 **완전히 같은가** | 🔑 이게 §2-3의 근거이고, `Set`이나 카운트로 바꾸는 순간 **초1의 결론이 통째로 뒤집힙니다** |
| 🔴 | **`overall()`을 32.333으로 단언하지 마세요** — 그건 raw 6칸 평균입니다 (§2-9 ③) |

## 2-11. 🧰 재느라 만든 하네스 (②) — 🔴 **커밋 안 했습니다**

`/tmp/claude-0/-workspace-grow-games/79cf1e76-c844-41c3-8122-5fc7f1594e2e/scratchpad/bal-arc/`

| 파일 | 무엇 |
|---|---|
| `boot.js` · `lib.js` | 변이를 지원하는 페이지 부트 (`bootPage` 위) · 난수원 3갈래 |
| `a.js` | A 검산 — `POOL`·`overall()`·리롤·표준편차 |
| `a2b.js` · `a2c.js` | `blendOf` 격자 (능력치 × 포지션) |
| `b1.js` · `b1.json` | **38경기 시즌** — 골·도움 짝지은 차이 |
| `b2.js` · `b2.json` | 리그 66명 부문상 (200시즌 × 3벌) |
| `dedup.js` | 🔑 **⚽×1 ≡ ⚽×3 확인** (4,000/4,000 일치) |
| `sum.js` | 판정표 |
