# 61 · engineer — 🥈 베스트11 문턱 한 벌 (실측 5-j)

**근거**: designer 판정 2026-08-29 (§6-4) · balancer 실측 5-h · 5-j
**대상**: `beta/winger2/career.js` **세 값 한 벌**. 다른 건 안 건드렸습니다.
**커밋 안 했습니다.** `tests/winger2/`는 손대지 않았어요.

---

## 1. 넣은 것

```js
if (hype >= 5.8 * bar) {                        // 4.5 → 5.8
  const posBar = rand(5.5 * bar, 8.0 * bar);    // (4.2, 6.2) → (5.5, 8.0)
```

주석에 박아 둔 것 (원칙 ⑨):

| 무엇 | 왜 |
|---|---|
| **셋은 한 벌** — 하한만 94.3% · 상한만 67.5% · 셋 다 넣어야 48.9% | 따로 넣으면 안 고쳐집니다 |
| 🚫 **상한을 8.5 이상으로 올리지 말 것** — 130에서 리그MVP와 **역전** | 옛 `else if` 사고(*"MVP를 받으면 베스트11을 아예 못 받는다"*)와 **형태는 달라도 결과가 같습니다.** 거기는 `else if`, 여기는 **문턱 배치** |
| 위 리그MVP 문턱(5.5 · 3.5~7.8)과 **함께** 봐야 한다 | 두 상의 격이 서로에게 걸려 있어요 |
| 노장 98.7%도 같은 뿌리 — `agePen`을 되살려 **가리는** 게 아니라 문턱을 고친다 | designer 판정 |

---

## 2. 🔴 측정 — 요청받은 둘 다 확인했습니다

**값을 손으로 옮겨 적지 않았습니다.** `career.js` 소스에서 `POS_AXIS` · `posAxis()` ·
`AXIS_K` · `AXIS_OFF` · **hype 줄** · **리그MVP 판정 블록** · **베스트11 판정 블록**을
정규식으로 뜯어 `new Function`으로 굴렸고(직접 `eval` 안 씀), 시즌은 **진짜 엔진**
`WingerEngine._t.playMatch`로 38라운드씩 돌렸습니다.
환경: K리그1(`bar 1` · `prestige 1`) · 클럽 62 vs 62 · `s = 0.522` · 4포지션 × 250시즌 × 38R.

### ① 유효 능력치별 — 새 문턱

| 유효 능력치 | 평균 hype | 🥈 베스트11 | 🏆 리그MVP | 역전 |
|---|---|---|---|---|
| 70 | 5.75 | **13.4%** | 4.5% | ✅ |
| **90** | 6.61 | **44.8%** | 22.1% | ✅ |
| 110 | 7.35 | **74.3%** | 57.3% | ✅ |
| 130 | 7.90 | **91.6%** | 85.1% | ✅ |
| 150 | 8.30 | **98.2%** | 97.8% | ✅ (여유 **0.4%p**) |

**능력치 90에서 44.8~45.9%** — designer 목표(48.9%) 근처입니다. ✅

### ② 문턱 조합별 — balancer 값과 대조 (같은 hype 표본에 판정만 갈아 끼움)

| 조합 | 제 측정 | balancer |
|---|---|---|
| 옛 값 (4.5 · 4.2 · 6.2) | **95.8%** | 97.1% |
| 하한만 (4.5 · **5.5** · 6.2) | **89.0%** | 94.3% |
| 상한만 (4.5 · 4.2 · **8.0**) | **67.2%** | 67.5% |
| ✅ **한 벌** (5.8 · 5.5 · 8.0) | **45.9%** | 48.9% |

제 값이 **일관되게 2~5%p 낮습니다** — 제 하네스는 🎖️ 시즌 칭호·🎉 피버 버프를 안 켜고
클럽 전력을 62로 고정했어요. **순서와 크기 관계는 그대로**라, *"셋이 한 벌"*이라는
판정은 두 자에서 같게 나옵니다.

### ③ 🚫 상한 8.5 — **역전이 실제로 납니다** (inspector 검사 44 자리)

| 유효 능력치 | 상한 **8.0** (구현) | 상한 **8.5** (변이) |
|---|---|---|
| 110 | 베스트11 74.3% vs MVP 57.3% ✅ | 60.4% vs 53.3% ✅ |
| **130** | 91.6% vs 85.1% ✅ | **80.6% vs 86.4% 🔴 역전** |
| **150** | 98.2% vs 97.8% ✅ | **92.2% vs 97.4% 🔴 역전** |

designer가 짚은 130 역전이 그대로 재현됐고, **150에서는 더 벌어집니다.**

> ### 🟠 하나 남깁니다 — **150의 여유가 0.4%p입니다**
> 구현값(8.0)에서도 유효 능력치 150은 **베스트11 98.2% vs MVP 97.8%**로 거의 붙어요.
> 지금은 역전이 아니지만 **가장 얇은 자리**입니다. 리그MVP 쪽 문턱(5.5 · 3.5~7.8)이나
> `AXIS_OFF`가 움직이면 여기가 먼저 뒤집힙니다.
> **여섯 번째 냄새는 두 상을 함께 봐야 한다**는 뜻이니, 5-j 재측정 때
> **150 칸을 반드시 포함**해 주세요 (제 하네스는 칭호를 안 켠 값입니다).

---

## 3. 🔴 되돌려 확인 (원칙 ⑩) — **둘 다 아무도 안 잡습니다**

`tests/winger2/*-test.js` 8종 + `smoke`로 돌렸습니다.

| 변이 | 결과 |
|---|---|
| 🚫 **상한 8.0 → 8.5** (130·150에서 MVP와 역전) | 🟢 **아무도 안 잡음** |
| 옛 값으로 통째 복귀 (4.5 · 4.2 · 6.2 — 능력치 90에서 96%) | 🟢 **아무도 안 잡음** |

> ⚠️ 한 번 `league-test`가 **종료 코드 137**로 죽었는데, 같은 변이로 단독 실행하니
> `exit 0`이었습니다 — **자원 경합에 의한 kill이지 빨간불이 아닙니다.**
> (`_load.js`가 크래시를 2로 갈라 주지만 137은 그 밖이에요. 모아 돌릴 때
> `[ $c -ge 2 ]`로 묶으면 *"안 돈 것"*으로 잡힙니다 — 실제로 그렇게 잡혔습니다.)

### inspector에게 — 검사 44의 형태 제안

**상수를 검사에 박지 않고도** 잡히는 형태입니다. 두 문턱을 **소스에서 뜯어** 비교해요:

```js
// career.js에서 리그MVP·베스트11 문턱을 각각 뜯어옵니다
const mvpHi = 7.8;                 // rand(3.5*bar, 7.8*bar)의 상한  ← 소스에서 추출
const bonHi = 8.0;                 // rand(5.5*bar, 8.0*bar)의 상한  ← 소스에서 추출
check(bonHi <= mvpHi + 0.2, "🥈 베스트11 상한이 🏆 리그MVP 상한을 넘지 않는다");
```

- 지금: `8.0 ≤ 8.0` 🟢 · 상한을 8.5로 올리면 즉시 🔴
- **왜 이 형태인가** — 수상률 자체는 커리어 몬테카를로가 필요해 검사에서 못 잽니다
  (`league-test` 33-B가 세운 규칙과 같아요). 대신 **역전의 원인인 문턱 배치**를
  직접 봅니다. `AXIS_OFF`가 움직여도 이 관계는 그대로 유효해요.
- **함께**: 33-B 같은 「유효 조건」 핀에 **5-j 수상률(90 → 48.9%)과 그때의 문턱 셋**을
  적어 두면, 셋 중 하나만 움직여도 *"수상률을 다시 재세요"*가 뜹니다.
- **41b**(만성 공격수 · 전력 62 ≥ 7경기)는 제 59번 §3 실측으로 지금 **8.0**입니다 — 여유 1경기.

---

## 4. 확인한 것

```
node tests/smoke-test.js beta      ✅ beta/winger2
tests/winger2/*-test.js (8종)      ✅ 전부 초록불
```

## 5. 다음

- 🔴 **inspector** — §3의 검사 44 (+ 5-j 유효 조건 핀). **제 쪽 구현은 끝났습니다.**
- 🟠 **balancer** — 5-j 재측정 시 **유효 능력치 150 칸 포함** (§2-③의 0.4%p)
- ✅ **designer** — 이번 범위에서 새 판단 항목은 없습니다
