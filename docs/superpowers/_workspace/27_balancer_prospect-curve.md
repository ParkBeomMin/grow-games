# 실측 — 🎂 나이 축 · 성장타입 · 유망주 3택이 곡선을 흔드나

2026-08-29 · grow-balancer
선행: `26_balancer_minigame-curve.md` · `56_engineer_growth-curve.md` · `57_inspector_prospect-fix.md` · `58_engineer_train-neutral.md`
설계: `13_designer_v2-final.md` §6-2 · §6-4 · §7-1

> ## 판정 요약
>
> | # | 항목 | 판정 |
> |---|---|---|
> | 🔴 1순위 | **게이트 곡선** | ✅ **통과 — 한 점도 안 움직였습니다.** `AXIS_OFF 2.35` 유지 |
> | 🔴 2순위 | **`agePen` 폐기 크기** | 🟠 **크기 보고.** 만 34~38세 리그MVP **0.1% → 57.5%** · 베스트11 **5.5% → 99.6%** — designer 판단 자리 |
> | 🟠 3순위 | **중립 재현** (훈련 총량 · 커리어 면적) | ✅ **통과** — engineer 값 소수점까지 재현 |
> | 🟠 4순위 | **유망주 3택** | ❌ **중립이 아닙니다.** 나이×타입 결합 칸 **벌어짐 20.3%** (목표 ±5%) |
> | 🟡 5순위 | **v1 대조** | 🟡 총 상 **36.2 → 40.2 (+11.0%)** · 시즌당은 **−7.4%** |
> | 🔴 새로 | **🕯️ 은퇴 제안이 뒤집혀 있습니다** | ❌ **만성이 만 19~22세에 은퇴 권유를 받고, 늙어서는 영영 안 받습니다** |
>
> ### 🔴 **선수 설정은 아직 닫을 수 없습니다.**
>
> **곡선은 섭니다 — 계수는 하나도 안 바꿔도 됩니다.** 막는 것은 곡선이 아니라 **나이 칸 세 자리**예요:
>
> 1. 🕯️ **은퇴 제안이 방향을 안 봅니다** (`ageMul < 0.75`가 오르막에서도 참) — 아래 §확정 계수. **이건 제가 값을 드릴 수 있습니다**
> 2. 🪑 **🌳 만성 신인이 데뷔 시즌에 벤치입니다** (출전<15가 **75.9%**) — 구조 문제, designer 자리
> 3. 🎂 **카드 나이가 커리어 길이를 그대로 정합니다** (17세 19시즌 · 19세 17시즌 → 총 상 **12.5%** 차) — 구조 문제, designer 자리

---

## 무엇을 쟀나

**엔진은 진짜를 부릅니다** (`tests/winger2/_load.js`). `beta/winger2/prospect.js`도 **파일째 `new Function`에 태워** 살아 있는 `ageOf`·`curveAt`·`nowStats`·`trainMul`·`trainStep`·`seasonTally`·`suggestRetire`·`rollCards`·`spread`를 그대로 부릅니다 — 산식을 옮겨 적지 않았어요. 엔진 밖(`career.js`·`squad.js`·`game.js`)은 종전대로 정규식으로 뜯었고(`v2/bits.js`), **훈련 산식도 소스에서 뜯었습니다**(`prepAction`의 `gain` 줄 · `doTraining` · `wear` · `WEEKS_PER_CB`).

| 측정 | 표본 |
|---|---|
| 게이트 곡선 (표준 하네스) | 칸당 단독 시즌 **1,500회** × 4포지션 × 5능력치 = 30,000시즌 |
| 게이트 곡선 (칭호 켠 정상 상태) | 칸당 리그 시즌 **450회** |
| 게이트 곡선 (커리어 환경) | 칸당 리그 시즌 **500회** × 4포지션 = 2,000시즌/칸 |
| **커리어 앙상블 v2** | **1,000벌 · 18,016시즌** (유스 36턴 + 시즌당 훈련 78턴 + 리그 38R) |
| **커리어 앙상블 v1** | **1,000벌 · 15,000시즌** (옛 소스 `3baa9ff`에서 뜯은 산식) |
| `agePen` 대조 | **600벌 · 10,800시즌** — 같은 시즌 기록에 두 산식 |
| 유망주 3택 | **판 440벌 × 3장 × 2회 = 2,640커리어** |
| 회귀 (도움÷골·MOM·평점) | 칸당 리그 시즌 200회 × 4포지션 |
| 세 갈래 도움÷골 · 31b | 경로당 **30,000경기** |

### 🕰️ v1 하네스는 **옛 커밋에서 뜯었습니다**

`git show 3baa9ff:beta/winger2/{career,game}.js`를 파일로 떨궈 정규식으로 뜯었어요 —
`CAREER_MAX 15` · `GROW_UNTIL 4` · `DECLINE_FROM 11` · 옛 `yearMod` · 옛 `agePen` ·
옛 결산 성장/노쇠 · 옛 `rollStats` · 옛 hype 줄. **값을 손으로 옮겨 적지 않았습니다.**

### ⚠️ 이 실측이 **고정한 것** (단일 변수)

리그 = 🇰🇷 K리그1 (`bar 1` · `prestige 1`) · 클럽 전력 **62 고정** · **이적·승강 없음** ·
나라 특성 배수 1 · 조작 성공도 `s = 0.522`(26번 실측).
→ 나이 축 하나만 움직이게 하려고요. 이적을 섞으면 무엇이 곡선을 흔들었는지 못 찾습니다.
**절대 수상률은 이 환경의 값입니다** — 실제로는 위 리그로 올라가며 `bar`가 커져 내려갑니다.
**타입 간·판 간 비교(이 문서가 판정에 쓰는 값)는 환경이 같아서 그대로 유효해요.**

### 🐛 제 하네스에서 잡은 결함 하나 (기록)

유스 해넘이를 `if (컨디션<40) { 쉬기; continue; }` **뒤에** 두어, 쉬는 달이 12월이면
나이를 안 먹었습니다 → 데뷔 나이가 흔들렸어요. 게임은 `advanceMonth`에서 행동과
무관하게 해넘이를 하니 **하네스가 틀린 것**입니다. 고치고 **전부 다시 돌렸습니다**
(이 문서의 숫자는 전부 고친 뒤 값이에요).

---

## 중립 검증

> **여기가 빨간불이면 아래는 볼 것도 없습니다.** 셋 다 초록입니다.

### ① 🧮 훈련 총량 — engineer 58번 §2 재현 (목표 ±3%)

`P.trainMul({age, growthType})` **살아 있는 함수**를 불렀습니다.

| 창 | 🌱 조숙 | 🌿 보통 | 🌳 만성 | 최대 편차 |
|---|---|---|---|---|
| 역산 창 (20~37세) | 100.0 | 100.0 | 100.0 | **0.01%** ✅ |
| 면적 창 (20~38세) | 100.7 | 100.0 | 99.5 | 0.64% ✅ |
| 데뷔 21세 (21~38) | 99.8 | 100.0 | 100.8 | 0.61% ✅ |
| **데뷔 22세 (22~38)** | 98.7 | 100.0 | 102.4 | **2.02%** ✅ (여유 0.98%p) |
| 카드 17/18/19 균등 평균 | 99.8 | 100.0 | 100.8 | 0.61% ✅ |

**engineer 값과 소수 둘째 자리까지 같습니다.** 되돌림 대조도 재현됩니다 —
`TRAIN_NEUTRAL`을 지우면 **81.5 / 100.0 / 119.2 (18.90%)** ❌.
그리고 상수가 계단의 **종속값**인 것도 확인: 역산 `1.2262 / 1.0000 / 0.8390` ↔ 소스 `1.226 / 1.000 / 0.839`.

### ② 📐 커리어 총 면적 — **데뷔~38세** (목표 ±8%)

`P.curveAt(g, a, 0)`를 시즌 단위로 적분했습니다.

| 창 | 🌱 조숙 | 🌿 보통 | 🌳 만성 | 최대 편차 |
|---|---|---|---|---|
| 데뷔 20세 (카드 17) ~38세 | 98.8 | 100.0 | 97.2 | 1.46% ✅ |
| 데뷔 21세 (카드 18) ~38세 | 97.9 | 100.0 | 98.0 | 1.40% ✅ |
| 데뷔 22세 (카드 19) ~38세 | 96.9 | 100.0 | 98.8 | 1.67% ✅ |
| **카드 균등 평균** | **97.9** | **100.0** | **98.0** | **1.40%** ✅ |

**engineer 값과 정확히 같습니다.** 창을 「은퇴 제안까지」로 끊으면 조숙이 −9.7%로 벌어지는 것도 재현됐어요 — designer 판정(*"제안은 중립 대상이 아니다"*)이 숫자로 다시 확인됩니다.

### ③ 🆕 **제가 새로 연 자리** — 카드 편중이 「평균 x축」을 무너뜨리나

여태 곡선은 `statsOf(a)`(여섯 칸 전부 같은 값)로 쟀습니다. 그런데 v2는
`overall()`이 **여섯 칸 평균 × 곡선**이고, 엔진 `blendOf`는 **포지션 가중 3칸**이에요
(`fw: shoot .60 / speed .25 / dribble .15`). 유망주 카드는 편중이 일부러 들어가 있으니
(`SHAPE_W 1.35 · POS_W 1.25 · FOCUS_W 1.30`) **평균은 같은데 blend가 다른** 카드가 나옵니다.

**커리어가 실제로 만드는 벡터**로 쟀습니다 (스케일 조작 없이 시뮬레이터 출력 그대로):

| 포지션 | 표본 | 상대 편중 (표준편차 ÷ 평균) |
|---|---|---|
| fw / wg / mf / df | 각 1,100여 | **1.7 ~ 1.8%** |

| 평균 | 평평한 벡터 | 커리어 벡터 | 차 |
|---|---|---|---|
| 70 | 5.2% | 7.3% | +2.0%p |
| 90 | 20.8% | 20.9% | **+0.1%p** ✅ |
| 110 | 54.3% | 55.3% | +1.0%p ✅ |
| 122 | 75.0% | 75.9% | +1.0%p ✅ |
| 130 | 86.1% | 83.7% | −2.4%p ✅ |

**훈련이 여섯 칸에 고르게 더해져서 편중이 상대적으로 1.7%까지 납작해집니다.**
그래서 **평평한 벡터로 잰 곡선이 그대로 유효합니다** ✅ — 지금까지의 모든 곡선 측정이 섭니다.

> 🟡 **다만 「고르게 훈련한다」가 전제입니다.** 반대쪽 끝(주 스탯 하나만 상한까지, 나머지 방치)을
> 재 보면 같은 **평균 90**에서 리그MVP가 **20.6% → 56.8% (+36.2%p)**예요.
> `overall()`은 여섯 칸 평균인데 엔진은 3칸만 보고, 그중 첫 칸이 0.60이라 그렇습니다.
> **나이 축이 만든 문제가 아닙니다** — v1에도 있던 자리예요(`overall()`의 정의가 그대로입니다).
> 다만 나이 축이 `overall()`의 짐을 키웠으니(선발 경쟁·클래스·이적·전당이 전부 이 값)
> 같이 봐 둘 값입니다. **몰빵은 생산량을 올리는 대신 `overall()`을 내려 선발에서 불리해집니다** —
> 지금은 그게 유일한 반대 압력이에요.

---

## 🔴 1순위 — 게이트 곡선이 여전히 서는가 → ✅ **섭니다**

### ⓐ 기준선이 **한 점도 안 움직입니다**

계수는 디스크 그대로(`AXIS_OFF 2.35` · `POS_AXIS.n` 1.072/0.967/0.873/0.794 · `NPC_SPOT` 3.05/6.50/4.90).

| | 70 | 90 | 110 | 130 | 150 |
|---|---|---|---|---|---|
| 25번 기록 (`s=0.5` · 버프 없음) | 4.4% | 19.3% | 52.3% | 83.8% | 96.6% |
| **지금 디스크 (`s=0.5` · 버프 없음)** | **4.4%** | **19.3%** | **52.3%** | **83.8%** | **96.6%** |
| 밴드 | 0~8 ✅ | 8~25 ✅ | 45~72 ✅ | 78~95 ✅ | 90~100 ✅ |

포지션 격차 0.6~4.9%p (문턱 12%p) ✅.

**왜 안 움직이나** — 나이 축은 엔진에 들어가는 스탯 벡터를 **균일하게 곱할 뿐**이고,
엔진·`posAxis`·`POS_AXIS`·`AXIS_K`·`AXIS_OFF`·부문상 판정은 이번 커밋에서 한 줄도 안 바뀌었습니다
(`git show bfed587 --stat`: `engine.js`·`squad.js` 없음). hype 줄에서 사라진 건 `agePen` 하나인데,
**그건 `proYear ≤ 10`에서 늘 0**이었고 이전 곡선 측정은 전부 `proYear 5`였어요.

### ⓑ 체감 곡선 (수동 `s=0.522` + 🎖️ 칭호)

| `AXIS_OFF` | 70 | 90 | 110 | 130 | 150 | 밴드 |
|---|---|---|---|---|---|---|
| 2.25 | 6.6% | 26.9% | 64.0% | 92.6% | 98.9% | ❌ 90 |
| **2.35 (현행)** | **5.4%** | **23.1%** | **58.0%** | **89.8%** | **98.3%** | ✅ |
| 2.45 | 4.4% | 19.7% | 51.9% | 86.3% | 97.4% | ✅ |

26번 값(5.6 / 22.2 / 58.8 / 89.2 / 98.7)과 표본 잡음 안입니다. **150에서도 오릅니다**(89.8 → 98.3).

### ⓒ 🔑 **나이별 실측이 그 곡선 위에 그대로 놓입니다**

커리어 앙상블(1,000벌 · 18,016시즌)의 나이별 실측을, **같은 리그 환경에서 다시 잰 곡선**과 나란히.
능력치가 상한(130)에 붙은 뒤(만 24세~)에는 여섯 칸이 전부 130이라 **평균 = blend**여서 대조가 정확합니다.

| 나이 | 지금 실력 (= 정점 130 × 곡선) | **실측 리그MVP** | **곡선이 예측** | 차 |
|---|---|---|---|---|
| 만 26세 | 122.9 | **84.5%** | 83.6% (122) | +0.9%p |
| 만 30세 | 123.3 | **85.7%** | ~84% | +1.7%p |
| 만 34세 | 114.2 | **71.1%** | 70.1% (110↔122 보간) | +1.0%p |
| 만 36세 | 106.7 | **55.1%** | 56.8% (98↔110 보간) | −1.7%p |
| 만 38세 | 97.5 | **42.2%** | 39.2% (98) | +3.0%p |

**커리어 전 구간이 곡선의 ±3%p 안입니다.** 나이 축은 **곡선을 바꾸지 않고, 선수를 곡선 위에서 움직입니다.**
설계가 의도한 그대로예요 (*"곡선이 그 일을 합니다"*).

> ### ✅ **`AXIS_OFF`는 2.35 그대로입니다. 역산할 것이 없습니다.**

---

## 🔴 2순위 — `agePen` 폐기의 크기 (실측 5-h)

**같은 시즌 기록(act)에 두 산식을 나란히 굴렸습니다.** 옛 산식은 지금 소스의 hype 줄에
`- agePen`을 도로 끼워 만들었고(값 복사 없음), **되살리기가 실제로 먹었는지 먼저 확인**했습니다
(`proYear 15`에서 hype 8.44 → 4.44, 벌점 4.0 ✅ — 안 먹으면 그 자리에서 멈추게 해 뒀어요).

커리어 600벌 · 10,800시즌:

| 나이 | 평균 벌점 | hype 평균 | **리그MVP 지금** | **옛 벌점 있을 때** | 차 | 베스트11 (지금 → 옛) |
|---|---|---|---|---|---|---|
| 만 20~29세 | 0.00 | 3.2~7.9 | 4.8 ~ 87.7% | 4.3 ~ 85.5% | **±2%p (잡음)** | 같음 |
| 만 30세 | 0.25 | 7.89 | 83.2% | 71.0% | **+12.2%p** | 100.0% → 99.7% |
| 만 31세 | 0.77 | 7.89 | 85.3% | 47.8% | **+37.5%p** | 100.0% → 98.2% |
| 만 32세 | 1.57 | 7.81 | 80.7% | 18.0% | **+62.7%p** | 100.0% → 85.2% |
| 만 33세 | 2.37 | 7.73 | 76.3% | 4.5% | **+71.8%p** | 100.0% → 58.7% |
| **만 34세** | 3.17 | 7.62 | **72.5%** | **0.5%** | **+72.0%p** | 100.0% → 22.5% |
| **만 35세** | 3.97 | 7.52 | **68.2%** | **0.0%** | **+68.2%p** | 99.2% → 5.2% |
| **만 36세** | 4.77 | 7.37 | **56.7%** | **0.0%** | **+56.7%p** | 99.8% → 0.0% |
| **만 37세** | 5.57 | 7.22 | **47.5%** | **0.0%** | **+47.5%p** | 99.7% → 0.0% |
| **만 38세** | 6.37 | 7.06 | **42.7%** | **0.0%** | **+42.7%p** | 99.5% → 0.0% |

> ### 🔴 **만 34~38세 묶음 — 리그MVP 0.1% → 57.5% · 베스트11 5.5% → 99.6%**
> 축 상(신인왕·MVP·베스트11·발롱도르) 커리어 총계 **19.19 → 29.22 (+52.3%)**

### 판정 — **방향은 옳고, 크기는 designer가 봐야 합니다**

- ✅ **나이를 두 번 세지 않는다**는 판정은 맞습니다. 그리고 **곡선이 실제로 일을 합니다** —
  만 26세 84.5% → 만 38세 42.2%로 **절반으로 내려갑니다.** 그건 감쇠가 맞아요.
- 🔴 **다만 「끝나는 느낌」이 사라졌습니다.** v1에서는 `proYear 13`부터 MVP가 3.1%로 사실상 은퇴
  선언이었어요. v2에서는 **만 38세에도 리그MVP 42.2% · 베스트11 98.0%**입니다.
  17시즌 연속 베스트11이에요.
- 🔑 **문제는 MVP가 아니라 베스트11입니다.** 문턱이 `hype ≥ 4.5·bar` · `posBar = rand(4.2, 6.2)·bar`인데
  만 38세의 hype 평균이 **7.06**이라 상한(6.2)보다도 위예요. **유효 능력치 95 언저리부터 베스트11은
  사실상 자동**입니다(커리어 환경 곡선 표: 90에서 이미 99.9%). 나이가 아니라 **베스트11 문턱이
  포화**된 겁니다 — v1에서는 `agePen`이 그걸 가리고 있었어요.
- ⚠️ **이 환경(K리그1 · 클럽 62 고정)의 값입니다.** 실제로는 위 리그로 올라가며 `bar`가 커져
  내려갑니다. 그래도 **v1 대비 차이(0.1% → 57.5%)는 환경과 무관**해요 — 같은 기록에 두 산식이니까요.

> **balancer 의견**: `agePen`을 되살리는 건 반대합니다(같은 축을 두 번 세는 게 맞아요).
> 손댈 자리가 있다면 **`agePen`이 아니라 베스트11 문턱**입니다 — 그건 나이와 무관하게
> 능력치 90에서 이미 99.9%예요. **다만 그건 ①-G 게이트를 다시 여는 일이라 이 실측의 범위 밖입니다.**
> designer 판단으로 되돌립니다.

---

## 🟠 4순위 — 유망주 3택의 총합 (실측 5-a) → ❌ **중립이 아닙니다**

**같은 판에서 뽑힌 세 장**을 각각 끝까지 굴렸습니다(판 440벌 × 3장 × 2회 반복 = 2,640커리어).
카드마다 다른 판을 쓰면 판 운이 섞여 "세 장이 같은가"를 못 봅니다.

### ⓐ 칸 위치는 중립 ✅

| | 총 상 | 전당점 |
|---|---|---|
| 1번째 칸 | 41.34 | 71.5 |
| 2번째 칸 | 41.23 | 71.3 |
| 3번째 칸 | 41.03 | 71.2 |
| **편차** | **0.41%** ✅ | **0.23%** ✅ |

`rollCards`의 셔플이 제 일을 합니다.

### ⓑ 🔴 나이 × 성장타입 **결합 칸이 20.3% 벌어집니다** (목표 ±5%)

| 카드 | 총 상 | 평균 대비 | 전당점 | 커리어 시즌 |
|---|---|---|---|---|
| **17세 🌿 보통** | **44.32** | **+10.5%** | 76.8 | 19.0 |
| 17세 🌱 조숙 | 44.01 | +9.8% | 75.8 | 19.0 |
| 18세 🌿 보통 | 40.76 | +1.6% | 71.3 | 18.0 |
| 18세 🌱 조숙 | 39.82 | −0.7% | 68.6 | 18.0 |
| 17세 🌳 만성 | 39.61 | −1.2% | 68.5 | 19.0 |
| 19세 🌿 보통 | 39.53 | −1.4% | 68.4 | 17.0 |
| 18세 🌳 만성 | 38.69 | −3.5% | 67.3 | 18.0 |
| 19세 🌳 만성 | 37.34 | −6.9% | 65.0 | 17.0 |
| **19세 🌱 조숙** | **36.84** | **−8.1%** | 63.1 | 17.0 |

**최고 ÷ 최저 = 1.203.** 두 축으로 갈라 보면:

| 축 | 값 | 벌어짐 |
|---|---|---|
| 🎂 **카드 나이** | 17세 **42.65** · 18세 **39.76** · 19세 **37.90** | **12.5%** ❌ |
| 📈 **성장타입** | 보통 41.97 · 조숙 40.19 · 만성 38.64 | **8.6%** (평균 대비 만성 −4.0%) 🟡 |

- 🎂 **나이는 순수한 상향/하향입니다.** 카드 나이가 데뷔 나이를 정하고, 데뷔 나이가
  **커리어 길이를 그대로 정합니다**(17세 → 19시즌 · 19세 → 17시즌). 곡선 면적은 중립인데
  **시즌 수가 중립이 아니에요.** 카드가 약속하는 *"시간이 지나면 같아져요"*는 **능력치**를
  같게 만들지 **커리어 길이**를 같게 만들지 않습니다.
- 📈 성장타입은 ±5% 안이지만(만성 −4.0%) **여유가 1%p**입니다. 그리고 그 −4.0%의 원인은
  곡선 면적이 아니라 **데뷔 벤치**예요 (아래 §새로 발견).

### ⓒ 잡음 대조군 — 플레이어가 한 판에서 알아챌 수 있나

**같은 카드를 다른 시드로** 굴린 폭과 비교했습니다.

| | 총 상 최대−최소 |
|---|---|
| 같은 카드, 다른 시드 (= 순수 잡음) | **4.6개** |
| 한 판 안 세 장 | **5.7개** |
| → 카드 차이가 설명하는 몫 | **19%** |

**한 번 해서는 못 알아챕니다.** 다만 기댓값으로는 늘 유리한 쪽이 있어요:

| 고르는 법 | 커리어당 총 상 | 무작위 대비 |
|---|---|---|
| 무작위로 한 장 | 40.01 | — |
| 🎂 **가장 어린 카드** | 41.39 | **+3.5%** |
| 🎂 + 🗣️ 어리고 「기본기부터 차근히」 아닌 카드 | **41.72** | **+4.3%** |
| (참고) 결과를 알고 최고를 고르면 | 42.85 | +7.1% |

**"가장 어린 카드를 고른다"가 항상 통하는 규칙입니다.** 크기는 +3.5%로 작지만 **늘 같은 방향**이에요.

스카우트 코멘트는 설계대로 흐릿합니다 (한 타입을 완전히 배제만 합니다):

| 코멘트 | 조숙 | 보통 | 만성 |
|---|---|---|---|
| 「또래보다 몸이 빨리 여물었다는 평이에요」 | 69% | 31% | **0%** |
| 「기본기부터 차근히 쌓는 스타일이래요」 | **0%** | 26% | 74% |
| 「코치들 사이에 평이 갈려요」 | 32% | 33% | 35% |

### ⓓ 🔧 **반사실 — 세 장을 같은 나이에 데뷔시키면**

유스 기간을 3년 고정에서 **「만 20세가 될 때까지」**로 바꾼 판(17세 카드 3년 · 19세 카드 1년)을
같은 시드로 다시 굴렸습니다. 유스 훈련을 그만큼 덜 받으니 **정점 기준값이 낮아지는 반대급부**가 생겨요.

| | 지금 (유스 3년 고정) | 반사실 (데뷔 나이 20세 고정) |
|---|---|---|
| 결합 칸 벌어짐 | **20.3%** ❌ | **12.7%** |
| 🎂 카드 나이 축 | 17세 +6.4% · 19세 −5.5% (**12.5%**) ❌ | 17세 +1.7% · 18세 −1.5% · 19세 −0.2% (**1.7%**) ✅ |
| 📈 성장타입 축 | 만성 −4.0% 🟡 | 만성 **−5.8%** ❌ |
| 「가장 어린 카드」 이득 | +3.5% | **+0.4%** ✅ |
| 정보로 고르는 이득 | +4.3% | **+1.2%** ✅ |

**나이 축은 깨끗이 중립이 됩니다.** 남는 12.7%는 성장타입(만성 −5.8%)과 잡음이에요 —
그건 아래 §데뷔 벤치가 원인이라 따로 풀어야 합니다.

> ⚠️ **이건 후보지 확정 계수가 아닙니다.** 유스 기간을 카드 나이에 묶는 건 설계 변경이에요
> (유스 1년만 뛰는 카드는 유스 리그·연말 평가전을 거의 못 봅니다). **designer 자리입니다.**
> 제가 드리는 건 *"그렇게 하면 나이 편차가 12.5% → 1.7%가 된다"*는 숫자예요.

---

## 🔴 새로 발견 — 나이 칸이 **두 자리에서 뒤집혀 있습니다**

> 둘 다 **진짜 페이지를 JSDOM에 띄워** 게임 코드로 확인했습니다 (`bootPage` + 픽스처 세이브를
> 실제 버튼으로 열기 — 시뮬레이터가 아닙니다).

### ⓐ 🕯️ **은퇴 제안이 오르막에서 뜹니다**

```js
const suggestRetire = (st) =>
  !mustRetire(st) && ageMul(st) < RETIRE_CURVE && (st && st.lowApps || 0) >= LOW_RUN;
```

`ageMul(st) < 0.75`는 **곡선이 0.75 밑이면 참**입니다 — 정점 **앞**이든 뒤든요.
그런데 🌳 만성의 곡선은 **만 22세까지 0.75 밑**이에요(19세 0.605 · 20세 0.650 · 21세 0.695 · 22세 0.740).

진짜 페이지에서 `lowApps = 2`(출전<15가 두 시즌 연속)로 두고 부른 결과:

| | 만 19 | 20 | 21 | 22 | 23 | … | 33 | 34 | 35 | 36 | 37 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 🌱 조숙 | · | · | · | · | · | | · | · | **🕯️** | **🕯️** | **🕯️** |
| 🌿 보통 | **🕯️** | · | · | · | · | | · | · | · | · | **🕯️** |
| 🌳 만성 | **🕯️** | **🕯️** | **🕯️** | **🕯️** | · | | · | · | · | · | · |

> ### 🔴 **🌳 만성은 스무 살에 은퇴 권유를 받고, 서른일곱에는 안 받습니다.**
> 화면 문구: `🕯️ 만 21세 · 출전이 줄었어요 — 은퇴를 생각해 볼 때입니다 (더 뛰어도 돼요)`
> (`career.js:2769`)

커리어 앙상블에서 **만성 커리어의 5%**에 실제로 떴고, **처음 뜬 나이는 만 21세**입니다.
그리고 designer가 §6-4에 적은 목표(*"조숙 34 · 보통 36 · 만성 37세"*)는 **만성이 미달**이에요 —
만성 37세 곡선이 **0.760**으로 문턱 0.75를 아슬하게 못 넘습니다(38세는 강제 은퇴라 제안 자리가 없어요).

### ⓑ 🪑 **🌳 만성 신인이 데뷔 시즌에 벤치입니다**

`meRow().str = overall()`이고(`squad.js:206`), `overall()`은 이제 **정점 기준값 × 곡선**입니다.
데뷔 클럽은 리그 하위 3팀(K리그1이면 전력 52·57·62)이라 동료가 그 폭(±14)으로 흩어져요.

커리어 앙상블 1,000벌의 데뷔 시즌:

| 데뷔 | 정점 기준값 | **지금 실력** | 출전 | **출전<15인 비율** |
|---|---|---|---|---|
| 만 20세 🌱 조숙 | 80.6 | **74.2** | 36.9/38 | **0.8%** |
| 만 20세 🌿 보통 | 77.6 | **61.3** | 28.3/38 | **21.6%** |
| **만 20세 🌳 만성** | 72.3 | **47.0** | **9.8/38** | **75.9%** ❌ |
| 만 21세 🌳 만성 | 71.9 | 50.0 | 12.5/38 | 60.9% ❌ |
| 만 22세 🌳 만성 | 72.6 | 53.7 | 17.4/38 | 50.8% 🟡 |

진짜 페이지에서도 같은 그림이 나옵니다 (픽스처 세이브 · 정점 기준값 평균 49.4 · 클럽 전력 52):

| 타입 | 만 20세 | 만 21세 | 만 22세 |
|---|---|---|---|
| 🌱 조숙 | 종합 **45.5** | 47.5 | 49.4 |
| 🌿 보통 | 종합 **39.1** | 41.3 | 43.5 |
| 🌳 만성 | 종합 **32.1** | 34.4 | 36.6 |

**설계는 *"만성은 초반 시즌을 버려야 한다"*고 적었는데, 게임은 「버티는 것」이 아니라
「출전 0」으로 처리합니다.** 그러면 —

- 실전 성장·부문상·MOM·명성이 **통째로 막히고**
- `lowApps`가 두 번 쌓여 **ⓐ의 은퇴 권유**가 뜹니다

**두 결함이 같은 뿌리에서 만납니다.** 그리고 이게 성장타입 편차(만성 −4.0%)의 원인이에요 —
곡선 면적은 1.40%로 중립인데 **데뷔 벤치가 그 위에 얹혀** 있습니다.

### ⓒ 🟡 「전성기가 지났어요」가 커리어의 대부분을 덮습니다

`career.js:2769`의 세 번째 갈래는 `myAge > peakAgeOf(S)`입니다.

| 타입 | 정점 | 이 문구가 뜨는 시즌 |
|---|---|---|
| 🌱 조숙 | 22세 | **만 23세부터 = 커리어의 16/19시즌** (정점 이동 전 기준) |
| 🌿 보통 | 26세 | 만 27세부터 = 12/18시즌 |
| 🌳 만성 | 30세 | 만 31세부터 = 8/17시즌 |

조숙 카드는 **데뷔 두 번째 시즌부터 은퇴할 때까지 매 결산에 "몸이 예전 같지 않습니다"**를 봅니다.
그 시즌에 리그MVP를 받아도요. **밸런스는 아니고 director/designer 자리**라 기록만 남깁니다.

---

## 🟡 5순위 — v1 대조 (실측 5-g)

같은 리그 환경 · 같은 시드 계열 · 각 1,000벌.

| | v1 (15시즌 · 정체 있음 · `agePen`) | **v2 (나이 축)** | 차 |
|---|---|---|---|
| 커리어당 시즌 | 15.00 | **18.02** | **+20.1%** |
| **커리어당 총 상** | **36.21** | **40.21** | **+11.0%** |
| **시즌당 상** | **2.41** | **2.23** | **−7.4%** |
| 총 상 분포 중앙값 | 약 38개 | 약 40개 | +2개 |

**커리어 모양은 완전히 달라졌습니다:**

| | v1 | v2 |
|---|---|---|
| 최고점 | 5~9시즌 MVP **92%** (능력치 130 고정) | 만 27~30세 MVP **85~88%** (유효 123~124) |
| 정체 구간 | **5~10시즌이 통째로 평평** (전부 능력치 130) | **없음** — 매 시즌 곡선이 움직입니다 |
| 마무리 | 13~15시즌 MVP **3.1 → 0.2 → 0.0%** (절벽) | 만 36~38세 **55.1 → 51.1 → 42.2%** (완만) |

> ### 판정 🟡 — **+11.0%는 「크게 벌어진다」로 안 봅니다. 다만 전당 정규화는 아직 열려 있어요.**
>
> designer의 예상(*"곡선 면적을 맞추는 한 총 상도 비슷해진다"*)이 **대체로 맞았습니다** —
> 시즌이 20% 길어졌는데 총 상은 11%만 늘었어요. **시즌당으로는 오히려 7.4% 줄었습니다.**
>
> ⚠️ **다만 이건 「⚽ 더 윙어 II의 나이 축 전/후」 대조입니다.** 명예의 전당은 `grow-hof-v1`으로
> **8종이 공유하는 표**라, 진짜로 봐야 할 대조는 **⚽ 더 윙어(`beta/soccer`) vs ⚽ 더 윙어 II**예요.
> 그건 이 실측의 범위 밖입니다 — **다음 실측 항목으로 남깁니다.**

---

## 🟡 회귀 — 여유가 얇은 자리

**`engine.js`·`squad.js`는 이번 커밋에서 한 줄도 안 바뀌었습니다**(`git show bfed587 --stat` 확인).
그래도 나이 축이 만드는 유효 능력치 구간(**98~130**) 전체에서 다시 쟀습니다.

| 항목 | 유효 98 | 유효 110 | 유효 122 | 유효 130 | 문턱 |
|---|---|---|---|---|---|
| 리그 도움 ÷ 골 | 0.751 | **0.753** | 0.752 | 0.753 | 0.70~0.80 ✅ |
| 🏅 MOM 비율 (`MOM_MIN 8.40`) | 5.2% | **6.8%** | 8.5% | 10.1% | 5~8% 🟡 |
| 시즌 평균 평점 (최대 = mf) | 7.09 | **7.20** | 7.30 | 7.39 | 7.0~7.2 🟡 |

- 🅰️ **도움÷골은 구간 전체에서 0.751~0.753** — 26번 값(0.753)과 같습니다 ✅
- 🏅 **MOM은 능력치를 따라 올라갑니다.** 110에서 6.8%(26번 6.2%, 클럽 환경 차)이고 130에서 10.1%예요.
  **나이 축이 나쁘게 만든 자리가 아닙니다** — v1은 능력치 130에 **15시즌 중 12시즌**을 붙어 있었고,
  v2는 곡선이 깎아 유효 122~125에 **8시즌**만 머뭅니다. **v2가 더 낫습니다.**
- 📊 평점은 26번 그대로 `s ≥ 0.55`에서 mf가 7.2를 넘는 자리인데, 유효 122 이상에서도 넘어요.
  26번의 🟡가 그대로 남습니다.

| 항목 | 값 | 문턱 |
|---|---|---|
| 🅰️ **세 갈래 도움÷골** — 대등 70:70 | ② 0.750 · ③ 0.768 (차 2.43%) | ≤3% ✅ |
| 〃 최대 전력차 78:52 | ② 0.705 · ③ 0.721 (차 **2.27%**) | ≤3% ✅ |
| 🅰️ **33-D1 도움 축 mf ÷ 2위** (검사 그대로) | **2.44배** (mf 57.3% · 2위 wg 23.5%) | ≥2.0 ✅ |
| 🛡️ **31b 실점 비대칭** — 대칭 명단 시드 넷 | −0.15 / +0.58 / +3.41 / −1.84% | ±15% ✅ |
| 🔥 `CLUTCH 1.25` | `engine.js:74` **안 바뀜** (커밋에 파일 없음) | — |
| ✅ `tests/winger2/` 8파일 | **초록 270줄 · 빨간불 0 · 죽음 0** | — |

> 🔴 **31b는 26번의 판단(승격 금지)이 그대로입니다.** 제 하네스는 대칭 명단이라 ±3.4%로 나오는데,
> 26번이 −15.9%를 잰 건 **inspector 픽스처의 특정 단면**이에요. 같은 코드가 조건에 따라 통과도
> 실패도 한다는 26번의 결론을 뒤집는 값이 아닙니다.

### 🗄️ 옛 세이브 — 무너지지는 않지만 **종합이 최대 21% 내려갑니다**

`age`·`growthType`·`peakShift`·`lowApps`·`hotRun`이 전부 `undefined`인 세이브를 살아 있는 함수에 넣었습니다.

| 안전성 | 결과 |
|---|---|
| 없는 스탯 칸(`speed: undefined`) | ✅ **나머지 평균**으로 채워집니다 (`|| 0` 아님) |
| `stats`가 통째로 없는 세이브 | ✅ 기본 40 × 곡선 |
| `typeOf({})` · `peakAgeOf({})` · `ageMul({peakShift: undefined})` | ✅ 보통 · 26 · 0.700 |
| `traitsOf({})` · `flawOf({})` | ✅ `[]` · `null` |
| `trainMul` · `mustRetire` · `suggestRetire` | ✅ 전부 통과 |

**하지만 밸런스는 움직입니다** (능력치 120 · `year 3` 세이브 기준):

| `proYear` | 되짚은 나이 | 곡선 | **종합 v1 → v2** | 차 | 남은 시즌 v1 → v2 |
|---|---|---|---|---|---|
| 1 | 21 | 0.835 | 120.0 → **100.2** | **−16.5%** | 14 → **17** |
| 5 | 25 | 0.970 | 120.0 → 116.4 | −3.0% | 10 → 13 |
| 10 | 30 | 0.930 | 120.0 → 111.6 | −7.0% | 5 → **10** |
| 14 | 34 | 0.820 | 120.0 → 98.4 | −18.0% | 1 → 4 |
| **15** | **35** | 0.790 | 120.0 → **94.8** | **−21.0%** | **0 → 3** |

- 🟡 **진행 중인 캐릭터가 종합 3~21%를 잃습니다.** 최악은 신인(−16.5%)과 노장(−21.0%)이에요.
  56번 §5-ⓐ의 `league-test 32-0`(픽스처가 벤치가 됨)이 정확히 이 자리입니다.
- ✅ **대신 시즌을 3~5개 더 받습니다** (v1은 `proYear ≥ 15` 강제 은퇴, v2는 만 38세).
  15시즌짜리 세이브는 **끝난 줄 알았던 커리어가 3시즌 더 열립니다.**
- **마이그레이션은 하면 안 되고(규칙), 읽는 쪽 기본값도 이미 중립(되짚기)입니다.**
  이건 결함이 아니라 **설계된 밸런스 변경**이에요 — 다만 진행 중인 세이브에 이만큼 걸린다는 걸
  기록해 둡니다. `year`가 1인 세이브(유스를 안 거친 옛 형태)는 나이가 두 살 어리게 되짚여
  오히려 유리해집니다(−15.3%, 남은 시즌 5).

---

## 판정

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 중립 | 훈련 총량 (±3%) | ✅ **통과** | 0.01~2.02%. 최악은 데뷔 22세 2.02% (여유 0.98%p) |
| 중립 | 커리어 총 면적 데뷔~38세 (±8%) | ✅ **통과** | 97.9 / 100.0 / 98.0 — **1.40%** |
| 중립 | 🆕 카드 편중이 「평균 x축」을 무너뜨리나 | ✅ **통과** | 커리어 벡터의 상대 편중 **1.7%** → 곡선 차 ≤2.4%p |
| 1순위 | 게이트 곡선 기준선 | ✅ **통과** | **4.4 / 19.3 / 52.3 / 83.8 / 96.6** — 25번과 동일 |
| 1순위 | 체감 곡선 (`s=0.522` + 칭호) | ✅ **통과** | **5.4 / 23.1 / 58.0 / 89.8 / 98.3** — 다섯 점 밴드 안 |
| 1순위 | **나이별 실측이 곡선 위에 놓이나** | ✅ **통과** | 만 26~38세 전 구간 **곡선의 ±3%p 안** |
| 2순위 | `agePen` 폐기 크기 | 🟠 **크기 보고** | 만 34~38세 MVP **0.1% → 57.5%** · 베스트11 **5.5% → 99.6%** |
| 4순위 | 3택 — 칸 위치 | ✅ **통과** | 0.41% |
| 4순위 | 3택 — **나이 × 타입** | ❌ **중립 아님** | 결합 칸 **20.3%** · 나이 축 **12.5%** (목표 ±5%) |
| 5순위 | v1 대조 | 🟡 **허용 범위** | 총 상 +11.0% · 시즌당 −7.4% |
| 새로 | 🕯️ **은퇴 제안 방향** | ❌ **뒤집혀 있습니다** | 만성 만 19~22세에 뜨고 늙어서는 안 뜸 |
| 새로 | 🪑 **만성 데뷔 벤치** | ❌ | 출전<15가 **75.9%** · 지금 실력 47.0 vs 클럽 62 |
| 회귀 | 도움÷골 · 33-D1 · 31b · 검사 273 | ✅ **통과** | 0.753 · 2.44배 · ±3.4% · 빨간불 0 |
| 회귀 | `MOM_MIN 8.40` · 평점 | 🟡 **26번 그대로** | 능력치를 따라 오르는 자리. **v2가 v1보다 낫습니다** |
| 회귀 | 옛 세이브 | ✅ **안 무너집니다** · 🟡 종합 −3~21% | 없는 칸 전부 안전. 시즌은 3~5개 늘어남 |

> # 🔴 **선수 설정은 아직 닫을 수 없습니다**
>
> **곡선 때문이 아닙니다 — 곡선은 한 점도 안 움직였고 계수는 그대로 두면 됩니다.**
> 막는 것은 **나이 칸 세 자리**예요:
>
> | # | 무엇 | 누구 자리 |
> |---|---|---|
> | 1 | 🕯️ 은퇴 제안이 오르막에서 뜸 (만성 만 21세) | **balancer가 값을 드립니다 ↓** |
> | 2 | 🪑 만성 신인이 데뷔 시즌 벤치 (75.9%) | **designer** — 구조 |
> | 3 | 🎂 카드 나이가 커리어 길이를 정함 (총 상 12.5% 차) | **designer** — 구조 |
>
> 2·3은 계수로 못 풉니다. **계수를 억지로 짜내지 않고 designer에게 되돌립니다.**

---

## 확정 계수

### ✅ 그대로 두는 것

| 상수 | 값 | 파일:줄 | 이번 실측에서 |
|---|---|---|---|
| `AXIS_OFF` | **2.35** | `career.js:108` | **유지** — 다섯 점 전부 밴드 안. 역산할 것 없음 |
| `POS_AXIS.n` | fw 1.072 · wg 0.967 · mf 0.873 · df 0.794 | `career.js:77` | 유지 — 포지션 격차 0.6~4.9%p |
| `TRAIN_NEUTRAL` | `{early:1.226, normal:1.000, late:0.839}` | `prospect.js:304` | **유지** — 훈련 총량 0.01~2.02% ✅ |
| `GROWTH_TYPES` 앵커 | 설계표 그대로 | `prospect.js:63-70` | **유지** — 면적 1.40% ✅ |
| `MOM_MIN` · `CLUTCH` · `NPC_SPOT` · `ASSIST_P2` · `half(a)` | 전부 | — | 유지 — 엔진 미변경 |
| `POOL` | 194 | `prospect.js:147` | 유지 — 총합 중립은 inspector가 굳혔고 재현됩니다 |

### 🔴 바꿔야 하는 것 — **🕯️ 은퇴 제안**

**두 값이 한 벌입니다. 따로 넣을 수 없습니다.**

| # | 자리 | 지금 | **바꿀 값** |
|---|---|---|---|
| ⓐ | `RETIRE_CURVE` (`prospect.js:137`) | `0.75` | **`0.78`** |
| ⓑ | `suggestRetire` (`prospect.js:349-350`) | `!mustRetire(st) && ageMul(st) < RETIRE_CURVE && …` | **`ageOf(st) > peakAgeOf(st) &&`를 AND로 추가** |

```js
/* 🕯️ 문턱은 **내리막에서만** 봅니다 — 곡선은 정점 앞에서도 0.78 밑이라,
 * 방향을 안 보면 🌳 만성이 스무 살에 은퇴 권유를 받아요 (27번 실측). */
const suggestRetire = (st) =>
  !mustRetire(st) && ageOf(st) > peakAgeOf(st)
  && ageMul(st) < RETIRE_CURVE && (st && st.lowApps || 0) >= LOW_RUN;
```

#### 🚨 **어느 하나만 넣으면 무엇이 무너지나 — 실측값**

| | 🌱 조숙 | 🌿 보통 | 🌳 만성 |
|---|---|---|---|
| **ⓐ 지금 그대로 (0.75, 가드 없음)** | 젊은 쪽 없음 ✅ · 늙은 쪽 35세 ✅ | 젊은 쪽 **만 19세** ❌ · 늙은 쪽 37세 | 젊은 쪽 **만 19~22세** ❌ · 늙은 쪽 **영영 안 뜸** ❌ |
| **ⓑ 가드만 (문턱 0.75 유지)** | 없음 ✅ · 35세 ✅ | 없음 ✅ · 37세 ✅ | 없음 ✅ · **영영 안 뜸** ❌ |
| **ⓒ 문턱만 0.78 (가드 없음)** | 없음 ✅ · 34세 ✅ | **만 19세** ❌ · 36세 | **만 19~22세** ❌ · 37세 |
| **ⓓ 0.78 + 가드 (한 벌)** | 없음 ✅ · **만 34세** ✅ | 없음 ✅ · **만 36세** ✅ | 없음 ✅ · **만 37세** ✅ |

- **ⓑ만 넣으면** — 오르막 제안은 사라지지만 **🌳 만성은 은퇴 제안을 영영 못 받습니다**
  (만성 37세 곡선 **0.760** > 0.75 · 38세는 강제 은퇴라 제안 자리가 없음).
  designer가 §6-4에 적은 *"셋 다 닿습니다"*가 **거짓이 됩니다.**
- **ⓒ만 넣으면** — 만성이 늙어서 뜨긴 하지만 **오르막 제안이 그대로**이고,
  보통도 만 19세에 그대로 뜹니다. **지금보다 나빠지지도 나아지지도 않는 절반짜리**예요.
- **ⓓ만이 designer 표(조숙 34 · 보통 36 · 만성 37)와 정확히 일치합니다.**

> ⚠️ **`RETIRE_CURVE`를 「세기 손잡이」로 읽지 마세요.** 이건 *"정점의 몇 할 아래로 내려갔나"*를
> 재는 눈금이고, 0.78은 **곡선 앵커에서 역산된 값**입니다 —
> `late(37세) = 0.760 < 0.78 ≤ late(36세) = 0.800`이 유일한 창이에요.
> **앵커(`GROWTH_TYPES.pts`)를 만지면 다시 역산해야 합니다.** `TRAIN_NEUTRAL`과 같은 성격입니다.

### 🟠 designer 판단을 기다리는 자리 (제가 값을 정하지 않습니다)

| # | 무엇 | 제가 잰 것 |
|---|---|---|
| 1 | 🪑 **만성 데뷔 벤치** — 데뷔 클럽(`DEBUT_POOL`, `career.js:300`)이 「지금 실력」을 안 봅니다 | 만성 데뷔 지금 실력 **47.0** vs 클럽 전력 62 → 출전 **9.8/38** · 출전<15 **75.9%** |
| 2 | 🎂 **카드 나이 = 커리어 길이** | 지금 총 상 17세 42.65 / 19세 37.90 (**12.5%**). 데뷔 나이를 20세로 고정하면 **1.7%** |
| 3 | 🏆 **베스트11 문턱 포화** | 유효 능력치 **90에서 이미 99.9%**. `agePen`이 가리고 있던 자리 |
| 4 | 🕯️ **「전성기가 지났어요」 문구** | 조숙 카드는 **커리어의 16/19시즌**에 뜹니다 |

---

## 다른 사람에게

### grow-designer

- 🟢 **곡선은 섭니다. `AXIS_OFF 2.35`를 포함해 계수를 하나도 안 바꿔도 됩니다.**
  나이별 실측이 곡선의 ±3%p 안에 놓여요 — *"곡선이 그 일을 한다"*가 숫자로 확인됐습니다.
- 🔴 **§6-4의 「셋 다 닿습니다(조숙 34 · 보통 36 · 만성 37)」가 지금 코드에서는 참이 아닙니다.**
  만성 37세 곡선이 **0.760**이라 문턱 0.75를 못 넘어요. 위 ⓓ(0.78 + 방향 가드)로 정확히 맞습니다.
  **두 값은 한 벌이라 따로 넣으면 안 됩니다** — 표를 그대로 넘겨 주세요.
- 🔴 **§6-2의 「세 장의 총합이 같다」는 정점 기준값에서는 참이지만 커리어 결과에서는 아닙니다.**
  결합 칸이 **20.3%** 벌어져요. 주범은 성장타입이 아니라 **카드 나이**(12.5%)입니다 —
  나이가 데뷔 나이를, 데뷔 나이가 **커리어 길이**를 그대로 정하거든요.
  *"시간이 지나면 같아져요"*는 **능력치**를 같게 만들지 **시즌 수**를 같게 만들지 않습니다.
  반사실(데뷔 나이 20세 고정)에서는 나이 편차가 **12.5% → 1.7%**가 됩니다. 후보로만 드립니다.
- 🔴 **§7-1의 「만성은 초반 시즌을 버려야 한다」가 게임에서는 「출전 0」으로 처리됩니다** —
  데뷔 시즌 출전<15가 **75.9%**. 버티는 게 아니라 못 뜁니다. 그리고 그게 은퇴 권유로 이어져요.
- 🟠 **`agePen` 폐기는 방향이 맞습니다. 크기는 판단해 주세요** — 만 34~38세 MVP **0.1% → 57.5%**.
  다만 제 의견으로는 되살릴 자리가 아니라 **베스트11 문턱**이 진짜 자리예요
  (유효 능력치 90에서 이미 99.9%라 나이와 무관하게 포화). ①-G를 다시 여는 일이라 designer 판단입니다.

### grow-engineer

- **손댈 것은 하나입니다** — `prospect.js:137`의 `RETIRE_CURVE`와 `prospect.js:349-350`의
  `suggestRetire`. **둘은 한 벌입니다.** 위 §확정 계수의 코드 조각을 그대로 쓰세요.
  designer 승인 뒤에요.
- 그 밖에는 **바꿀 계수가 없습니다.** `AXIS_OFF`·`TRAIN_NEUTRAL`·앵커·`POOL` 전부 유지.
- 🟡 56번 §6-ⓐ(데뷔 유효 능력치)는 **실제로 부딪히고 있습니다** — 다만 계수가 아니라
  구조라 designer 답을 기다립니다. `DEBUT_POOL`(`career.js:300`)이 그 자리예요.

### grow-inspector

실측을 검사로 굳혀 주세요.

| # | 무엇 | 문턱 | 변이 |
|---|---|---|---|
| 45 | 🕯️ **은퇴 제안은 정점 **뒤에서만** 뜬다** — 세 타입 × 만 19~정점까지 전부 `false` | 0건 | 방향 가드를 빼면 만성 19~22세가 🕯️ → 🔴 |
| 46 | 🕯️ **세 타입 전부 만 37세 이전에 제안이 뜬다** (`lowApps ≥ 2`일 때) | 조숙·보통·만성 각각 ≤37세에 첫 `true` | `RETIRE_CURVE`를 0.75로 되돌리면 만성이 영영 안 뜸 → 🔴 |
| 47 | 📐 **`RETIRE_CURVE`가 앵커의 종속값인가** — `late(37) < RETIRE_CURVE ≤ late(36)` | 살아 있는 `curveAt`으로 대조 | 앵커만 옮기고 문턱을 안 옮기면 → 🔴 (38번 38-3과 같은 형태) |
| 48 | 🪑 **데뷔 시즌에 세 타입의 `overall()` 격차** | 지금 조숙(만20세) ÷ 만성(만20세) = **1.42배** (곡선 0.920 ÷ 0.650). 문턱은 designer가 정하면 굳히기 | 곡선을 `overall()`에서 빼면 격차 1.00배 → 🔴 |
| 49 | 📊 **게이트 곡선 다섯 점** (`s=0.5` · 버프 없음) | **4.4 / 19.3 / 52.3 / 83.8 / 96.6** ±3%p | `AXIS_OFF`를 2.15/2.55로 옮기면 → 🔴 |
| 50 | 🎂 **카드 나이 → 커리어 시즌 수** | 17세 19시즌 · 18세 18 · 19세 17 (정확히) | `CARD_AGE_HI`를 20으로 올리면 16시즌이 생김 |

- ⚠️ **45~47은 한 벌입니다.** 46만 넣으면 오르막 제안이 통과하고, 45만 넣으면 만성이
  영영 안 뜨는 걸 아무도 안 잡아요.
- 🟡 **31b는 26번대로 승격 금지 그대로입니다.** 제 대칭 명단 하네스로는 ±3.4%인데,
  26번의 −15.9%는 픽스처의 한 단면이에요. **조건에 따라 값이 바뀐다는 것 자체가 승격 금지 근거**입니다.

### grow-director

- 🟡 **결산 화면의 「🕯️ 전성기가 지났어요」가 조숙 카드에게 커리어의 16/19시즌에 뜹니다**
  (`career.js:2769`, `myAge > peakAgeOf(S)`). 리그MVP를 받은 시즌에도 그 줄이 나가요.
- 🟡 **「🕯️ 만 21세 · 은퇴를 생각해 볼 때입니다」**가 실제로 뜰 수 있습니다 (만성 커리어의 5%).
  고치기 전까지는 화면에 나갑니다.

---

## 시뮬레이션 스크립트

`/tmp/claude-0/-workspace-grow-games/79cf1e76-c844-41c3-8122-5fc7f1594e2e/scratchpad/g5/`

| 파일 | 하는 일 |
|---|---|
| `prosp.js` | **`beta/winger2/prospect.js`를 파일째** `new Function`에 태워 살아 있는 함수를 부릅니다 |
| `train.js` | 훈련 산식을 **소스에서 뜯어옵니다** (`prepAction` gain · `doTraining` · `wear` · 시즌당 78턴) |
| `career.js` | 커리어 한 벌 — 유스 36턴 → 프로 시즌(선발 경쟁 · 리그 · 수상 · 칭호) → 은퇴 |
| `v1.js` · `v1career.js` | **옛 커밋 `3baa9ff`에서 뜯은** v1 산식과 커리어 |
| `agg.js` | v1·v2 앙상블 (나이별 · 성장타입별 · 데뷔 · 은퇴 제안) |
| `agepen.js` | 같은 시즌 기록에 두 산식 — **되살리기가 먹었는지 먼저 확인**하고 진행 |
| `three.js` · `three2.js` · `three3.js` | 3택 (같은 판 · 잡음 대조군 · 데뷔 나이 고정 반사실) |
| `neutral.js` | 훈련 총량 · 커리어 면적 · 앵커 (살아 있는 함수) |
| `shape2.js` · `mono.js` | 카드 편중이 「평균 x축」을 무너뜨리나 |
| `refcurve.js` | 커리어와 **같은 리그 환경**에서 잰 게이트 곡선 (겹쳐 보기용) |
| `retire.js` | 🕯️ 은퇴 제안 후보 ⓐ~ⓓ 대조 |
| `oldsave.js` | 옛 세이브 |
| `page.js` | **진짜 페이지를 JSDOM에 띄워** 게임 코드로 확인 |
| `regress.js` · `axis331.js` | 회귀 |
| `sim.js` | `g4/sim.js`에서 승계 — 스탯 **객체**를 받게만 고쳤어요 |

## 관련

- 앞 실측: `26_balancer_minigame-curve.md` (곡선 기준선 · `s` 창 `[0.44, 0.58]`) · `25_balancer_gate-close.md`
- 구현: `56_engineer_growth-curve.md` · `58_engineer_train-neutral.md` · 검증: `57_inspector_prospect-fix.md`
- 설계: `13_designer_v2-final.md` §6-2 · §6-4 · §7-1 · §12-4 실측 ④
