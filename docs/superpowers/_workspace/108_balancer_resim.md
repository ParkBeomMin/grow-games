# 실측 — 🔁 시드 재측정 (18 · 19)

> 잰 소스: **`d1899fb`** (`🏫 초·중·고가 경기가 됐어요 · 🧱 차단에 손이 먹힙니다`)
> `town.js` `cad8832c…` · `engine.js` `3ecdbc17…` · `winger-moment.js` `e3da6d04…` ·
> `career.js` `683bf8a4…` · `squad.js` `f7666517…`
> 🔒 **구현은 한 줄도 안 고쳤습니다.** 측정 전후 sha256 동일 · `git status`에 변경 없음.
> 스크립트는 `scratchpad/bal18/`에 있습니다 (저장소 밖).

---

## 0. 🔴 먼저 — 결론 두 줄

| | |
|---|---|
| **18** | ✅ **부호는 안 뒤집힙니다.** `SIGN_SHAKE = [[0, 0.20], [1, 0.80]]` **그대로 둡니다.** 차이 **+0.131 ~ +0.189%p** — 창 `0 < 차이 ≤ 0.5%p` 안. 게다가 **어떤 `d` 분포에서도 못 벗어납니다**(§1-3 구조 증명) |
| **19** | 🔴 **폭을 12%p로 올리면 안 됩니다.** 빈도가 **안 고릅니다** — 🧱 비율 **df 78.1% vs fw 27.3%(2.9배)**. 그리고 ②가 더 셉니다: **8%p로 낮춰도** 🛡️ 철벽상이 조작으로 **Δ19.3%p** 갈립니다 → **폭 손잡이만으로는 못 맞춥니다. designer에게 되돌립니다** |

---

## 1. 무엇을 쟀나

### 1-1. 하네스 — 🔒 시드가 판정에 걸리는지부터

`beta/winger2/index.html`을 jsdom에 통째로 싣고 디스크의 `<script src>`를 그대로 인라인합니다.
**산식을 한 줄도 안 베꼈어요** — `WingerTown._t`(`deal`·`judgeFor`·`rollOffers`·`rollSigned`·
`tierOfD`·`OFFER`·`SHAKE`·`SIGN_SHAKE`)와 `WingerEngine`(`createMatch`·`autoMatch`·
`shareByWeight`·`half`)의 **그 함수·그 표**를 부릅니다. 직접 `eval` 없음.

건강 검사(`repro.js`)가 inspector의 진단을 그대로 재현합니다 —

```
Math.random만  → a:perfect g:ok g:ok …  /  a:perfect g:perfect g:miss …  🔴 매번 다름
+ _t.seed(n)   → a:miss g:miss g:ok …   ×3 완전 일치                      ✅ 재현됨
```

> 🚨 **하네스 함정 하나를 새로 찾았습니다 — inspector에게 넘깁니다.**
> 지시받은 두 줄을 **같은 시드**로 걸면 안 됩니다:
> ```js
> W.Math.random = mulberry32(seed);
> W.WingerEngine._t.seed(seed);        // ← 🔴 같은 seed면 두 스트림이 같은 수열입니다
> ```
> `mulberry32(7)` 두 벌은 **1000/1000 일치**합니다. 학교 아크가 엔진 8 draw ·
> 제안 굴림이 Math 8 draw라 **보폭까지 같아 평생 lockstep**이 되고,
> 그러면 *조작 결과와 제안 흔들림이 붙습니다.*
> 제 첫 판이 실제로 그렇게 나왔어요 — 차이가 s=0.90에서 **−0.109%p(부호 뒤집힘)**로 찍혔고,
> 시드를 가르니(`seed` / `seed ^ 0x9E3779B9`) **+0.143%p**로 돌아왔습니다.
> 🔒 게임에서는 `Math.random`이 브라우저 것이라 안 생기는, **하네스만의 함정**입니다.

### 1-2. 실측 18 — 무엇을 짝지었나

한 아크가 낳은 **같은 `d`** 위에서 두 갈래를 나란히 잽니다.

```
거절 = max( rollOffers(d) 5곳의 mul )     승낙 = rollSigned(d, m).mul
```

갈래 **안의** 흔들림은 전수 열거로 지웠습니다 — `SHAKE`를 5곳에 대해 3⁵ = 243가지,
`SIGN_SHAKE`를 2가지 전부 짚어 **조건부 기댓값**을 냅니다. 표의 칸을 짚는 방법도
소스 의존이에요: `shakeBy`가 `r = Math.random()`을 표에 누적으로 대므로 **칸의 중앙값**을
주면 그 칸이 나옵니다(중앙값은 `T.SHAKE`/`T.SIGN_SHAKE`에서 계산). 그래서 남는 표본오차는
**`d` 분포 하나**뿐이고, 두 값이 문자 그대로 같은 표본을 봅니다.
🎲 날것 몬테카를로도 같이 돌려 두 자가 맞는지 대조했습니다 (§1-4).

**표본: 조건당 100,000 아크 · 시드 40개 (`1013 · 2026 · … · 40520`).**
`s`(조작 성공도)는 **모델 다섯 벌** — `0.00` / `1.00` / `0.537`(설계 평균) /
`U(0.2, 0.9)` / `반반(0.377·0.697)`. 🔒 한 벌로 재지 않았습니다.

### 1-3. 실측 19 — 두 갈래

**①** 포지션별 🧱 빈도: `WingerEngine._t.playMatch`를 그대로 굴려
`moments.d.t / mineCards`를 셉니다. 명단 픽스처는 `engRow` 모양 그대로이고,
**spin 12벌 앙상블**입니다(고정 SPREAD 한 벌이면 에이스가 늘 같은 자리에 서요).
조건당 **24,000 ~ 48,000 경기**.

**②** 🛡️ 철벽상: 시즌 한 벌을 `career.js`의 형태 그대로 재구성합니다 —
6클럽 × 선발 11 = 66명 · 38라운드 · 라운드당 3경기 · 내 경기는 `createMatch`(카드 종류별로
다른 `s`를 `m.judgeFor(s)`로 넣습니다) · 나머지 둘은 `autoMatch` · 무실점은 그 클럽의
실점 0인 경기에 **선발 11명 전원 +1**(`career.js:963`) · 상대 클럽 차단은
`shareByWeight(XI, oppStops, "defend")`(`career.js:634`) · 상은 **cs 내림차순, 동점이면 차단**
(`career.js:1044·1049`). 클럽 표는 `game.js`의 `CLUBS[1]`을, `STR_SPREAD = 14`는
`squad.js`에서 **정규식으로 대조**하고 씁니다.
⚠️ **이건 재구성 모델입니다** — 절대값보다 **폭에 따른 차이**를 보세요.
조건당 **1,200 ~ 1,500 시즌**(= 13.7만 ~ 17.1만 경기).

### 1-4. 두 자가 맞나

같은 25,000 아크에서 전수 열거와 날것 MC:

| 조건 | 전수 열거 차이 | 날것 MC 차이 |
|---|---|---|
| s=0.30 | +0.1837%p | +0.2046%p |
| s=0.50 | +0.1757%p | +0.1976%p |
| s=0.90 | +0.1431%p | +0.1590%p |

MC 쪽이 늘 ~+0.02%p 높은데, 짝지은 MC의 표준오차가 **0.018%p**라 1σ입니다. ✅ 두 자가 같은 답.

---

## 2. 🔒 중립 검증 — 여기가 빨간불이면 아래는 볼 것도 없습니다

| 검사 | 결과 |
|---|---|
| 밴드 대칭 — 모든 `d`에서 `tierOfD(d) + tierOfD(−d) === 4` (d = −20…20) | ✅ |
| `OFFER` 등간격 대칭 — 간격 0.05/0.05/0.05/0.05 · 중립 tier 배수 **정확히 1.00** | ✅ |
| 🗄️ 옛 세이브 — `townScore`·`schoolN`이 **없는** 상태로 산식 통과 → `d = 0` · tier 2(중립) | ✅ |
| `s = 0.50`에서 `E[d] = −0.011` (N = 50,000 · 1σ 안) · `d` 분포 좌우 최대 비대칭 **0.17%p** | ✅ |
| 🎯 포지션이 `d`에 안 닿음 — fw/wg/mf/df 네 벌이 **base 분포까지 비트 단위로 동일** | ✅ |

`d` 분포 (s=0.50 · N=50,000):
`−4:4.7 −3:8.0 −2:11.7 −1:14.4 0:15.6 +1:14.3 +2:11.7 +3:8.0 +4:4.8` — 0을 축으로 대칭.

---

## 3. 측정값 — 🔴 실측 18

### 3-1. 🔑 부호는 **base tier마다** 살아 있습니다 (전수 열거 · 근사 아님)

| base tier | 거절 `E[max5]` | 승낙 `E[sign]` | 차이 | |
|---|---|---|---|---|
| 0 (☆) | 0.938135 | 0.940000 | **+0.1865%p** | ✅ 승낙 |
| 1 (⭐) | 0.988086 | 0.990000 | **+0.1914%p** | ✅ 승낙 |
| 2 (⭐⭐) | 1.038086 | 1.040000 | **+0.1914%p** | ✅ 승낙 |
| 3 (⭐⭐⭐) | 1.088086 | 1.090000 | **+0.1914%p** | ✅ 승낙 |
| 4 (⭐⭐⭐⭐) | 1.099951 | 1.100000 | **+0.0049%p** | ✅ 승낙 |

> 🔑 **이 표가 곧 증명입니다.** `offerOf`는 `d`를 **`tierOfD(d)` 하나로만** 봅니다 —
> 두 갈래 다 같은 함수를 지나요. 그래서 전체 차이는
> `Σ_t P(base = t) · (승낙|t − 거절|t)`이고, **괄호 안이 다섯 칸 전부 양수**입니다.
> → 🔒 **어떤 `d` 분포에서도 부호가 안 뒤집힙니다.**
> → 🔒 그리고 **최대값이 0.1914%p**라 `≤ 0.5%p` 창도 **못 벗어납니다.**
> 아크가 어떻게 바뀌든(카드 수·난이도·조작 실력) 이 두 성질은 그대로예요.

### 3-2. 실측 (조건당 100,000 아크 · 시드 40개)

| 조작자 모델 | base 분포 0/1/2/3/4 (%) | 거절 | 승낙 | **차이** |
|---|---|---|---|---|
| `s = 0.00` (최악) | 30.8/33.5/30.4/4.5/0.7 | 0.99330 | 0.99519 | **+0.1885%p** ✅ |
| `s = 0.537` (설계 평균) | 7.3/18.3/44.2/21.0/9.2 | 1.03782 | 1.03956 | **+0.1739%p** ✅ |
| `s ~ U(0.2, 0.9)` | 7.0/17.9/44.4/21.2/9.6 | 1.03874 | 1.04048 | **+0.1731%p** ✅ |
| 두 벌: 반은 서툼·반은 능숙 | 7.3/18.3/44.5/20.7/9.2 | 1.03773 | 1.03947 | **+0.1739%p** ✅ |
| `s = 1.00` (완벽) | 1.2/5.8/29.9/31.0/32.1 | 1.06941 | 1.07072 | **+0.1314%p** ✅ |

**목표 창 `0 < 차이 ≤ 0.5%p` — 다섯 벌 전부 안쪽.**

### 3-3. 옛 값(+0.166%p)은 뒤집힐 수 있었나 — 직접 쟀습니다

25,000 표본 × 40회 반복 (`s = 0.537`):

| 재는 법 | 차이 평균 | 표준편차 | 범위 | 🔴 부호 뒤집힘 |
|---|---|---|---|---|
| ① **짝지음** (같은 `d`) | +0.1713%p | 0.0197 | 0.1214 ~ 0.2034 | **0 / 40** |
| ② **안 짝지음** (서로 다른 아크) | +0.1817%p | 0.0470 | 0.0856 ~ 0.2652 | **0 / 40** |

> 🟢 **옛 값 +0.166%p은 살아 있습니다.** 짝짓기가 깨졌어도 흩어짐이 ±0.047%p라
> 0.17%p짜리 차이를 **못 지웁니다** — 부호가 뒤집히려면 3.6σ가 필요해요.
> 🔴 **다만 「운이 좋았다」가 아니라 「구조가 지켰다」입니다** — §3-1 때문에 그렇습니다.
> 🚨 그리고 **한 종류의 깨짐은 실제로 부호를 뒤집었습니다** — §1-1의 lockstep이에요.
> 그건 잡음이 아니라 **편향**이라 표본을 늘려도 안 없어집니다.

---

## 4. 측정값 — 🔴 실측 19-① 포지션별 🧱 `defend` 카드 빈도

### 4-1. 🔴 **안 고릅니다** (전력 70대70 · s=0.50 · spin 12벌 · 조건당 24,000경기)

| 능력치 | fw | wg | mf | **df** |
|---|---|---|---|---|
| 70 | 24.9% (0.30장) | 25.5% (0.33장) | 44.8% (0.67장) | **78.9% (1.09장)** |
| 90 | 26.1% (0.37) | 26.4% (0.39) | 45.3% (0.79) | **79.1% (1.23)** |
| **110** | **27.3% (0.43)** | **27.5% (0.46)** | **45.8% (0.91)** | **78.1% (1.36)** |
| 130 | 28.4% (0.48) | 28.6% (0.52) | 46.0% (1.00) | **76.8% (1.45)** |
| 150 | 29.3% (0.53) | 29.6% (0.58) | 46.4% (1.08) | **76.2% (1.55)** |

*(비율 = 내 카드 중 🧱이 차지하는 몫 · 괄호는 경기당 🧱 장수)*

**df : fw = 2.9배(비율) · 3.2배(경기당 장수).** 능력치 전 구간에서 유지됩니다.

### 4-2. 세 종류 다 (능력치 110 · 조건당 48,000경기)

| | ⚽ 결정 | 🅰️ 전개 | 🧱 수비 | 경기당 내 카드 |
|---|---|---|---|---|
| fw | 48.3% | 24.3% | **27.4%** | 1.55장 |
| wg | 39.1% | 33.4% | **27.5%** | 1.68장 |
| mf | 23.3% | 31.1% | **45.6%** | 1.99장 |
| df | 10.9% | 11.0% | **78.0%** | 1.74장 |

### 4-3. 무엇이 이 빈도를 흔드나

| 축 | 결과 |
|---|---|
| 🏟️ 전력비 | **큽니다.** 우리85대55 → fw 18.1% / df 70.0% · 우리55대85 → fw 38.6% / df 85.0%. **어느 조합에서도 df가 가장 높습니다** |
| 📊 전력 절대값 | **안 닿습니다.** 90대90 · 70대70 · 50대50이 전부 fw 27.2% / df 78.0% (`sceneOf`가 비율만 봐요) |
| 🎮 조작 실력 | **안 닿습니다.** s=0.20/0.50/0.80 → fw 27.1/27.2/27.2% · df 78.0/78.0/78.3% |

> 🟢 마지막 줄이 좋은 소식입니다 — **빈도 자체는 조작을 안 탑니다.**
> 조작이 타는 건 그 카드의 **결과**뿐이에요.

### 4-4. 판정 — ① 은 **designer의 추정이 맞았습니다**

> designer §4-2 ①: *"`defend`는 df가 가장 자주 보는 카드라 같은 폭이면 포지션 형평이 깨집니다"*
> → 🔴 **확인. 빈도가 안 고릅니다.**
> → 🔒 §4-2의 *"빈도가 고르면 폭을 12%p 쪽으로 올려도 됩니다"* 조건은 **안 섭니다.**
> **12%p로 올리지 마세요.**

같은 폭 `X`%p가 시즌에 만드는 「막아낸 장면」 차이 (38경기 · 능력치 110):

| 폭 | fw | wg | mf | df | **df − fw** |
|---|---|---|---|---|---|
| 8%p | 1.31회 | 1.40회 | 2.77회 | 4.13회 | **2.82회** |
| 12%p | 1.96회 | 2.10회 | 4.15회 | 6.20회 | **4.24회** |

---

## 5. 측정값 — 🔴 실측 19-② 조작 폭이 🛡️ 철벽상을 얼마나 움직이나

> ⚠️ **이건 실측 23이 아닙니다.** 23은 *"개편 전후로 수상률 ±3%p"*(같은 조작자에서 옛↔새)고,
> 여기는 designer가 §4-2 ②에서 물은 ***"조작 폭이 부문상을 결정하는가"*** 입니다.
> 폭(%p)을 `s` 폭으로 옮기는 자리도 소스에서 옵니다 — `cardP`의 `Δp = 2·half(a)·Δs`,
> `half(70) = 0.185`(엔진에서 읽음). 조작 평균은 **`E[s] = 0.537`에 고정**하고 폭만 벌립니다.
> 🔒 ⚽🅰️ 카드의 `s`는 **0.537로 묶어** 🧱 폭만 움직였습니다 (하나만 바꾸고 잽니다).

### 5-1. 폭 → 수상률 기울기 (능력치 110 · 시즌 1,200회/조건)

| 🧱 폭 | `Δs` | **df** 서툰 → 능숙 | **mf** 서툰 → 능숙 |
|---|---|---|---|
| **2%p** (🗑️ 개편 **전**) | 0.054 | 63.1% → 66.3% (**Δ3.2%p**) | 33.2% → 38.3% (Δ5.1%p) |
| **8%p** (목표 하한) | 0.216 | 53.8% → 73.1% (**🔴 Δ19.3%p**) | 29.7% → 43.2% (Δ13.5%p) |
| **12%p** (목표 상한) | 0.324 | 51.7% → 77.4% (**🔴 Δ25.7%p**) | 25.9% → 48.3% (Δ22.3%p) |

*상위 클럽(한강 FC · str 78) 기준. 중위 클럽(백두 시티 · str 62)에서는
2%p → Δ0.4%p · 8%p → Δ6.4%p · 12%p → Δ8.7%p (df).*

### 5-2. 능력치·클럽별 전개 (폭 11.8%p ≒ 12%p · 시즌 1,500회/조건)

| 능력치 · 클럽 | df | mf | wg | fw |
|---|---|---|---|---|
| 70 · str 78 | 8.9% → 25.5% (Δ16.7%p) | 0.6% → 3.6% (Δ3.0) | 0.0% → 0.0% | 0.0% → 0.0% |
| 110 · str 78 | 48.9% → 76.8% (**Δ27.9%p**) | 24.3% → 47.7% (Δ23.5) | 1.0% → 2.5% (Δ1.5) | 0.5% → 1.6% (Δ1.1) |
| 110 · str 62 | 2.4% → 9.5% (Δ7.1%p) | 0.9% → 3.5% (Δ2.6) | 0.0% → 0.1% | 0.0% → 0.1% |
| 150 · str 78 | 86.3% → 98.3% (Δ11.9%p) | 75.2% → 89.6% (Δ14.4) | 9.9% → 21.2% (Δ11.3) | 5.9% → 11.3% (Δ5.4) |

> 🔑 **왜 이렇게 세게 걸리나 — 축이 두 겹이라서입니다.**
> 무실점은 **클럽 단위**라 우리 팀 11명이 전부 같은 `cs`를 받습니다 → 동료 10명과 **항상 동점**이고,
> 그러면 `career.js:1049`의 **차단 타이브레이커가 진짜 판정자**가 돼요.
> 🧱 조작은 **두 축을 동시에** 밉니다 — ① 내 `miss`가 곧 실점이라 **팀 `cs`**를 움직이고
> ② `perfect/ok`가 **내 차단**을 쌓습니다. 그래서 한 손잡이가 분자와 분모를 같이 밀어요.
> (능력치 150·str 78에서 Δ가 도로 줄어드는 건 천장 효과입니다 — 98.3%는 더 못 올라가요.)

### 5-3. 판정 — ② 는 **폭 손잡이로 못 맞춥니다**

| | |
|---|---|
| 🔴 | **8%p로 낮춰도 df 철벽상이 Δ19.3%p 갈립니다.** 목표 밴드 **8~12%p 전 구간이 「조작이 부문상을 결정」**입니다 — 낮은 쪽을 고르는 것으로 해결되지 않아요 |
| 🔴 | **2%p(개편 전)에서 Δ3.2%p.** 즉 *"조작 폭이 부문상을 안 흔든다"*가 성립하던 유일한 자리가 **폐기된 옛 폭**이었습니다. 미니게임을 미니게임으로 만드는 순간 이 축이 열립니다 |
| 🔒 | **그래서 이건 계수 문제가 아니라 설계 문제입니다.** 폭을 8%p로 두는 판단 자체는 옳아요(①·② 둘 다 그쪽) — 다만 **②를 「폭으로 해결했다」고 적으면 안 됩니다** |
| ⚠️ | **다른 축의 손잡이로 고치지 마세요**(원칙 ⑪). `DEF_W`·`NPC_SPOT.defend`·`FLOOR_SHARE`는 **빈도 축**이고 🛡️ 철벽상은 **무실점 축**입니다 — 여기서 저기를 고치려 든 자리가 CLAUDE.md에 이미 두 번 적혀 있습니다 |

**→ designer에게 되돌립니다.** 결정할 것은 *"🧱 폭을 8%p에 두고, 🛡️ 철벽상이 조작을
Δ19%p만큼 타는 것을 받아들일 것인가"* 또는 *"철벽상의 축을 다시 볼 것인가"* 이고,
**어느 쪽을 포기할지는 계수로 정할 수 없습니다.**

---

## 6. 확정 계수

### 6-1. 실측 18 — 🔒 **바꿀 것 없음**

| 상수 | 파일:줄 | 값 | 판정 |
|---|---|---|---|
| `SIGN_SHAKE` | `beta/winger2/town.js:321` | `[[0, 0.20], [1, 0.80]]` | ✅ **그대로** |
| `SHAKE` | `beta/winger2/town.js:283` | `[[-1, 0.25], [0, 0.50], [1, 0.25]]` | 🔒 **안 만짐** |
| `BAND_EDGE` | `beta/winger2/town.js:266` | `[-4, -2, 2, 4]` | 🔒 **안 만짐** |
| `OFFER` | `beta/winger2/town.js:203` | 0.90/0.95/1.00/1.05/1.10 | 🔒 **안 만짐** |

> 🔴 **네 줄은 한 벌입니다. 따로 넣을 수 없습니다.**
> `SIGN_SHAKE`만 재는 것이 성립하는 이유가 *"나머지 셋이 그대로라 거절 쪽 좌우 대칭이
> 살아 있다"*예요. 어느 하나만 움직이면 §2의 중립 검증 다섯 줄이 그 자리에서 깨지고,
> §3-1의 「모든 base에서 양수」 증명도 같이 죽습니다.
> ⚠️ *"`SIGN_SHAKE` 하나만 보면 된다"*로 요약하지 마세요 — **`NPC_SPOT` 사고의 형태**입니다.

`p(+1)`을 움직이면 어떻게 되는지 (설계 §18-1의 표를 이번 하네스로 재확인):
`0.50 → 부호 뒤집힘` · `0.75 → 차이 ≈ 0` (축 B가 통째로 남아 **거절이 항상 이득**) ·
**`0.80 → +0.17%p ✅`** · `1.00 → 창 밖`.

### 6-2. 실측 19 — 🔒 **balancer가 확정할 수 있는 계수가 없습니다**

| | |
|---|---|
| ✅ | **`BLK_WIN = 14.2` 그대로** (`beta/winger-moment.js:119`) — 이번 측정은 이 값을 안 건드렸습니다 |
| 🔴 | **폭을 12%p로 올리지 마세요** — §4-4 |
| 🔴 | **폭을 8%p로 낮춰도 ②는 안 풀립니다** — §5-3. 설계 판단으로 되돌립니다 |

> 🚨 **폭을 실제로 움직이라는 결정이 나면, 이 셋은 한 벌로 움직입니다** (designer 104번 §12-1·§12-2c):
>
> | 손잡이 | 하는 일 | 따로 넣으면 |
> |---|---|---|
> | `BLK_WIN` (`winger-moment.js:119`) | 🔑 **폭** | 평균 `E[s]`가 같이 밀립니다 → 실측 20′ 재측정 필요 |
> | 2단계 시간 | 🔑 **폭** | 〃 |
> | `BLK_READ` (`beta/winger-moment.js:131`) | 🔒 **읽기↔손 지분** — 폭이 아닙니다 | 표를 끝에서 끝까지 밀어도 폭은 14.6 → 11.0%p뿐 (designer 실측). **여기로 폭을 잡으려 들면 평균만 부숩니다** |
>
> **셋 중 하나만 바꾸면 `E[s]` 평균 0.537이 깨지고, 그러면 §5의 「폭만 움직였다」가
> 통째로 무효가 됩니다.** 그때는 실측 20′·21·23을 **전부 다시** 재야 합니다.

---

## 7. 📮 grow-inspector — 테스트로 굳힐 것

| # | 굳힐 것 | 왜 |
|---|---|---|
| 1 | 🔒 **`tierOfD(d) + tierOfD(−d) === 2 × NEUTRAL_TIER`** (d = −20…20) · `OFFER` 등간격 · 중립 tier 배수 == 1.00 | §2. 셋이 함께 서야 `E[등급] = 1.000`이 **정의로** 성립합니다. 변이: `BAND_EDGE` 한쪽 경계만 옮기기 |
| 2 | 🔒 **`d`의 모든 값에서 `E[승낙 mul] > E[거절 max5 mul]`** — 전수 열거로, 표본 아님 | §3-1. **이게 실측 18의 계약입니다.** 표본으로 재면 잡음이 삼켜요. 변이: `SIGN_SHAKE`를 `[[0,0.25],[1,0.75]]`로 → base 0~3에서 부호가 죽어야 빨간불 |
| 3 | 🔒 **차이 ≤ 0.5%p** — 위와 같은 전수 열거의 최댓값 | 변이: `SIGN_SHAKE`를 `[[1,1.0]]`(+1 확정)로 → 창 밖 |
| 4 | 🗄️ **`deviationOf(undefined)` · `deviationOf({})` == 0** | 옛 세이브가 무명이 되는 그 사고 |
| 5 | 🚨 **하네스 검사: `W.Math.random`과 엔진 `_rng`가 같은 수열이 아닐 것** | §1-1. 지금 지시된 두 줄을 그대로 쓰면 **lockstep**입니다. `mulberry32(s)` 두 벌 1000/1000 일치 |
| 6 | 📍 **🧱 카드 빈도가 포지션에 따라 다를 것** (df > mf > wg ≈ fw) — 값이 아니라 **순서**로 | §4. 값으로 박으면 `DEF_W`를 바꿔도 검사가 못 잡거나 우연으로 깨집니다. 변이: `DEF_W`를 네 포지션 동일값으로 → 순서가 무너져야 빨간불 |
| 7 | 🎮 **🧱 카드 빈도가 조작 실력을 안 탈 것** (s=0.2/0.5/0.8에서 ±1%p 안) | §4-3. 빈도가 조작을 타기 시작하면 원칙 ⑥의 자리가 하나 더 생깁니다 |

> ⚠️ **6·7은 문턱을 검사에 직접 적으세요.** `DEF_W`나 `_t.K`에서 읽어 오면
> 상수를 바꿔도 검사가 따라가서 아무것도 안 잡힙니다.

---

## 8. 📮 grow-designer — 결정이 필요한 자리

1. 🔴 **§4-2 ⑤의 「고르면 12%p」 조건은 안 섭니다.** 빈도가 df 78.1% vs fw 27.3%예요.
   **8%p 유지**를 권합니다 — 그건 ①의 답이고, 계수로 정할 수 있는 유일한 부분입니다.
2. 🔴 **②는 폭으로 못 풉니다.** 8%p에서도 🛡️ 철벽상이 조작으로 **Δ19.3%p** 갈립니다.
   목표 밴드 전 구간이 그렇습니다. **「폭을 낮춰서 ②를 지켰다」고 적으면 안 됩니다.**
3. 🔒 **원인은 축이 두 겹이라서입니다** (§5-2 인용문). 무실점이 **클럽 값**이라 동료와 늘 동점이고,
   차단 타이브레이커가 실판정자가 되는데, 🧱 조작이 **그 둘을 동시에** 밉니다.
   → 손대야 할 자리는 **폭이 아니라 「철벽상의 축」**입니다. 🔴 다만 그건 제 결정이 아닙니다 —
   `DEF_W`나 `NPC_SPOT.defend` 같은 **빈도 축 손잡이로 고치지 마세요**(원칙 ⑪).

---
---

# 실측 22 — 🔒 견줌: 🎮 조작 축 vs 📈 능력치 축

> designer §13 요청분. 소스 상태 동일 (`d1899fb` · `engine.js 3ecdbc17…` · `career.js 683bf8a4…`).
> 🔒 **구현 안 고쳤습니다** — 측정 뒤 sha256 재확인 완료.

## 9. 무엇을 쟀나

19-②와 **완전히 같은 조건·같은 하네스**입니다 (6클럽 × 11 · 38라운드 · `createMatch` + `autoMatch` ·
무실점 클럽 단위 · 차단 타이브레이커). 축을 **하나씩만** 움직입니다:

| 축 | 움직이는 것 | 고정하는 것 |
|---|---|---|
| **A 🎮 조작** | 🧱 카드의 `s`만 서툰↔능숙 | 능력치 · ⚽🅰️ 카드의 `s`는 **0.537** |
| **B 📈 능력치** | 능력치 70 → 150 | 🎮 `s`를 **세 종류 전부 0.537**(= 🧱 폭 0) |

폭(%p) → `s` 폭 환산은 소스에서: `cardP`의 `Δp = 2·half(a)·Δs`, `half(70) = 0.185`(엔진에서 읽음).
**시즌 1,200~1,500회/조건 · 시드 3벌(8181 · 3131 · 5757).**

## 10. 측정값

### 10-1. 🔴 designer가 지정한 조건 (df · 상위 클럽 한강 FC str 78)

**■ 축 B 📈 능력치 — 🎮 조작 0.537 고정**

| 포지션 | 70 | 90 | 110 | 130 | 150 | **Δ(70→150)** |
|---|---|---|---|---|---|---|
| **df** | 14.8% | 36.7% | 64.8% | 84.4% | 95.0% | **+80.2%p** |
| (무실점/시즌) | 13.3 | 14.6 | 16.6 | 18.9 | 21.2 | +7.9경기 |
| mf | 1.6% | 12.7% | 37.1% | 61.8% | 84.6% | **+83.0%p** |

시드 3벌: **79.8 / 80.9 / 80.9 %p** — 흔들림 ±0.6%p.

**■ 축 A 🎮 조작 — 능력치별로 다릅니다 (🔴 단조가 아닙니다)**

| 🧱 폭 | 능력치 70 | 90 | **110** | 130 | 150 |
|---|---|---|---|---|---|
| 8%p · df | 8.6%p | 16.7%p | **19.3%p** | 11.3%p | 6.4%p |
| 12%p · df | 14.3%p | 22.7%p | **25.7%p** | 22.0%p | 11.7%p |

> ⚠️ **한 점으로 판정하지 마세요** — 능력치 110에서 최대이고 양끝에서 줄어드는
> **뒤집힌 U자**입니다(양끝은 바닥·천장 효과). 견줄 때는 **최댓값**을 씁니다.
> 시드 3벌의 최댓값: 8%p → **19.1 / 21.5 / 19.3 %p** · 12%p → **26.1 / 27.4 / 29.1 %p**.

### 10-2. 🔒 **견줌 — designer 판정표에 넣을 값**

| 🧱 폭 | 🎮 조작 축 (최대) | 📈 능력치 축 | 조작 ÷ 능력치 | designer 기준 |
|---|---|---|---|---|
| **8%p** | **+20.0%p** (19.1~21.5) | **+80.5%p** (79.8~80.9) | **24.8%** | 🟢 *"능력치가 40%p면 통과"* → **80%p. 두 배 여유** |
| 12%p | +27.5%p (26.1~29.1) | +80.5%p | 34.2% | 🟡 통과하지만 여유가 줄어듭니다 |

> 🟢 **원칙 ⑥ 통과입니다.** 조작 축은 육성 축의 **약 ¼**(8%p 기준)이에요.
> designer가 세운 *"능력치가 10%p면 조작이 육성을 이긴다"*의 반대편입니다 — **8배 차이**로 안전합니다.

### 10-3. 🔒 원칙 ⑥ 정면 — 20시즌 총량

*"능력치 70 최고조작 vs 능력치 130 최악조작"* (df · 20시즌 · 시즌 1,200회 표본)

| 클럽 | 🧱 폭 | 능력치 70 · **최고**조작 | 능력치 130 · **최악**조작 | |
|---|---|---|---|---|
| 한강 FC | 8%p | 🛡️ **4.2회** / 무실점 279경기 | 🛡️ **15.9회** / 무실점 362경기 | ✅ 능력치 **3.8배** |
| 한강 FC | 12%p | 🛡️ 4.8회 / 283경기 | 🛡️ 14.2회 / 346경기 | ✅ 능력치 **3.0배** |
| 백두 시티 | 8%p | 🛡️ 0.1회 / 170경기 | 🛡️ 1.9회 / 227경기 | ✅ 능력치 **19배** |
| 백두 시티 | 12%p | 🛡️ 0.1회 / 174경기 | 🛡️ 1.3회 / 220경기 | ✅ 능력치 **13배** |

**네 칸 전부 능력치가 이깁니다.** 무실점 총량(279 vs 362경기)에서도 같은 방향이에요.

### 10-4. 🔴 그런데 — **중위 클럽에서 여유가 사라집니다**

같은 자로 중위 클럽(백두 시티 · str 62)을 재면:

| 🧱 폭 | 🎮 조작 축 (최대) | 📈 능력치 축 | 조작 ÷ 능력치 | |
|---|---|---|---|---|
| **8%p** | +20.3%p (20.2~20.4 · **능력치 150**에서) | +28.4%p (27.3~30.1) | **71%** | 🟡 능력치가 1.4배 — 겨우 |
| **12%p** | **+30.2%p** (28.4~31.2) | +28.4%p | **106%** | 🔴 **조작이 능력치를 넘습니다** |

시드 3벌 전부에서 같은 방향입니다 (12%p: 30.9 / 31.2 / 28.4 vs 27.3 / 27.7 / 30.1).

> 🔑 **왜 상위 클럽과 다른가 — 두 축이 서로 다른 자리에서 포화합니다.**
> 상위 클럽은 팀이 이미 무실점을 자주 내서 **능력치 축이 14.8% → 95.0%로 크게 벌어지고**,
> 조작 축은 양끝에서 바닥·천장에 눌립니다. 중위 클럽은 **능력치 축이 0.1% → 27.4%로 눌린 채**
> 조작 축의 최댓값이 **능력치 150 쪽으로 밀려와** 둘이 같은 자리에서 겹칩니다.
> → 🔴 **잘 키운 수비수일수록 조작이 상을 가릅니다.** 원칙 ⑥이 *"덜 키운 쪽은 +0"*이라
> 적은 그 방향의 반대는 아니지만, **다 키운 뒤에 조작이 마지막을 정하는** 모양이에요.

## 11. 판정

| | |
|---|---|
| 🟢 | **원칙 ⑥ 통과 — 8%p 기준.** designer가 지정한 조건에서 조작 축은 육성 축의 **24.8%**입니다(20.0%p vs 80.5%p). *"조작이 부문상을 결정한다"*는 **절대값의 착시였습니다** — 제 §5-3 판정을 이 값으로 **교정합니다** |
| 🔴 | **12%p는 안 됩니다.** 상위 클럽에서 34.2%로 올라가고, 중위 클럽에서는 **조작 축(30.2%p)이 능력치 축(28.4%p)을 넘습니다.** ①(빈도 불균형)에 이어 **두 번째 독립 근거**입니다 |
| 🟡 | **8%p도 중위 클럽에서는 71%로 붙습니다** — 통과지만 여유가 얇아요. designer의 새 목표(§23′)를 잡을 때 **상위 클럽 하나로 재지 마세요.** 최댓값이 클럽에 따라 **다른 능력치 칸**에서 납니다 |
| 🔒 | **§5-3의 「설계 재검토 필요」는 철회하지 않습니다 — 근거만 좁힙니다.** 원칙 ⑥은 통과했지만 *"버린 축이 타이브레이커라는 이름으로 돌아왔다"*(designer §13)는 **그대로 남아 있는 문제**입니다. 그건 크기가 아니라 **형태**의 문제라 이 견줌으로 안 지워집니다 |

## 12. 확정 계수 (실측 22 기준)

| | |
|---|---|
| ✅ | **🧱 폭은 8%p 쪽.** ①(빈도 df:fw 2.9배) · ②(중위 클럽에서 12%p가 육성을 넘음) **두 근거가 같은 방향**입니다 |
| ✅ | `BLK_WIN = 14.2` (`beta/winger-moment.js:119`) — 이번에도 안 건드렸습니다 |
| 🔴 | **balancer가 확정할 수 있는 새 계수는 없습니다.** 폭을 실제로 옮기라는 결정이 나면 §6-2의 **한 벌 표**(`BLK_WIN` × 2단계 시간 · `BLK_READ`는 폭 손잡이 아님)를 그대로 따르고, 짝지은 `E[s]` 차이 0을 확인한 뒤 **22를 다시** 재세요 |

## 13. 📮 designer — 23′을 잡을 때

새 목표 *"🛡️ 철벽상의 서툰↔능숙 격차가 개편 전(Δ3.2%p) 대비 +3%p를 안 넘는 최대 폭"*에 대해,
이번 하네스가 이미 잰 값입니다 (df · 상위 클럽 · 능력치 110):

| 🧱 폭 | 서툰↔능숙 격차 | 개편 전(2%p · Δ3.2%p) 대비 |
|---|---|---|
| 2%p | Δ3.2%p | 기준선 |
| 8%p | Δ19.3%p | **+16.1%p** 🔴 |
| 12%p | Δ25.7%p | **+22.5%p** 🔴 |

> 🔴 **+3%p 창은 지금 곡선에서 폭 ≈ 2.5%p 언저리입니다** — 사실상 **개편 전 폭**이에요.
> 🔒 **그 목표를 그대로 쓰면 🧱 미니게임 개편이 통째로 무의미해집니다.**
> 그런데 §10-2가 보여주듯 **8%p는 원칙 ⑥을 두 배 여유로 통과**합니다.
> → 🔴 **두 목표가 서로 다른 답을 냅니다. 무엇을 포기할지는 제 결정이 아닙니다.**
> 두 값을 나란히 둡니다:
>
> | 기준 | 8%p의 성적 | 판정 |
> |---|---|---|
> | 원칙 ⑥ (조작 ÷ 능력치) | 24.8% (상위) · 71% (중위) | 🟢 통과 |
> | 새 23′ (개편 전 대비 +3%p) | +16.1%p | 🔴 탈락 |
>
> ⚠️ 그리고 23′을 쓰신다면 **능력치 110 한 칸으로 재지 마세요** — §10-1의 조작 축이
> **뒤집힌 U자**라 어느 칸에서 재느냐로 6.4%p ~ 19.3%p까지 갈립니다(CLAUDE.md 2026-08-29
> *"단조가 아닌 값을 문턱 하나로 판정 금지"*). **능력치 × 클럽 격자의 최댓값**으로 잡으세요.

## 14. 📮 grow-inspector — 22가 더한 항목

| # | 굳힐 것 | 왜 |
|---|---|---|
| 8 | 🔒 **능력치 축 > 조작 축** — 같은 격자에서 `Δ(능력치 70→150) > max_능력치 Δ(서툰→능숙)` | 원칙 ⑥의 계약. **최댓값끼리** 견줘야 합니다(한 점으로 재면 U자가 삼킵니다). 변이: 🧱 폭을 두 배로 → 중위 클럽에서 뒤집혀야 빨간불 |
| 9 | 🔒 **원칙 ⑥ 20시즌 총량** — 능력치 70 최고조작 < 능력치 130 최악조작 (🛡️ 수상 수 · 무실점 총량 **둘 다**) | §10-3. 회당이 아니라 **총량**으로 재는 게 원칙 ⑥의 정의입니다 |
| 10 | ⚠️ **격자를 상위 클럽 하나로 좁히지 말 것** | §10-4. 조작 축의 최댓값이 클럽에 따라 **다른 능력치 칸**(110 ↔ 150)에서 납니다 |
