# 41 · engineer — ①-G 확정 계수 (마지막 코드 변경)

**계수만 바꿨습니다.** 산식·축·배선은 한 글자도 안 건드렸어요.
`beta/soccer/`는 그대로, 커밋 안 했고, `tests/winger2/`는 **돌리기만** 했습니다.

## 넣은 것

| 계수 | 이전 | **확정** | 자리 |
|---|---|---|---|
| `NPC_SPOT.goal` | 2.80 | **3.05** | `engine.js:98` |
| `NPC_SPOT.assist` | 5.80 | **6.50** | |
| `NPC_SPOT.defend` | 4.50 | **4.90** | |
| `POS_AXIS.n` | 1.062 / 0.980 / 0.880 / 0.784 | **fw 1.072 · wg 0.967 · mf 0.873 · df 0.794** | `career.js:78-81` |

그대로 둔 것: `MOM_MIN 8.40` · `AXIS_OFF 2.35` · `ASSIST_P2 0.48` ·
`ACE_POOL {goal:["fw","wg"], assist:["mf","wg","fw"], defend:["df"]}` ·
`SPOT 4.00` · `FLOOR_SHARE 0.12` · `CLUTCH 1.25` · `AXIS_K 3.00` · `chance(pf)` · 🎖️ 칭호 버프 배선.

### 값이 박힌 주석도 함께 옮겼습니다 (원칙 ⑨)

`ACE_POOL` 위 주석의 도움 축 계산에 `5.80`이 숫자로 들어 있었어요. 새 값으로 고치고,
한 곳은 아예 **상수 이름(`NPC_SPOT.assist`)으로** 바꿔 다음에 또 안 어긋나게 했습니다.

```
에이스가 mf면 — mf 넷 1.0×6.50 + 1.0×3 = 9.50  vs  fw 둘 1.00
에이스가 fw면 — mf 넷 4.00              vs  fw 둘 0.5×6.50 + 0.5 = 3.75
```

`SPOT 4.00 < NPC_SPOT.assist` 주석의 값도 6.50으로 갱신했습니다.

---

## 🔴 원칙 ⑩ — 되돌려 봤습니다

두 묶음을 하나씩 옛 값으로 돌리고 전 검사를 다시 돌렸어요. **복원은 `cmp`로 바이트 확인**했습니다.

| 되돌린 것 | 빨간불이 된 검사 |
|---|---|
| `NPC_SPOT` → 2.80/5.80/4.50 | `engine-test` 「NPC_SPOT이 카드 종류별 객체다」가 **초록으로 바뀌고**, 「POS_AXIS의 n」만 남음 |
| `POS_AXIS.n` → 1.062/0.980/0.880/0.784 | 「POS_AXIS의 n」이 **초록으로 바뀌고**, 「NPC_SPOT」만 남음 |

**두 pin이 서로 안 섞입니다** — 각자 자기 계수만 봅니다. 교차 오염 없음.

### 🟠 그런데 **행동 검사는 하나도 안 움직였습니다**

계수를 옛 값으로 되돌려도 `award-test` · `league-test` · `mutation-test` ·
`neutral-test` · `wiring-test`가 **전부 초록불**이었어요.

| 되돌림 | award | league | mutation | neutral | wiring |
|---|---|---|---|---|---|
| `NPC_SPOT` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `POS_AXIS.n` | ✅ | ✅ | ✅ | ✅ | ✅ |

**이 계수들을 지키는 건 지금 `engine-test`의 pin 두 줄뿐입니다.**
수상률 0.7%p 격차는 커리어 몬테카를로라야 보이는 값이라 빠른 단위 검사로는 못 잡는 게
자연스럽지만, **"계수가 조용히 바뀌어도 밸런스 검사는 아무 말 안 한다"**는 사실은
기록으로 남겨 둡니다 — pin이 유일한 방어선이에요.

> inspector께: pin을 새 값으로 갱신하실 때 *"이 값을 지키는 행동 검사는 없다"*를
> 주석에 적어 주시면 다음 사람이 pin을 가볍게 고치지 않습니다.

---

## 회귀 — 앞 게이트에서 세운 것

| | 40번 | **지금** | |
|---|---|---|---|
| 세 갈래 도움÷골 최대 차 | 2.39% | **2.48%** | ✅ ≤3% · 여유 **0.52%p** |
| 내 클럽 ↔ 다른 클럽 **득점** 차 | +0.7% | **+1.1%** | ✅ |
| 내 클럽 ↔ 다른 클럽 **실점** 차 | −14.5% | **−14.7%** | 🟠 ±15% 여유 **0.3%p** |
| 무실점 차 | +25.3% | +26.7% | |

세 갈래: ① 0.749 · ② 0.751 · ③ 0.768.
실게임(게임 입구로 12라운드 완주) 리그 전체 도움÷골 **0.753**.

⚠️ **여유가 둘 다 얇습니다** — 도움÷골 0.52%p · 실점 대칭 0.3%p.
`ASSIST_P2`나 `NPC_SPOT_A`를 다음에 또 움직이면 둘 중 하나가 먼저 문턱에 닿습니다.

### `NPC_SPOT` 셋의 대각선 — 유지됩니다

| | 리그 1위 골 | 도움 | 차단 |
|---|---|---|---|
| **확정 3.05 / 6.50 / 4.90** | **18.5** | **18.8** | **29.5** |
| `_G` → 1.00 | **11.2** ↓ | 19.3 | 29.7 |
| `_A` → 1.00 | 19.1 | **8.0** ↓ | 30.2 |
| `_D` → 1.00 | 18.5 | 18.8 | **12.9** ↓ |

40번에 보고한 교차항(`_G`를 누르면 도움이 조금 오름)은 그대로예요 — 18.8 → 19.3 (**+2.7%**,
40번엔 +3.4%). 풀이 겹쳐 fw가 골 에이스 경쟁에서 풀려나 도움 쪽으로 오는 몫입니다.

### 🅰️ 도움 축은 여전히 mf가 쥡니다 (`NPC_SPOT.assist` 5.80 → 6.50 뒤)

| | fw | wg | **mf** | df |
|---|---|---|---|---|
| `assist: ["mf","wg"]` (되돌림) | 6.4% | 26.4% | **62.8%** | 4.4% |
| **확정** `["mf","wg","fw"]` | 13.8% | 23.9% | **57.6%** | 4.7% |

2위(wg 23.9%)와 **2.41배** 차. 에이스 배수를 올려도 축은 안 넘어갑니다.
📈 겹침 비율은 67% → **74%** 그대로고, 리그 1위 공격P 26.2 → **27.2 (+3.7%)**.

---

## 검사 결과 — 빨간불 **둘, 둘 다 pin**

| 검사 | exit | |
|---|---|---|
| `engine-test.js` | 1 | ❌ **2건 — 아래** |
| `award-test.js` | 0 | ✅ |
| `league-test.js` | 0 | ✅ **31b가 31로 승격돼 통과합니다** |
| `mutation-test.js` | 0 | ✅ **다섯 변이 검사가 다시 돕니다** (두 게이트 만에) |
| `neutral-test.js` · `wiring-test.js` | 0 | ✅ |
| `smoke-test.js beta`(9종) · `check-page-test.js` | — | ✅ |

### 갱신이 필요한 pin 둘

```
❌ NPC_SPOT이 카드 종류별 객체다 — goal 2.8 · assist 5.8 · defend 4.5
   → goal 3.05 · assist 6.50 · defend 4.90
❌ POS_AXIS의 n이 설계 §3-2 표와 같다 — fw 1.062, wg 0.98, mf 0.88, df 0.784
   → fw 1.072 · wg 0.967 · mf 0.873 · df 0.794
```

38~40번에서 빨간불이던 `AXIS_OFF`·`MOM_MIN`·`ACE_POOL` pin은 inspector가 이미 갱신하셔서
이번엔 초록불입니다. **`mutation-test`도 살아났습니다** — 39·40번에 적은
`NPC_SPOT[kind] || NPC_SPOT.goal` 형태로 변이 정규식이 맞춰졌어요.

## 배선 재확인 (계수만 바꿨지만 한 번 더)

```
✅ 🥇+🏅 칭호 → 내 줄 buff g 1.230 · a 1.080 · d 1.080 (= buffMul)
✅ 🚨 경쟁자 65명 전원 buff = null
✅ 🎉 피버도 같은 길로 (1 → 1.200)
✅ 🛡️ 무실점 축 — 정렬·표시·요약 줄 12항목 전부 통과 · 오류 0
```

## ①-G에서 제가 바꾼 것 전체 (36~41번 누적)

`beta/winger2/` 네 파일뿐입니다.

| 파일 | 무엇 |
|---|---|
| `engine.js` | G-3 전개 장면 정정 · `shareByWeight` pBig 재구성 · `chance(pf)` · `ASSIST_P2` · `NPC_SPOT` 종류별 셋 · `ACE_POOL` 두 자리 · `w(i)`의 버프 항 |
| `career.js` | 🛡️ 무실점 축(집계·`raceRank`·표시) · `POS_AXIS.n` · `AXIS_OFF` · `MOM_MIN` · `engRow`의 버프 · 컵 `act.cs` 제거 · `shareByWeight` 호출부 `atkW` |
| `squad.js` | `resetSeason`의 `cs` |
| `game.js` | 시즌 칭호 `wall`의 `need` 문구 |

⚠️ `git status`의 `.claude/**` · `CLAUDE.md` · `docs/.../13·23·24·43·44` ·
`tests/winger2/**` · `.gitignore` · `scripts/shoot.js`는
**다른 세션 것이라 손대지 않았습니다.**
