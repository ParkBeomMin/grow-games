# 검증 — ⚽ 더 윙어 II · 🎉 중립 드리프트 승격 · 🔢 차례 표시 · 🧬 2택

2026-09-01 · grow-inspector
입력: `102_engineer_fixes.md`(§1 · §2 · §3 · §5) · `101_designer_feedback.md` · `97_inspector_school-tests.md`
출력: `tests/winger2/town-neutral-test.js`(승격) · `tests/winger2/creation-test.js`(신설) · `tests/winger2/foot-map-test.js`(주석 갱신)

---

## 0. 한 줄 요약

| | 무엇 | 결과 |
|---|---|---|
| **①** | 🎉 **S-3e 승격** — 🚧 「알려진 미달」 → **등식 계약**. 변이 방향을 뒤집었습니다 | ✅ 통과 |
| **②** | 🔢 **차례 표시** 검사 신설 (`creation-test.js` C-0~C-4) | ✅ 통과 · 변이 2종 잡음 |
| **③** | 🧬 **2택** 검사 신설 (B-1~B-4b) — **난수 소비량을 관계로** | ✅ 통과 · 변이 3종 잡음 |
| **④** | 🔴 `passArc` 결정성 — engineer 신고 확인 · **원인 한 줄** | 🔴 재현됨 → D-0으로 굳힘 |
| **⑤** | ⚠️ 102번 §3의 *"`SYM_EPS_D`를 ±2%로"* — **재 보니 아니었습니다** | 🟡 ±1%로 정정 |

**검사 21종 · 실패 0건.** `smoke-test beta` ✅ 9종 · `check-page-test` ✅

---

## 1. ① 🎉 S-3e 승격 — 「상한을 박은 🚧」에서 「배정도 등식」으로

### 승격이 갈린 것을 확인했습니다

97번에서 제가 이렇게 적어 뒀습니다:

> 🚧 **중립이 「정의로」가 아니라 「거의」입니다.** … 해소되면 ❌ 「승격하세요」로 갈립니다.

engineer의 `cbcc160` 뒤에 실제로 갈렸습니다 (그 빨간불 3건은 **설계된 신호**였어요).
그리고 **변이 M-REF-FIX가 소스에 안 걸리게 됐습니다** — 겨누던 줄(`town: 32.0`)이 없어졌으니까요.
0번이 그걸 잡아 함께 빨간불이 됐고, 그건 **정확히 의도한 동작**입니다.

### 🔴 승격을 통계로만 하지 않았습니다 — 그러면 같은 사고가 반복됩니다

> **남길 교훈(engineer): 검사가 이미 적어 둔 불일치를 아무도 안 읽었습니다.**
> *"±0.5% 안이니 통과"*가 **원인을 안 묻게** 만들었어요.

승격된 검사가 다시 통계(±0.003)뿐이면, **이 불일치가 절반만 돌아와도 또 문턱 안에 숨습니다.**
그래서 등식 세 줄을 앞에 세웠어요.

| 줄 | 무엇을 | 어떻게 |
|---|---|---|
| **S-3e** | 🎯 **카드 한 장의 기대 점수 = 정확히 1점** | `outcome`을 **engine.js 소스에서 정규식으로 뜯어** `new Function`으로 되살리고, `rnd`를 우리가 넣어 **판정이 갈리는 자리를 이분법으로** 찾습니다. 표본이 아니라 **정의역 전체** |
| **S-3e-조건** | 📊 카드 3종이 **서로 다른 갈래 3가지**를 쓴다 | 이게 없으면 S-3e는 껍데기 — 셋 다 같은 kind여도 E는 셋 다 1이라 초록불이거든요 |
| **S-3e-a** | 🔗 `evenBody().x / PEER_REF.town === 1` (배정도) | 경계면 교차 비교 — 왼쪽 `town.js` ↔ 오른쪽 `game.js` |
| **S-3e-b** | 🔗 **`town`이 숫자가 아니라 종속값이다** | `evenStats()`를 평균 60으로 갈아 끼우고 `town`이 **60이 되는지** 봅니다 |
| **S-3e-c** | ♻️ 위 갈아 끼우기를 **되돌렸다** | 안 되돌리면 뒤 검사가 전부 "다른 세계"를 잽니다 |
| **S-3f** | 📏 카드당 드리프트 ≤ 0.003 (통계) | 🚧 → **그냥 계약**. 등식이 실제 판에서도 지켜지는지 |

**실측(고침 뒤):**

```
autoP  goal 0.3333333333333333  ·  assist 0.3333333333333333  ·  defend 0.5
E[점수/장]  ⚽ 1.000000000000  ·  🅰️ 1.000000000000  ·  🧱 1.000000000000
1과의 차   0.00e+0  (허용 1e-12)          ← **정확히 0입니다**
evenBody().x / PEER_REF.town = 1        (32.333333333333336 / 32.333333333333336)
```

> 🔑 **왜 `1.5p + 0.5` 같은 기댓값 공식을 검사에 안 적었나** — 적는 순간 `outcome`의 갈래가
> 바뀌어도 검사가 안 따라갑니다. 소스에서 뜯어 **계단의 자리를 재면** 갈래가 바뀌어도 살아요.
> 검사에 박은 값은 **「1」 하나**뿐이고, 그건 실측이 아니라 **중립의 정의**입니다.

### 📏 문턱을 새 실측값에 안 붙였습니다 (engineer 당부)

> **문턱을 새 실측값에 딱 붙이지 마세요** — 목표 0.003이 지금 4σ 여유입니다.

**조이기 전에 1σ부터 쟀습니다** (5시드 × 20,000판 = 100,000판을 **한 묶음**으로 · 묶음 6벌):

| | 기준선 | 🧪 M-REF-CONST | 문턱 | 여유 |
|---|---|---|---|---|
| **카드당 드리프트** | 평균 **+0.000053** · 1σ 0.000829 · 최대\|·\| 0.001415 | 평균 **+0.007121** · 1σ 0.000777 · 최소 +0.005726 | **0.003** | 기준선 +3.6σ · 변이 −5.3σ |
| **`d` 분포 좌우차** | 평균 **0.145%** · 1σ 0.090% · 최대 0.244% | 평균 **1.836%** · 1σ 0.192% · 최소 1.585% | **1%** | 기준선 +9.5σ · 변이 −4.4σ |
| **E[spotMul]** (N=2/5/8) | 벗어남 ≤ **0.04%** (고침 전 0.12%) | — | ±0.5% 유지 | 약 25σ |

🔒 **`EPS`(±0.5%)는 안 건드렸습니다.** 기준선이 좁아졌다고 조이면 M-S1(−0.085%)을
문턱으로 잡으려는 유혹이 생기고, 그러면 **S-1(항등식)이 왜 있는지가 흐려집니다.**

### 🟡 ⚠️ 102번 §3의 *"`SYM_EPS_D`를 ±2%로 조이라"* — **재 보니 아니었습니다**

그 줄은 **실측 전에 적힌 것**이에요. 실제로 재 보니 **M-REF-CONST의 최대가 2.078%**입니다 —
±2%로 잡으면 **시드에 따라 변이가 그냥 지나갑니다.** 그래서 **±1%**로 정했어요.
(±1%: 기준선에서 +9.5σ · 변이에서 −4.4σ — 양쪽 어디에도 안 붙었습니다.)

### 🎯 M-REF를 「방향을 뒤집어」 되살렸습니다

요청하신 그대로입니다.

```js
/* 🎯🔴 M-REF-CONST — `PEER_REF.town`을 다시 「숫자」로 되돌립니다 (종속값 → 상수 32.0). */
M_REF_CONST: { "game.js": [[
  /return ks\.length \? ks\.reduce\(\(a, k\) => a \+ st\[k\], 0\) \/ ks\.length : 0;/,
  "return 32.0;"]] },
```

| 옛 판 (M-REF-FIX) | ★ 지금 (M-REF-CONST) |
|---|---|
| `town: 32.0` → 32.333 **고치는** 변이 | `get town()` → 숫자 32.0 **되돌리는** 변이 |
| 「미달이 해소되면 🚧가 ❌로 갈리는가」 | 「누가 이 칸에 **숫자를 다시 적으면** 잡히는가」 |
| 고침이 들어와 **겨눌 것이 없어짐** | `game.js` 주석의 *"여기에 숫자를 적지 마세요"*를 지킴 |

🔒 **getter 껍데기는 그대로 두고 돌려주는 값만** 바꿉니다 — `evenStats()`가 여전히 불려서
**난수 소비량이 안 바뀝니다.** 변이가 재려던 것 말고 다른 것까지 흔들면 그건 신호가 아니라 잡음이에요.

**결과 — 승격된 4줄이 전부 갈립니다:**

```
✔ S-3e 등식 · ✔ S-3e-b 종속 · ✔ S-3f 드리프트 · ✔ S-3d 분포
비율 1.010417 · 카드 기대 점수 goal=1.005208 · assist=1.005208 · defend=1.010417
autoP goal=0.336806 · assist=0.336806 · defend=0.505208
드리프트 +0.00759/장 (문턱 0.003) · 분포 좌우차 1.95% (문턱 1%) · 100,000판
```

> ✅ 승격이 **「현재값을 정답으로 단언」한 게 아닙니다** — 되돌리면 그 자리에서 잡힙니다.

### ⚠️ `eval`·`survival`은 안 건드렸습니다

확인했습니다 — `eval: 32.0` · `survival: 36.5` 그대로이고, ⚔️ 평가전 곡선을 보는
`award-test`·`grade-test`·`youth-*` 전부 초록불입니다. **`town`만 종속값이 됐어요.**

---

## 2. ②③ 검사가 없던 자리 둘 — `tests/winger2/creation-test.js` 신설

> 🔬 engineer가 **말로 하지 않고 실제로 되돌려** 20종을 돌렸고, 둘 다 **전부 초록불**이었습니다.

### ② 🔢 차례 표시 — 「3 / 3」이 거짓말이던 자리

**게임 입구를 통해** 타이틀 → ✏️ 이름 → 🦶 주발 → 🗺️ 동네 → 🏫 초등 → 🎯 자리 →
🏫 중·고 → 🏟️ 제안 → 🧬 조립대까지 실제 버튼으로 가면서, **그 화면이 떠 있는 동안**
`.screen.active .step-no`를 읽습니다 (지나간 뒤에 읽으면 다음 화면 칸을 보게 돼요).

```
✏️ 이름 "1 / 5" · 🦶 주발 "2 / 5" · 🗺️ 동네 "3 / 5" · 🎯 자리 "4 / 5" · 🧬 조립대 "5 / 5"
```

| 줄 | 무엇 |
|---|---|
| C-0 | 🚪 게임 입구를 통해 만들기 화면 **5개**를 순서대로 지난다 |
| C-1 | 다섯 화면 **전부**에 차례 칸(`.step-no`)이 있다 (칸이 없으면 `stampStep`이 불려도 안 찍혀요) |
| C-2 | 다섯 화면 **전부**가 차례를 찍는다 (빈 칸 0개) |
| C-3 | 분모가 **전부 5**이고 분자가 **1 → 5 순서대로** |
| **C-4** ★ | 🔑 **「끝」을 말하는 화면은 마지막 하나뿐** — `N / N`을 찍는 화면 = 🧬 조립대 |

> 🔑 **C-4는 값이 아니라 관계입니다.** 숫자를 안 세고 *"끝났다고 말하는 화면이 정말 끝인가"*만 봐요.
> 📏 키 화면이 들어와 **여섯이 돼도 이 문장은 그대로 살아 있습니다.**
> (C-2·C-3은 그때 빨간불이 되는데, 그건 고장이 아니라 **이 파일을 읽으라는 신호**예요 —
> 파일에 그렇게 적어 뒀습니다.)

### ③ 🧬 2택 — engineer가 겨누라고 준 넷 ⓐⓑⓒⓓ 전부

| 줄 | 계약 | engineer |
|---|---|---|
| B-1 | `.pb-opt`가 **언제나 정확히 2개** (시드 3 × 🎲 6번) | ⓐ |
| B-1a | ♿ `aria-pressed="true"`가 **하나** — 색이 아니라 글자·속성이 말한다 | (덤) |
| B-2 | 📊 **두 벌 다** 총합 정확히 **194** | ⓑ |
| B-3 | 🌱 아래 등급 **6줄** 그대로 · 2택 줄은 `.pbo-row` **12줄로 따로** | ⓒ |
| B-3a | 🔒 🅱️를 누르면 **표시만 뒤집히고 축은 안 움직인다** (난수 소비 0 · 6칸 평균 그대로 · `build ≡ picks[pick]`) | ⓒ |
| **B-4** ★ | 🎲 한 번의 **난수 소비량 = `rollShape` 두 번**과 정확히 같다 | ⓓ |
| **B-4b** ★ | 🎲 소비량이 **「지금 어느 쪽을 고르고 있었는지」를 안 탄다** | ⓓ |

#### 🎲 ⓓ를 어떻게 잡았나 — **되감을 수 있는 난수 흐름**

> ⚠️ engineer 경고: **「`rollShape` 항상 2회」는 소비량이라 값만 봐서는 안 잡힙니다.**

`__mathRandomCalls`처럼 **횟수만 세면** 문제가 하나 있어요 — `rollShape` 한 번이 쓰는
난수 개수가 **자리마다 다릅니다**(14~16개를 봤습니다). 그래서 「2」를 검사에 못 박아요.

🔧 **뽑은 값을 쌓아 두고 `i`만 옮기는 흐름**을 깔고, 같은 시드로 세 판을 띄워
**같은 자리(`s.i`)에서** 세 갈래를 견줍니다:

```
시드 11: 출발자리 35/35/35 → 🎲 15 ↔ rollShape×2 15 ↔ 🅱️뒤 🎲 15
시드 23: 출발자리 35/35/35 → 🎲 14 ↔ rollShape×2 14 ↔ 🅱️뒤 🎲 14
시드 37: 출발자리 35/35/35 → 🎲 16 ↔ rollShape×2 16 ↔ 🅱️뒤 🎲 16
```

🔑 **횟수 「2」를 안 적고 관계로 씁니다.** `B-4-조건`이 **출발 자리가 같은지**를 스스로 찍어요 —
그게 없으면 B-4는 「다른 것을 견주는」 검사입니다.

---

## 3. ④ 🔴 `passArc` 결정성 — engineer 신고를 재현했고, **원인은 한 줄**입니다

> engineer: *"시드를 고정해도 A/A 대조에서 학교 아크 점수가 달라졌어요 — 제 드라이버가
> 결정적이지 않습니다. ±0.17%는 잡음 폭이고 계수 판정에 쓰지 마세요."*

**재현했습니다.** 같은 시드로 아크를 3번:

| 드라이버 | 아크 결과 (점수/카드 · 판정열) | |
|---|---|---|
| `W.Math.random`만 갈아 끼움 | `8/8 pmpmmmpp` · `7/8 pooommmp` · `9/8 moopmpop` | 🔴 **매번 다름** |
| `+ WingerEngine._t.seed(n)` | `8/8 ppmmmoop` · `8/8 ppmmmoop` · `8/8 ppmmmoop` | ✅ **같음** |

### 원인

`beta/winger2/engine.js:32`

```js
let _rng = Math.random;      // ← 로드 시점에 **함수를 잡아 둡니다**
```

로드 **뒤에** `W.Math.random`을 갈아도 `_rng`는 원래 네이티브 함수를 계속 가리켜요.
그래서 `deal`·`rollOffers`(페이지 `Math.random`)는 걸리는데 **판정(`judgeAtP` → `outcome`)만 안 걸립니다.**
👉 아크 점수가 시드를 안 타고, **판정이 섞인 값은 전부 잡음**이 됩니다.

### 제 드라이버는 결정적입니다 — 그리고 **매번 확인합니다**

`tests/winger2/`의 페이지 드라이버 넷(`school` · `foot-map` · `town` · `offer`)은 전부
`WingerEngine._t.seed()`를 함께 부르고 있었습니다. `town-neutral-test`의 하네스도요.
새로 만든 `creation-test.js`도 같습니다.

🔧 그리고 **그 한 줄이 없어지면 잡히도록 D-0을 양방향으로** 만들었어요:

| | |
|---|---|
| **D-0** | 씨를 다 뿌리면 아크가 판마다 **똑같다** |
| **D-0a** | `_t.seed()`를 **빼면 달라진다** ← 🔑 이게 없으면 D-0은 "원래 같은 값"을 재는 껍데기 |

📮 **engineer에게**: 실측 하네스에 `W.WingerEngine._t.seed(n)` **한 줄**만 더하면
36턴 종합 표를 짝지어 다시 잴 수 있습니다. 그 전까지 그 표의 ±0.17%는 잡음 폭이 맞아요.

---

## 4. 🧪 변이 표 — **전부 기준선 초록불에서 확인했습니다**

### `town-neutral-test.js` (변이 6 · 정규식 7)

| 변이 | 되돌린 것 | 기준선 | 변이 아래 | 갈린 줄 |
|---|---|---|---|---|
| 🧪 M-S1 | 📏 밴드 0축 대칭을 **한 칸만** 깸 | ✅ | ❌ | **S-1만** (E[spotMul] −0.195% → 문턱의 39% · **S-3은 통과**) |
| 🧪 M-S1B | 경계 배열을 `[-4,-2,1,4]`로 | ✅ | ❌ | S-1 + S-3 (5시드 전부 +0.7%) |
| 🧪 M-S2 | 📣 바닥을 0.80으로 | ✅ | ❌ | S-2 + T-4 |
| 🧪 M-C2 | 🎲 흔들림을 0.10/0.50/0.40 | ✅ | ❌ | S-3 (5시드 전부 · 문턱의 2.8배) |
| **🧪🎯 M-REF-CONST** ★ | `get town()` → **숫자 32.0** | ✅ | ❌ | **S-3e 등식 · S-3e-b 종속 · S-3f 드리프트 · S-3d 분포 = 4/4** |
| 🧪 M-G | `clamp` 제거 + 흔들림 ±2 | ✅ | 💥 던짐 | T-4 |

### `creation-test.js` (변이 5 · 정규식 6)

| 변이 | 되돌린 것 | 갈린 줄 | 값 쪽은? |
|---|---|---|---|
| 🧪 M-STEPS3 | 🔢 `STEPS` 5칸 → **3칸** | **C-2 · C-3 · C-4 = 3/3** (`"1/3" "2/3" "3/3" "" ""`) | — |
| 🧪 M-NOSTAMP | 🔢 `stampStep("screen-prospect")` 삭제 | **C-2 · C-3 · C-4 = 3/3** (`… "4/5" ""`) | — |
| 🧪 M-PICK1 | 🧬 2택 → 1택 (`open()`·`rollPair` **양쪽**) | **B-1** (`.pb-opt` 0개) | — |
| 🧪 M-PICKBIG | 한쪽 벌 총합을 **200**으로 | **B-2** (194 / 200) | — |
| **🧪🔑 M-PICKCOND** ★ | 🅱️를 고른 채로는 **한 벌만** 굴림 | **B-4b만** (🅰️뒤 15 ↔ 🅱️뒤 7) | 🟢 `.pb-opt` 2개 · 총합 194/194 — **멀쩡해 보입니다** |

> 🔑 **M-PICKCOND가 이 파일의 존재 이유입니다.** 값 쪽이 완전히 정상인데 소비량만 갈려요 —
> **값만 보는 검사는 이걸 절대 못 잡습니다.**

---

## 5. 📊 검증 결과 — 통과 / 실패 / **검증 불가**

### ✅ 통과 (검사 21종 · 실패 0건)

```
award · bench · creation ★ · engine · foot-map · grade · league · moment · mutation ·
neutral · offer · prospect · school · town-neutral ★ · town · wiring · worldcup ·
youth-ability · youth-card · youth-clamp · youth-moment
smoke-test beta ✅ 9종  ·  check-page-test ✅
```

경계면 등록 지점도 다시 확인했습니다 — `beta/index.html`(GAMES) · `beta/cloud.js`(4곳) ·
`beta/stats/index.html`(GAMES) · `tests/smoke-test.js`(`GAMES`+`BETA_ONLY`) ·
`scripts/sync-beta.sh`(`CONTENT`) · `beta/_check.html`(`GAME_ORDER`) **전부 winger2 있음.**
`beta/winger2/sw.js`의 `ASSETS`도 `index.html`이 싣는 파일과 맞습니다
(`ads.js`·`cloud.js`·`stats.js`가 빠진 건 **8종 전부 같은 관례**라 결함이 아니에요).

### ❌ 실패

없습니다.

### 🚧 검증 불가 — **조용히 넘기지 않습니다**

📮 **grow-director에게 — 실기기 확인 목록으로**

| # | 무엇 | 왜 기계가 못 보나 |
|---|---|---|
| 1 | 🧬 2택이 **390px에서 나란히** 보이는가 (`.pb-opts`가 세로로 안 쌓이는가) | 레이아웃. jsdom엔 렌더 박스가 없어요. 🔴 스크롤이 둘을 가르면 **견줌이 사라집니다** |
| 2 | 🅰️🅱️의 **고른 쪽 대비**가 색맹·고대비에서 읽히는가 | 색. `aria-pressed`와 글자(`고른 모양`/`이걸로`)는 B-1a가 지켰습니다 |
| 3 | 🎲 뒤 **초점 이동**이 낭독기에서 자연스러운가 | 초점 이동 여부는 코드로 보이지만 *"자연스러운가"*는 사람 판단 |
| 4 | 🔢 **「3 / 5」 뒤에 경기가 나오는 게 어색하지 않은가** | 서사 판단. 검사는 *"끝났다고 거짓말 안 하는가"*만 봅니다 |
| 5 | 👟 주발 이모지 · 🗺️ 지도 (다른 세션 미커밋 변경) | CSS. 기준선이 움직이는 중이라 안 겼습니다 |
| 6 | 🦶 주발 **320ms 전환**의 손맛 | 타이밍 감각 |

⚠️ **미니게임 · 🏫 초중고 경기 진행에는 검사를 안 걸었습니다** — 범민 님 답을 기다리는
중이라, 지금 걸면 판정이 오는 날 **「옛 계약을 지키는 검사」**가 됩니다.

---

## 6. 📮 남기는 것

### grow-engineer에게

1. **`beta/winger2/engine.js:32` `let _rng = Math.random;`** — 실측 하네스에
   `W.WingerEngine._t.seed(n)` **한 줄**을 더하세요. 그 전까지 36턴 표는 짝이 안 맞습니다 (§3).
2. `beta/winger2/focus.js`(299줄)가 **디스크에 있는데 `index.html`에도 `sw.js` `ASSETS`에도
   없습니다.** 미커밋(다른 세션)이라 손대지 않았어요 — 배선을 붙일 때 **`ASSETS`도 같이** 채워
   주세요. 🔴 안 넣으면 온라인에선 멀쩡하고 **오프라인에서만** 깨집니다.

### grow-designer / grow-balancer에게

- 📏 키 화면이 들어오면 `creation-test.js`의 `SCREENS`에 **한 줄 더하면** 됩니다
  (C-2·C-3이 그날 빨간불이 되는 건 **읽으라는 신호**예요 — C-4는 그대로 삽니다).
- 🔒 **🧬 조립대를 🏫 학교 앞으로 옮기는 판정**이 나오면 `town-neutral-test.js`의
  **S-3e 등식이 통째로 옛 계약**입니다. 「전원 같은 몸」이 깨지면 한 장의 기대 점수가 1이 아니게 돼요.

### 다음 사람에게 — 이번에 배운 것

> ⏳ **「곧 죽을 것」이라 검사를 미룰 때는, 죽었는지 확인하는 사람을 정해 두세요.**
> `foot-map-test.js`가 넷을 미뤘고 **셋은 실제로 죽었는데 `STEPS`만 살아남았습니다.**
> 비어 있는 채로요. 그 파일 머리말에 이 사실을 적어 뒀습니다.

> ⚠️ **문턱을 통과한 편향에도 원인이 있습니다.** *"±0.5% 안이니 통과"*가
> `PEER_REF.town`의 불일치를 **여덟 판 내내 숨겼어요.** 검사가 이미 적어 둔 줄이었는데도요.
