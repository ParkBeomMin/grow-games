# 실측 — ⚽ 더 윙어 II · 선수 만들기 (3택 → 🎲 굴리기)

2026-08-30 · grow-balancer
입력: `74_designer_player-creation.md` §balancer · `13_designer_v2-final.md` · `70_designer_ui-overhaul.md`
선행 실측: `27_balancer_prospect-curve.md` · `28_balancer_prospect-close.md`

> **소스 동결 확인** — 측정 시작 전과 끝난 뒤가 같습니다 (sha256).
>
> | 파일 | sha256 (앞 16) |
> |---|---|
> | `beta/winger2/prospect.js` | `5a7abb5d0e1c4676` |
> | `beta/winger2/game.js` | `d4be35a9b5b3d4b8` |
> | `beta/winger2/engine.js` | `5df13cd352604f9d` |
> | `beta/winger2/career.js` | `203724f19e113090` |
> | `beta/winger2/squad.js` | `f7666517eb79d161` |
> | `beta/winger2/grade.js` | `4df04b8b8b6da113` |
>
> `beta/`·`tests/`는 **읽기만** 했고 아무것도 안 고쳤습니다. 커밋하지 않았습니다.
> `git status`는 시작·끝 모두 `?? beta/winger2/focus.js` 하나뿐입니다(제 것이 아니고, 미완성 고아 파일이라 손 안 댔어요).
> ⚠️ 중간에 제 `d2.out`·`d3.out` 네 개가 저장소 루트에 떨어진 적이 있는데 **지웠습니다** (내용 0바이트).

---

## 판정 요약

| # | 무엇 | 목표 | 실측 | 판정 |
|---|---|---|---|---|
| **D** | 🎲 예산 0 → 무제한, 커리어 총 공격P | ±3% | **+1.37% ± 0.18%p** | ✅ **전제가 섭니다** |
| **B-0** | `spread(194,…)` 총합 ≠ 194 비율 | 0% | **0.0000%** (30만 회) | ✅ |
| **A-2** | `HEIGHT_SHAPE` 6칸 평균 | 1.000 | **1.000000000000** | ✅ |
| **A-0** | 키만 바꾼 커리어 (포지션 고정) | ±5% | **0.15 ~ 0.88%** | ✅ |
| **A** | 키 4 × 포지션 4 = 16조합 | ±8% | **1.14%p** (수상 기준 2.31%p) | ✅ |
| **A-3** | 몰빵이 평균 1을 깨뜨리나 | ±3% | **0.84%** | ✅ |
| **C** | 잠재력 잠금이 곡선을 얼마나 내리나 | 복원 | **−0.29%** (새 예산0) → **+0.56%** (새 예산3) | ✅ **안 내려갑니다** |
| **B** | ⭐ 특능 12종 값어치 폭 · `max of 4` | 좁아야 | **0.000%** | 🔴 **잴 대상이 없습니다** |

> ### 🟢 **①(예산 3회)·②(총합 고정)·③(키 b′)·⑤(3택 폐기)는 전부 실측을 통과했습니다.**
>
> 그리고 designer가 가장 걱정한 것(*"잠재력 잠금이 곡선을 끌어내린다"*)은 **반대로 나왔습니다** —
> 새 방식(🎲 3 + 🔒 잠금)이 현행보다 **+0.56% ± 0.23%p 높습니다.**
> **`rollTalents`의 하한을 올릴 이유가 없습니다.** 올리면 오히려 더 올라가요.

### 🔴 그런데 **한 자리가 샙니다 — 🧬 성장타입입니다**

designer는 🎁 타고난 것 묶음에 **🧬 성장타입 + ⭐ 특능 + 🩹 결함** 셋을 넣고 🎲 한 번에 함께 굴리게 했습니다.
그런데 셋 중 **둘(⭐·🩹)은 값어치가 정확히 0**이고, **🧬 하나만 살아 있습니다.**
그래서 🎁 리롤은 사실상 **「성장타입 다시 뽑기」**입니다.

같은 환경·같은 시드에서 **성장타입을 고를 수 있느냐만** 바꿔 잰 값입니다:

| 🎲가 무엇을 굴리나 | 예산 0 → ∞ 커리어 총 | 첫 3시즌 | 성장타입 조/보/**만** (0회 → ∞) |
|---|---|---|---|
| 📊 배분만 (🧬 잠금) | **+0.35% ± 0.18%p** | +3.56% | 34/31/**36** → 35/31/**34** |
| 📊 배분 + 🧬 성장타입 | **+1.37% ± 0.18%p** | **+19.84%** | 34/31/**36** → 71/29/**0** |

**커리어 총량은 둘 다 ±3% 안입니다.** 무너지는 건 곡선이 아니라 **타입 다양성**이에요 —
🌳 만성이 **36% → 10%(예산 3) → 0%(무제한)**로 사라집니다.

원인은 `HINTS`의 **0**입니다 (`prospect.js:76-80`) —
`"또래보다 몸이 빨리 여물었다"`의 가중이 `[0.7, 0.3, **0.0**]`이라
**그 코멘트가 뜨면 만성일 확률이 정확히 0**입니다. 굴려서 그 코멘트를 기다리면 만성을 **확실히** 피할 수 있어요.

→ **🧬 성장타입을 🎁에서 빼서 📏 잠금 쪽으로 옮기세요.** (아래 §6 확정 계수)

---

## 무엇을 쟀나

### 부르는 것은 전부 살아 있는 함수입니다

- **엔진** — `tests/winger2/_load.js`가 `beta/winger2/engine.js`를 진짜로 부릅니다 (`playMatch`·`autoMatch`·`shareByWeight`·`blendOf`·`K`)
- **`prospect.js` 전체** — 파일째 `new Function`에 태워 `rollCards`·`_t.spread`·`_t.rollTalents`·`nowStats`·`ageMul`·`trainMul`·`birthday`·`seasonTally`·`suggestRetire`·`mustRetire`를 그대로 씁니다
- **엔진 밖 상수·산식** — `career.js`(`POS_AXIS`·`AXIS_K`·`AXIS_OFF`·`RATE`·`ratingParts`·`matchRating`·`posAxis`·`hype`·`MOM_MIN`·리그MVP/베스트11 판정) · `squad.js`(`POS_SLOTS`·정규화 IIFE·`FORMATION`·`AGE_MIN~RETIRE_AGE`·`ageCurve`·`STR_SPREAD`·`MEAN_CURVE`·`YOUTH_BONUS`·`BENCH_GAIN`·`BENCH_COND`) · `game.js`(`LEAGUES`·`CLUBS`·`STAT_DEFS`·`POS_INFO`·`STAT_CAP`·`TALENT_MAX`·`talentStars`)를 **정규식으로 뜯어옵니다**
- **훈련 산식** — `career.js prepAction`의 `gain` 줄 · `condMod` · 컨디션 소모 · 휴식 · 실패 확률, `game.js doTraining`의 유스 판, `WEEKS_PER_CB`·`CB_PER_YEAR`를 **소스에서** 뜯어 시즌당 78턴을 셉니다
- 🔒 **직접 `eval`을 쓴 곳이 없습니다.** 전부 `new Function(...)` + `return`입니다

**아직 소스에 없는 것 하나 — `HEIGHT_SHAPE`.** 이건 designer 문서에만 있어서
`docs/superpowers/_workspace/74_designer_player-creation.md`에서 **정규식으로 뜯었습니다**
(산식의 `180`·`150`, `dir` 여섯 값, 키 범위 `165~195`·중앙 `180`). 값을 옮겨 적지 않았어요 —
designer가 문서를 고치면 시뮬레이션이 따라 움직입니다. `dir`이 6칸이 아니면 그 자리에서 던집니다.

### ⚠️ 정규식 하나가 죽어 있었습니다 — 고치고 시작했습니다

이전 하네스(`v2/bits.js`)의 베스트11 추출 정규식이 `if \(hype >= 4\.5 \* bar\)`로 박혀 있어서,
28번 실측 뒤 engineer가 문턱을 **5.8**로 바꾸자 **정규식이 안 걸려 하네스가 죽었습니다.**
값에 의존하지 않게 `if \(hype >= [\d.]+ \* bar\)`로 고쳤습니다.
(이 저장소가 세 번 데인 자리 그대로예요 — 값이 박힌 변이 정규식.)

### 측정 조건 — 전부 적습니다

| | 값 | 어떻게 정했나 |
|---|---|---|
| **리그 / 클럽** | 🇰🇷 K리그1 · 클럽 전력 **52·57·62·66·71·78** · 내 클럽 **62** | 28번과 **같은 환경** (`CLUBS=52,57,62,66,71,78 MYCLUB=2`) |
| **데뷔 나이** | 만 20세 (`CARD_AGE 17` + 유스 3년) | 28번 §0 |
| **커리어 길이** | 유스 36턴 → 프로 **19시즌** (만 20~38) | `mustRetire`가 판정 |
| **훈련 턴** | 시즌당 **78턴** · **경기 사이에** 배치 (개막 3 → R1 → 2 → R2 …) | 28번 §1-ⓑ의 정정을 그대로 승계 |
| **훈련 배치(조작자 모델)** | 기본 **골고루(rr)** · 대조로 **몰빵 BLEND** · **몰빵 재능順** | 27·28번이 쓴 모델이 rr이라 기준선을 맞췄습니다 |
| **조작 성공도** | `s = 0.522` | 26번 실측 |
| **컨디션** | 진짜 고리 (훈련 소모·휴식·벤치 회복) | 고정하지 않았습니다 |
| **표본** | 아래 표 참조. **전부 짝지은 표본**(같은 카드·같은 잠재력·같은 커리어 시드, 바꾼 축 하나만) | |
| **시드** | 뽑기 `SEED0 + i×7919 + pos.charCodeAt(0)×977` · 커리어 `SEED0×3 + i×104729 + pos×31` · `SEED0 ∈ {4400001, 5500001, 6600001, 7700001, 8800001, 9200001}` | 완전 재현 |

| 측정 | 표본 |
|---|---|
| B-0 | `spread` **300,000회** (10만 × 시드 3벌) + 변이 4,000회 |
| A-2 | 165.000 ~ 195.000cm를 0.001 눈금으로 **30,001점** |
| A-3 | 유스 36턴 **각 칸 400벌 × 4포지션 × 5키 × 5정책 = 40,000벌** |
| D | **1,200커리어/예산 × 5예산 × 4정책 = 24,000커리어** |
| C | **600커리어/변이 × 7변이 = 4,200커리어** |
| A-0 / A | **300커리어/칸 × 16칸 = 4,800커리어** |
| 현행↔새 | **680커리어/변이 × 8변이 = 5,440커리어** |
| B | **240커리어/칸 × 12칸** + 탐침 1커리어 + 양성 대조 360커리어 |

### 기준선 재현 확인 — **곡선은 안 움직였습니다**

28번이 남긴 다섯 점을 **오늘 소스에서 같은 스크립트·같은 시드·같은 표본으로** 다시 쟀습니다.

| | 70 | 90 | 110 | 130 | 150 |
|---|---|---|---|---|---|
| 28번 (2026-08-29) | 5.8 | 24.6 | 63.6 | 91.0 | 99.0 |
| **오늘 재현** | **6.0** | **23.9** | **62.4** | **91.6** | **98.8** |

시즌 1,600회/칸. 다섯 점 전부 잡음 안입니다. **기준선은 그대로예요.**

> ⚠️ 처음에 기본 리그 환경(전력 76~66 · 내 클럽 70)으로 잘못 돌려 `4.9 / 21.4 / 56.6 / 89.7 / 98.7`이 나왔고
> **회귀로 오인할 뻔했습니다.** 원인은 소스가 아니라 **환경 변수**였어요 —
> 28번의 `careers.sh`가 `CLUBS=52,57,62,66,71,78 MYCLUB=2`를 걸고 돌렸습니다.
> `career.js`·`squad.js`·`game.js`에서 하네스가 뜯어 쓰는 **모든 조각을 옛 커밋과 바이트 비교**해
> 하나도 안 바뀐 것을 확인했고, `engine.js`는 파일 전체가 동일했습니다.
> **🔴 곡선 값을 인용할 땐 클럽 전력 환경을 반드시 함께 적으세요** — 같은 소스에서 110이 62.4 ↔ 56.6으로 갈립니다.

---

## 중립 검증

### B-0 · `spread(194, …)`의 총합 고정 — **0.0000%**

살아 있는 `WingerProspect._t.spread`를 유스 5종 × 포지션 4종 × `shapeKey` 6종에 골고루 태워 굴렸습니다.

| 시드 | 총합 ≠ 194 | 관측 총합 범위 | 하한(18)에 닿은 칸 | 상한(54)에 닿은 칸 |
|---|---|---|---|---|
| 777001 | **0.0000%** (0 / 100,000) | [194, 194] | 0.00% | 0.79% |
| 991117 | **0.0000%** (0 / 100,000) | [194, 194] | 0.00% | 0.78% |
| 20260830 | **0.0000%** (0 / 100,000) | [194, 194] | 0.00% | 0.77% |

**🔬 변이 검증** — `if (!room.length) break;`가 정말 걸리는 상황을 만들어 검사가 빨간불을 내는지 확인했습니다.

| 변이 | 총합 ≠ 목표 |
|---|---|
| 목표를 `6 × STAT_HI + 1`(담을 수 없음)로 | **100.0%** ✅ 빨간불이 뜹니다 |
| 목표를 `6 × STAT_LO − 1`(담을 수 없음)로 | **100.0%** ✅ 빨간불이 뜹니다 |

> **판정: 총합 고정은 지켜지고 있고, 그걸 재는 검사도 눈이 멀지 않았습니다.**
> 상한(54)에 닿는 칸이 0.78%나 되는데도 우수리 되돌리기가 전부 회수합니다.

### A-2 · `HEIGHT_SHAPE` 6칸 평균 — **1.000000000000**

`1 + (키 − 180) / 150 × dir[k]` · `dir = { 체력 +1, 수비 +1, 스피드 −1, 드리블 −1, 슛 0, 패스 0 }`

| 키 | 🫀 체력 | 🛡️ 수비 | ⚡ 스피드 | 🏃 드리블 | ⚽ 슛 | 🎯 패스 | **6칸 평균** |
|---|---|---|---|---|---|---|---|
| 165 | 0.900 | 0.900 | 1.100 | 1.100 | 1.000 | 1.000 | **1.000000000000** |
| 175 | 0.967 | 0.967 | 1.033 | 1.033 | 1.000 | 1.000 | **1.000000000000** |
| 180 | 1.000 | 1.000 | 1.000 | 1.000 | 1.000 | 1.000 | **1.000000000000** |
| 185 | 1.033 | 1.033 | 0.967 | 0.967 | 1.000 | 1.000 | **1.000000000000** |
| 195 | 1.100 | 1.100 | 0.900 | 0.900 | 1.000 | 1.000 | **1.000000000000** |

165.000~195.000을 0.001 눈금으로 **30,001점** 전수. 산술평균 최대 이탈 **2.220e-16** (부동소수점 한 톨).
(참고: **기하평균**은 1이 아닙니다 — 165·195에서 −0.33%. 훈련은 칸마다 따로 곱해 **더하는** 구조라 산술평균이 맞는 자입니다.)

### 🚨 `dir`의 여섯 값은 **한 벌입니다 — 따로 넣을 수 없습니다**

넷 중 하나만 빼면(= 요약에서 한 요소를 지우면) 평균 1이 그 자리에서 깨집니다.

| 무엇을 빼면 | 195cm에서 6칸 평균 | 어긋남 |
|---|---|---|
| 🏃 드리블 −1 을 빼면 | 1.016667 | **+1.67%** ❌ |
| 🛡️ 수비 +1 을 빼면 | 0.983333 | **−1.67%** ❌ |
| 🫀 체력 +1 을 빼면 | 0.983333 | **−1.67%** ❌ |
| ⚡ 스피드 −1 을 빼면 | 1.016667 | **+1.67%** ❌ |
| **여섯 값 전부** | **1.000000** | **0** ✅ |

> ### 🔴 그리고 이 깨짐은 **커리어 지표로는 안 잡힙니다.**
>
> `+1` 둘만 넣은 반쪽 `dir`(6칸 평균 1.0333)로 800커리어를 굴려 봤습니다 —
> 커리어 총 공격포인트가 **+0.13%**밖에 안 움직였어요. 잡음 안입니다.
> `−1` 둘만 넣은 쪽(평균 0.9667)도 **−0.46%**입니다.
>
> **커리어 회귀 검사로는 반쪽 `dir`을 절대 못 잡습니다.**
> 능력치가 4시즌이면 상한에 닿아 초기 배수 차이를 통째로 삼키거든요 (§아래 「왜 이렇게 둔감한가」).
> → inspector는 **6칸 평균이 정확히 1인지를 직접 검사**해야 합니다. 커리어로 재면 안 됩니다.

### ⑥ 옛 세이브 — `S.height`가 없으면 **키 180과 비트 단위로 같습니다**

| 변이 | 6칸 평균 | 커리어 총 공격P | 최고 유효 능력치 |
|---|---|---|---|
| 키 180 (중앙) | 1.0000 | 480.2 ± 5.6 | 129.7 |
| **⑥ 옛 세이브 (`height` 없음 → 아무것도 안 곱함)** | 1.0000 | **480.2 ± 5.6** | 129.7 |

800커리어 전부 **한 커리어도 안 다릅니다.** 읽는 쪽에서 180을 주든 곱셈 자체를 건너뛰든 결과가 같아요 —
`HEIGHT_SHAPE(180)`이 여섯 칸 모두 정확히 1.000이기 때문입니다. **기본값이 중립입니다.**

---

## 측정값

### 🔴 D — 🎲 예산 0 / 1 / 3 / 5 / 무제한 (**먼저 잰 것**)

**같은 후보 열을 미리 뽑아 두고 예산만 바꿔 고르게** 했습니다. 잠재력은 예산 전체가 같은 값(1회 굴림·잠금),
커리어 난수도 예산마다 같은 시드에서 출발합니다. 예산 사이의 차이는 **오직 「어느 카드를 골랐나」**뿐이에요.

#### ⓐ 📊 배분만 굴릴 때 (🧬 성장타입은 못 고름)

플레이어는 등급 6줄을 보고 **유효 능력치(BLEND 가중합)를 최대화**합니다 — 화면에서 낼 수 있는 가장 센 최적화예요.

| 예산 | 커리어 총 공격P | vs 0회 | 첫 3시즌 | vs 0회 | 총 수상 | MVP | 데뷔 출전 | **고른 카드 blend** | 조/보/만 |
|---|---|---|---|---|---|---|---|---|---|
| **0회** | 479.5 ± 4.5 | 0.00% | 49.1 | 0.00% | 37.88 | 12.37 | 22.9 | 35.08 | 34/31/36 |
| 1회 | 480.0 ± 4.5 | +0.12% | 49.9 | +1.55% | 37.80 | 12.32 | 23.3 | 36.68 | 35/32/33 |
| **3회** | 481.2 ± 4.6 | **+0.36%** | 50.1 | +2.05% | 38.02 | 12.39 | 23.2 | 38.30 | 34/33/33 |
| 5회 | 480.6 ± 4.6 | +0.23% | 50.3 | +2.38% | 37.92 | 12.39 | 23.1 | 39.12 | 35/31/34 |
| **20회(≈무제한)** | 481.2 ± 4.6 | **+0.35%** | 50.9 | +3.56% | 38.02 | 12.42 | 23.2 | 41.30 | 35/31/34 |

**짝지은 차이 (∞ − 0) : +1.70 ± 0.87 포인트 = +0.35% ± 0.18%p**

> **고른 카드의 유효 능력치가 35.08 → 41.30으로 17.7% 올랐는데 커리어는 0.35%밖에 안 움직입니다.**
> 이게 designer가 말한 *"총합이 고정이면 모양만 바뀐다"*의 실측입니다.

**몰빵 조작자로 다시** (BLEND 세 칸만 훈련) — `+0.02% ± 0.18%p`. 더 작아집니다.

#### ⓑ 📊 배분 + 🧬 성장타입까지 고를 때 (스카우트 코멘트로 거르기)

| 예산 | 커리어 총 공격P | vs 0회 | **첫 3시즌** | vs 0회 | 총 수상 | MVP | 데뷔 출전 | 조/보/**만** |
|---|---|---|---|---|---|---|---|---|
| **0회** | 479.5 ± 4.5 | 0.00% | 49.1 | 0.00% | 37.88 | 12.37 | 22.9 | 34/31/**36** |
| 1회 | 481.8 ± 4.6 | +0.49% | 52.4 | +6.54% | 38.02 | 12.37 | 25.2 | 46/31/**23** |
| **3회** | 484.7 ± 4.6 | **+1.10%** | 55.7 | **+13.25%** | 38.21 | 12.48 | 27.7 | 59/32/**10** |
| 5회 | 485.1 ± 4.6 | +1.18% | 57.1 | +16.25% | 38.30 | 12.52 | 28.7 | 64/31/**5** |
| **20회(≈무제한)** | 486.0 ± 4.6 | **+1.37%** | 58.9 | **+19.84%** | 38.34 | 12.52 | 29.6 | 71/29/**0** |

**짝지은 차이 (∞ − 0) : +6.57 ± 0.85 = +1.37% ± 0.18%p** (첫 3시즌은 **+19.84% ± 0.86%p**)

#### ⓒ 오라클 (성장타입이 **완전히 공개**됐다고 가정 — 상한선)

`+1.95% ± 0.17%p`. 성장타입 분포는 무제한에서 **100% 보통**으로 수렴합니다.
(커리어 총량으로 재면 🌿 보통이 최고고, 🌱 조숙 −0.49%, 🌳 만성 −4.10%입니다 — 440커리어/타입, 카드·잠재력·시드 전부 같음.)

> ### 판정 D — **✅ 통과. designer 문서의 전제가 섭니다.**
>
> 0회 ↔ 무제한이 **+0.35% ~ +1.95%**로, 어느 정책에서도 **±3% 안**입니다.
> 그리고 **예산 3이 이미 무제한의 대부분을 먹습니다** (1.10 / 1.37) —
> 즉 **예산 숫자가 곡선의 손잡이가 아닙니다.** ①(예산 3회)의 근거가 성립합니다.
>
> 🟡 다만 **커리어 총량이 아니라 「첫 3시즌」과 「타입 분포」가 밀립니다** (ⓑ).
> 이건 곡선 사고가 아니라 **다양성 사고**예요. 손잡이는 §6에 적었습니다.

#### 🔑 왜 이렇게 둔감한가 — **능력치 상한이 4시즌 만에 모두를 같은 자리에 세웁니다**

240커리어의 궤적입니다 (훈련 골고루):

| 시즌 | 나이 | 정점 기준값 (6칸 평균) | 유효 능력치 | 출전 | **상한(130) 도달 비율** |
|---|---|---|---|---|---|
| 1 | 20 | 88.4 | 69.5 | 23.2 | 0.0% |
| 2 | 21 | 110.2 | 91.8 | 37.9 | 0.0% |
| 3 | 22 | 124.8 | 109.5 | 38.0 | 17.9% |
| 4 | 23 | 129.7 | 116.2 | 38.0 | **94.2%** |
| 5 | 24 | 129.9 | 118.7 | 38.0 | **100.0%** |
| 6~19 | 25~38 | 129.9 | 120.5 → 하락 | 38.0 | 100.0% |

> **만 24세부터는 모든 커리어의 정점 기준값이 같습니다** (`STAT_CAP = 130`, `game.js:436`).
> 그 뒤로 선수를 가르는 건 **나이곡선(성장타입) 하나**뿐이에요.
>
> 🔴 **그래서 D의 결론은 「총합 고정」만의 공이 아닙니다.** 능력치 상한이 절반을 합니다.
> `STAT_CAP`을 올리거나 커리어를 짧게 만들면 **D를 다시 재야 합니다.**
> 그리고 🎲·잠재력·키의 효과가 **전부 데뷔~3시즌에 갇혀 있다**는 뜻이기도 합니다 —
> 커리어 총량으로만 보면 어느 것도 안 움직이는 것처럼 보여요.

---

### C — 📏 잠재력 잠금 (`max of 3` → 1회)

#### ⓐ 먼저 정정 — **현행은 `max of 3`이 아닙니다**

`prospect.js:799-806`의 🎲 버튼은 `draw.cards`·`draw.talents`를 **통째로 덮어씁니다.**
되돌리기가 없어요. 즉 현행은 **무회상 최적정지(3벌)**이지 `max of 3`이 아닙니다.
`max of 3`은 **도달 못 하는 상한**입니다. 셋 다 쟀습니다 (문턱은 분포에서 실측: q1 = 1.1750 · q2 = 1.1334).

| 잠재력을 어떻게 얻나 | 평균 재능 | 커리어 총 공격P | vs `max3` |
|---|---|---|---|
| **1회 굴림 (새 방식 · 잠금)** | 1.1365 | 468.5 ± 6.4 | **−0.58% ± 0.15%p** |
| **무회상 최적정지 (= 진짜 현행)** | 1.1860 | 470.3 ± 6.4 | −0.21% ± 0.09%p |
| `max of 3` (도달 못 하는 상한) | 1.2000 | 471.2 ± 6.4 | 0.00% |

→ **잠금이 진짜 현행 대비 잃는 것은 −0.37%입니다.** designer가 걱정한 "곡선이 내려간다"의 크기가 이겁니다.

#### ⓑ 손잡이 (`rollTalents` 하한) 를 돌려 봤습니다 — **거의 안 듣습니다**

| 하한 | 평균 재능 | 커리어 총 공격P vs `max3` | 데뷔 출전 | 상한 도달 시즌 |
|---|---|---|---|---|
| **0.80 (현행)** | 1.1365 | −0.58% | 22.3 | 3.85 |
| 0.85 | 1.1586 | −0.37% | 22.8 | 3.76 |
| 0.90 | 1.1811 | −0.34% | 23.3 | 3.68 |
| 0.95 | 1.2041 | −0.26% | 23.7 | 3.57 |
| 1.00 | 1.2277 | −0.08% | 24.2 | 3.48 |

**하한을 0.80 → 1.00으로 25% 올려도 커리어는 0.50%p밖에 안 움직입니다.** (상한이 삼킵니다.)

#### ⓒ 🚨 하한을 올리면 **⭐ 별 표시가 무너집니다 — 이 셋은 한 벌입니다**

`talentStars = clamp(round((t − 0.6) / (1.8 − 0.6) × 5), 1, 5)` (`game.js:851`)

| 하한 | ⭐1 | ⭐2 | ⭐3 | ⭐4 | ⭐5 |
|---|---|---|---|---|---|
| **0.80 (현행)** | **20.3%** | 41.2% | 36.9% | 1.5% | 0.0% |
| 0.85 | 15.2% | 43.2% | 40.0% | 1.7% | 0.0% |
| 0.90 | 9.0% | 45.5% | 43.7% | 1.8% | 0.0% |
| 0.95 | 1.7% | 48.1% | 48.2% | 2.0% | 0.0% |
| **1.00** | **0.0%** ❌ | 44.3% | 53.5% | 2.2% | 0.0% |

**하한 1.00에서 ⭐1성이 사라집니다** (현행 20.3%). 잠재력 화면이 2~3성 두 칸으로 납작해져요.
`rollTalents` 하한 · `t[ps] = max(t[ps], 1.05)` 포지션 바닥 · `talentStars` 눈금 — **셋을 함께 봐야 합니다.**

#### ⓓ 끝에서 끝까지 — **현행 ↔ 새 방식** (같은 환경 · 680커리어/변이 · 짝지은 표본)

| | 평균 재능 | 카드 blend | 커리어 총 공격P | vs 현행 | 첫 3시즌 | 총 수상 | MVP | 데뷔 출전 |
|---|---|---|---|---|---|---|---|---|
| 현행 3택 · 🎲 0회 | 1.1351 | 38.03 | 480.8 ± 6.1 | −0.09% | 50.6 | 37.89 | 12.44 | 23.1 |
| **현행 3택 · 🎲 2회 (무회상) ← 기준** | 1.1801 | 37.86 | **481.2 ± 6.1** | **0.00%** | **51.3** | **38.32** | **12.53** | **23.8** |
| 현행 3택 · 🎲 2회 (상한 `max3`) | 1.1982 | 37.86 | 480.9 ± 6.1 | −0.08% | 51.6 | 38.21 | 12.49 | 24.1 |
| 새 · 🎲 **0회** · 🔒 잠금 | 1.1306 | 35.21 | 479.9 ± 6.1 | **−0.29% ± 0.24%p** | 50.5 | 37.77 | 12.43 | 23.3 |
| **새 · 🎲 3회 · 🔒 잠금 · 하한 0.80 그대로** | 1.1306 | 36.69 | **483.9 ± 6.1** | **+0.56% ± 0.23%p** | **55.3** | 37.87 | 12.44 | **27.2** |
| 새 · 🎲 3회 · 잠금 · 하한 0.85 | 1.1532 | 36.69 | 483.4 ± 6.1 | +0.45% | 55.9 | 37.86 | 12.44 | 27.6 |
| 새 · 🎲 3회 · 잠금 · 하한 0.90 | 1.1763 | 36.69 | 484.5 ± 6.1 | +0.68% | 56.5 | 37.98 | 12.44 | 28.1 |
| 새 · 🎲 3회 · 잠금 · 하한 0.95 | 1.1997 | 36.69 | 485.7 ± 6.1 | +0.92% | 57.3 | 38.29 | 12.55 | 28.4 |

> ### 판정 C — **✅ 통과. 곡선이 안 내려갑니다. 하한을 건드리지 마세요.**
>
> 잠금이 잠재력을 −4.2%(1.1801 → 1.1306) 깎는데도, **🎲 3회의 이득이 그보다 커서**
> 새 방식(하한 **0.80 그대로**)이 현행보다 **+0.56% ± 0.23%p 높습니다.**
>
> 🔴 **하한을 올리는 건 「복원」이 아니라 「추가 상향」입니다.**
> 0.95를 넣으면 **+0.92%**가 되고 ⭐1성이 20.3% → 1.7%로 사라집니다.
> designer 문서의 *"하한을 올려 기댓값을 회복한다"*는 **실측상 필요 없습니다.**
>
> 🟡 유일하게 눈에 띄는 건 **데뷔 출전 23.8 → 27.2 (+14%)**와 **첫 3시즌 +7.8%**입니다.
> 이건 잠재력이 아니라 **🎁 리롤이 성장타입을 밀어서** 생긴 것이고(§D-ⓑ), 그 손잡이는 §6에 있습니다.

---

### B — ⭐ 특능 · 🩹 결함의 값어치 — **0.000%. 잴 대상이 없습니다.**

#### ⓐ 먼저 — 소스에 **12종이 없습니다**

| | 설계 문서 | 디스크 (`prospect.js`) |
|---|---|---|
| ⭐ 특능 | "일반 **12종**" | **3종** (늦은 시간의 사나이 · 추격자 · 빅게임 플레이어) |
| 🩹 결함 | 8종 | **8종** ✅ |

`prospect.js:112`의 주석이 그 이유를 적어 뒀습니다 — *"나머지 9종은 designer가 특능 엔진(§11-7)에서 확정합니다 — 지어내지 않았어요."*

#### ⓑ 🔬 탐침 — 커리어 한 벌 동안 **몇 번 읽는지** 셌습니다

`S`의 칸에 세는 getter를 걸고 커리어(19시즌 × 38경기 + 훈련 1,482턴)를 통째로 굴렸습니다.

| 세이브 칸 | 읽은 횟수 |
|---|---|
| `S.stats` | 10,792회 |
| `S.growthType` | 1,438회 |
| `S.talents` | 267회 |
| **`S.traits`** | **0회** ❌ |
| **`S.flaw`** | **0회** ❌ |
| **`S.traitSlots`** | **0회** ❌ |

같은 탐침에서 `S.stats`가 만 번 읽힙니다 — **탐침이 안 도는 게 아니라 읽는 코드가 없습니다.**

저장소 전체 확인도 같습니다. `S.traits`·`S.flaw`·`S.traitSlots`를 만지는 곳은 **쓰기 3줄과 화면 3줄**뿐이고,
`prospect.js`가 내보내는 `flawOf()`·`traitsOf()`는 **호출부가 저장소에 하나도 없습니다.**
경기·수상에 들어가는 배수(`buffMul`·`activeBuffs`)는 `S.buffs`(시즌 칭호)만 읽습니다 — 다른 칸이에요.

#### ⓒ 그래도 커리어로 확인 — **240커리어/칸, 한 커리어도 안 다릅니다**

| 심은 것 | 커리어 총 공격P | 없음 대비 |
|---|---|---|
| — (없음) | 478.25 ± 10.30 | 0.000% |
| ⭐ 늦은 시간의 사나이 / 추격자 / 빅게임 플레이어 | 478.25 ± 10.30 | **0.000%** (완전 동일) |
| 🩹 유리몸 / 욕심쟁이 / 새가슴 / 굼벵이 / 독불장군 / 원정 울렁증 / 슬로 스타터 / 냉정 | 478.25 ± 10.30 | **0.000%** (완전 동일) |

**값어치 폭 = 0.000%.**

**🔬 양성 대조** — 하네스가 눈이 먼 게 아님을 보이려고, 효과가 **걸렸다면** 어떻게 보이는지 재 봤습니다.

| | 커리어 총 공격P | 없음 대비 |
|---|---|---|
| 효과 없음 (지금) | 477.99 ± 14.59 | 0.00% |
| 🔬 골 +11%가 걸렸다면 | 487.18 ± 14.67 | **+1.92%** |
| 🔬 골 +22%가 걸렸다면 (= `late70`의 `eff.goal`) | 496.88 ± 14.67 | **+3.95%** |

> ### 판정 B — 🔴 **지금은 잴 수 없습니다. 이건 밸런스 결과가 아니라 「아직 안 만들어진 것」입니다.**
>
> - **값어치 폭 = 0.000%** → `max of 4`가 올리는 기댓값도 **정확히 0**입니다
> - 그래서 designer의 *"폭이 넓으면 예산을 3 → 2로"*는 **지금은 발동 조건이 아닙니다. 예산 3을 유지하세요**
> - 🔴 **특능 엔진(§11-7)이 붙는 순간 B를 다시 재야 합니다.** 그때가 예산 3 ↔ 2를 판정할 자리예요.
>   양성 대조가 보여주듯 **`late70` 하나(`goal +22%`)만 걸려도 커리어가 +3.95% 움직입니다** —
>   12종의 폭이 그 정도로 벌어지면 `max of 4`가 곧장 곡선 손잡이가 됩니다
> - ⚠️ 🩹 결함이 §6-2의 **묶음 균형추**라는 designer의 논리는 유효하지만,
>   **지금은 균형을 잡을 무게 자체가 양쪽 다 0**입니다. 그래도 쪼개지 마세요 — 엔진이 붙으면 그때 필요해집니다

---

### A-0 / A — 📏 키 × 🎯 포지션

키만 바꾸고 **카드·잠재력(키 곱하기 전)·커리어 시드를 전부 같게** 뒀습니다. 300커리어/칸.

#### ⓐ A-0 — 키만 바꾼 커리어 (포지션 고정) · 목표 **±5%**

| 포지션 | 165cm | 175cm | 185cm | 195cm | **폭** |
|---|---|---|---|---|---|
| ⚔️ fw | 539.6 ± 1.5 | 537.8 ± 1.5 | 537.3 ± 1.5 | 536.6 ± 1.4 | **0.56%** ✅ |
| 🪽 wg | 605.5 ± 1.6 | 605.8 ± 1.6 | 605.3 ± 1.5 | 600.8 ± 1.5 | **0.83%** ✅ |
| 🎯 mf | 562.6 ± 1.6 | 563.4 ± 1.6 | 562.6 ± 1.7 | 563.3 ± 1.7 | **0.15%** ✅ |
| 🛡️ df | 213.6 ± 0.9 | 213.8 ± 0.9 | 214.4 ± 0.9 | 215.5 ± 1.0 | **0.88%** ✅ |

**짝지은 차이 (195cm − 165cm · 같은 선수)**

| 포지션 | 차이 | 방향 |
|---|---|---|
| ⚔️ fw | −3.00 ± 1.25 = **−0.56% ± 0.23%p** | 큰 키가 조금 손해 (BLEND에 ⚡스피드·🏃드리블이 있음) |
| 🪽 wg | −4.71 ± 1.53 = **−0.78% ± 0.25%p** | 〃 (S1이 🏃드리블) |
| 🎯 mf | +0.69 ± 1.29 = **+0.12% ± 0.23%p** | 상쇄 (S1이 🎯패스 — `dir` 0) |
| 🛡️ df | +1.87 ± 0.95 = **+0.88% ± 0.45%p** | 큰 키가 조금 이득 (S1이 🛡️수비) |

**부호는 designer가 예측한 그대로이고, 크기는 목표의 1/5 이하입니다.**

#### ⓑ A — 16조합 · 목표 **±8%**

> ⚠️ **날값 16칸의 폭은 183.6%인데, 그건 키가 아니라 포지션이 만든 폭입니다.**
> 🛡️ 수비수는 공격포인트가 원래 적어요 (213 vs 🪽윙어 605). 키를 안 넣어도 그대로 있는 폭이라 A의 자가 될 수 없습니다.
> **A가 묻는 건 「키가 포지션의 유불리를 바꾸는가」**라서, 각 칸을 그 포지션의 키 평균으로 나눠 **상호작용만** 남겼습니다.

**커리어 총 공격포인트** (각 포지션의 키 평균 = 100%)

| 포지션 | 165cm | 175cm | 185cm | 195cm | 폭 |
|---|---|---|---|---|---|
| ⚔️ fw | 100.33% | 100.01% | 99.90% | 99.77% | 0.56%p |
| 🪽 wg | 100.19% | 100.24% | 100.16% | 99.41% | 0.83%p |
| 🎯 mf | 99.93% | 100.08% | 99.93% | 100.06% | 0.15%p |
| 🛡️ df | 99.68% | 99.74% | 100.04% | 100.55% | 0.87%p |

**A (16조합 상호작용) 폭 = 1.14%p** ✅ ±8% 안

**커리어 총 수상** (같은 자)

| 포지션 | 165cm | 175cm | 185cm | 195cm | 폭 |
|---|---|---|---|---|---|
| ⚔️ fw | 100.78% | 100.27% | 100.04% | 98.90% | 1.88%p |
| 🪽 wg | 100.42% | 100.42% | 100.69% | 98.47% | 2.23%p |
| 🎯 mf | 99.86% | 100.48% | 99.53% | 100.13% | 0.96%p |
| 🛡️ df | 99.29% | 99.77% | 100.27% | 100.67% | 1.38%p |

**총 수상 상호작용 폭 = 2.31%p** ✅ ±8% 안

**데뷔 시즌 출전** — 네 키가 소수점 첫째 자리까지 같습니다 (fw 19.2~19.3 · wg 22.7~22.8 · mf 25.4~25.5 · df 24.3~24.4).

> ### 판정 A-0 / A — **✅ 통과. 키는 힌트로 남습니다.**
>
> `191cm면 무조건 수비수`가 되지 않습니다. 195cm 수비수가 165cm 수비수보다 **0.88% 낫고**,
> 195cm 윙어가 165cm 윙어보다 **0.78% 못합니다.** 포지션 선택을 대체할 크기가 아니에요.
>
> 🔴 **다만 「말하는 것」과 「하는 것」의 크기를 맞추세요.** 화면에 `🛡️ 수비수 쪽이 맞아요`라고 크게 적으면,
> 플레이어는 그 배지가 **0.88%짜리**라는 걸 모릅니다. 힌트 문구가 실측보다 세게 읽히면 그것도 「명령」이 돼요.
> (director·designer 항목 — §7)
>
> 🟡 **관측 하나 (키와 무관 · 이번 범위 밖):** 🛡️ 수비수의 커리어 총 수상이 **29.5~29.9**로
> 🎯 미드필더 **41.7~42.1**보다 **29% 적습니다.** 키를 안 넣어도 이미 그렇고, 지금 설계와도 무관합니다.
> 하지만 *"수비수를 고르면 상을 덜 받는다"*가 사실이라면 **포지션 선택의 값어치가 그만큼 기울어 있습니다.**
> designer가 볼지 말지 정할 자리입니다.

#### ⓒ A-3 — **몰빵이 「평균 1」을 깨뜨리는가** · 목표 ±3%

유스 36턴 · 카드·잠재력·난수 전부 같고 **키만** 다릅니다. 각 칸 400벌 × 4포지션.

**「그 키에서 낼 수 있는 최선」** (다섯 훈련 정책 중 가장 좋은 것) 을 키끼리 견줍니다 —
플레이어는 자기 키를 못 고르고 **훈련만 고를 수 있으니까요.**

| 포지션 | 165cm | 175cm | 180cm | 185cm | 195cm | 폭 |
|---|---|---|---|---|---|---|
| ⚔️ fw 종합 | 47.3 | 46.9 | 46.9 | 46.9 | 47.1 | **0.86%** ✅ |
| 🪽 wg 종합 | 47.3 | 47.0 | 46.9 | 46.9 | 47.2 | **0.73%** ✅ |
| 🎯 mf 종합 | 47.3 | 47.0 | 46.9 | 46.9 | 47.1 | **0.94%** ✅ |
| 🛡️ df 종합 | 47.3 | 46.9 | 46.9 | 46.9 | 47.0 | **0.92%** ✅ |
| **네 포지션 통틀어** | | | | | | **0.84%** ✅ |

훈련 정책별로 뜯어보면 이렇습니다 (**종합** = 6칸 평균 · 180cm 대비):

| 훈련 정책 | 165cm | 175cm | 180cm | 185cm | 195cm |
|---|---|---|---|---|---|
| 골고루 | +0.03% | +0.01% | 0.00% | −0.01% | −0.03% |
| 몰빵 → 🫀체력·🛡️수비 (큰 키 유리 칸) | −2.10% | −0.69% | 0.00% | +0.63% | **+1.87%** |
| 몰빵 → ⚡스피드·🏃드리블 (작은 키 유리 칸) | **+1.85%** | +0.62% | 0.00% | −0.68% | −2.05% |
| 몰빵 → 재능 높은 칸 | +1.03% | +0.15% | 0.00% | +0.06% | +0.57% |

> ### 판정 A-3 — **✅ 통과. 몰빵으로 평균 1을 깨뜨릴 수 없습니다.**
>
> 한 방향으로 몰아붓는 순간 키가 ±1.9%씩 갈리는 건 맞습니다 —
> **그런데 그 몰빵 자체가 골고루보다 종합에서 손해**라서(45.5 vs 46.9), 결국 아무도 그 길을 안 갑니다.
> 「그 키의 최선」끼리 견주면 **0.84%**입니다. 어느 키든 최선은 「재능 높은 칸부터」이고, 그건 키와 무관해요.
>
> 🔴 **이 판정은 「그 키의 최선」이라는 자로 잰 것입니다.** 정책을 하나 고정해 놓고 키를 비교하면
> ±1.9%가 나오는데(위 표 2·3행), 그건 **키의 성질이 아니라 제가 고른 칸의 성질**이에요.
> 이 자를 바꿔 인용하지 마세요.

---

## 판정

| 항목 | 판정 |
|---|---|
| **D** (🎲 예산) | ✅ **통과** — 0 ↔ 무제한 +0.35 ~ +1.95%. 예산 **3회 승인** |
| **B-0** (총합 고정) | ✅ **통과** — 0.0000%. 변이 검증도 빨간불이 뜹니다 |
| **A-2** (6칸 평균) | ✅ **통과** — 1.000000000000 (30,001점 전수) |
| **A-0** (키만) | ✅ **통과** — 0.15 ~ 0.88% (목표 ±5%) |
| **A** (16조합) | ✅ **통과** — 상호작용 1.14%p / 수상 2.31%p (목표 ±8%) |
| **A-3** (몰빵) | ✅ **통과** — 0.84% (목표 ±3%) |
| **C** (잠재력 잠금) | ✅ **통과 — 손잡이를 쓰지 마세요.** 새 방식이 현행보다 **+0.56%**입니다 |
| **⑥** (옛 세이브) | ✅ **통과** — `S.height` 없음 = 키 180과 **비트 동일** |
| **B** (특능 값어치) | 🔴 **측정 불가** — `eff`가 엔진 어디에도 안 걸려 있어 폭이 정확히 0. **예산 3 유지**, 특능 엔진이 붙으면 재측정 |
| **🧬 성장타입** | 🟠 **계수 조정 필요** — 🎁 묶음에 두면 예산 3에서 🌳 만성이 **36% → 10%**, 첫 3시즌 **+13.3%** |

### 🟠 설계에 되돌리는 것 셋

**① 🧬 성장타입을 🎁 타고난 것에서 빼세요.**
🎁 묶음의 세 칸 중 ⭐·🩹은 값어치가 0이라, 🎁 리롤은 실질적으로 **성장타입 리롤**입니다.
designer 자신의 논리(*"굴리면 새는 축은 첫 굴림에 확정하고 🔒 잠근다"*)가 그대로 적용되는 자리예요.
📏 타고난 몸 화면에서 **키 · ⭐ 잠재력 · 🧬 성장타입 셋을 한 번에 확정하고 함께 잠그면**,
🎲의 곡선 영향이 **+1.37% → +0.35%**로 내려가고 타입 분포가 안 밀립니다.

**② `rollTalents`의 하한을 건드리지 마세요.**
designer 문서의 *"🔒 잠금으로 `max of 3`이 사라진 만큼을 하한에서 회복"*은 **실측상 회복할 것이 없습니다.**
새 방식이 이미 현행보다 +0.56% 높고, 하한 0.95를 넣으면 +0.92%가 되면서 ⭐1성이 20.3% → 1.7%로 사라집니다.

**③ 문서의 「현행 = 잠재력 `max of 3`」은 상한이지 실제가 아닙니다.**
🎲가 되돌리기 없이 덮어쓰기라 실제는 **무회상 최적정지**입니다 (`max3`보다 −0.21%).
새 방식의 ↩️ 되돌리기(직전 하나)는 designer 말대로 **실질 `max of (N+1)`이 맞습니다** —
좋은 굴림에서 되돌린 뒤 다시 굴리면 또 그 굴림으로 돌아올 수 있으니까요.

### 🟡 조건이 바뀌면 다시 재야 하는 것

| 무엇이 바뀌면 | 다시 재야 하는 것 |
|---|---|
| `STAT_CAP`(130)을 올리면 | **D 전부.** 지금 D가 통과하는 이유의 절반이 「4시즌이면 다 상한」입니다 |
| 커리어 길이(`CARD_AGE`·`RETIRE_AGE`)를 바꾸면 | **D · C · A 전부.** 초기 조건이 몇 시즌을 지배하는지가 바뀝니다 |
| ⭐ 특능 엔진(§11-7)이 붙으면 | **B**, 그리고 **D의 🎁 축** (예산 3 ↔ 2 판정) |
| 클럽 전력 환경을 바꾸면 | **곡선 다섯 점** (같은 소스에서 110이 62.4 ↔ 56.6으로 갈립니다) |

---

## 확정 계수

### 1. 📏 `HEIGHT_SHAPE` — **여섯 값이 한 벌입니다. 따로 넣을 수 없습니다.**

들어갈 자리: `beta/winger2/prospect.js` — `rollTalents()` (현재 `:437-444`) 뒤,
또는 📏 키 화면에서 `S.talents`에 한 번 곱해 박는 자리. **곱하는 곳은 딱 한 군데여야 합니다.**

```js
/* 📏 키가 정하는 것은 talents의 **크기가 아니라 방향**이에요.
 * ⚠️ dir 여섯 값은 한 벌입니다 — 6칸 평균이 정확히 1.000이 되도록 방향을 짝지어 둔 값이에요.
 *    하나만 빼면 평균이 ±1.67% 어긋나고, 그건 **커리어 지표로는 안 잡힙니다**
 *    (반쪽 dir로 800커리어를 굴려도 총 공격P가 +0.13%밖에 안 움직여요 — 능력치 상한이 삼킵니다).
 *    그래서 검사는 **6칸 평균 자체**를 봐야 합니다. 실측 75번 §중립 검증. */
const HEIGHT_MID = 180, HEIGHT_DIV = 150;
const HEIGHT_DIR = { stamina: +1, defense: +1, speed: -1, dribble: -1, shoot: 0, pass: 0 };
const heightShape = (h, k) => 1 + ((h - HEIGHT_MID) / HEIGHT_DIV) * (HEIGHT_DIR[k] || 0);
```

| 상수 | 값 | 근거 |
|---|---|---|
| `HEIGHT_MID` | **180** | 이 값에서 여섯 칸이 전부 1.000 → 옛 세이브(`S.height` 없음)가 중립 |
| `HEIGHT_DIV` | **150** | 195cm에서 ±10% |
| `HEIGHT_DIR.stamina` | **+1** | ← 이 여섯이 **한 벌** |
| `HEIGHT_DIR.defense` | **+1** | 〃 |
| `HEIGHT_DIR.speed` | **−1** | 〃 |
| `HEIGHT_DIR.dribble` | **−1** | 〃 |
| `HEIGHT_DIR.shoot` | **0** | 〃 (0도 한 벌의 일부입니다 — 빼면 칸 수가 안 맞아요) |
| `HEIGHT_DIR.pass` | **0** | 〃 |
| 키 범위 | **165 ~ 195cm** | A-0 폭 0.15~0.88%로 확인한 구간 |
| `S.height` 기본값 | **180** (읽는 쪽에서) | 옛 세이브 커리어가 키 180과 비트 동일 |

> 🚨 **`{체력 +1, 수비 +1}`만 말하고 `{스피드 −1, 드리블 −1}`을 빼면 평균 1이 깨집니다.**
> 어느 쪽 둘만 넣었을 때 6칸 평균: `+1`쪽만 **1.0333** · `−1`쪽만 **0.9667** (195cm).
> **"두 칸을 올리는 것과 같습니다" 같은 요약을 쓰지 마세요. 같지 않습니다.**

### 2. ⭐ `rollTalents` — **바꾸지 않습니다**

| 상수 | 값 | 자리 | 근거 |
|---|---|---|---|
| `rand(0.8, 1.45)` 하한 | **0.8 그대로** | `prospect.js:440` | 잠금 손실이 −0.37%뿐이고, 🎲 3회가 그보다 크게 갚습니다 (새 방식 **+0.56%**) |
| `t[ps] = Math.max(t[ps], 1.05)` | **1.05 그대로** | `prospect.js:442` | 하한과 **한 벌** |
| `talentStars`의 `(t − 0.6) / (1.8 − 0.6)` | **그대로** | `game.js:851` | 하한을 1.00으로 올리면 ⭐1성이 **20.3% → 0%** |

> 🚨 **셋은 한 벌입니다.** 하한만 올리면 별 눈금이 따라오지 못해 잠재력 화면이 2~3성 두 칸으로 납작해집니다.

### 3. 🔴 `rollTalents`를 `rollCards` 밖으로 (designer가 이미 지목한 자리)

`prospect.js:493` — `return { cards, talents: rollTalents(pos) };`
🎲마다 잠재력이 같이 굴러가는 자리입니다. 📏 키 화면에서 **한 번만** 부르고 `S.talents`에 박으세요.
**곡선 영향: −0.37%** (진짜 현행 대비). 새 방식 전체로는 **+0.56%**라 문제없습니다.

### 4. 🟠 🧬 성장타입을 🎁에서 📏 잠금으로

| | 지금 설계 (🎁에 있음) | 제안 (📏 잠금으로) |
|---|---|---|
| 🎲 0 → ∞ 커리어 총 | +1.37% ± 0.18%p | **+0.35% ± 0.18%p** |
| 🎲 0 → ∞ 첫 3시즌 | +19.84% | **+3.56%** |
| 🌳 만성 비율 (예산 3) | 36% → **10%** | 36% → **33%** |
| 🌳 만성 비율 (무제한) | 36% → **0%** | 36% → **34%** |

새는 자리는 `prospect.js:76-80`의 `HINTS`입니다 —
`{ id: "early", w: [0.7, 0.3, **0.0**] }`와 `{ id: "late", w: [**0.0**, 0.3, 0.7] }`의 **0**이
*"이 코멘트가 뜨면 그 타입은 절대 아니다"*를 만듭니다.

**두 갈래 중 하나를 고르세요 (둘 다 하지 마세요 — 겹치면 어느 쪽이 들었는지 못 가립니다):**

| 갈래 | 무엇 | 곡선 | 잃는 것 |
|---|---|---|---|
| **🟢 갈래-1 (권장)** | 🧬 성장타입을 📏 타고난 몸에서 **키·잠재력과 함께 확정·잠금**. 🎁 = ⭐특능 + 🩹결함만 | **+0.35%** | 🎁 리롤이 값어치 0인 두 칸만 굴립니다 — **연출은 남고 이득은 0.** 지금 특능 엔진이 없어서 그렇고, §11-7이 붙으면 살아납니다 |
| 🟡 갈래-2 | `HINTS`의 `w`에서 **0을 없앱니다** (예: `[0.65, 0.25, 0.10]` / `[0.10, 0.25, 0.65]` — **여섯 값이 한 벌**) | 미측정 | 코멘트의 정보량이 줄어 §6-2의 *"흐릿하게 두는 장치"*가 더 흐려집니다 |

**갈래-1을 권합니다.** designer 자신의 원칙(*"새는 축은 첫 굴림에 확정하고 잠근다"*)이 그대로 적용되고,
**하나의 🔒 잠금이 셋(키 · 잠재력 · 성장타입)을 덮습니다.**

### 5. 🎲 예산 — **3회 승인. 되돌리기도 승인.**

| 상수 | 값 | 근거 |
|---|---|---|
| 🎲 예산 | **3** | 0 ↔ 무제한이 +0.35%(갈래-1)/+1.37%(현행 설계). **예산이 곡선 손잡이가 아닙니다** |
| ↩️ 되돌리기 | **직전 하나 · 예산 소비 없음** | 실질 `max of (N+1)`이 맞고, 그 값으로 재도 ±3% 안입니다 |
| `POOL` | **194 그대로** (`prospect.js:171`) | B-0 통과 |
| `STAT_LO / STAT_HI` | **18 / 54 그대로** (`prospect.js:172`) | 〃 |
| `CARDS` | **3 → 1** | 새 방식 |
| `REROLL_MAX` | **2 → 3** (`prospect.js:170`) | 위 |

> ⚠️ **예산을 2로 줄이자는 designer의 조건부 항목은 지금 발동 안 합니다** — 특능 값어치 폭이 0이라
> `max of 4`가 아무것도 안 올립니다. **특능 엔진(§11-7)이 붙은 뒤에 다시 판정하세요.**

---

## 📮 다른 에이전트에게

### grow-designer에게

- 🟢 **①·②·③·⑤는 실측을 통과했습니다.** 문서를 그대로 진행하세요
- 🔴 **위험 표의 첫 줄(*"잠재력 잠금이 곡선을 끌어내린다"*)은 실측으로 반대입니다** —
  새 방식이 현행보다 **+0.56% ± 0.23%p** 높아요. `rollTalents` 하한 조정 항목을 **지워 주세요**
- 🟠 **§④의 🎁 묶음에서 🧬 성장타입을 빼 주세요** (§6-4 갈래-1).
  지금 묶음의 셋 중 둘(⭐·🩹)은 값어치가 **정확히 0**이라, 🎁 리롤 = 성장타입 리롤입니다
- 🔴 **§B(특능 12종)는 지금 잴 수 없습니다** — 디스크에 **3종**뿐이고 `eff`가 엔진에 안 걸려 있어요.
  *"폭이 넓으면 예산 3 → 2"*는 **특능 엔진이 붙은 뒤에** 판정할 항목입니다
- 🟡 **§③의 *"talents는 이미 두 경로로 닿습니다 — 훈련 배수 + 클러치 보정"*을 정정해 주세요.**
  `clutch()`를 쓰는 `ratingOf`는 **컵·월드컵에서만** 불립니다 (`career.js:2223` · `worldcup.js:1118`).
  **v2 리그(= 곡선과 수상을 만드는 경로)에서 잠재력이 닿는 길은 「훈련 배수」 하나뿐입니다.**
  c(판정에 직접)를 반대하는 결론은 그대로 유효하지만(키는 `talents → 훈련 → stats → blend`로 이미 닿음),
  근거 문장은 바꿔야 합니다
- 🟡 **키 힌트의 문구 세기를 실측에 맞추세요.** 195cm 수비수의 이득은 **+0.88%**입니다.
  `🛡️ 수비수 쪽이 맞아요`를 크게 쓰면 플레이어는 그게 몇 %인지 모릅니다 — 힌트가 실측보다 세게 읽히면 그것도 명령이에요
- 🟡 **범위 밖 관측 하나:** 🛡️ 수비수의 커리어 총 수상이 🎯 미드필더보다 **29% 적습니다** (29.6 vs 41.9).
  키와 무관하고 이번 설계와도 무관하지만, 포지션 선택의 값어치가 이미 기울어 있습니다

### grow-engineer에게

| # | 무엇 | 자리 |
|---|---|---|
| 1 | `rollTalents`를 `rollCards` 밖으로 | `beta/winger2/prospect.js:493` |
| 2 | `CARDS` 3 → **1** · `REROLL_MAX` 2 → **3** | `prospect.js:169-170` |
| 3 | `POOL` **194** · `STAT_LO/HI` **18/54** — **건드리지 않습니다** | `prospect.js:171-172` |
| 4 | `rand(0.8, 1.45)` · `t[ps] = max(…, 1.05)` — **건드리지 않습니다** | `prospect.js:440, 442` |
| 5 | `HEIGHT_DIR` 여섯 값 + `HEIGHT_MID 180` + `HEIGHT_DIV 150` (§6-1의 코드 블록 그대로) | `prospect.js` — `rollTalents` 뒤 **한 군데** |
| 6 | 🧬 성장타입을 🎁에서 빼 📏 잠금으로 (키·잠재력과 함께 한 번만) | `prospect.js` `rollCards` / 키 화면 |
| 7 | `S.height` 기본값 **180**을 **읽는 쪽에서** | 마이그레이션 금지 |

- 🚨 **`HEIGHT_DIR`을 「보너스」로 읽히게 이름 붙이지 마세요.** designer가 `HEIGHT_SHAPE`로 정한 이유가
  *"다음 사람이 밸런스 손잡이로 쓴다"*입니다. **여섯 값이 한 벌이고 평균이 정확히 1이라는 것을
  코드 옆 주석에 실측값(±1.67%)과 함께 적어 주세요**
- ⚠️ 곱하는 곳은 **`talents[k]` 한 군데**입니다. 두 군데 곱하면 195cm가 ±20%가 되고 평균 1이 깨집니다
- ⚠️ `const g = W2Grade.of(card.stats[d.key]);` 줄은 **글자 그대로** (grade-test 변이가 정규식으로 찾습니다)

### grow-inspector에게

실측을 테스트로 굳혀야 할 항목입니다. **괄호 안이 「그걸 되돌렸을 때 반드시 빨간불이 떠야 하는 것」**입니다.

| # | 검사 | 변이 검증 |
|---|---|---|
| 1 | `spread()` 총합 — **10만 회 전부 194** (문턱을 **검사에 직접 적으세요**. `POOL`에서 읽지 마세요) | `POOL`을 200으로 / `if (!room.length) break;`를 지우기 |
| 2 | 🔴 **`HEIGHT_SHAPE` 6칸 평균 = 1.000** — 165~195 전 구간. **`dir` 여섯 값을 하나씩 0으로 지워 ±1.67%가 뜨는지** | `dir`의 `speed: -1`만 지우기 → 평균 1.016667 |
| 3 | 🔴 **② 검사를 커리어로 만들지 마세요.** 반쪽 `dir`로 800커리어를 굴려도 총 공격P가 **+0.13%**밖에 안 움직입니다 — 커리어 회귀로는 절대 못 잡습니다 | — |
| 4 | `S.height`가 `undefined`인 옛 세이브가 키 180과 **같은 결과**인지 | 기본값을 185로 바꾸면 빨간불 |
| 5 | 🎲 뒤에도 `S.talents`가 **같은 객체(같은 값)**인지 — 잠금이 걸렸는가 | `rollTalents`를 `rollCards` 안으로 되돌리기 |
| 6 | 🎲 뒤에도 `S.growthType`이 **안 바뀌는지** (§6-4 갈래-1을 넣었다면) | 성장타입을 🎁에 되돌리기 |
| 7 | 🎲 예산이 실제로 줄어드는지 — **게임 입구를 통해 버튼을 눌러서**. 상수를 소스에서 읽지 마세요 | `REROLL_MAX`를 0으로 |
| 8 | ↩️ 되돌리기가 **예산을 안 쓰는지**, 그리고 **직전 하나만** 되돌아가는지 | 스택으로 바꾸기 |

⚠️ **문턱 여유:** 위 2번은 부동소수점 한 톨(2.2e-16)까지 1.000이라 `Math.abs(mean − 1) < 1e-12`로 잡아도
여유가 **12자릿수**입니다. 커리어 지표(3번)로 잡으려 하면 여유가 **0.13% vs 잡음 1.2%**라 절대 못 잡아요.

---

## 부록 — 스크립트

`/tmp/claude-0/-workspace-grow-games/79cf1e76-c844-41c3-8122-5fc7f1594e2e/scratchpad/g6/`
(공용 조각은 `../g5/`·`../v2/`·`../g4/`에서 승계했습니다)

| 파일 | 무엇 |
|---|---|
| `hs.js` | 📏 `HEIGHT_SHAPE`를 **designer 문서에서 정규식으로** 뜯어옵니다 (`dir`이 6칸이 아니면 던짐) |
| `prospM.js` | `prospect.js`를 파일째 태우되 **`rollTalents` 하한만 변이** — 변이가 실제로 적용됐는지 확인 |
| `pick.js` | 🎲를 누르는 플레이어 모델 (`blend`·`sum`·`type`·`hint`·`bhint`·`both`). `BLEND`/`BLEND_W`는 **엔진의 `K` 창구**에서 |
| `career2.js` | `g5/career.js` 복사 + **훈련 배치 정책**과 **📏 키** 옵션 두 곳만 추가 |
| `b0.js` | **B-0** (총합 고정 · 변이 검증) + **A-2** (6칸 평균 · `dir` 한 벌 검증) |
| `d.js` | 🔴 **D** — 같은 후보 열 · 예산만 바꿈 · 짝지은 표본 |
| `c.js` | **C** — 잠금 ↔ 무회상 최적정지 ↔ `max3` · 하한 sweep |
| `ab.js` | **현행 ↔ 새 방식** 끝에서 끝까지 |
| `a.js` | **A-0 / A** — 키 4 × 포지션 4 (상호작용 자 포함) |
| `a3.js` · `a3b.js` | **A-3** — 몰빵 (`a3b`가 본편. `a3`는 네 포지션을 섞어 자가 흐려진 판) |
| `b.js` | **B** — 🔬 세이브 칸 읽기 탐침 + 12칸 커리어 + **양성 대조** |
| `half.js` | `dir` 반쪽 · **⑥ 옛 세이브** |
| `traj.js` | 커리어 궤적 — 언제 `STAT_CAP`에 닿는가 |
| `gt.js` | 🧬 성장타입 서열 (오라클 순서를 감이 아니라 **재서** 정하려고) |
| `curve.js` | 곡선 다섯 점 재현 |

**환경 변수:** `CLUBS=52,57,62,66,71,78 MYCLUB=2` (28번과 같은 커리어 환경) ·
`REP`(표본) · `TRAIN`(rr / blend / talent) · `POLICY` · `BUDGETS` · `LOS` · `HS` · `SEED0`

⚠️ 한 군데 고쳤습니다 — `../v2/bits.js`의 베스트11 추출 정규식을 값 비의존으로
(`if \(hype >= 4\.5 \* bar\)` → `if \(hype >= [\d.]+ \* bar\)`). 원본은 `bits.js.bak6`에 있습니다.
**저장소 파일은 아닙니다** (제 스크래치패드예요).
