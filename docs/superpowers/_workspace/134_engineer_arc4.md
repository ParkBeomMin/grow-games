# 구현 — ⚽ 더 윙어 II · 🧒 어린 시절 네 해 + 🆕 갈리는 천장

2026-09-03 · grow-engineer
입력: `130_designer_arc4.md` · **`133_designer_arc4b.md`(최신 — 130을 여러 군데 뒤집습니다)** ·
`132_balancer_arc4.md` + **balancer의 `bal-arc4/knobs.js`·`lib4.js`**(계수를 잰 **모델 코드**) ·
`131_director_childbtn.md`(화면 · CSS는 이미 들어와 있었습니다)

> 🔴 **커밋 안 했습니다.** `git status`에 뜬 `beta/winger2/focus.js`·`132`·`133`은 **남의 것**이라
> 손대지도, 커밋에 넣지도 않았습니다.
> 🔒 **`style.css` 0줄 · `tests/` 0줄 · `engine.js` 0줄 · `town.js` 0줄 · 다른 7종 0줄.**

---

## 0. 요약

| | |
|---|---|
| **①** | ✅ **화면 넷이 실제로 섭니다** — 🧸초1 → 👦초2 → 🌙초3 → 🔑초4 → 🎯자리 → 🏫초5. 게임 입구에서 걸어 확인했습니다 (§5-1) |
| **②** | ✅ **초2는 제3안 · `run`의 이모지 🏃 → 🔁** · 문구 세 벌 전부 `133` §2 그대로. **자수 계약 안**(§2) |
| **③** | ✅ **초4의 `desc`는 그 해의 문장을 「참조」합니다** — 베껴 적지 않았어요. 화면에서 실제로 그렇게 나옵니다 (§5-1) |
| **④** | ✅ **`childCap` — Σ가 81조합 전부에서 정확히 0**, 옛 세이브(`[]`·모르는 키)는 **여섯 칸 전부 0**. designer의 §4-5 손계산 표와 **숫자까지 일치** (§3) |
| **⑤** | 🚧 **`CHILD_CAP_STEP`은 「임시 3.0」입니다** — designer 제안값 그대로 두고 주석에 **🚧 임시**라고 적었습니다. **balancer R-1이 오면 한 줄만** 갈면 돼요 (§3-3) |
| **⑥** | ✅ **되돌려 빨간불 확인 — 변이 9종 전부 빨간불** (§6). 🔬 우수리 되돌리기는 **`CHILD_CAP_STEP`을 2.0으로 두어야** 잡힙니다(3.0에서는 증상이 0장) |
| **⑦** | 🔴 **어긋난 검사 11건 — 고치지 않고 적었습니다** (`tests/winger2/` 31종 중 초록 20 · 빨간불 9 · 죽음 2 · §7). 🚨 뿌리는 **드라이버가 「자리 뒤가 중등부」인 세계에 산다**는 것 하나예요 |
| **⑧** | 🔴 **designer에게 되돌려 묻습니다 — 「중학교에 갑니다」** (§8). 자리 화면이 만 11세로 왔는데 문구가 옛말이라, **지어내지 않고 남겼습니다** |
| **⑨** | ✅ **🦶 약발 배지 한 줄** 넣었습니다. 🔴 그런데 `ceil-perfect-test`의 변이 정규식이 그 자리에서 죽습니다 (§7-2) |

---

## 1. 고친 파일

| 파일 | 무엇 | 줄 |
|---|---|---|
| `beta/winger2/prospect.js` | 표 셋 신설 · `spread` 5번째 인자 · `rollTalents` ①②③④ · `rollBuild` 기울임 · `childPush` 내보내기 | +153 |
| `beta/winger2/game.js` | `CHILD_CAP_STEP`·`childCap`·`statCap` 한 항 · 흐름 4단 체인 · 뒤로 셋 · 옛 주석 다시 쓰기 | +185 |
| `beta/winger2/intro.js` | `CHILD_PICKS2/3/4` · `CHILD_CHIP` · `CHILD_ARC` · `openChild(yr, ctx, done)` · `STEPS` 6 → 9 | +191 |
| `beta/winger2/index.html` | 섹션 셋(`data-yr`과 id만 갈아 복사) | +42 |
| `beta/winger-moment.js` | 🎖️ 약발 배지 한 줄 | +9 |

🔒 **`STAT_CAP = 130`은 한 글자도 안 건드렸습니다** (I-16).

---

## 2. 👦 초2 — 제3안과 문구

```js
const CHILD_TALENT = {
  fin:   ["shoot", "speed"],      // 🎯 골문 앞까지 뛰어가 찼어요
  run:   ["pass", "stamina"],     // 🔁 해 질 때까지 주고받았어요
  steal: ["dribble", "defense"],  // 🥷 붙으면 뺏고 몰고 나갔어요
};
```

🔴 **키는 안 바꿨습니다** — 짝만 갈았어요. 그래서 코드 주석에
*"이번에 간 것은 짝뿐 · 다음에 짝을 또 갈면 문구가 조용히 어긋납니다. 두 곳을 같이 보세요"*
라고 적어 뒀습니다 (I-12가 볼 자리).

**자수 — 실측**(공백·마침표 포함):

| 해 | key | title | desc | echo |
|---|---|---|---|---|
| 초2 | `fin` / `run` / `steal` | 14 / 14 / 14 ✅ | 17 / 17 / 18 ✅ | 17 / 16 / 14 ✅ |
| 초3 | `ge` / `gn` / `gl` | 13 / 14 / 11 ✅ | 14 / 18 / 14 ✅ | 17 / 15 / 15 ✅ |
| 초4 | `h1` / `h2` / `h3` | 12 / 14 / 12 ✅ | 🔁 **참조** | 15 / 14 / 14 ✅ |

> ⚠️ **inspector에게 — I-13(자수)을 🧸초1에 적용하지 마세요.**
> 배포된 초1은 `body` **15자** · `eye` **18자**로 계약(10~14) 밖입니다.
> 131번이 잰 79.91px은 **그 문장들로** 잰 값이라, 초1을 계약 안으로 줄이면
> **director가 잰 박스 높이가 무효**가 됩니다. 새 세 벌만 계약 안입니다.

🔴 **`run`의 이모지는 🔁입니다.** 🏃가 이제 남의 스탯(스피드 = `fin`)을 가리켜요.
🔒 초1 ⚽🛡️👀 · 초3 🌱🌿🌳 · 초4 🧸👦🌙과 겹침 0.

### 2-1. 🔑 초4 `desc`는 **참조**입니다

```js
{ key: "h1", emoji: "🧸", title: "그 공놀이를 계속했어요", from: 0, echo: "…" },
…
function childDesc(c, picks) {
  if (c.from == null) return c.sub;
  const src = CHILD_ARC[c.from];
  const got = (src && src.picks || []).find((x) => x.key === picks[c.from]);
  return got ? got.sub : "";
}
```

🔴 **`sub` 칸 자체가 없습니다** — 베껴 적을 자리를 안 만들었어요.
그 해를 안 골랐으면(구조상 없는 길) **줄을 아예 안 그립니다** — 지어내지 않습니다.
(`.card-desc` 없이도 68px로 선다고 director가 §5-2 ③에 적어 뒀습니다.)

---

## 3. 🆕 갈리는 천장 — `childCap`

### 3-1. 산식 (`game.js` · `statCap` 바로 위)

```js
const statCap = (key) => STAT_CAP + transLv(key) * TRANS_CAP_STEP + childCap(key);
//                        └ 130 · 곡선          └ 엔드게임            └ 🆕 어린 시절
```

- `q(k)` = 🧸초1이 민 칸 + 👦초2가 민 칸 + 🔑초4가 그 해를 다시 민 칸 (`h3`는 0)
- `q̄ = Σq / STAT_DEFS.length` — 🧮 **종속값. 6을 안 박았습니다** (I-19)
- `raw(k) = CHILD_CAP_STEP × (q(k) − q̄)` → 반올림 → **우수리 되돌리기**

🔒 **표는 `prospect.js`에 있고 `game.js`는 몫만 셉니다** — `WingerProspect.childPush(picks)`
하나를 내보냈어요. 글과 산식을 한 파일에 두지 않습니다.

🔴 **우수리 되돌리기가 무작위가 아닙니다.** `spread()`는 `pick(room)`으로 고르는데,
`statCap`은 **렌더마다 불려서** 같은 세이브가 늘 같은 천장을 내야 합니다.
반올림에서 **가장 많이 깎인 칸부터** 되돌립니다(결정적 · 400회 guard).

⚡ **메모이즈** — `S.childPicks` **배열 그 자체**를 열쇠로 씁니다. 세이브를 갈아 끼우면
새 배열이라 그 자리에서 무효예요. 🔴 `S`에 캐시를 달면 **그 칸이 세이브에 실려 나갑니다.**

### 3-2. 실측 — designer §4-5 손계산 표와 대조

| 조합 | 실측 `childCap` (슛/패스/드리블/수비/체력/스피드) | designer 손계산 |
|---|---|---|
| 🔴 정렬 `ball`+`fin`+`h1` | **+6 / −3 / +3 / −3 / −3 / 0** | 동일 ✅ |
| 🟢 고르게 `ball`+`run`+`h1` | **+3 / 0 / +3 / −3 / 0 / −3** | 동일 ✅ |
| 🔴 어긋남 `body`+`run`+`h2` | **−3 / +3 / −3 / 0 / +6 / −3** | 동일 ✅ |

`statCap("shoot") = 136` · `statCap("pass") = 127` (정렬 조합).

**Σ = 0 · 81조합 전수** → 최대 오차 **0**.

**옛 세이브** (마이그레이션 없음):

| `childPicks` | Σ | 여섯 칸 |
|---|---|---|
| 없음 / `[]` | 0 | **전부 정확히 0** — 개편 전과 같은 130 ✅ |
| `["zzz"]`(모르는 키) | 0 | **전부 정확히 0** ✅ |
| `["ball"]` | 0 | `+2 / −1 / +2 / −1 / −1 / −1` → 132/132/129×4 (133 §6 표와 일치) ✅ |
| `["ball","run"]` | 0 | `+1 / +1 / +1 / −2 / +1 / −2` |

> 🟢 **화면은 새로 안 만들었습니다.** `statCap(d.key)`가 이미 유스·프로에 *「상한 130」*으로
> 칸마다 떠 있어서, 갈린 천장이 **그 자리에 그대로** 나옵니다 (designer §12 ④).
> 🔴 어린 시절 화면에는 천장 숫자를 **한 글자도** 안 적었습니다.

### 3-3. 🚧 `CHILD_CAP_STEP` — **임시값입니다**

```js
const CHILD_CAP_STEP = 3.0;   // 🚧 임시 (balancer 확정 전)
```

주석에 적어 둔 것:
> *"🚧 임시값입니다 — balancer의 R-1(정렬 폭)이 오면 **이 한 줄만** 갈면 됩니다.
> designer 예측: **3.0은 밴드 위(4~6%p) · 1.5는 들어옴(2~3%p)**.
> 🔴 밴드를 넘으면 여기만 내리세요 — `TAL_SHIFT`·`GROW_TILT`·`FOCUS_W_HELD`는 다른 축의 손잡이입니다."*

🔴 **0으로 두지 않았습니다** — 0이면 범민 님 결정이 통째로 무효이고,
「기능이 꺼진 상태」와 「값을 아직 못 정한 상태」가 화면에서 구별이 안 됩니다.
designer가 표에 적은 **제안값(K-9)을 그대로** 옮겼고, 제 판단은 안 들어갔습니다.

---

## 4. 🔒 balancer 모델과 형태를 맞춘 자리 — **계수표만 안 봤습니다**

> 🚨 원칙 ⑧ — *"폐기된 것을 이름만 바꿔 되살리지 마세요. 계수를 잰 **모델 코드**까지 확인하세요."*
> 그래서 `bal-arc4/knobs.js`(balancer가 *"사실상 사양서"*라고 적은 파일)와
> `lib4.js`의 **변이 3종**을 읽고, **그 훅 자리에 그대로** 옮겼습니다.

| balancer 모델 | 구현 | 같은가 |
|---|---|---|
| `if (focus.indexOf(k) >= 0) w[k] *= window.__focusW(k)` — **키별 함수** | `spread(…, focusW)` — `focusW(k)` 함수 | ✅ |
| `__focusW = (k) => pair.includes(k) ? HELD : FOCUS_W` (pair = **초1 짝**) | `held = heldIs(picks,"h1") ? childFocus(picks) : null` | ✅ |
| ②(주스탯 바닥) **뒤**, `return t` **앞**에 전이 | 같은 자리 | ✅ |
| `down = shift × nPush ÷ (nKeys − nPush)` — **식** | 같은 식 (상수 금지) | ✅ |
| 400회 되돌리기 · `per = diff / room.length` | 같은 형태 | ✅ |
| `pickW(GROWTH_TYPES, __growTilt(hint.w))` — **곱하기** | `pickW(GROWTH_TYPES, mulW(hint.w, growTilt(picks)))` | ✅ |
| `idx = TYPES.indexOf(want)` — **id로 자리 찾기** | `GROWTH_TYPES.findIndex((g) => g.id === want)` | ✅ |

> 🔴 **`spread`의 5번째 인자를 스칼라로 두지 않았습니다.**
> `130` E-2는 *"`focusW` 인자(기본값 `FOCUS_W`)"*라고 스칼라처럼 적었지만,
> `132` §10-1은 *"`FOCUS_W_HELD`는 **초4 `h1`을 고른 사람의 초1 두 칸에만** 걸립니다"*이고
> balancer 모델도 **키별 함수**입니다. 스칼라로 두면 `YOUTH_FOCUS`가 미는 칸까지 같이 올라가
> **「굳힌 것이 어린 시절이 아니게」** 됩니다. 🔒 형태를 모델에 맞췄고, 그 이유를 코드 주석에 남겼습니다.

---

## 5. 재측정 — 제가 직접 잰 것

> 🔒 **검사가 아닙니다.** `tests/`에 아무것도 안 만들었어요 —
> 변이 검증 없는 검사를 남기면 그게 초록불 함정입니다. inspector에게 넘깁니다.
> 하네스: `scratchpad/eng-arc4/{drive,mech,mech2,mut}.js` (커밋 안 함).

### 5-1. 🖥️ 화면 넷 — **게임 입구를 통해 실제로 눌렀습니다**

`ball → fin → gl → h2`를 눌러 지나온 화면 (`pointerdown → pointerup → click` 셋 다):

```
초1 · 4/9 · 버튼 3 · 칩 (없음)      📍 서울특별시 · 초등학교 1학년 · 여덟 살
초2 · 5/9 · 버튼 3 · 칩 ⚽초1·공     📍 … 2학년 · 아홉 살   한 살 더 먹으니 붙는 상대가 달라졌어요.
초3 · 6/9 · 버튼 3 · 칩 ⚽ 🎯        📍 … 3학년 · 열 살     그해엔 몸이 저 혼자 자랐어요.
초4 · 7/9 · 버튼 3 · 칩 ⚽ 🎯 🌳     📍 … 4학년 · 열한 살   세 해가 지나고, 남은 게 하나 있었어요.
   🧸 그 공놀이를 계속했어요   /  발에 붙는 감각이 먼저 자라요   ← 🧸초1에서 고른 그 문장
   👦 그 친구들과 계속 붙었어요 /  차고 튀어나가는 게 빨리 늘어요 ← 🎯초2에서 고른 그 문장
   🌙 몸은 그해 그대로였어요   /  늦게 피는 쪽으로 기울어요     ← 🌳초3에서 고른 그 문장
→ screen-position → (fw) → screen-town  ✅ 🏫 초5로
탭 횟수: 초1 1 · 초2 1 · 초3 1 · 초4 1 · pos 1  (🔴 「도달」이 아니라 「눌렀는가」)
```

### 5-1a. 🔙 「← 뒤로」로 앞 해를 다시 골라 봤습니다

```
초4 도착         chosenChild ["ball","fin","gl"]
← 뒤로(초4)   →  screen-child3 · 버튼 3개 중 **잠긴 것 0개** · 칩 [⚽ 초1 · 공][🎯 초2 · 골문]
🌱 ge로 다시  →  screen-child4 · chosenChild ["ball","fin","ge"]   ← 🌳gl이 버려졌습니다
   초4 화면의 설명 줄  … | … | **「일찍 피는 쪽으로 기울어요」**   ← 🌳「늦게 피는」에서 따라 바뀜
🧸 h1         →  screen-position · chosenChild ["ball","fin","ge","h1"]  (길이 4)
```

🔑 **마지막 줄이 「참조」 계약의 증거입니다** — 초4의 설명이 **앞 해를 다시 고른 순간 따라 움직였어요.**
베껴 적었으면 「늦게 피는」이 그대로 남았을 겁니다.

### 5-2. 🧮 산식 층

| 재는 것 | 값 | 계약 |
|---|---|---|
| Σ `childCap` · 81조합 전수 | **최대 오차 0** | 0 ✅ |
| 옛 세이브(`[]`·`null`·모르는 키) | **여섯 칸 전부 0** | ✅ |
| `sum(stats) === POOL` · 120,000굴림 | **어긋난 굴림 0건** | ✅ |
| Σ`talents` 평균 (갈래 6종 × 60,000) | **Δ −0.035% ~ +0.043%** | 중립 ✅ |
| 🧸 `h1` 굳히기 (🇧🇷 · 40,000) | 드리블 **+4.47** · 슛 **+4.57** · 나머지 넷 −2.2 | 그 두 칸만 ✅ |
| 👦 `fin` 전이 (40,000) | 슛 **+0.097** · 스피드 **+0.096** · 나머지 넷 **−0.050** | `0.10` / `0.10×2÷4` ✅ |
| 👦 `fin`+`h2` | **+0.194 / −0.097** | `0.20` ✅ |
| 🔴 `hint = early`인 30,000굴림에서 🌳만성 | **0건 / 9,961** | `hint.w`의 0이 삽니다 ✅ |

**🌙 초3 — 「고른 대로 될 확률」** (40,000굴림 · 🔴 **「작동률」이라고 안 부릅니다**):

| 갈래 | 안 고름 | 고름 | 🔑 굳히면(`h3`) | balancer 표 |
|---|---|---|---|---|
| `ge` → 조숙 | 34.6% | **44.1%** | **52.4%** | 34~35 → 44 → 52 ✅ |
| `gn` → 보통 | 31.1% | **47.5%** | **64.1%** | 30.9 → 47.1 → 64 ✅ |
| `gl` → 만성 | 34.2% | **44.2%** | **52.0%** | 34.8 → 44.6 → 52 ✅ |

> 🔑 **`132`가 재 둔 값과 소수점까지 겹칩니다** — 제 구현이 balancer가 계수를 잰
> **그 구조 위에** 서 있다는 뜻이에요. 표만 베꼈으면 이 줄이 안 맞았을 겁니다.

---

## 6. 🔬 되돌려 빨간불 확인 — **변이 9종 전부 빨간불**

> 🔴 초록불은 「고쳐졌다」 아니면 「아무도 안 보고 있다」입니다. 둘을 가르는 건 이것뿐이에요.
> 🔒 **기준선이 초록불인 것을 먼저 찍고** 걸었습니다.

| # | 되돌린 것 | 결과 |
|---|---|---|
| **M1** | `statCap`에서 `+ childCap(key)` 삭제 | ❌ 천장 136/127 → **130/130** |
| **M2** | `CHILD_CAP_STEP = 0` | ❌ **130/130** |
| **M3** | `CHILD_CAP_STEP = 2.0` (기준선 확인용) | ❌ 134/128 — 값이 따라 움직입니다 |
| **M4** | 🔬 **2.0 + 우수리 되돌리기 루프 삭제** | ❌ **Σ오차 0 → 2** |
| **M5** | ⭐ 전이 블록 삭제 | ❌ ⭐Δ 0.0972 → **−0.0011** |
| **M6** | 🌙 기울임 삭제 (`mulW`를 안 씌움) | ❌ 63.9% → **31.0%** |
| **M7** | 🌙 `mulW`를 곱하기 → **더하기** | ❌ **early 판에서 🌳만성 615/4093** — `hint.w`의 0이 죽습니다 |
| **M8** | 🧸 `spread`에 `focusW`를 안 넘김 | ❌ 🧸Δ 4.47 → **−0.04** |
| **M9** | `CHILD_TALENT`를 **본안**으로 되돌림 | ❌ 짝이 다름 · 천장 pass 127 → **130** |

> 🚨 **M4가 이 표에서 제일 중요합니다 — 그리고 `CHILD_CAP_STEP`이 3.0이면 안 잡힙니다.**
> 3의 배수면 `q − q̄`의 분모가 3이라 반올림이 아무 일도 안 해서, 되돌리기 루프는
> **단독으로는 증상이 0장**이에요(「방어가 겹침」의 전형).
> 🔴 **inspector — I-17은 반드시 3의 배수가 아닌 값(2.0)으로 굴리세요.** 저도 그래야 잡혔습니다.

---

## 7. ⚠️ 어긋난 검사 — **고치지 않고 적습니다** (`tests/` 0줄)

`tests/winger2/` 29종 · 종료 코드 3갈래로 갈라 돌렸습니다.

### 7-1. 🚨 뿌리는 **한 곳** — `_load.js`의 `tapChild` / `passArc`

```js
// tests/winger2/_load.js:594
if (cur() !== "screen-child") return null;
press(D.querySelector(`#child-list .card[data-child="${key}"]`), `🧒 초1 ${key}`);
```

이 드라이버는 **「🧒 초1 한 화면」 세계**에 살고 있습니다. 초1을 누르면 `screen-child2`에
서는데, `passArc`는 그다음에 바로 `passStage`(🏫 초등부)를 부르니 **아무것도 못 누르고**
흐름이 그 자리에 멈춥니다. 🔴 그 파일 주석이 이미 예고해 뒀어요 —
*"[다음]이 붙으면 여기서 화면이 안 넘어가 던지므로 **이 함수부터 다시 보세요**"*.

👉 **고쳐야 할 것 둘** (inspector 몫):
1. `tapChild`를 **네 화면을 걷게** — `#child2-list`·`#child3-list`·`#child4-list`,
   `want`를 배열(`["ball","fin","gl","h2"]`)로. 🔴 **복붙본을 만들지 마세요** — 그 파일이
   *"사본 셋이 한꺼번에 죽어 검사 7종이 종료 코드 2가 됐다"*고 적어 둔 자리입니다.
2. `passArc`의 순서를 **🎯 자리 → 🏫 초5**로. 지금은 `tapChild` 다음이 바로 `passStage`예요.

### 7-2. 검사별 — **31종 중 초록 20 · 빨간불 9 · 죽음 2**

| 검사 | 무엇이 | 왜 | 이건 |
|---|---|---|---|
| ❌ **`offer-test`** | O-1 ~ O-9a **12건** + 변이 2건 | `passArc`가 `screen-child2`에서 멈춤 (§7-1) | 🔧 드라이버 |
| 💥 **`town-test`** (코드 2) | T-7 · T-5 · T-5a · T-6a → `press("🏟️ 입단 제안")`에서 던짐 | 같은 뿌리. 🔴 **T-7의 「첫 순간 카드 앞의 결정 4개」는 이제 8입니다** (이름·주발·동네·초1~4·자리) | 🔴 **계약 갱신** + 드라이버 |
| 💥 **`creation-test`** (코드 2) | C-0 ~ C-4 + `st.picks` null 크래시 | 화면 목록이 **6 고정**이고 분모 6을 단언. 지금은 **9**예요 (`1/9` ~ `9/9`) | 🔴 **계약 갱신** + 드라이버 |
| ❌ **`school-test`** | 0(정규식) · S-6 · **S-6a** · S-6b · S-9-조건 · 변이 2 | 🚨 **S-6a가 옛 계약을 「지키고」 있습니다** — *"🎯 자리가 🏫 초등부 **뒤**, 중등부 **앞**"*. 설계가 그걸 뒤집었어요(I-5의 자리). 그리고 `M_NOPOS_SKIP` 정규식 `goSchool("e", () => goEarly("e", goPosition))`이 **죽었습니다** | 🔴 **계약 갱신** |
| ❌ **`school-scene-test`** | A-2b · B-1 · C-1 · 변이-D | 자체 드라이버가 `tapChild` 뒤 바로 `stage("e")` (196줄) | 🔧 드라이버 |
| ❌ **`bench-test`** | 0(정규식) · B-변이① · C-변이① · G-1 | `POOL_LOOSE` 정규식이 **죽었습니다**: `/stats: spread\(POOL, posKey, focus, shapeKey\),/` → 지금은 `…, shapeKey, focusW)` | 🔧 정규식 + 드라이버 |
| ❌ **`child-test`** | **C-0만** (변이 `TWO_CARDS`) | `/list\.innerHTML = CHILD_PICKS\.map\(\(c\) =>/` → 지금은 `a.picks.map`. 🔑 **C-1 ~ C-5는 전부 초록불**입니다 | 🔧 정규식 |
| ❌ **`foot-next-test`** | N-8 (탭 7 · 첫 순간 카드) | 탭은 7이 맞는데 **도착이 `screen-child2`**. 「첫 순간 카드까지」가 옛말이에요 | 🔴 **계약 갱신** |
| ❌ **`ceil-perfect-test`** | C-0 (변이 `WEAK_WORD_OFF`) | `/W\.great \+ \(ctx\.weak \? "[^"]*" : ""\)/`가 큰따옴표만 봅니다. 배지가 **작은따옴표 + `<b>`**라 안 걸려요. 🔑 **C-4 자체는 초록불** — 배지가 붙어도 축하 문구는 뜹니다 | 🔧 정규식 |
| ❌ **`youth-clamp-test`** | **M-C2 하나** (변이 검증) | 🔑 **`childCap`이 아닙니다 — 실측으로 확인했습니다.** 이 드라이버는 이름 다음에 자리 카드를 **바로** 눌러 🧒 어린 시절을 건너뜁니다 → `childPicks: []` → `childCap` **여섯 칸 전부 0**. 바뀐 것은 **학교 카드 수 6 → 8**이에요(자리 뒤가 중등부에서 **초5**로 바뀌었으니까요) — 궤적이 달라져 M-C2의 문턱 여유가 사라졌습니다. 🔒 **기준선 B-1·C-1·D-1은 전부 초록불** | 🔧 드라이버(그리고 문턱 여유) |
| ❌ **`youth-moment-test`** | A-7 · A-M5 | 🚧 ***"수비 경기가 안 나와서 못 쟀습니다 — 시드가 안 뽑았어요"*.** 같은 드라이버가 학교 8장을 지나며 난수를 더 쓰는 바람에 평가전 카드 뽑기가 달라졌어요 — **커버리지가 난수에 걸린** 형태입니다 | 🔧 드라이버(시드 커버리지) |
| 🟢 나머지 20종 | | | |

> 🔑 **`youth-clamp`·`youth-moment` 둘은 「내 산식이 틀렸다」가 아닙니다** —
> `childPicks`가 `[]`라 천장이 **정확히 0**인 상태에서 난 실패예요.
> 🔴 그래도 **초록불로 만들지 마세요.** 두 드라이버가 「자리 뒤가 중등부」인 세계에 살고 있고,
> 그 세계가 바뀐 것이 맞습니다.

**`tests/winger2/` 밖:** `node tests/smoke-test.js beta` 🟢 **9/9** · `node tests/smoke-test.js`(상용) 🟢 **9/9** ·
`node tests/check-page-test.js` 🟢 **전부 통과**.

### 7-3. 🔴 검사가 **없는** 자리 (초록불이 아니라 빈 자리입니다)

| 무엇 | 왜 없는 걸 아나 |
|---|---|
| 🚨 **`childCap`·`statCap`을 보는 검사 0건** | `tests/winger2/`에 `statCap`·`STAT_CAP` 단언이 **0건**. M1(항 삭제)·M2(0)를 넣어도 **29종 전부 초록불**이 됩니다. 제가 제 하네스로만 잡았어요 |
| 🚨 **초2·초3·초4 화면을 보는 검사 0건** | `child-test` C-4는 **초1만** 봅니다. 그리고 초1을 누르면 `screen-child2`로 가니 **여전히 초록불**이에요 — 세 화면이 통째로 사라져도 아무도 안 웁니다 |
| 🚨 **`CHILD_TALENT`·`GROW_TILT`를 보는 검사 0건** | M5·M6·M7·M9를 넣어도 29종 전부 초록불 |
| 🟡 **초4 `desc` 참조** | 베껴 적기로 되돌려도 잡는 검사가 없습니다 (I-12가 덮을 자리) |

👉 `133` §11의 **I-11 ~ I-21**이 정확히 이 빈 자리들입니다. 🔴 **제가 만들지 않았습니다** —
변이 검증 없는 검사를 남기는 게 초록불 함정 그 자체라서요.

---

## 8. 🔴 designer에게 되돌려 묻는 것 — **지어내지 않고 남겼습니다**

### 8-1. 🚨 「중학교에 갑니다」 — **자리 화면 문구가 옛말입니다**

```js
// game.js · btn-name-next 핸들러
$("position-hint").textContent = `${chosenName}, 중학교에 갑니다. 이제 어느 자리에서 뛸까요?`;
```

🎯 자리 화면이 **초4 뒤(만 11세)**로 왔습니다. 다음은 **🏫 초5 대항전**이에요 — 중학교가 아닙니다.
🔴 **안 고쳤습니다.** 대체 문구는 설계에 없고, 사용자 문구는 designer 몫입니다.
(제가 조용히 한 줄 지으면 그게 «구현자가 내린 설계 판단»이에요.)

### 8-2. 🟡 「🏘️ 동네 대회로」 — 🗺️ 지도의 [다음] 라벨

`intro.js:348`. 🔒 **제 변경 전부터 있던 문구**인데, 다음이 **네 화면 뒤**가 되면서 더 멀어졌습니다.
같은 이유로 안 건드렸습니다.

### 8-3. 🟡 「← 뒤로」의 착지점 — 제가 정한 것 하나

- 🧸초1 → 🗺️ 지도 (**기존 그대로**)
- 👦초2·🌙초3·🔑초4 → **앞 해를 다시 엽니다** (`goChildYear(yr-1)`)

🔴 **`show()`가 아니라 `goChildYear()`인 이유를 코드에 적었습니다** — `show()`만 하면
그 화면 버튼이 이미 눌려 **잠긴 채(`disabled`)** 서 있어서 아무것도 못 고릅니다.
🔒 그리고 새 화면 셋은 **`BACK_SAFE`에 안 넣었습니다** — 브라우저 뒤로 가기로 들어오면
`openChild`를 안 지나 그 잠긴 화면에 그대로 서게 되거든요.
🔒 앞 해로 돌아와 다시 고르면 **뒤의 해는 버립니다**(`slice(0, yr-1)`) —
안 그러면 「🌙초3을 다시 골랐는데 🔑초4가 옛 해를 굳히고 있는」 상태가 됩니다.

> ⚠️ **이게 설계에 없던 자리입니다.** `130` §7이 *"화면마다 ↩️ 되돌리기"*를 안 하기로 했는데,
> 그건 **「고른 것을 무르는 버튼」** 이야기이고 화면의 「← 뒤로」는 초1에 **이미 있었습니다.**
> 셋에만 안 달면 초1과 어긋나서 달았어요. 🔓 **빼라고 하시면 세 줄 지우면 됩니다.**

---

## 9. 🎨 director에게

| # | |
|---|---|
| **①** | ✅ **🦶 약발 배지 넣었습니다** — `beta/winger-moment.js`, 정확히 그 한 줄. `ender`가 `innerHTML`이라 `<b>`가 그대로 섭니다. 🔴 다만 §7-2의 `ceil-perfect` 정규식이 그 자리에서 죽어요 |
| **②** | 🟡 **`style.css` 2줄이 옛말이 됐습니다** — 🔒 제가 안 고쳤습니다(`style.css` 금지). `.w2m-win-weak` 위 *"🚧 지금은 아무도 안 켭니다"* → **이제 켭니다.** `.arc-tally` 위 *"🚧 오늘은 아무도 안 켭니다"* → **초2부터 켭니다** |
| **③** | ✅ 섹션 셋을 **`data-yr`과 id만 갈아** 복사했습니다. `.card`·`data-child`·`role`·`aria-live` 전부 그대로 · `data-yr`은 `<section>`에만 · `.arc-steps`에 id 안 붙였습니다 |
| **④** | ✅ 칩은 `133` §2-6 그대로. **초2부터 켜고 초4에는 안 만듭니다**(초4 화면에는 앞의 **셋**이 뜹니다) · 숫자·스탯 이름 0 |
| **⑤** | 🔴 **실기기로 봐 주실 것** — 131번 §8의 일곱 중 **③(진행 띠가 부담으로 읽히나)**과 **⑤(🌇🌙🌌 톤)**은 이제 **실제로 볼 수 있습니다.** 지금까지는 초1(🌤️ 낮)만 볼 수 있었어요 |
| **⑥** | 🟡 **`beta/_check.html:257`도 옛말이 됐습니다** — *"지금은 초1(낮)만 볼 수 있어요"*. 🔒 베타 공용 파일이라 **제가 안 고쳤습니다** |

---

## 10. 안 한 것

| 무엇 | 왜 |
|---|---|
| `tests/` · `beta/_check.html` · `beta/_fixtures.js` | 🔴 금지. §7이 inspector 몫입니다 |
| `beta/winger2/style.css` | 🔴 금지(director) — §9 ② |
| `beta/winger2/engine.js` · `town.js` | 🔒 자물쇠. 🔑 **`town.js`는 손댈 필요가 없었습니다** — `town.js:583`이 `stage.id === "e"`에서 `ELEM_POS`를 이미 강제해요. *"골랐더라도 초등 화면이 자리를 아는 순간 추천의 뒷문이 열린다"*고 거기 적혀 있고, 오늘이 그 날이었습니다. `game.js`의 옛 주석(*"초등부에는 chosenPos가 아직 없습니다"*)만 **「이제 있습니다. 그래도 안 봅니다」**로 고쳐 썼습니다 |
| 다른 7종의 `game.js` | 🔴 `CHILD_CAP_STEP`·`childCap`을 복사하지 않았습니다 (I-20 · 전수 0건) |
| `S.childPicks`에 새 필드 | **0개.** 길이만 1 → 4 |
| 천장 값을 세이브에 저장 | 🔴 저장하면 `CHILD_CAP_STEP`을 바꾼 날 옛 세이브만 옛 천장을 씁니다 |
| 검사 신설 | 변이 검증 없는 검사가 곧 초록불 함정이라 §7-3으로 **빈 자리를 적었습니다** |
| 커밋 | 🔴 안 했습니다 |

---

## 11. 📮 범민 님께 — 한 줄

> **네 해가 다 섰습니다.** 🧸 뭘 하고 놀았는지 → 👦 누구와 붙었는지 →
> 🌙 그해 몸이 어땠는지 → 🔑 그중 뭐가 남았는지, 넷을 고르고 나서 🎯 자리를 정하고
> **초5 학교대항전**으로 나갑니다. 🔴 초4까지는 경기가 한 판도 없어요.
>
> 🆕 그리고 **「이 선수는 여기까지」가 능력치 칸마다 달라집니다.**
> 어릴 때 골문 앞에서만 논 선수는 **슛이 136까지**, 대신 **패스는 127까지**예요.
> 🔒 **평균은 여전히 정확히 130**이라, 게임이 쉬워지는 게 아니라 **모양이 갈립니다.**
> 🔑 **아무것도 안 고른 옛 세이브는 여섯 칸이 전부 130 그대로**고요 —
> 실측으로 확인했습니다(여섯 칸 전부 정확히 0).
>
> 🚧 **딱 하나가 아직 임시입니다** — *「천장이 얼마나 갈리나」*(`CHILD_CAP_STEP`).
> 지금은 designer 제안값 3.0을 넣어 뒀는데, designer가 *"그러면 자기 포지션에 딱 맞게
> 고른 사람이 은퇴할 때까지 너무 많이 앞선다"*고 봤어요. **balancer가 재고 나면 한 줄만**
> 갈아 끼웁니다 — 코드에 **🚧 임시**라고 적어 뒀습니다.
