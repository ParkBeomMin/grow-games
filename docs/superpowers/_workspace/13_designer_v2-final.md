# ⚽ 더 윙어 II — 1차 착수 확정본

2026-08-28 · grow-designer
근거: `10~12_designer_*` · `20~21_balancer_*` · `00_input/decisions.md`

> **이 문서 하나만 읽고 구현할 수 있게 썼습니다.**
> 10~12번에 흩어진 확정 사항을 모았고, **폐기된 안은 담지 않았습니다**(§13에 목록만).
> 수치의 원본 근거는 `21_balancer_revised-engine.md`입니다.

---

## 목차

| § | 무엇 | 누가 |
|---|---|---|
| 1 | 게임 정체 · 등록 지점 | engineer |
| 2 | **경기 엔진 — 확정 산식 전체** | engineer |
| 3 | **확정 계수 전체 목록** | engineer |
| 4 | 미니게임 4종 · 카드 3종 매핑 | engineer · director |
| 5 | 화면이 약속해도 되는 것 / 안 되는 것 | director |
| 6 | 선수 설정 (유망주 3택 · 성향 · 나이) | engineer · director |
| 7 | 성장 (특능 · 성장타입 · 등급) | engineer |
| 8 | 포지션 · 주발 · 전환 | engineer |
| 9 | **저장 데이터와 읽는 쪽 기본값** | engineer |
| 10 | **시드 주입 창구 스펙** | engineer · inspector |
| 11 | **1차 순서와 실측을 끊는 지점** | 전원 |
| 12 | 남은 위험 · 후속 실측 | balancer |
| 13 | 폐기 목록 — 읽지 마세요 | 전원 |
| 14 | **범민 님께 올릴 판단 항목** | — |

---

## 1. 게임 정체 · 등록 지점

| | |
|---|---|
| 슬러그 | **`winger2`** |
| 이름 | **⚽ 더 윙어 II** |
| 폴더 | `beta/winger2/` (`cp -a beta/soccer beta/winger2`로 시작) |
| `SAVE_KEY` | **`"winger2-save-v1"`** — 기존 `winger-save-v1`을 절대 재사용하지 마세요 |
| `LEGACY_KEY` | `SAVE_KEY + "-legacy"` |
| `sw.js` `CACHE` | **`"winger2-v1"`** — 접두사가 겹치면 다른 게임 캐시를 지웁니다 |
| 전용 파일 | `beta/winger2/*` + **`beta/winger-moment.js`** (미니게임 4종) |
| 건드리면 안 되는 것 | `timing.js` · `base.css` · `cloud.js` · `match.js` · `help.js` — 8개 게임 공용 |

**등록 9지점은 `grow-new-game` 스킬의 체크리스트를 그대로 따릅니다.**
`beta/index.html`의 `GAMES`+게임 수 · `stats/index.html` · `cloud.js` 4곳 ·
`tests/smoke-test.js` · `tests/cloud/*` 2개 · `scripts/sync-beta.sh`의 `CONTENT` ·
`beta/_check.html`의 `GAME_ORDER` · `scripts/make-fixtures.js`.

⚠️ **`beta/winger-moment.js`를 `sw.js`의 `ASSETS`에 손으로 넣으세요.** 자동 생성이 없습니다 —
빠뜨리면 온라인에선 멀쩡하고 **오프라인에서만** 깨져요.

---

## 2. 경기 엔진 — 확정 산식

**점진 확정**입니다. 카드마다 그 자리에서 굴리고, **마지막 카드 전까지 스코어가 미확정**이에요.
`splitMine` · `deriveOppGoals` · `holdConceded` · 에이스 계단은 **전부 폐기**합니다.

### 2-1. 한 경기의 뼈대

```
카드 N장 (N_MIN 6 ~ N_MAX 8)
  |스코어차| ≥ 3 이 되면 6장에서 끊습니다 (이미 끝난 경기)

카드 k = 1..N 을 순서대로 해결합니다.
```

### 2-2. STEP 1 — 이 장면의 주인은 누구인가

```js
atkW = teamAtk / (teamAtk + oppDef);
defW = oppAtk  / (oppAtk  + teamDef);

p(우리 공격) = SCENE_ATK * atkW / (atkW + defW);
p(상대 공격) = SCENE_ATK * defW / (atkW + defW);
p(중립)      = 1 - SCENE_ATK;              // 중원 공방 필러 카드 — 아무도 주인공이 아님
```

> ⚠️ **스코어를 보지 않습니다.** 리드/열세로 장면 배분이 움직이지 않아요.
> 이유와 2차 후보는 §5-3에.

### 2-3. STEP 2 — 결정적 장면(빅찬스)인가

**우리 공격 장면일 때만** 굴립니다.

```js
pBig(k) = BIG_BASE * edge * urgency(k) * condMul;

  edge       = (teamAtk / (teamAtk + oppDef)) / 0.5;        // 1.0이 중립
  urgency(k) = 1 + URG * behind(k) * (k / N);               // behind = 지고 있는 골 수
  clutch(k)  = (k === N && |스코어차| <= 1) ? CLUTCH : 1;    // 마지막 카드 = 추가시간
  condMul    = 0.76 + 0.30 * (condition / 100);             // §2-7
```

> ### ⏱️ `clutch` — 9번째 카드 대신 마지막 카드의 무게를 올립니다 (2026-08-28 판정 · engineer §4-④)
> §5-5가 *"1점 차일 때 카드가 하나 더"*라고 적었는데 `N_MAX 8`과 충돌합니다.
> **9번째 카드는 넣지 않습니다** — 생산량이 늘어 곡선이 통째로 움직여요.
>
> 대신 **마지막 카드가 1점 차 이내면 그 카드가 추가시간(`90+1~5분`)이 되고,
> 결정적 장면이 될 확률이 올라갑니다.** **`CLUTCH = 1.25`** (확정 — 실측 ①-F).
>
> **1.35에서 내렸습니다.** 시즌 골이 **+3.6%**로 ±3% 밴드를 아슬하게 넘었어요.
> 밴드를 넓히지 않은 이유는 셋입니다 —
> ① ±3%는 제가 임의로 정한 게 아니라 **(라′) 중립 기준을 이어받은 것**이고,
>    그 밴드가 곧 원칙 ④(*새 축을 얹는 것만으로 세지면 안 된다*)의 운영 정의입니다
> ② **`clutch`의 값어치는 세기가 아니라 존재입니다.** 화면에서 느끼는 건 배수가 아니라
>    **`90+3분` 표시와 타이핑 연출**이에요. 1.25도 `urgency`의 최대치(1.36)와 비슷한 크기라
>    효과가 죽지 않습니다
> ③ **한 번 넓히면 다음 항목도 넓히게 됩니다.** 이 저장소가 데인 자리(골든부츠 43%→60%)가
>    정확히 *"조금인데 뭐 어때"*였어요
>
> `urgency`는 **뒤질 때만** 걸립니다(`behind`). `clutch`는 **1점 차로 앞설 때도** 걸려요 —
> §C-3 "긴장이 곧 조작량"의 마지막 조각입니다. 마지막 카드가 상대 공격 장면이면
> 🧱 수비 카드가 그대로 열리니 **수비수도 이 순간을 받습니다.**
>
> 카드 수가 안 늘어 영향이 `CLUTCH` 하나로 통제됩니다.

**빅찬스면 → ⚽ 결정 카드 후보. 아니면 → 🅰️ 전개 카드 후보.**

> ⚠️ **`fatigue`는 여기 없습니다.** STEP 3으로 옮겼어요 —
> `pBig`는 장면의 속성이고 `mineHit`은 사람의 속성이라, 경쟁자 11명을 함께 굴릴 때
> 이 자리에서는 **정의가 안 됩니다** (주인공을 아직 모르니까요).

### 2-4. STEP 3 — 누가 주인공인가

선발 11명 각자에 대해 무게를 구하고 그 비율로 뽑습니다. **내 선수도 그중 하나예요.**

```js
w(i) = posW(카드종류, pos_i)          // 포지션 가중  ← 이게 없으면 센터백이 스트라이커만큼 골을 넣습니다
     * slot_i.X                       // 자리 가중 (결정→g · 전개→a · 수비→d)
     * blend(i)                       // 능력치 혼합 (아래)
     * traitMul(i) * footMul(i) * trustMul(i)
     * fatigue(i)                     // = 1 / (1 + FAT * goalsScored_i)   ← "내가 넣은 골"만
     * (isAce(i, 카드종류) ? spotOf(i) : 1)   // 나 → SPOT · 경쟁자 클럽 에이스 → NPC_SPOT
     * (i === me ? ME_P : 1);

pMe = w(me) / Σ w(i);

blend(i) = st(S1)*0.60 + st(S2)*0.25 + st(S3)*0.15;
```

> ⚠️ **`condMul`은 여기 없습니다.** STEP 2와 판정 창 두 곳뿐이에요 (§2-7).
> 컨디션이 낮다고 감독이 덜 쓰는 건 선발/벤치 결정에서 이미 처리됩니다 — 이중 계상이에요.

**🌟 에이스는 카드 종류마다 따로 뽑습니다.** 한 클럽에 골 에이스 · 도움 에이스 ·
수비 에이스가 각각 한 명이고, 같은 사람일 수도 있어요.

```js
const ACE_POOL = { goal: ["fw"], assist: ["mf", "wg"], defend: ["df"] };
// 그 풀 안에서 해당 카드 종류의 무게가 가장 큰 한 명 → SPOT(나) 또는 NPC_SPOT(경쟁자)
```

### 🔴 내 몫에는 **바닥**이 있습니다 — `FLOOR_SHARE` (2026-08-28 판정 · inspector F3)

```js
w(me) = max(w(me), FLOOR_SHARE * Σ w(i));      // FLOOR_SHARE = 0.12 (제안)
```

**나에게만 겁니다.** 경쟁자에게 걸면 리그 득점이 다시 11명에게 흩어져 골든부츠가 무너져요.

> ### 왜 계단은 두고 바닥을 까나
> inspector가 잰 세 증상은 **하나의 원인**에서 나옵니다 — `SPOT`이 승자독식이라
> ① 능력치 5점에 **2.6배 계단** ② **강팀으로 이적하면 카드가 1/3** ③ 검사 13번이
> 능력치 70 전 포지션 미달(0.31~0.43).
>
> **그런데 한 노브에 목표가 둘 묶여 있습니다.**
> ⓐ **리그 득점 분포** — 골이 소수에게 몰려야 골든부츠가 성립합니다
>    (`career.js:931-934`가 적어 둔 `ACE_W`의 원래 목적: *"이게 없으면 44골이 11명에게
>    고르게 흩어져서 팀 최다 득점자가 8골이 돼요"*)
> ⓑ **내 조작량** — §C-3 "긴장이 곧 조작량" · 검사 13번
>
> **둘을 분리합니다.** ⓐ는 `SPOT`/`NPC_SPOT` 계단이 계속 맡고, ⓑ는 바닥이 맡아요.
>
> **바닥은 아래쪽에서만 작동합니다.** 능력치 110의 `pMe ≈ 0.25`는 안 건드리고,
> 능력치 70의 `≈ 0.06`만 0.12로 올려요 —
>
> | | 바닥 없음 | 바닥 0.12 |
> |---|---|---|
> | 능력치 70 (동료 str 70) | 0.34회 ❌ | **≈ 0.70회** ✅ |
> | 능력치 70 (동료 str 80 · 강팀) | 0.30회 ❌ | **≈ 0.70회** ✅ |
> | 능력치 80 | 0.93회 | 0.93회 (그대로) |
> | 능력치 110 | 1.05회 | 1.05회 (그대로) |
>
> **계단이 2.6배 → 1.3배로 눌리고, 이적 페널티가 사라지고, 검사 13번이 통과합니다.**
> 세 증상이 한 노브로 완화돼요.
>
> ### 그리고 계단을 **화면에 보이게** 합니다 (원칙 ③)
> *"우리 팀 에이스가 되는 순간"*은 버그가 아니라 **좋은 사건**입니다.
> 화면에 안 보이면 노이즈로 읽히고, 보이면 서사가 돼요 —
> 명단·준비 화면에 **🌟 배지**(골/도움/수비 종류별)를 달고, 처음 되는 시즌에
> `🌟 이제 우리 팀의 해결사예요` 한 줄을 띄웁니다. director 항목입니다.
>
> ⚠️ **원칙 ④**: 바닥은 생산량을 늘립니다. 저연차 구간에서만 실질적으로 작동하니
> **목표 곡선 70(5%)에 오히려 가까워질 수 있어요** (현재 1.8%). balancer 확인 항목 ①-I.

**`posW`는 카드 종류마다 다른 표를 씁니다.**

| 카드 | `posW` | `slot.X` |
|---|---|---|
| ⚽ 결정 | `GOAL_W[pos]` (`career.js:929`) | `slot.g` |
| 🅰️ 전개 | `ASSIST_W[pos]` (`career.js:939`) — **`MAKER_W`는 곱하지 않습니다** (§3-2) | `slot.a` |
| 🧱 수비 | `matchContribution`의 `D` 가중 (`game.js`) | `slot.d` |

**`S1/S2/S3`는 포지션마다 다릅니다.**

| 포지션 | S1 (0.60) | S2 (0.25) | S3 (0.15) |
|---|---|---|---|
| fw 공격수 | 슛 | 스피드 | 드리블 |
| wg 윙어 | 드리블 | 스피드 | 패스 |
| mf 미드필더 | 패스 | 드리블 | 체력 |
| df 수비수 | 수비 | 체력 | 스피드 |

⚠️ **스탯 읽기는 반드시 이 형태입니다** (§9-2 규칙 6):

```js
const allStat = 평균(존재하는 값들);
const st = (k) => (S.stats[k] == null ? allStat : S.stats[k]);
```

`S.stats.speed`는 나중에 추가된 스탯입니다. `undefined * 0.25 = NaN` → `wMe`가 NaN →
**그 선수는 영원히 순간 카드를 못 받습니다.** 소리 없이 무명이 돼요.

### 2-5. STEP 4 — 갈림

```js
if (장면 === "우리 공격") {
  const 주인공 = STEP3(빅찬스 ? "결정" : "전개");
  if (주인공 === me)  → 🔥 순간 카드를 엽니다 (미니게임)
  else                → 자동 굴림:
       pFinish = clamp(FIN * atkW * sc(주인공), 0, 1);
       // 성공하면 그 동료의 골. 여기에 내 도움이 붙는 경로는 아래.
}
if (장면 === "상대 공격") {
  const 주인공 = STEP3("수비");                 // 우리 팀에서 그 장면을 맡는 사람
  if (주인공 === me)  → 🧱 수비 카드를 엽니다
  else                → 자동 굴림:
       pConcede = clamp(1 - (1 - CON * defW) * sc(주인공), 0, 1);
}
if (장면 === "중립") → 필러 텍스트 1줄
```

**`sc(x)` — 자동 확률도 그 장면 주인공의 능력치를 탑니다. 이건 필수입니다.**

```js
const sc = (x) => succ(abilityOf(x), 0.5) / succ(70, 0.5);   // 능력치 70에서 정확히 1
```

> ### 🔒 왜 필수인가
> 자동 확률을 상수로 두면 **능력치가 두 번 실립니다** — 카드는 능력치를 타는데
> 자동은 안 타니, 그 차이가 통째로 새 생산량이 됩니다 (능력치 150에서 도움 **+44%** ·
> 차단 **+84%** · 실점 **−35%**).
>
> 반대로 **카드 쪽에서 능력치를 빼서** 맞추면 중립은 지켜지지만
> **차단에 능력치가 안 실려 수비수만 성장 축이 죽습니다** — 능력치 110에서
> 포지션 격차 **35.8%p**. `POS_AXIS`의 `n`은 레벨만 옮길 뿐 기울기를 못 고쳐요.
>
> **이건 새 발명이 아니라 되살리는 것입니다.** 현행 `deriveOppGoals`(`game.js:1832`)는
> 이미 `((defStat || 40) − 60) / CONC_DEF`로 수비 능력치를 봅니다.
> 제 초안이 `pConcede = CON × defW`로 적으면서 그 항을 떨어뜨린 것이었어요.

**🅰️ 도움이 기록되는 두 경로 — 장면의 성격이 다릅니다**

| 장면 | 도움은 누구에게 | 확률 |
|---|---|---|
| 🅰️ **전개 장면** = "찬스를 **만드는**" 장면 | **그 장면의 주인공에게** (카드든 자동이든) | **항상** — `ASSIST_P2`를 곱하지 않습니다 |
| ⚽ **결정 장면**에서 골이 났을 때 | `ASSIST_P2`(0.75) 확률로 도움자를 **`a` 무게로 지명** (득점자 제외) | `ASSIST_P2` |

> ### 🔴 전개 장면의 도움에 `ASSIST_P2`를 곱하지 마세요 (2026-08-28 판정 · engineer §4-⑤)
> 곱하면 **카드 갈래와 자동 갈래가 4~6배 어긋납니다.**
>
> ```
> 곱하면 →  카드(내가 주인공)   P(내 도움) = perfect              ≈ 0.46
>           자동(내가 주인공)   P(내 도움) = pFinish × ASSIST_P2 × 몫 ≈ 0.07
> ```
>
> **어긋나는 이유는 두 갈래가 다른 사건을 재고 있었기 때문입니다.**
> `ASSIST_P2 × 몫`은 *"동료가 넣은 골에 내가 어시스트로 붙을 확률"*인데,
> 전개 장면은 이미 **내가 그 장면을 만드는 사람으로 뽑힌 뒤**예요. 몫을 또 곱하면
> 주인공 지명을 두 번 세는 셈입니다.
>
> **전개 장면의 정의가 "만드는 장면"이니, 성공하면 도움이 붙습니다.**
> 그러면 두 갈래가 같은 자를 씁니다 —
>
> ```
> 카드(내가 전개 주인공)   P(내 도움) = autoP(me)   ← §2-6 개정으로 정의상 같아졌습니다
> 자동(내가 전개 주인공)   P(내 도움) = FIN × atkW × sc(me) = autoP(me)
> ```
>
> **§2-6이 카드의 중심을 `autoP`로 넘겼으므로 `FIN`을 손댈 필요가 없습니다.**
> (개정 전에는 `succ(70,0.5) = 0.46` vs `0.884 × 0.5 = 0.442`로 4% 어긋났어요.)
>
> **동료가 전개 장면의 주인공이면 내 도움은 없습니다** — 그 동료가 가져갑니다.
> engineer가 §2-8에서 고친 *"우리 팀 동료도 도움을 받는다"*와 정확히 같은 방향이에요.
>
> **`ASSIST_P2`는 폐기가 아니라 자리를 옮깁니다** — 결정 장면의 골에 도움자를 붙이는
> 확률로. 그래야 **리그 전체 도움 ÷ 리그 전체 골 ≈ 0.75**라는 축구다운 비율이 섭니다.

> 🔴 **`a` 무게로 도움자를 지명할 때 현행 `splitMine`의 도움 몫 식
> (`ASSIST_W × (overall/70)^1.2 × 자리결`)을 쓰지 마세요.**
> 그 식에는 **패스 스탯이 없습니다.** inspector가 라이브에서 확인한 결함이에요 —
> 미드필더가 주 스탯(패스)을 특화하면 종합이 덜 올라 **축이 오히려 줄어듭니다(×0.978).
> 키울수록 손해예요.**
>
> v2의 `blend`(포지션별 주 스탯 0.60 가중)를 쓰면 **패스를 올린 만큼 전개 카드의
> 주인공이 될 확률이 직접 오릅니다.** ①이 구조적으로 풀려요.
> 도움자 지명도 같은 `w_a(i) / Σ w_a(i)` 비율을 씁니다.
> 같은 이유로 `sc(x)`의 `abilityOf(x)`도 **종합이 아니라 그 카드 종류의 `blend`**입니다.

### 2-6. 미니게임 성공률 — **중심은 자동 확률, `succ`는 폭만 정합니다**

> ### 🔴 2026-08-28 판정 (inspector F4 · engineer §4-⑤) — 두 문제가 한 뿌리였습니다
> `succ` 표가 **성공률의 중심점**을 독립적으로 정하는 한, 카드 갈래와 자동 갈래는
> **반드시 어긋납니다.** 실제로 두 곳에서 터졌어요 —
> 🅰️ 전개 카드의 도움이 4~6배(engineer §4-⑤) · 🧱 수비 카드의 실점이 능력치 150에서
> −9.8%(inspector F4, `outcome()`의 clamp가 `miss`를 자름).
>
> **중심을 자동 확률에 넘기고, `succ`는 조작이 흔드는 폭만 맡습니다.**

```js
// 한 카드의 "사건이 일어날 확률" — 카드 종류와 무관하게 같은 형태
P(사건 | 카드) = clamp( autoP(me) + 2*half(a)*(s - 0.5), 0, 1 );

  autoP(me) = 그 장면을 자동으로 굴렸을 때의 확률 (§2-5)
              ⚽ 결정  → FIN × atkW × sc(me)          (골)
              🅰️ 전개  → FIN × atkW × sc(me)          (골 + 내 도움)
              🧱 수비  → 1 − pConcede(me)             (막을 확률)
  half(a)   = 선형보간(40 → 0.17, 80 → 0.19, 120 → 0.20)
  s         = 조작 숙련도 ∈ [0, 1] (실제 플레이에서는 판정 결과가 s를 대신합니다)
```

**`s = 0.5`에서 `P(사건) = autoP(me)`입니다 — 중립이 정의상, 모든 능력치에서 성립합니다.**

| | 이전 (`mid(a)` 독립) | **개정 (`autoP` 중심)** |
|---|---|---|
| 중립 | 능력치 70에서만 우연히 맞음 | **모든 능력치에서 정의상 성립** |
| 🧱 수비 clamp | 능력치 150에서 −9.8% | **안 걸림** (150에서 `0.737 + 0.20 = 0.937 < 1`) |
| 🅰️ 전개 도움 | 4~6배 어긋남 | 맞음 |
| 조작 체감 | `±half(a)` | **`±half(a)` 그대로** |
| 검사 10번 밴드 | ±3% | 🔴 **±1%로 조입니다** — 이제 남는 차이는 몬테카를로 잡음뿐이에요 |

> **`mid(a)` 표는 폐기합니다.** `autoP(me)`가 그 자리를 대신해요.
> 그리고 **balancer가 이미 맞춰 놓은 것을 정의로 승격하는 것**이라 계수가 안 움직입니다 —
> `FIN 0.884`·`CON 1.111`이 `s = 0.5`의 `succ`에 맞춰 역산된 값이었어요.
>
> **원칙 ⑥은 오히려 더 잘 지켜집니다.** `autoP`가 능력치를 정직하게 타므로
> 조작 폭(`±0.17~0.20`)이 능력치 폭보다 작으면 역전이 안 나요.
> 검증은 여전히 커리어 단위로 합니다 (§12 실측 2-a).

**미니게임 결과 → 카드 결과**

| 판정 | ⚽ 결정 | 🅰️ 전개 | 🧱 수비 |
|---|---|---|---|
| `perfect` | 내 골 | 동료 골 + **내 도움** | 실점 안 함 |
| `ok` | 슛 시도 (기록 없음) | **무위** (연출만 다름) | 실점 안 함 |
| `miss` | 무위 | 무위 | **실점** |

**`P(사건)`이 곧 위 표의 사건 확률입니다.**

| 카드 | `P(사건)`이 무엇인가 | 나머지 |
|---|---|---|
| ⚽ 결정 | `perfect` = 내 골 | `ok`/`miss` 배분은 **연출 몫** (둘 다 기록이 안 남아요) |
| 🅰️ 전개 | `perfect` = 동료 골 + 내 도움 | `ok`/`miss` = **둘 다 무위**. 🔴 **`ok`가 골을 주면 안 됩니다** — 자동 갈래에서 *골*과 *내 도움*은 **같은 사건**이라, `ok`가 골을 주면 도움 중립은 맞아도 **골 중립이 깨집니다** |
| 🧱 수비 | **`perfect + ok` = 막음** · `miss` = 실점 | 막은 것 중 `perfect`(🛡️ 차단 기록) 비율은 판정 품질이 정합니다 |

> 🧱 수비만 `P(사건)`이 **`1 − miss`**인 것에 주의하세요. `perfect`와 `ok`가 결과가 같아서
> (둘 다 실점 안 함) **중립이 걸리는 자리는 `miss`**입니다.
> `outcome()`이 `miss`를 `1 − perfect`로 clamp하면 그 자리가 깨집니다 (inspector F4).

> **이 표는 지킵니다.** *"내가 킬패스를 성공하면 도움"*은 게임의 약속이고,
> 성공했는데 도움이 확률로 날아가면 원칙 ①(화면의 기대 ↔ 메커닉)을 정면으로 어깁니다.
> **중립은 표를 고쳐서가 아니라 자동 갈래를 고쳐서 지킵니다** (§2-5).

### 2-7. 컨디션 — 두 곳에만 겁니다

```js
condMul = 0.76 + 0.30 * (condition / 100);      // condition 80에서 정확히 1.000
```

| 어디 | 왜 |
|---|---|
| STEP 2 `pBig` | 지쳐 있으면 **찬스가 덜 옵니다** |
| **미니게임 판정 창** | 지쳐 있으면 **판정이 좁아집니다** — 원칙 ③은 여기 하나로 지켜집니다 |
| ~~STEP 3 `wMe`~~ | **뺐습니다.** 컨디션이 낮다고 감독이 덜 쓰는 건 선발/벤치 결정에서 이미 처리돼요 — 이중 계상입니다 |

> **기준점이 80인 이유** — 새 커리어 시작(`game.js:483`)과 시즌 리셋(`career.js:331`)이
> 둘 다 80입니다. 50을 기준으로 잡았더니 **기본 상태에서 생산량이 +12.8% 부풀었어요.**
>
> ⚠️ **`c* = 80`은 잠정값입니다.** 컨디션은 훈련/휴식 선택이 정하는 값이라
> 플레이 스타일에 따라 시즌 평균이 움직입니다. **v2의 훈련 루프가 붙은 뒤 실측해서
> `c*`를 박으세요** (§12 실측 ③).

### 2-8. 평점 · 축 · 수상

| | |
|---|---|
| `matchRating` (`career.js:150-179`) | **그대로.** 마지막 카드가 끝나면 그 경기에 일어난 일이 확정되니 그때 호출합니다 |
| `RATE`의 `b` (아무 일 없던 90분) | **+10** — 카드 8칸이라 사건 수가 줄어 평점이 6.0으로 내려갑니다. 골·도움 값은 안 건드려요 |
| `res` (W/L/D) | **마지막 카드 후 확정** |
| hype | `AXIS_K * ln(축 × prestige) − AXIS_OFF` |
| 축 | 공격포인트 기반. **절대값의 로그** — 천장이 없어야 하니까요 |
| 부문상 (골든부츠 등) | **리그 분위수** — 원래부터 순위라 원칙 ⑤에 안 걸립니다 |
| 🔥 `momAdj` (승부처 평점 보정) | **0. 얹지 않습니다** — v1은 승부처가 경기당 딱 한 번이라 ±0.8을 따로 얹었지만, v2는 카드 결과가 **이미 골·도움·차단으로 `matchRating`에 들어갑니다.** 또 얹으면 같은 일을 두 번 세요(원칙 ④) |
| 🏅 **MOM** | **절대 문턱 `MOM_MIN`을 추가합니다** — 그 라운드 리그 최고 평점이면서 `MOM_MIN` 이상일 때만. **`MOM_MIN = 8.40`** (확정 — ①-G) |

> ### 🏅 MOM에 문턱을 다는 이유 (2026-08-28 확정)
> v2는 카드가 8칸이라 **평점 분산이 작아** 상위 선수가 라운드 최고를 더 자주 가져갑니다 —
> 실측에서 K1 기준 현행 6% → **12%**였어요. **K1 기준 6% 근처로 되돌립니다.**
>
> - 🏆 명예의 전당은 **`grow-hof-v1`으로 8종이 공유하는 표**입니다. v2 선수만 MOM이
>   두 배면 **같은 순위표에서 v2가 상위를 잡아먹어요**
> - 이건 의도한 설계가 아니라 **구조의 부산물**입니다. 의도하지 않은 부산물을 남기면
>   나중에 원인을 못 찾습니다
> - 전당 가중치만 절반으로 낮추는 안은 **가중치가 게임마다 달라져** 반드시 헷갈립니다
>
> **손잡이 둘 중 `MOM_MIN`을 씁니다.** `RATE`의 흔들림(`rand(-4,4)`) 폭을 넓히는 쪽은
> **평점 전체가 흔들려** 🔥 물오른 폼·전당 점수·승격이 같이 움직여요. 문턱 하나가 더 국소적입니다.
> 문턱 조정이라 곡선 전체를 다시 잡을 필요는 없지만 **확인은 필요합니다** (실측 ①).

### 2-7b. 🔴 시즌 칭호·피버의 버프가 **카드 엔진에 안 들어갑니다** (1차 항목)

balancer가 G-6을 재다 발견했습니다. **`buffMul`을 부르는 곳이 `game.js:1778-1780`의
`matchContribution` 하나뿐**인데, v2 리그 경기는 `WingerEngine.createMatch`를 씁니다.
`engRow`가 엔진에 넘기는 건 `{name, pos, slot, me, stats, str, foot, row}`뿐이에요.

> ### 🔴 그래서 시즌 칭호 여덟 개의 `g`·`a`·`d` 효과가 **v2 리그 경기에 하나도 안 닿습니다**
> 🥇 골든부츠 위너 · 🎯 리그 최고 도우미 · 🛡️ 리그 최고 수비수 · 📈 공격포인트왕 ·
> 🥈 베스트11 · 🏅 발롱도르 위너 · 🕯️ 설욕의 각오 · 🌏 월드컵 위너.
> **닿는 건 🏆 컵(`matchContribution`)뿐입니다.**
>
> 개념집이 *"효과가 붙는 자리는 **전부 경기 안**입니다"*라고 적어 둔 그 체계가
> **리그 38라운드에서 통째로 죽어 있어요.** 🎉 피버 타임도 같은 자리라 같이 죽습니다.
>
> **이건 §1의 미완입니다.** §2-4의 `w(i)` 식에 `traitMul(i)`가 있는데, 그 자리에
> **칭호·피버 버프가 함께 들어가야** 합니다 —
> ```js
> w(i) = ... * buffMul(i, 카드종류)   // g / a / d 키를 카드 종류에 맞춰
> ```
> `engRow`가 `buffs`(또는 이미 곱한 `buffMul`)를 함께 넘기면 됩니다.
> **경쟁자에게는 안 넣습니다** — 칭호는 내 커리어가 쌓은 것이고, NPC에게는 그 개념이 없어요.
>
> ⚠️ **생산량이 늘어납니다.** 곡선을 다시 확인해야 해요 (실측 ①-L).
> 그리고 `moment` 키(승부처 성공 +%p)는 **미니게임 판정 창**에 붙습니다 (§4-5).

> ### ✅ `BUFF_CAP`이 한 번도 안 물리는 것 — **의도대로입니다. 유지합니다**
> balancer 실측에서 전 칸 0% 물림. **안 물리는 게 정상입니다.**
>
> `BUFF_CAP`(`game.js:590` — `g/a/d 0.4 · rate 0.6 · moment 0.12 · train 0.3`)은
> **안전장치**예요. 브레이크가 안 밟히는 게 정상이듯, 상한이 안 물리는 게 정상입니다.
> 개념집이 적은 목적 그대로 — *"한 번 앞서간 커리어가 영원히 앞서가는 것을 막는 장치."*
>
> **물리도록 낮추면 그게 곧 "잘하는 사람만 손해"가 됩니다.** 🎉 피버 타임을 상한 **밖**에
> 둔 이유와 같은 논리예요 — *"칭호를 많이 단 사람에게는 이벤트가 아무 일도 안 일어나요.
> 잘하는 사람만 이벤트에서 소외됩니다."*
>
> ### ✅ ①-G 이후 재측정 — **제 예상이 틀렸고, 판정은 그대로입니다**
> *"부문상 넷이 살아나면 물릴 것"*이라고 적었는데 **트리거가 아니었습니다.**
> K리그1 천장이 **`g` 합 0.26**(상한 0.4의 **65%**)이고, 실제로 물리는 건
> **🏅 발롱도르 + 🌏 월드컵 우승이 한 시즌에 겹칠 때**뿐이에요.
>
> 부문상 넷은 축이 서로 달라서(골·도움·수비) **같은 키에 안 쌓입니다** —
> 🥇는 `g`, 🎯는 `a`, 🛡️는 `d`. 같은 키에 겹치는 건 **모든 기여**에 붙는 칭호들
> (🏅 발롱도르 +8% · 🥈 베스트11 +5% · 🌏 월드컵 위너 +10%)입니다. 제가 그걸 못 봤어요.
>
> **판정은 그대로입니다 — 유지.** 오히려 이게 `BUFF_CAP`이 **정확히 설계 목적대로**
> 있다는 증거예요. 개념집이 적은 *"한 번 앞서간 커리어가 영원히 앞서가는 것을 막는 장치"*가
> 막으려던 건 **최상위 커리어의 중첩**이지 부문상 몇 개가 아니었습니다.

### 2-8b. 🔴 부문상 셋이 같은 자를 쓰게 하는 규칙 — **별도 게이트 ①-G**

실측에서 능력치 110 기준 🎯 플레이메이커 **88%** · 🥇 골든부츠 **17%** · 🛡️ 철벽상 **0%**가
나왔습니다. `career.js:2325`가 적어 둔 *"포지션마다 노릴 트로피가 하나씩"*이 안 서요.

**🛡️ 철벽상 0%는 "수비수를 고른 플레이어에게 그 상이 없다"는 뜻이고,
포지션 넷을 대등하게 대접하기로 한 결정(D-(나))과 정면으로 부딪힙니다.**

#### (1) 재야 할 값 — **딱 하나입니다**

부문상은 *"내 생산량 vs 리그 1위 생산량"*으로 갈립니다. 세 축의 난이도가 같으려면 —

> ## 🔑 세 축에서 `내 생산량 ÷ 리그 1위 생산량`이 같아야 합니다.

| 축 | 내 기록 (110) | 리그 1위 | **비** |
|---|---|---|---|
| ⚽ 골 | 15.3 | 21.6 | **0.71** |
| 🅰️ 도움 | 19.8 | 15.3 | **1.29** |
| 🛡️ 수비 | 26.7 | 50.1 | **0.53** |

**⚽ 골(0.71)을 기준선으로 삼습니다.** 이유 셋 —
① `SPOT`·`NPC_SPOT`이 **골 기준으로 역산**됐고 곡선이 그 위에 서 있습니다. 기준을 옮기면
   곡선을 처음부터 다시 잡아야 해요 ② 골든부츠는 가장 오래된 부문상이고 실측 이력
   (43%→60% 사고 포함)이 전부 골 기준입니다 ③ 17%는 `career.js:2240` 주석이 말한
   *"좋은 시즌이면 닿고 평범하면 안 닿는"* 자리에 맞습니다.

#### (2) ✅ 손잡이 — `NPC_SPOT`을 **카드 종류별로 쪼갭니다** (부활 · 2026-08-28)

> ### 한 게이트 늦게 맞았습니다
> 이 절을 (10)에서 한 번 폐기했다가 **되살립니다.**
> *"`NPC_SPOT_D`가 무실점(클럽 기록)에 안 닿는다"*는 판정은 **후보 D-1 이전의 세계**에서
> 잰 것이었어요. D-1이 클럽 간 무실점 격차를 좁히자 **차단 타이브레이커가 실제 판정자**가
> 됐고, 그때부터 `_D`가 철벽상에 직접 닿습니다 — **`_D` 10 → 1에서 철벽상 8% → 29%.**
>
> 제가 §2-8b (10)에 *"손잡이가 축에 안 닿는데 최소 개입이라고 부른 게 제 잘못"*이라고
> 적었던 것은 **거둡니다.** 원래 설계가 맞았어요.
>
> 그리고 이 위험은 제가 §12-1에 이미 적어 뒀던 것입니다 —
> *"🛡️ 무실점은 팀 기록이라 실질 판정자가 차단 타이브레이커가 됩니다.
> **`NPC_SPOT_D`(G-2)를 움직이면 수상률이 차단 쪽으로 반응할 수 있어요.**"*
> **한 게이트 뒤에 그대로 일어났습니다.**

**확정값**: `NPC_SPOT_G 3.05` · `NPC_SPOT_A 6.50` · `NPC_SPOT_D 4.90`
→ 상 **🥇 18.3% · 🎯 17.6% · 🛡️ 18.1%** — **격차 0.7%p** ✅ (역대 최고)

`NPC_SPOT`을 통째로 움직이면 골 곡선이 무너집니다. 그래서 —

| 상수 | 값 | 무엇을 맞추나 |
|---|---|---|
| `NPC_SPOT_G` | **3.05** (확정) | 🥇 골든부츠 **18.3%** |
| `NPC_SPOT_A` | **6.50** (확정) | 🎯 플레이메이커 **17.6%** |
| `NPC_SPOT_D` | **4.90** (확정) | 🛡️ 철벽상 **18.1%** — **차단 타이브레이커**를 통해 닿습니다 |

**`SPOT`은 쪼개지 않습니다.** 내 쪽을 종류별로 다르게 주면 *"내가 어느 종류의 에이스냐"*로
포지션 유불리가 생겨요. **경쟁자 쪽만 쪼개면 리그 분포만 조정되고 내 생산량은 안 움직입니다** —
최소 개입입니다.

#### (3) 🔴 `autoMatch`가 전개 장면을 다르게 해석하는 것은 **버그입니다**

> `createMatch`는 전개 장면에서 **주인공이 만들고 다른 사람이 마무리**하는데,
> `autoMatch`(`engine.js:715-727`)는 **주인공을 득점자로 넣습니다.**
>
> **§2-5가 정한 것은 하나뿐입니다** — *"🅰️ 전개 장면 = 찬스를 **만드는** 장면.
> 도움은 그 장면의 주인공에게."* 카드든 자동이든, 나든 경쟁자든 같아야 합니다.
> **나와 경쟁자가 다른 규칙을 쓰는 것**이야말로 §2-9와 §2-10이 없애려던 바로 그 병이에요.
>
> **의도가 아니라 버그입니다. 다만 이번 게이트에서 고치지 않습니다** — 아래.

#### (4) 🛡️ 철벽상의 **축이 잘못 잡혀 있습니다**

이게 더 깊은 문제예요. 지금 축은 **차단 횟수**인데 —

| 문제 | 왜 |
|---|---|
| **기회가 상대 공격 장면에서 나옵니다** | **약한 팀일수록 두들겨 맞아서 차단이 쌓입니다.** 강팀 수비수는 막을 일이 적어요 |
| **1위가 "가장 잘 막는 수비수"가 아닙니다** | *"가장 자주 두들겨 맞는 팀의 수비수"*가 됩니다. 실제 축구의 태클 1위도 대개 하위권 팀 선수예요 |
| **능력치를 올려도 1위가 안 따라옵니다** | 실측: 내 차단은 110→150에서 26.7→37.5로 오르는데 **리그 1위는 50.1→50.6으로 거의 고정**입니다. 남의 팀 기회는 내 능력치와 무관하니까요 |

**이건 원칙 ⑤의 변형입니다** — 축을 잘못 고르면 능력치를 올려도 결과가 안 따라옵니다.
[[분석 — 순위 천장과 성장 곡선]]이 순위 축을 버린 것과 같은 자리예요.

> ### 판정: 🛡️ 철벽상의 축을 **무실점 경기 수**로 바꿉니다 (실측 ①-G에서)
>
> **주축 = 내가 선발로 뛴 경기 중 팀 무실점 경기 수.** 차단 횟수는 **동점 시 타이브레이커**로.
>
> | 왜 | |
> |---|---|
> | **누적이고 상한이 없습니다** | 원칙 ⑤. 38이 이론상 상한이지만 실질적으로 안 닿아요 |
> | **능력치가 실립니다** | 내 🧱 수비 카드 성공이 곧 실점을 막는 것이라 무실점에 직접 기여합니다 |
> | **골든부츠와 대칭입니다** | 골든부츠도 팀 전력이 실리고(좋은 팀이 많이 넣음) 개인 능력이 실려요 |
> | **실제 축구의 상과 같은 형태** | 최다 클린시트 상이 그것입니다 |
> | **"두들겨 맞을수록 유리"가 뒤집힙니다** | 무실점은 안 맞아야 좋으니까요 |
>
> ⚠️ **시즌 칭호 「🛡️ 리그 최고 수비수」는 자동으로 따라갑니다** — `AWARD_BUFF`가 수상
> 목록을 그대로 읽으니 획득률 = 철벽상 수상률이에요. 효과(`d: 0.15`)는 계속 차단에 붙습니다.

#### (5) 🔴 `posAxis`(hype)는 **차단을 계속 봅니다** — 바꾸지 않습니다 (2026-08-28 판정)

engineer가 되돌려 물은 자리입니다. 제가 §2-8b (4) 초판에 *"축을 바꾸면 `POS_AXIS.df`의
정규화 · hype가 함께 움직인다"*고 적은 것은 **틀렸습니다.** 정정합니다.

> ### 부문상과 hype는 원래부터 **다른 물건**입니다
> §1-3에서 이미 선을 그어 뒀어요 —
> **부문상은 리그 분위수**(순위 경쟁) · **hype는 절대값의 로그**(천장 없는 축).
> ⚽ 골도 그렇습니다. 골든부츠는 *"리그 1위인가"*를 보고 hype는 *"몇 골 넣었나"*를 봐요.
> 같은 `goals` 값을 쓰지만 **쓰는 방식이 다릅니다.** 이건 병이 아니라 설계예요.
>
> §2-9·§2-10이 없애려던 병은 *"**나와 경쟁자가** 다른 자로 잰다"*이지
> *"부문상과 hype가 다른 방식으로 쓴다"*가 아닙니다.

**그리고 무실점을 hype에 넣으면 안 되는 이유가 넷 더 있습니다.**

| # | 왜 |
|---|---|
| 1 | 🔴 **hype는 개인 생산량이어야 합니다.** 무실점은 **팀 기록**이라 같은 클럽 선발 11명이 **전부 같은 값**입니다(engineer 실측: 1~5위가 전부 무실점 7). 개인차가 사라져요 |
| 2 | 🔴 **`d` 가중은 네 포지션이 전부 갖습니다** (fw 0.15 · wg 0.15 · mf 0.30 · df 0.55). **공격수가 팀 무실점으로 hype를 받는 건** 말이 안 됩니다 |
| 3 | 🔴 **강팀에 있는 것만으로 hype가 오릅니다** — 이적이 육성을 대체하는 구조예요 (원칙 ⑥의 변형) |
| 4 | 재측정 범위가 **네 포지션의 `n` 전부 + 리그MVP·베스트11·발롱도르**로 번집니다. 지금 곡선은 최대 오차 **2.3%p**인데 다시 잡을 이유가 없어요 |

**부문상이 팀 결과를 봐도 되는 이유**는 그게 **순위 경쟁**이기 때문입니다 —
골든부츠도 좋은 팀이 유리하고, 실제 축구의 최다 클린시트 상도 그래요.
그리고 **클럽 안에서는 차단이 타이브레이커로 개인차를 가릅니다.**

> ✅ **`df.n = 0.816`은 그대로입니다.** G-3도 G-5도 차단 분포를 안 바꾸니까요.
> **G-6은 재산정이 아니라 회귀 확인으로 바뀝니다** (§12-4b).
>
> ⚠️ 다만 **간접 경로 하나**가 남습니다 — 철벽상이 0%에서 살아나면 df가
> 「🛡️ 리그 최고 수비수」(`d: 0.15`)를 받게 되고, 그게 차단을 늘리고, 차단이 hype에 들어가요.
> **그 크기는 재야 합니다** (G-6).

#### (6) 🟠 `shareByWeight`도 같은 자로 재게 합니다 — **`pBig`로 사후 재구성**

G-3 정정 뒤 **라운드마다 리그 6팀 중 한 팀만 옛 규칙**이 남았습니다.

| 길 | 누구 | 도움 ÷ 골 |
|---|---|---|
| `createMatch` (카드) | 내 클럽 | 전개/결정을 갈라요 |
| `autoMatch` | 다른 4팀 | **0.881** (G-3으로 맞춤) |
| **`shareByWeight`** | **내 경기의 상대 클럽** | **0.75** ← 여기만 옛 규칙 |

> ### 🔴 이건 무작위 오차가 아니라 **계통 오차**입니다
> 다른 자를 쓰는 그 한 팀은 **언제나 내 상대**예요. 즉 **내 상대 클럽 선수만
> 구조적으로 도움이 덜 나옵니다.** 리그 도움 순위가 매 라운드 그쪽으로 기울어요.
> 2.2%는 작지만 특정 클럽에 몰리는 편향이라 **평균으로 안 씻깁니다.**

**engineer가 짚은 대로 개별 골의 출처는 알 수 없습니다** — 스코어가 이미 정해진 클럽이니까요.
**하지만 분포는 엔진이 알고 있습니다.**

```js
// 새 상수를 만들지 않습니다. BIG_BASE·edge를 그대로 재사용해요
pBigNorm = 그 클럽의 pBig(= BIG_BASE × edge)를 결정/전개 성공률로 정규화한 값

골 하나마다:
  결정 출신 (pBigNorm)      → 득점자를 g 무게로 · 도움은 ASSIST_P2 확률로 a 무게 지명
  전개 출신 (1 − pBigNorm)  → 도움자를 a 무게로 먼저 · 마무리는 나머지에서 g 무게 (도움 항상)
```

**"골 중 몇 %를 전개 출신으로 볼 것인가"를 새로 정하지 않습니다** — 엔진이 이미 아는 값을
사후 재구성하는 것뿐이에요. 그래야 셋이 정말 같은 자가 됩니다.

⚠️ 이건 **G-4(도움÷골)를 다시 움직입니다.** G-4 안에서 함께 처리하세요.

#### (7) 🔴 `autoMatch`는 **한 장면에 한 번만 굴립니다** — §1의 정정입니다 (2026-08-28 판정)

balancer가 게이트를 막는 것이 계수가 아니라 **구조**라고 짚었습니다.

| | 팀 득점/경기 | 팀 실점/경기 | 무실점률 |
|---|---|---|---|
| `createMatch` (**내 클럽만**) | 1.142 | 1.344 | **22.6%** |
| `autoMatch` (다른 4팀) | 0.612 | 0.618 | **52.5%** |

```
클럽별 시즌 무실점  18.6 / 18.1 / 18.4 / [10.8] / 17.7 / 17.4   ← 대괄호가 내 클럽
```

**클럽 전력(76 → 66)이 무실점을 18.6 → 17.4밖에 못 움직이는데,
"카드 엔진이냐 아니냐"가 18.4 → 10.8을 움직입니다.**

> ### 이건 §1을 다시 여는 게 아니라 **§1에서 놓친 것을 고치는 것**입니다
> **§2-5 STEP 4는 장면마다 굴림이 하나입니다.** 우리 공격 장면이면 `pFinish` 하나,
> 상대 공격 장면이면 `pConcede` 하나. 한 장면은 STEP 1에서 이미 둘 중 하나로 갈려요.
> **한 장면에서 두 확률을 곱하는 형태는 제 설계 어디에도 없습니다.**
>
> 그리고 **엔진이 스스로 그렇게 적어 뒀습니다** (`engine.js` `autoMatch.oneWay` 주석) —
> *"마무리와 수비를 **한 번의 굴림**으로 봅니다 — 두 번 굴리면 같은 장면을 두 번 세는
> 셈이라 **골이 반으로 줄어요**."* 그런데 코드는 `chance(pf * pc)`, **확률의 곱**입니다.
> **주석이 막으려던 일을 코드가 하고 있었어요.**
>
> §2-10의 「세 가지 냄새」에 네 번째를 더합니다 —
> **4. 한 사건을 두 확률의 곱으로 정하는 것** → 같은 장면을 두 번 세는 것입니다.

**`chance(pf * pc)` → `chance(pf)` (후보 D)를 받습니다.**

**D와 E 중 D인 이유** — `createMatch`는 **칸마다 우리 팀에서 한 명만** 봅니다.
우리 공격 칸은 우리 공격 주인공(`pFinish`), 상대 공격 칸은 우리 수비 주인공(`pConcede`).
**상대 팀 개인은 아예 모델링하지 않아요**(플레이어 중심 설계).
E(`pc / pConcede(dw, 70)`)는 `autoMatch`에만 **항을 하나 더 얹어** 새 비대칭을 만듭니다.
실측도 그렇습니다 — 세 상 격차가 D 3%p vs E 12%p이고, E는 철벽상이 8%로 밴드 아래예요.

> ### ⚠️ D가 남기는 비대칭 — 명시하고 2차로 넘깁니다
> `autoMatch`에서 **경쟁자 수비수의 개인 능력치는 어디에도 안 실립니다.**
> 경쟁자 클럽의 무실점은 **클럽 전력만** 가르는데, 내 클럽은 거기에 **내 수비 카드**가
> 더해져요. 철벽상 14%는 그 위의 값입니다.
>
> **진짜 대칭은 칸마다 관점을 굴리는 것입니다** (2차 후보) —
> ```
> A의 공격 칸:
>   50% → A 관점: A의 공격 주인공, pFinish(A주인공)
>   50% → B 관점: B의 수비 주인공, pConcede(B주인공)
> ```
> 이러면 공격수와 수비수가 균등하게 실리고 `createMatch`와 형태가 같아집니다.
> **곡선을 다시 잡아야 해서 1차에는 안 넣습니다.**
>
> **크기**: 득점은 맞았지만(**+0.9%**) **실점 −16.3% · 무실점 +31.6%**가 남습니다.
> 계수에서 옵니다 — 내가 먹는 길 `CON 1.111 × 0.5 = 0.5555` vs
> 남이 먹는 길 `FIN 0.884 × 0.5 = 0.4420`, **차 25.7%**.
> 거기에 내 수비수의 `sc`가 `pConcede`를 더 눌러요.
> **방향이 나에게 유리한 쪽**이라 철벽상 14%에 이미 반영돼 있습니다.

> ### 🚧 검사는 **득점만 빨간불**로 봅니다 — 실점은 상한 고정 (2026-08-28 판정)
> **득점 비대칭은 버그였고, 실점 비대칭은 알려진 설계 한계입니다. 성질이 다르니
> 같은 검사에 묶지 않습니다.**
>
> 버그를 고쳤는데 검사가 계속 빨간불이면 **검사가 신호를 잃습니다** —
> *"빨간불인데 그냥 두는 검사"*가 하나 생기면 그게 다음 사고의 씨앗이에요.
> 이 저장소가 *"초록불인데 아무것도 안 지키는 검사"*로 여덟 번 데인 것의 반대 형태입니다.
>
> **그렇다고 숨기지도 않습니다.** 검사 31b가 **지금 크기(−16.3% / +31.6%)를 상한으로
> 박고, 더 나빠지면 빨간불**이 되게 합니다. 회귀는 잡히고 현상은 기록돼요.
> 2차의 「칸마다 관점 굴리기」가 들어가면 **31b를 31로 합칩니다.**

#### (8) 📈 공격포인트왕 — `ACE_POOL`의 **골 풀에 `wg`를 넣습니다**

세 상은 후보 D로 모이는데(격차 3%p) 📈만 54%로 남습니다.
원인은 `ACE_POOL`이 `goal: ["fw"]` · `assist: ["mf","wg"]`로 **안 겹쳐서**
리그에 *"골과 도움을 둘 다 하는 사람"*이 **구조적으로 없는** 것입니다
(리그 1위 골 24.1 · 1위 도움 24.8인데 1위 공격P가 28.7뿐).

> ### 📈를 부문상에서 빼지 않습니다 — **윙어의 트로피**입니다
> `career.js:2325`가 적은 *"포지션마다 노릴 트로피가 하나씩"* —
> fw 🥇 · mf 🎯 · df 🛡️ · **wg 📈**입니다. 빼면 윙어에게 트로피가 없어져요.
>
> 그리고 **윙어가 공격P왕을 받는 게 설계상 맞습니다.**
> `GOAL_W`(fw 1.0 / **wg 0.75** / mf 0.4) + `ASSIST_W`(mf 1.0 / **wg 0.9** / fw 0.5)에서
> **합이 가장 큰 게 윙어(1.65)**예요 — fw 1.5 · mf 1.4.
> §8-1의 인버티드 자리(골↑도움↓)와 전통 자리(도움↑골↓)도 *"둘 다 한다"*는 정체성입니다.

```js
ACE_POOL = { goal: ["fw", "wg"], assist: ["mf", "wg"], defend: ["df"] }
//                        ^^^^ 이것만 추가
```

**풀을 통째로 합치지 않습니다.** balancer가 합쳐 보고 *"골 축이 무너진다"*(골든부츠 52%)고
했는데, 그건 `GOAL_W`가 낮은 mf까지 골 에이스가 되기 때문이에요.
**`wg`만 더하면** 리그에 *"둘 다 하는 윙어"*가 생겨 공격P 1위가 올라가면서도
**골 축은 fw가 지킵니다** (실측: fw 42.6% > wg 30.8% · 되돌리면 54.0% / 19.8%). **최소 개입입니다.**

> ### ⚠️ 왜 골 축이 지켜지는가 — 근거를 정정합니다 (engineer 확인)
> 제가 *"`GOAL_W[wg] 0.75 < fw 1.0`이라 fw가 여전히 에이스가 되기 쉽다"*고 쓴 것은
> **틀렸습니다. `aceOf`는 `GOAL_W`를 보지 않습니다** — **능력치 최대 한 명**을 고를 뿐이에요.
> `fw 2 · wg 2` 포메이션이라 **골 에이스가 wg일 확률이 50.1%**이고,
> 에이스 윙어의 무게 `0.75 × 2.90 = 2.175`는 **비에이스 fw(1.000)를 넘습니다.**
>
> **결론을 만드는 것은 「에이스가 아닐 때의 무게 차이」입니다.**
> 팀에는 비에이스가 항상 한 명씩 남거든요 —
>
> | 에이스가 | fw 두 명의 골 무게 합 | wg 두 명의 합 |
> |---|---|---|
> | fw (49.9%) | `1.0×2.90 + 1.0` = **3.90** | `0.75 + 0.75` = 1.50 |
> | wg (50.1%) | `1.0 + 1.0` = 2.00 | `0.75×2.90 + 0.75` = **2.93** |
>
> 에이스가 fw일 때는 압도하고, wg일 때는 근소하게 집니다 — **평균에서 fw가 이겨요.**
>
> **근거가 틀린 채로 남으면 다음 사람이 「`GOAL_W`가 에이스 선택을 가른다」고 믿습니다.**
> 그러면 `GOAL_W`를 만져 에이스 분포를 바꾸려다 아무 일도 안 일어나요.

#### (8b) 🔴 📈 공격포인트왕 51% — **`ACE_POOL.assist`에 `fw`를 넣습니다** (2026-08-28 판정)

세 상은 모였는데(🥇 14.5 · 🎯 17.7 · 🛡️ 16.2 · 격차 3.2%p) 📈만 **51%**입니다.

**원인은 손잡이가 아니라 리그 분포입니다.**

```
리그 1위 골 22.5 · 1위 도움 22.8  →  그런데 1위 공격P가 29.7뿐
내 wg110 공격P 30.0  →  비 1.01
```

**골 1위와 도움 1위가 다른 사람이라 아무도 둘 다 높지 않습니다.**

##### 갈래와 트레이드오프

| 갈래 | 좋은 점 | 나쁜 점 |
|---|---|---|
| **(가) 리그에 "둘 다 하는 선수"를 만든다** | 원인을 직접 친다. 비가 내려감 | `ACE_POOL`을 건드리면 계수가 또 움직입니다. 통째로 겹치면 골 축이 무너져요(골든부츠 52%) |
| (나) 📈의 문턱만 다르게 (`bar` 배수) | 값싸다 | 🔴 **`career.js:2240`이 버린 방식입니다** — *"랜덤 문턱이면 화면에 뜨는 순위와 수상이 서로 모르는 사이가 됩니다."* **1위면 받는다**는 규칙을 깨면 안 돼요 |
| (다) 📈의 축을 바꾼다 | — | 🔴 윙어 전용 통계가 엔진에 없습니다(드리블 성공·크로스는 미니게임 기록이라 **경쟁자에게 없어요**). 그리고 §2-8b가 *"포지션마다 트로피 하나씩"*이라 뺄 수 없습니다 |
| (라) 게이트 문턱을 12%p에서 넓힌다 | — | 🔴 **36.5%p는 넓혀서 받을 크기가 아닙니다.** 그리고 실측에서 mf110도 📈를 58% 받아요 — **윙어 전용도 아니고 그냥 문턱이 낮은 상**입니다 |

> ### **(가)를 고릅니다.** 나머지 셋은 전부 증상을 덮는 쪽이에요.
> 특히 (라)는 도피입니다 — *"좋은 시즌이면 닿고 평범하면 안 닿는"*(`career.js:2240`)
> 자리가 아니라 **자동으로 들어오는 상**이 되는 거니까요.

##### 무엇을 어떻게 — **`assist` 풀에 `fw`를 넣습니다**

```js
ACE_POOL = { goal: ["fw", "wg"], assist: ["mf", "wg", "fw"], defend: ["df"] }
//                                                    ^^^^ 이것만 추가
```

**§2-8b (8)에서 `goal` 풀에 `wg`를 넣은 것과 정확히 대칭입니다.**
골 에이스 fw가 **도움 에이스도 될 수 있어** 리그에 *"둘 다 하는 사람"*이 생깁니다.

**도움 축은 mf가 지킵니다** — (8)에서 골 축이 지켜진 것과 **같은 메커니즘**이에요.
`ACE_POOL`은 능력치 최대 한 명을 뽑으니 풀이 넓어질수록 fw가 도움 에이스가 될 수 있지만,
**비에이스일 때의 무게 차이**가 결론을 만듭니다 —

| 에이스가 | mf 네 명의 도움 무게 합 | fw 두 명의 합 |
|---|---|---|
| mf | `1.0×5.80 + 1.0×3` = **8.80** | `0.5 + 0.5` = 1.00 |
| fw | `1.0×4` = 4.00 | `0.5×5.80 + 0.5` = **3.40** |

`ASSIST_W`가 mf 1.0 vs fw 0.5로 두 배 차이인 데다 **mf가 넷, fw가 둘**이라
어느 쪽이 에이스든 mf가 이깁니다. **(8)보다 안전합니다** — 거기는 fw 2 : wg 2였어요.

⚠️ **계수가 또 움직입니다.** 도움 분포가 바뀌니 `NPC_SPOT_A`·`ASSIST_P2`·`AXIS_OFF`를
다시 봐야 해요. **G-10에 묶어 한 벌로 재세요** (G-12).

##### ✅ 실측 결과 — 방향은 맞았고 크기가 모자랐습니다

`ACE_POOL.assist`+`fw`는 **노린 대로 작동했습니다** — 리그 1위 골·도움은 내려갔는데
**1위 공격P만 올라갔고**(25.2 → 26.1), 겹침 67% → 74%, 내 비 1.01 → **0.91**.
*"리그에 둘 다 하는 사람"*이 실제로 생겼어요. **이 변경은 유지합니다.**

그런데도 📈가 35.6%입니다. 그리고 balancer가 **계수로는 못 푼다는 것을 증명**했어요 —
`NPC_SPOT` 세 값을 어디에 놓아도 한 곡선 위에서만 움직이고,
**≤12%p는 🥇가 7.8%로 내려앉을 때만** 나옵니다.

#### (8c) 🔴 📈의 자리를 다시 잡습니다 — **G-7의 기준이 잘못 잡혀 있었습니다** (2026-08-29 판정)

##### 갈래와 트레이드오프

| 갈래 | 좋은 점 | 나쁜 점 |
|---|---|---|
| **(가) G-7의 기준을 「수상률」에서 「기대 이득」으로** | 코드 **0줄**. 세 축(0.7%p)을 안 흔듦. **이미 있던 설계에 근거** | 기준을 바꾸는 것이라 근거가 약하면 도피로 읽힘 |
| (나) 경쟁 범위를 **윙어 안으로** (`raceTop("p")`의 후보를 wg로) | 한 줄. 세 축 안 흔듦 | 🔴 **효과가 없습니다.** 나머지 54명은 어차피 나보다 공격P가 낮았으니 빼도 수상률이 그대로예요. 그리고 *"리그 윙어 중 1등"*은 상의 격이 떨어집니다 |
| (다) 리그 분포를 **더 겹치게** (겸업 에이스 클럽) | 원인을 직접 침 | 🔴 balancer 실측 — 완전 겹침은 🎯 폭주, `goal=assist`는 33-1 붕괴. **세 축(0.7%p)을 부수지 않고는 불가능**합니다. 제약 4 위반 |
| (라) 📈를 **다른 축으로** | — | 🔴 엔진에 윙어를 재는 다른 통계가 없습니다. 드리블·크로스는 미니게임 기록이라 **경쟁자에게 없어요**(제약 2). `min(골,도움)` 같은 겸업 지표는 **윙어를 더 유리하게** 만듭니다 |

> ### **(가)를 고릅니다.**

##### 왜 이제는 마지막 수단이 아닌가 — 근거가 셋 생겼습니다

**① §2-8b (1)이 요구한 것은 「세 축」이었습니다.**
*"세 축에서 `내 생산량 ÷ 리그 1위`가 같아야 한다"* — 📈는 거기 없었어요.
**넷째로 넣은 건 제가 G-7을 쓸 때고, 그때 파생 축의 성질을 안 따졌습니다. 제 잘못입니다.**

**② 파생 축의 1위는 구조적으로 낮습니다 — balancer가 실측으로 증명했습니다.**
`max(a+b) ≤ max(a) + max(b)`이고, 리그가 전문가로 채워질수록 간격이 벌어져요.
리그 1위 골 22.0 + 도움 22.9 = 44.9인데 **1위 공격P가 32.7** — **12.2가 구조적 손실**입니다.
그 손실을 메우는 유일한 길이 겸업자를 늘리는 것인데, 그러면 🎯가 폭주합니다.
**📈를 세 축과 같은 눈금에 올리는 것은 다른 상을 부수지 않고는 불가능합니다.**

**③ 🔴 그리고 📈는 이미 「가장 약한 상」으로 설계돼 있었습니다.**

| 상 | 효과 (`game.js:545-562`) |
|---|---|
| 🥇 골든부츠 위너 | 골 **+15%** |
| 🎯 리그 최고 도우미 | 도움 **+15%** |
| 🛡️ 리그 최고 수비수 | 수비 **+15%** |
| 📈 **공격포인트왕** | 골·도움 **+6%** ← 셋의 **40%** |

**문턱이 낮고 효과가 약한 것은 서로 정합적입니다.**
📈만 효과가 40%인데 수상률까지 같기를 요구한 게 앞뒤가 안 맞았어요.

##### 새 기준 — **「수상률 × 효과」로 잽니다**

```
기대 이득 = 수상률 × 효과 크기

🥇  0.183 × 0.15 = 0.0275
🎯  0.176 × 0.15 = 0.0264
🛡️  0.181 × 0.15 = 0.0272
📈  0.356 × 0.06 = 0.0214     ← 넷 중 가장 낮습니다
                    최대 격차 22%
```

**📈 35.6%는 기대 이득 기준으로 이미 정합적입니다** — 오히려 셋보다 조금 낮아요.
제가 새로 만든 잣대가 아니라 **개념집의 칭호 효과표가 원래 그렇게 적혀 있던 것**입니다.

> ### G-7 재정의
> | | 옛 기준 | **새 기준** |
> |---|---|---|
> | 세 독립 축(🥇🎯🛡️) | 수상률 격차 ≤12%p | **그대로** (현재 0.7%p ✅) |
> | 넷 전체 | 수상률 격차 ≤12%p | 🔄 **「수상률 × 효과」 격차 ≤30%** (현재 22% ✅) |
> | 📈 단독 상한 | — | 🆕 **25 ~ 45%** — 45%를 넘으면 *"자동으로 받는 상"*이 됩니다 |
>
> **상한을 따로 두는 이유**: 기대 이득 기준만으로는 수상률이 60%가 돼도 통과합니다.
> `career.js:2240`의 *"좋은 시즌이면 닿고 평범하면 안 닿는"* 자리를 지키려면
> 절대 상한이 필요해요.

##### 바뀌는 코드 — **없습니다**

`ACE_POOL.assist`+`fw`(G-12에서 확정)와 `NPC_SPOT` 세 값은 그대로 갑니다.
**engineer가 다시 돌 필요가 없고, balancer는 「수상률 × 효과」를 한 번 더 계산하면 됩니다.**
축을 바꾸는 갈래(다)(라)를 피한 것이 그 비용을 아낍니다.

#### (9) 🏆 컵 무실점 — **뺍니다** (조건이 충족됐습니다)

> ⚠️ **초판에서 "셉니다"로 정했던 것을 뒤집습니다.** 제가 건 조건 —
> *"±15%를 넘으면 컵을 카드 엔진으로 옮기거나 그때까지 한 줄을 뺀다"* — 이 충족됐어요.
> 실측 **+60.5 ~ +198.5%**입니다.

engineer가 *"경쟁자에겐 컵이 없어 비대칭이 이제 주축에 직접 실린다"*고 되돌려 물었습니다.

**+198%는 "조금 어긋남"이 아니라 다른 게임입니다.** `cupFinalize`의 `act.cs` 한 줄을 뺍니다.

| # | 왜 뒤집나 |
|---|---|
| 1 | 🔴 **"네 축이 같은 자"보다 "컵과 리그가 같은 자"가 더 근본입니다.** 앞은 축 **사이**의 일관성이고 뒤는 **같은 축 안**의 일관성이에요 |
| 2 | 골·도움·차단은 이미 그 비대칭 위에서 곡선이 잡혔지만, **무실점은 지금 새로 세우는 축**입니다. 부채를 처음부터 안 지는 게 낫습니다 |
| 3 | **컵을 카드 엔진으로 옮기면 되살립니다** — §11 순서 1의 미완 항목이에요 |

⚠️ **화면에 분모(출전 수)를 함께 띄우지 마세요** — `apps`는 컵을 세는데 `cs`는 안 세니
*"12경기 중 무실점 4"*가 어긋납니다. `raceRank`는 값만 정렬하니 분모가 필요 없어요.

<details><summary>초판에 "센다"로 정했던 근거 (기록용)</summary>

| # | 왜 |
|---|---|
| 1 | 🔴 **컵은 이미 `act.goals`·`assists`·`defense`·`apps`를 전부 올립니다**(`career.js:2198-2206`). 무실점만 빼면 **철벽상 하나만 다른 규칙**이 되고, 그건 §2-8b (1)이 요구한 *"세 축이 같은 자"*를 정면으로 어깁니다 |
| 2 | 곡선이 **그 위에서 잡혔습니다** — 골든부츠 17%가 컵 골을 포함한 값이에요. 새 축만 빼면 **비교 자체가 안 됩니다** |
| 3 | engineer가 `cupFinalize`의 **기존 자리**에 넣은 것이 맞습니다 |

</details>

> ### ⚠️ 그런데 진짜 문제는 따로 있습니다 — 둘로 나눠 다룹니다
>
> **(가) 컵이 아직 v1 산식(`deriveOppGoals`)을 씁니다.** 카드 엔진이 아니라
> **컵 경기의 무실점 비율이 리그와 안 맞습니다.** 이건 §11 순서 1(*점진 확정 경기 엔진*)의
> **미완 항목**이에요 — 컵도 같은 엔진을 써야 합니다.
> **크기를 재고**(G-8) 리그와 크게 어긋나면 컵을 카드 엔진으로 옮기거나,
> 그때까지 컵 무실점을 뺍니다 (`cupFinalize`의 한 줄).
>
> **(나) 리그 부문상이 컵 기록을 봅니다.** `raceTop`이 `act`를 보는데 거기 컵 기록이
> 섞여 있어요 — *"리그 득점왕"*이 컵 골로 정해질 수 있습니다. **v1부터의 구조적 결함**이고
> 네 축을 다 건드리므로 **①-G 밖**입니다. §14-2에 올립니다.

#### (10) 🔒 게이트 ①-G를 **한 벌로** 넣습니다

**(3)·(6)·(7)·(8)·(9)와 계수 한 벌은 서로 물려 있어 따로 넣을 수 없습니다.**
한 줄을 고치면 세 자리가 함께 움직여요 — `autoMatch`만 고쳤을 때
골든부츠 17% → **1%**, 도움÷골 0.777 → **0.847**이었습니다.

**후보 D 한 벌** (balancer 실측 · `23_balancer_award-gate.md` §후보 D)

| 상수 | 현재 | **①-G 확정** |
|---|---|---|
| `autoMatch.oneWay`의 굴림 | `chance(pf * pc)` | 🔴 **`chance(pf)`** |
| `ASSIST_P2` | 0.75 | **0.48** |
| `NPC_SPOT` (당시 단일) | 7.00 | 2.90 → **최종 분할 3.05/6.50/4.90** |
| `NPC_SPOT_A` · `NPC_SPOT_D` | — | **7.00 그대로 — 분할 자체가 불필요해집니다** |
| `AXIS_OFF` | 2.00 | **2.10** |
| `POS_AXIS.n` | 1.128 / 0.957 / 0.815 / 0.816 | **fw 1.045 · wg 0.955 · mf 0.885 · df 0.813** |
| `ACE_POOL.goal` | `["fw"]` | **`["fw", "wg"]`** ← (8), 재측정 필요 |
| `cupFinalize`의 `act.cs` | 셈 | **뺌** ← (9) |

> ⚠️ **여기 적었던 「`NPC_SPOT` 분할 폐기」는 취소됐습니다** → (2) 참조.
> 그 판정은 **후보 D-1 이전 세계의 측정**이었어요. D-1이 클럽 간 무실점 격차를 좁히자
> `_D`가 철벽상에 닿기 시작합니다(`_D` 10 → 1에서 8% → 29%).
> **분할은 유지하고 셋을 따로 잡습니다** — `_G 2.80` · `_A 5.80` · `_D 4.50`.
>
> 살아남은 지적은 하나뿐입니다 — *"`NPC_SPOT_G 7.00`과 「부츠 17%」가 양립 불가"*.
> 그래서 G-2의 기준선을 **「넷이 서로 비슷할 것」**으로 옮겼고, 그건 그대로 갑니다.

### 2-9. 경쟁자도 같은 8칸 루프를 돕니다

**닫힌 형태 λ 근사는 폐기했습니다.** 리그 6팀 × 선발 11명 = **66명이 같은 루프**를 돕니다.

| | |
|---|---|
| 비용 | 라운드당 24회 · **시즌 912회.** 브라우저에서 사실상 0 |
| 어디에 | `leagueRound`(`career.js:812`)의 자리 — 이미 *"골은 굴리지 않아요"* 원칙을 지키는 곳 |
| 경쟁자의 미니게임 | **없습니다.** `succ(능력치, 0.5)` 고정 판정 |
| `FAT_ADJ` | **만들지 마세요.** 근사가 사라져 필요 없습니다 |

> **이래야 "나와 경쟁자의 유일한 차이는 미니게임을 실제로 하느냐"가 문자 그대로 참이 됩니다.**
> 닫힌 형태로는 참이 아니었어요 — 능력치가 높을수록 평균 −9.4% · 분산 0.66까지 어긋났습니다.

⚠️ **`_check.html` 시나리오 생성기의 동기 루프에서 멈추지 않게 주의하세요.**
`cup.js`가 같은 자리에서 데였습니다 (`setTimeout(…, 0)`이라도 이벤트 루프가 돌아야 이어져요).

### 2-10. 🔴 `splitMine` 계열을 v2가 **어디까지** 이어받나

**함수는 폐기하고 표만 가져옵니다.** 여기가 헷갈리면 라이브의 결함 두 개가 그대로 따라옵니다.

> ### 🔴 폐기되는 것은 **상수 이름이 아니라 형태**입니다
> 실제로 한 번 미끄러진 자리예요 — engineer가 `ACE_W` 폐기 근거를 **읽고도**
> `(isAce(i) ? spot : 1)`로 **승자독식 계단을 그대로 다시 만들었습니다.**
> 이름만 바꾸고 형태를 옮겨온 거예요. 그래서 inspector F3(계단 2.6배 · 이적하면 카드 1/3)이
> 났습니다.
>
> **아래 표의 「폐기하는 형태」 열이 본문입니다.** 상수 이름은 그 형태가 어디 있었는지를
> 가리키는 주소일 뿐이에요. **같은 형태를 다른 이름으로 되만들면 폐기가 아닙니다.**

| 현행 (`career.js:885~940`) | **폐기하는 형태** | v2가 쓰는 형태 | 왜 |
|---|---|---|---|
| `splitMine()` 함수 자체 | 사후 재분배 (팀 골을 나중에 나눔) | **장면마다 그 자리에서 주인공 지명** (STEP 3) | 점진 확정 |
| `MY_P` 1.2 | — (배수 자리는 유지) | `ME_P` 1.80 | 값만 재적합 |
| `ACE_W` 2.6 `iAmAce ? ACE_W : 1` | 🔴 **승자독식 계단** — 불리언 분기로 배수가 튐 | **계단 + `FLOOR_SHARE` 바닥** (§2-4) | 계단만 두면 능력치 5점에 2.6배가 뛰고 강팀 이적에 카드가 1/3이 됩니다 |
| **`MAKER_W` 3.0** | 🔴 **같은 승자독식 계단**(도움판) | `SPOT`/`NPC_SPOT`에 흡수 · `ACE_POOL`로 종류별 지명 | `ACE_W`의 쌍둥이라 **같은 형태**입니다 |
| **`(overall/70)^1.2`** | 🔴 **종합 하나로 재는 능력치 항** | **`blend`** — 포지션별 주 스탯 0.60 가중 | **결함 ①의 원인.** 종합만 봐서 패스가 도움에 안 닿습니다 |
| `GOAL_W` · `ASSIST_W` | — (형태 그대로 좋음) | **표 값만 승계** — STEP 3의 `posW` | 포지션 간 차이를 내는 유일한 항 |
| `ASSIST_P2` 0.75 | 전개 골에 곱하던 자리 | **값 승계 · 자리 이동** — **결정 장면**의 골에 (§2-5) | 전개 장면은 "만드는 장면"이라 도움이 항상 붙어요 |
| 자리 결(`slot.g/a/d`) | — | **승계** — 정규화 IIFE 통째로 | 평균 1이 자동 유지 |
| **`c.def`가 `splitMine`을 안 타던 것** | 🔴 **축 하나만 다른 경로로 빠지는 형태** | **해당 없음** — 셋 다 STEP 3을 지납니다 | 결함 ②는 v2에서 구조적으로 불가능 — 아래 |

> ### 폐기한 형태가 되살아났는지 보는 법 — **세 가지 냄새**
> 1. **불리언 분기로 곱해지는 배수** (`x ? A : 1`) → 계단입니다. 바닥이나 연속 배수가 필요해요
> 2. **종합(overall) 하나로 재는 능력치 항** → 그 포지션의 주 스탯이 안 닿습니다
> 3. **골·도움·수비 중 하나만 다른 경로로 계산** → 그 축만 눈금이 달라집니다
> 4. 🔴 **한 사건을 두 확률의 곱으로 정하는 것** (`chance(pf * pc)`) → **같은 장면을 두 번 셉니다.**
>    엔진 주석이 *"두 번 굴리면 골이 반으로 줄어요"*라고 경고해 뒀는데도 코드가 그러고
>    있었어요. 내 클럽만 다른 산식으로 실점하는 원인이었습니다 (§2-8b (7))
> 6. 🔴 **상들의 문턱이 서로의 격을 역전시키는 것** → 아래 상이 위 상보다 받기 어려워집니다.
>    🥈 베스트11 상한을 8.5로 올리면 130에서 **리그MVP와 역전**해요.
>    볼트의 `else if` 사고(*"대상을 받으면 본상을 건너뛰어 잘한 시즌이 상을 덜 받는다"*)와
>    **형태가 다르고 결과가 같습니다.** 상을 건드릴 때는 **격의 순서**를 함께 재세요 (§6-4)
> 5. 🔴 **단조가 아닌 값을 문턱 하나로 판정하는 것** → 곡선이 뒤집힌 U자인데
>    `ageMul < 0.75`만 보면 **오르막(만 19~22세)도 같이 걸립니다.**
>    `🕯️ 만 21세 · 은퇴를 생각해 볼 때입니다`가 나갔어요 (§6-4).
>    **문턱을 걸 때는 「어느 방향에서 온 값인가」를 함께 보세요.**
>
> inspector 검사 11·12·13c·22·30이 이 넷을 각각 변이 검증으로 잡습니다.

**inspector가 라이브에서 찾은 결함 둘이 v2에서 어떻게 되나**

| 결함 | 현행 | v2 |
|---|---|---|
| **① 패스가 리그 도움에 안 닿음** — 미드필더가 특화하면 축이 ×0.978 (**키울수록 손해**) | `c.a = split.a`인데 도움 몫 식에 패스 스탯이 없음 | ✅ **풀립니다** — `blend`의 S1이 미드필더는 패스(0.60 가중)라 전개 카드 주인공 확률에 직접 실립니다. **단 §2-5의 "내 도움 몫"을 STEP 3 비율로 계산할 때만** |
| **② 수비수 독식** — 능력치 110에서 차단 몫 56.8% vs 윙어 3.0% | `c.def`만 `splitMine`을 안 타서 수비수가 자기 축을 온전히 가져감 | ✅ **구조적으로 불가능** — 🧱 수비 카드가 **상대 공격 장면**에서 독립적으로 열리고, STEP 3에서 **11명이 같은 자로 경쟁**합니다. 실측 포지션 격차 **≤ 6.4%p** |

> **둘 다 "같은 자로 재지 않아서" 생긴 결함입니다.** v2는 골·도움·차단 셋 다
> STEP 3이라는 **하나의 무게 식**을 지나므로 이 형태가 재발할 수 없어요.

---

## 3. 확정 계수 전체 목록

**원본은 `21_balancer_revised-engine.md` §확정 계수 갱신입니다.** 여기 표와 어긋나면 그쪽이 맞아요.

### 3-1. 새 상수 (경기 엔진)

| 상수 | 값 | 어디에 걸리나 |
|---|---|---|
| `SCENE_ATK` | **0.72** | STEP 1 — 카드가 공/수 장면일 확률 |
| `BIG_BASE` | **0.45** | STEP 2 — 빅찬스 기본 확률 |
| `FAT` | **0.55** | STEP 3 무게 — `1/(1 + FAT × 내가 넣은 골)` |
| `URG` | **0.18** | STEP 2 `urgency` — 뒤지면 찬스가 늘어남 |
| `FIN` | **0.884** | STEP 4 자동 마무리 |
| `CON` | **1.111** | STEP 4 자동 실점. ⚠️ **`CON × defW`를 1로 clamp** |
| `ME_P` | **1.80** | STEP 3 — 내 무게에만 |
| `SPOT` | **4.00** | STEP 3 — 내가 클럽 에이스일 때 |
| `NPC_SPOT_G` | 7.00 → **3.05** (확정) | STEP 3 — 각 클럽 **골** 에이스에게 (내 클럽 포함) |
| `NPC_SPOT_A` | 7.00 → **6.50** (확정) | 각 클럽 **도움** 에이스에게 |
| `NPC_SPOT_D` | 7.00 → **4.90** (확정) | 각 클럽 **수비** 에이스에게. 🛡️ 철벽상의 **차단 타이브레이커**에 닿습니다 (§2-8b (2)) |
| `N_MIN` / `N_MAX` | **6 / 8** | 카드 수. `|스코어차| ≥ 3`이면 6장에서 끊음 |
| `COND_K` | **0.30** | `condMul = (1 − COND_K×c*/100) + COND_K×(cond/100)` |
| `c*` | **80** (잠정) | 시즌 평균 컨디션 기준점. §12 실측 ③에서 확정 |

> ### `SPOT 4.00` < `NPC_SPOT 7.00`이 이상해 보이지만 맞습니다
> 둘은 **독립 노브가 아닙니다.** `NPC_SPOT`은 **내 클럽 에이스에게도 걸려서**
> 나와 같은 8칸을 놓고 겨룹니다 — 올리면 내 골이 함께 깎여요.
> 닫힌 형태에서는 경쟁자가 다른 클럽에 있어 이 결합이 없었고, 그래서 `2.20`이 안 맞습니다.
>
> `SPOT`은 "**내가** 우리 클럽 안에서 얼마나 공을 많이 만지나",
> `NPC_SPOT`은 "**각 클럽의 에이스가** 그 클럽 안에서 얼마나 만지나"예요.
> 나는 그 위에 `ME_P 1.80`을 더 받고 **미니게임을 실제로 합니다.**
> 경쟁자는 고정 성공률이에요. **그 둘을 합쳐야 같은 자리에 섭니다.**
>
> 4.00 / 7.00에서 셋이 동시에 섭니다 — 내 130골 **18.7** · 경쟁자 1위 **19.2** ·
> 골든부츠가 walkover가 아님(110/130/150 → 22%/52%/78%).

### 3-2. 승계·재산정 상수

| 상수 | 현행(`soccer/`) | **v2** | 자리 |
|---|---|---|---|
| `AXIS_K` | 3.00 | **3.00 — 🔒 잠금** | `career.js:79` |
| `AXIS_OFF` | 4.8 | **2.35** (①-G) | `career.js:97`. 칭호 버프(§2-7b)가 **곡선의 허리**를 밀어 올려서요 — 리그MVP가 90~110에서 +4.8~7.9%p |
| `POS_AXIS.fw.n` | 0.94 | **1.072** (확정) | `career.js:74` |
| `POS_AXIS.wg.n` | 1.02 | **0.967** (확정) | `career.js:75` |
| `POS_AXIS.mf.n` | 0.86 | **0.873** (확정) | `career.js:76` |
| `POS_AXIS.df.n` | 0.87 | **0.794** (확정) | `career.js:77` |
| `ASSIST_P2` | 0.75 | **0.48** (①-G) | `career.js:930`. 결정 장면의 골에 도움자를 붙이는 확률 (§2-5) |
| `ACE_POOL.goal` | — | **`["fw", "wg"]`** (①-G) | §2-8b (8). 리그에 *"둘 다 하는 사람"*을 만듭니다 |
| `autoMatch.oneWay`의 굴림 | `chance(pf * pc)` | 🔴 **`chance(pf)`** (①-G) | `engine.js:745` · §2-8b (7) — **한 장면에 한 번** |
| `POS_AXIS`의 `g/a/d` | — | **그대로** | 재산정한 건 `n`뿐 |
| `RATE`의 `b` (fw/wg/mf/df) | 54/54/54/55 | **64/64/64/65** | `career.js:128` |
| `RATE`의 골·도움 값 | — | **그대로** | `b`만 옮기면 "골 +1.0" 눈금이 살아 있어요 |
| `GOAL_W` · `ASSIST_W` | — | **그대로 — STEP 3의 `posW`로 씁니다** | `career.js:929` · `939` |
| `ACE_W` 2.6 (골 에이스 계단) | — | **폐기 → `SPOT`/`NPC_SPOT`** | `career.js:935` |
| **`MAKER_W` 3.0 (도움 에이스 계단)** | — | 🔴 **폐기 → `SPOT`/`NPC_SPOT`에 흡수** | `career.js:940` · §2-10 |
| **`(overall/70)^1.2`** (`splitMine`의 능력치 항) | — | 🔴 **폐기 → `blend`** | `career.js:886-925` · §2-10 |
| `MOM_MIN` (신규) | 없음 | **8.40** (확정) | MOM 절대 문턱 · §2-8. **올라간 이유**: 7.6은 사실상 안 물려 능력치 110에서 MOM이 8.9%였어요(목표 6%) |
| `CLUTCH` (신규) | 없음 | **1.25** (확정) | 마지막 카드 · 1점 차 이내 · §2-3 |
| `FLOOR_SHARE` (신규) | 없음 | **0.12** (제안) | STEP 3 — **내 무게에만** 바닥 · §2-4 |
| `TRAIN_NEUTRAL` (신규) | 없음 | **조숙 1.226 · 보통 1.000 · 만성 0.839** (제안) | 🔒 **중립화 상수** — 훈련 계단이 만든 불균형을 0으로 되돌립니다. **트레이드오프 손잡이가 아닙니다.** 계단(1.1→1.0→0.7→0.45)을 만졌으면 반드시 재계산 · §7-1 ① |
| `agePen` | hype에서 차감 | 🔴 **폐기** | 곡선이 이미 나이를 셉니다 — 두 번 세는 자리 · §6-4 |
| `RETIRE_CURVE` | — | 0.75 → **0.78** (한 벌) | §6-4. **`나이 > 정점 나이` 가드와 함께**만 넣으세요 |
| `YOUTH_BONUS` (신규) | 없음 | **18세 +18% · 19세 +14% · 20세 +9% · 21세 +5%** (확정) | 선발 경쟁 무게에만. **나이의 일반 규칙 — NPC에게도 겁니다.** ⚠️ ×2.2로 올리지 않습니다 (§7-1 ④) |
| 🥈 베스트11 문턱 | `4.5 · (4.2, 6.2)` | **`5.8 · (5.5, 8.0)`** (확정) | **세 값 한 벌.** 🚫 상한 8.5 이상이면 130에서 리그MVP와 역전 · §6-4 |
| 카드 나이 | 17~19 | 🔴 **폐기 — 셋 다 만 17세** (데뷔 만 20세) | 시간은 다른 무엇으로도 못 삽니다 · §6-2 |
| `momAdj` | perfect +8 / miss −8 | 🔴 **0 — 얹지 않습니다** | §2-8 · 이중 계상 |
| `POS_SLOTS` + 정규화 IIFE | — | **통째로 옮기세요** | `squad.js:41-74` |
| `FOOT_FIT` | 0.04 | **0.10** | `squad.js:82` |
| `FORMATION` | `{fw:2,wg:2,mf:4,df:3}` | **그대로** | `squad.js:26` |

### 3-3. 🔒 `AXIS_K`를 건드리면 안 되는 이유

`AXIS_K`는 hype와 **승격 문턱 양쪽**에서 `ln(prestige)`에 곱합니다
(`career.js:79` · `career.js:2499`의 `inLeague = hype − AXIS_K × ln(prestige)`).

3.00 → 7.00으로 올리면 **PL 소속인 것만으로 hype가 +6.13**이 되어 상한 12의 절반이
리그 격으로 찹니다 — **성적이 아니라 소속이 상을 정하게 돼요.**

**inspector 검사로 굳힙니다**: 3.00에서 바뀌면 빨간불 + *"리그 격에 곱해집니다"* 이유 문구.

---

## 4. 미니게임 4종 · 카드 3종 매핑

전부 **`beta/winger-moment.js`** 전용 파일에. 인터페이스는 `timing.js`와 같게 —
`fn(container, opts, cb)` → `cb("perfect" | "ok" | "miss")`.

### 4-1. 4종

| # | 이름 | 형태 | 화면 | 하는 일 | 물음 |
|---|---|---|---|---|---|
| 1 | 🏃 **컷인 돌파** | 조준+타이밍 | 수비수 둘 사이 갭이 좌우로 열렸다 닫힘 | 열린 순간에 통과 | **언제** |
| 2 | 🥅 **1대1 마무리** | 조준 | 키퍼가 나와 각을 좁혀 옴 | 틈이 남았을 때 코스 선택 | **어디로** |
| 3 | 🎯 **킬패스** | 조준+타이밍 | 오프사이드 라인과 뛰어드는 동료 3명 | 라인 넘기 직전에 찌르기 | **언제+어디로** |
| 4 | 🧱 **차단** | **읽기** | 드리블러가 몸을 트는 방향 힌트. **바 없음** | 방향 3택 | **무엇을** |

### 4-2. 🚨 절대 규칙 — 읽기와 타이밍의 화면 문법을 섞지 않습니다

> **읽기 게임** = 방향 버튼 2~3개. **움직이는 바가 화면에 없어야 합니다.**
> **타이밍/조준 게임** = 움직이는 표적 + 누르기. **판단으로 이기는 요소가 없어야 합니다.**
>
> 🧊 볼카운트는 *"타이밍처럼 생겼는데 핵심이 안 누르기"*라 **세 번 고쳐도 죽었습니다.**
> 🎯 코스 공략은 *"노려서 꽂는 화면이고 실제로 노려서 꽂는 게임"*이라 살았어요.
>
> 🧱 차단은 `cup.js`의 승부차기(`shootout`) 화면 문법을 **그대로 복제**합니다 —
> 이 저장소에서 유일하게 성공한 축구 조작이고, 검증된 형태의 복제라 새 위험이 아닙니다.

**실질 신규는 3종**입니다 (차단은 `cup.js` 뼈대 재사용).

### 4-3. 카드 3종 매핑

| 미니게임 | ⚽ 결정 | 🅰️ 전개 | 🧱 수비 |
|---|---|---|---|
| 🏃 컷인 돌파 | wg · fw | wg | — |
| 🥅 1대1 마무리 | fw · wg | — | — |
| 🎯 킬패스 | — | mf · wg · fw | — |
| 🧱 차단 | — | — | df · mf(CDM) |

### 4-4. 난이도 균등 — 두 가지를 지켜야 합니다

```
① 4종의 perfect 비율이 같은 능력치·같은 조작 숙련도에서  ±5%p 안
② 같은 미니게임이 결정/전개 어느 쪽으로 열려도 성공률이 같을 것
```

②를 어기면 **카드 종류가 난이도 라벨이 됩니다** — 플레이어가 "전개일 때 더 쉽네"를
학습해요. ①을 어기면 그 포지션이 구조적으로 유리해집니다(세부 자리 평균 1과 같은 성격).

#### 🔑 조작 난이도를 만질 때 보는 유일한 값 — `E[s] ∈ [0.44, 0.58]`

| `E[s]` | 체감 능력치 (110 기준) | |
|---|---|---|
| 0.440 | 106.4 | **밴드 하한** — 아래로 가면 110·130이 목표 밑으로 |
| 0.500 | 110.0 | 🤖 자동 진행의 값 |
| **0.522** | **111.2** | **현재 실측** |
| 0.580 | 114.2 | **밴드 상한** |
| 0.655 | 118.7 | ❌ 창 밖 — 능력치 90이 25%를 넘습니다 |

**조작 실력 전체가 능력치 약 17점**입니다. 난이도를 바꾸면 이 하나만 다시 재세요.

> ### `cardP`가 `s`에 **정확히 선형**입니다 (기울기가 이론과 소수 셋째 자리까지 일치)
> 그래서 **`s`의 분포를 몰라도 되고 `E[s]` 하나가 곡선을 정합니다.**
> 4종의 난이도가 서로 달라도, 판정 창이 능력치·컨디션·주발로 흔들려도,
> **평균만 창 안에 있으면 곡선이 섭니다.** §2-6이 중심을 `autoP`로 넘긴 덕이에요.

#### 🤖 자동 진행 — **유불리가 아니라 실력 하한선입니다**

교차점이 정확히 `s = 0.5`입니다 (`succ(a, 0.5) = autoP`, §2-6).

| 내 조작 | 결과 |
|---|---|
| `s = 0.35` (평균 이하) | **자동이 +8.3점 유리** |
| `s = 0.50` | 정확히 같음 |
| `s = 0.522` (현재 실측) | 수동이 조금 유리 |

**못하는 사람을 받쳐주되 잘하는 사람을 막지 않습니다.**
현행 `🤖 경기 자동 진행`이 *"개입 1번마저 확률 굴림으로 대체"*하던 것과 성격이 달라요 —
그건 게임을 없애는 버튼이었고, 이건 **바닥**입니다.
director는 이걸 *"자동은 평균 실력만큼 해 줘요"*로 설명하면 됩니다.

#### 실제 체감 곡선 (수동 0.522 + 🎖️ 칭호)

```
5.6 / 22.2 / 58.8 / 89.2 / 98.7        목표 5 / 25 / 60 / 85 / 계속 오름
```

**110이 목표 60%에 거의 정확합니다. 미니게임이 곡선을 목표 쪽으로 밀어 줘요.**
`AXIS_OFF 2.35` 유지가 맞습니다.

### 4-5. 주발 · 신체가 여기서 말합니다

| | |
|---|---|
| 🦶 주발 | 1·2번에서 **주발 쪽 코스는 판정 창 +25%, 약발 쪽은 −25%** |
| 🫀 컨디션 | 판정 창에 `× condMul` (§2-7) |

#### ✅ 이중 경로 둘 — **의도입니다. 유지합니다** (2026-08-29 판정)

미니게임이 붙으면서 컨디션과 주발이 각각 두 자리에서 말하게 됐습니다.
**기본값에서는 중립이라**(`condMul(80) = 1.0000` · `footMul(약발 5) = 1.000`) 곡선은 안 움직여요.
문제는 기본값을 벗어났을 때 두 경로가 곱해지는 것입니다.

##### 🫀 컨디션 — `pBig` + 판정 창

> **§2-7에서 제가 `wMe`에서 뺀 것과 다른 자리입니다.**
>
> | 경로 | 무엇을 재나 | 겹치는 곳 |
> |---|---|---|
> | ~~STEP 3 `wMe`~~ | **11명 중 누가 주인공인가** | 🔴 감독의 **기용 결정**과 겹칩니다 — 컨디션이 낮으면 애초에 선발이 안 되거나 교체돼요. **그래서 뺐습니다** |
> | STEP 2 `pBig` | **기회가 오는가** | 겹치는 곳 없음 |
> | 판정 창 | **온 기회를 살리는가** | 겹치는 곳 없음 |
>
> 뒤의 둘은 **축구적으로 서로 다른 사건**입니다 — 지친 선수는 침투를 덜 하고(기회↓),
> 어쩌다 온 기회도 놓칩니다(성공률↓). 실제로도 그래요.
>
> **그리고 §2-7이 처음부터 "두 곳"이라고 적었습니다.** 판정 창은 새로 생긴 게 아니라
> 그때 예정해 둔 두 번째 자리예요. 중립 검증도 이미 **제곱** 기준입니다
> (실측 ①-A-6 · 검사 14: *"시즌 평균 컨디션에서 제곱이 1.000 ± 0.01"*).
>
> 컨디션 60에서 `0.94² = 0.884` — **−11.6%**. 의도한 크기입니다.

##### 🦶 주발 — 약발 배수(±6%) + 판정 창(±25%)

> **둘이 서로 다른 값을 봅니다.**
>
> | 경로 | 보는 값 | 무엇을 말하나 |
> |---|---|---|
> | ② `w(i)` 배수 `1 + (약발−5) × 0.012` | **약발** (2~7 수치) | *"약발이 좋으면 어느 발로든 기여한다"* — 상시 |
> | ③ 판정 창 ±25% | **주발** (좌/우) | *"주발 쪽 코스가 넓다"* — 그 순간의 유불리 |
>
> **다른 값을 보므로 이중 계상이 아닙니다.** 그리고 §8-2의 「셋 다 켠다」는
> 범민 님 확정 사항이고(`decisions.md` §2차 4번), 3중 중립 검증도 이미 걸려 있어요
> (실측 ⑤ 4-d: **최선/최악 격차 ±12%**).

> ### 🟠 다만 ③이 **약발을 안 보는 것**은 설계의 미완입니다 (2차)
> 지금은 약발이 7(양발)이어도 약발 쪽 코스가 −25%로 똑같이 좁습니다.
> §8-2가 약발 7을 **「양발」**이라고 부르는데 화면에서 그 말이 지켜지지 않아요.
>
> ```js
> // 2차 후보 — 페널티를 약발에 연동
> 약발 쪽 판정 창 = 1 − 0.25 × (1 − (약발 − 2) / 5)
> //   약발 2 → −25%  ·  약발 5 → −10%  ·  약발 7 → 0% (양발)
> ```
> 이러면 ②와 ③이 **하나의 이야기**가 되고, 「약발 훈련」(§8-2)의 보상이 눈에 보입니다.
> **계수가 움직여서 1차에는 안 넣습니다.**
| 키·체격 | **2차** — 신체가 카드 풀을 바꾸는 것은 미니게임이 4종인 지금은 분기할 곳이 없어요 |

---

## 5. 화면이 약속해도 되는 것 / 안 되는 것 (director)

### 5-1. 카드가 열릴 때 **무엇이 걸렸는지 첫 줄에 반드시 밝힙니다**

같은 미니게임이 두 종류로 열리므로(🏃 돌파는 결정에도 전개에도) 여기가 원칙 ①의 자리입니다.

```
⚽ 결정  "🔥 골문 앞! 이걸 넣으면 동점입니다"
🅰️ 전개  "🎯 동료가 뒷공간으로 뛰어요 — 찔러 주세요"
🧱 수비  "🧱 상대 에이스가 달려듭니다 — 막아야 해요"
```

**같은 조작인데 결과가 다른 것은 괜찮습니다.** 돌파에 성공하면 슛이거나 크로스인데
축구에서 둘 다 자연스러워요. **무엇이 걸렸는지 모르는 것이 문제입니다.**

### 5-2. ❌ "경기당 개입 2회"를 약속하지 마세요

**실측 빈도 (능력치별 · 경기당)**

| 능력치 | | ⚽ 결정 | 🅰️ 전개 | 🧱 수비 | **합** |
|---|---|---|---|---|---|
| **70** | fw | 0.39 | 0.17 | 0.16 | **0.72** |
| | wg | 0.28 | 0.32 | 0.19 | **0.78** |
| | mf | 0.19 | 0.40 | 0.29 | **0.88** |
| | df | 0.05 | 0.09 | 0.67 | **0.81** |
| **110** | fw | 0.61 | 0.34 | 0.33 | **1.28** |
| | wg | 0.47 | 0.59 | 0.38 | **1.44** |
| | mf | 0.34 | 0.70 | 0.59 | **1.63** |
| | df | 0.11 | 0.19 | 1.17 | **1.47** |
| **130** | fw | 0.69 | 0.44 | 0.43 | **1.56** |
| | wg | 0.55 | 0.71 | 0.49 | **1.75** |
| | mf | 0.41 | 0.83 | 0.74 | **1.98** |
| | df | 0.14 | 0.25 | 1.38 | **1.77** |
| **150** | fw | 0.75 | 0.53 | 0.53 | **1.82** |
| | wg | 0.62 | 0.81 | 0.60 | **2.04** |
| | mf | 0.48 | 0.93 | 0.89 | **2.30** |
| | df | 0.17 | 0.32 | 1.57 | **2.06** |

> **"2회"는 능력치 130 이상 미드필더에서만 닿습니다.** 신인 시즌(능력치 70)은
> **0.72~0.88회** — 경기 하나에 한 번도 안 올 수 있어요.
>
> **그래서 화면은 사전에 횟수를 약속하지 않고, 사후에 셉니다** —
> 경기 결과 화면에 `🔥 이 경기의 내 순간 2회 (성공 1)`처럼.
> 그리고 **같은 팀에서 능력치가 오르면 그 숫자가 늘어나는 것이 성장의 체감**이 됩니다
> (능력치 70 → 110에서 0.70 → 1.05).
>
> ⚠️ **강팀으로 이적하면 한동안 줄었다가 다시 늡니다.** 동료가 세지니 스포트라이트를
> 나눠 갖게 되거든요 — 축구적으로 맞고, `FLOOR_SHARE`(§2-4)가 바닥을 깔아 줍니다.
> **이걸 감추지 말고 서사로 쓰세요**: *"빅클럽에서 자리를 잡아가는 중"*.
> 이적의 보상은 조작량이 아니라 **리그 격(prestige) · 수당 · 우승 기회**입니다.
> 시즌 요약에도 `🔥 순간 카드 55회`를 넣으면 수직 성장이 눈에 보여요.

### 5-3. ❌ "이기고 있으면 수비 카드가 **더 열린다**"를 약속하지 마세요

**산식에 그 항이 없습니다.** `SCENE_ATK`가 스코어를 안 봐서 상대 공격 장면의 **수 자체는
스코어와 무관**합니다. 실제로 일어나는 것은 이것뿐이에요 —

| 상황 | 실제로 | 화면이 말해도 되는 것 |
|---|---|---|
| 뒤지고 있음 | `urgency`가 `pBig`를 올려 **결정 카드가 늘고 전개가 줄어듭니다** | ✅ "따라가야 합니다 — 기회가 더 옵니다" |
| 앞서고 있음 | `behind = 0`이라 결정 카드가 안 늘어납니다. **수비 카드의 비중이 상대적으로 오를 뿐** | ❌ "지켜야 하니 수비 상황이 늘어납니다" |
| 3점 차 이상 | 카드가 6장에서 끊깁니다 | ✅ "이미 승부가 기울었어요" |

**정확한 서술**: *"수비수·CDM은 스코어와 무관하게 경기 내내 일정하게 개입합니다"*.
이게 참이고, 12번 §1-4에서 제가 *"이기고 있을 때도 할 일이 있다 — 그때가 주무대"*라고
쓴 것은 **절반만 맞았습니다.** 앞부분(할 일이 있다)은 참이고 뒷부분(주무대)은 거짓이에요.

> **왜 산식을 고치지 않고 서술을 고치나** — `SCENE_ATK`에 스코어를 물리면
> 새 계수가 하나 늘고 **곡선을 다시 재야 합니다.** 지금 곡선은 다섯 점 전부 밴드 안이에요.
> 그리고 **값이 0인 배선은 요청하지 않은 미래 대비 코드**입니다.
> **2차 후보로 남깁니다** — 형태는 §12-3에 적어 뒀어요. 리드하는 팀이 내려앉는 건
> 축구적으로 자명해서 언젠가는 넣을 값어치가 있습니다.

### 5-4. 골 연출 (`fx.js` 배선만 이으면 됩니다)

**soccer의 `Fx` 호출 20곳 중 골에 붙은 게 하나도 없습니다.** 각성·초월·칭호·이적·우승에만
붙어 있어요. 배선만 이으면 되는 자리입니다.

| 사건 | 연출 |
|---|---|
| ⚽ 내 골 | 팀 컬러 풀스크린 플래시 + `body` 셰이크 0.3s + "GOAL" 스케일업 + `Fx.burst` |
| ⚽ 동료 골 | 플래시만 (약하게) |
| 😣 실점 | 회색 플래시 + 좌우 흔들림 (짧게) |
| 🏆 결승골 | 위 + `Fx.confetti` |

- **`prefers-reduced-motion` 존중은 축약 대상이 아닙니다.**
- 진동은 **골에만 40ms 한 번**. **소리는 넣지 않습니다** — 저장소 전체에 오디오 호출이
  0건이고, 무음 기대를 깨는 건 되돌리기 어려워요.

### 5-5. 경기 화면 구조

```
[상단 sticky 스코어보드]  0-0  ⏱ 12'      ← 시계가 카드마다 흐름
────────────────────────────────
 12'  🟨 상대 압박이 거세요                ← 카드는 아래에서 push-in
 27'  ⚽ 동료의 선제골!
 41'  😣 실점…  1-1
 ── 🥅 하프타임 ──  점유 48% · 슛 6 · 내 평점 6.8
 58'  🔥 [내 순간] …                       ← 미니게임
 90+3' 삐— 종료 휘슬
```

| | |
|---|---|
| 카드 간 딜레이 | 1점 차 이내 900ms · 3점 차 이상 350ms |
| 타이핑 연출 | **순간 카드만** (40~60ms/자). 전부 타이핑하면 지루해집니다 — 밀도 차이가 핵심 |
| 하프타임 | 45분에 요약 카드 1장 (점유·슛·내 평점 3개만) |
| 추가시간 | **카드는 안 늘어납니다** (`N_MAX 8` 고정). 1점 차 이내면 **마지막 카드가 `90+1~5분`으로 표시되고 `CLUTCH`가 걸립니다** (§2-3). 그 카드에 타이핑 연출을 붙이세요 |
| `⏩ 빨리감기` | **연출만 짧게. 개입은 유지**합니다 — 현행처럼 개입을 확률 굴림으로 대체하면 게임이 사라져요 |
| 접근성 | **판정 창 전역 확대 옵션(+30%). 성적 페널티 없음** — 접근성은 축약 대상이 아닙니다 |

---

## 6. 선수 설정

### 6-1. 입단 흐름

| 단계 | 무엇을 | 되돌릴 수 있나 |
|---|---|---|
| 1 | 🏫 유스 5택 | 못 바꿈 |
| 2 | 📍 포지션 4택 | **커리어 중 전환 가능** (§8-3) |
| 3 | 🦶 주발 | 못 바꿈 |
| 4 | 🌱 **유망주 카드 3택** (리롤 2회) | 못 바꿈 |
| 5 | 🎭 성향 3택 | 못 바꿈 (1차) |
| 6 | 👤 외모 (헤어·피부) | 언제든 |
| 7 | ✍️ 이름 8자 | 못 바꿈 |

**유스는 초기 스탯을 실제로 바꿉니다** — 유망주 카드 3장이 뽑히는 **풀의 분포**를
바꿔요. 브라질 풀에서는 드리블 편중 카드가, 이탈리아 풀에서는 수비 편중 카드가 자주 나옵니다.
(현행은 유스가 `growth`·`spot`·`home` 숫자 3개뿐이라 *"브라질 유스로 시작해도 드리블이
안 높습니다"*.)

### 6-2. 유망주 카드 3택

한 장이 담는 것 — **다섯 칸이 한 묶음이라 쪼개서 고를 수 없습니다.**

| 칸 | 화면에 |
|---|---|
| 📊 초기 배분 (6스탯) | 레이더로 공개. **세 장의 총합은 같고 분포만 다름** |
| 📈 성장타입 | **흐릿하게** — 스카우트 코멘트로만 |
| ⭐ 특능 1 | 공개 |
| ⚠️ **결함 1** | 공개 |
| ~~🎂 나이~~ | 🔴 **칸에서 뺍니다 — 셋 다 같은 나이입니다** (아래) |

**총합을 맞추는 이유**는 원칙 ④입니다. 세부 자리 가중 평균 1과 같은 원리예요.

#### 🔴 🎂 나이 칸을 **뺍니다 — 셋 다 만 17세** (2026-08-29 재판정)

##### ⓑ안은 능력치를 풀었지만 **시간을 못 풀었습니다**

카드 스탯을 「정점 기준값」으로 둔 것(ⓑ)은 **능력치 반대급부를 정확히 만들었습니다** —
곡선이 낮은 카드는 지금 약하게 보이고 시간이 지나면 뒤집힙니다.

**그런데 커리어 길이가 그대로 남았습니다.** 실측: 17세 **19시즌** / 19세 **17시즌**
→ 총 상 **42.65 / 37.90**, **12.5% 차**.

> *"시간이 지나면 같아져요"*는 **능력치를 같게 만들지 시즌 수를 같게 만들지 않습니다.**

##### 제가 §6-2 초판에서 이미 적어 뒀던 우려입니다

> *"나이는 「총합이 같다」를 맞추기 가장 어려운 칸입니다 —
> **시간은 다른 무엇으로도 못 사니까요.**"*
>
> **ⓑ를 고르면서 그 우려를 덮었습니다.** ⓑ는 능력치만 풀었어요.

##### 갈래 둘

| 갈래 | 판정 |
|---|---|
| (가) **데뷔 나이 고정**(20세) · 카드 나이 유지 | 🟡 편차 12.5% → **1.7%**. 다만 카드 나이가 곧 **유스 기간**이 되어(17세 → 3년 · 19세 → 1년) **유스 훈련 총량이 달라집니다.** 그걸 또 중립화해야 해요 — **문제를 옮기는 것**입니다 |
| **(나) 카드 나이를 뺀다 — 셋 다 같은 나이** | ✅ **고릅니다.** 편차 **0** |

> ### 왜 (나)인가
> 1. **시간은 다른 무엇으로도 못 삽니다.** (가)는 유스 기간이라는 **새 불균형**을 만들어요
> 2. **카드 칸 넷으로 충분합니다** — 초기 배분 · 성장타입 · 특능(12종) · 결함(8종).
>    조합이 이미 수백 가지예요
> 3. 🔑 **나이 다양성은 성장타입이 이미 만듭니다.** 🌳 만성 카드는 *"늦게 피는 선수"*,
>    🌱 조숙은 *"일찍 피는 선수"*예요. 나이 칸이 없어도
>    **"이 선수는 언제 전성기인가"**가 카드마다 다릅니다

##### 🔴 고정값은 **만 17세**입니다 — 18세로 두면 데뷔가 만 21세가 됩니다

유스가 3년(§7-4)이라 **카드 나이 + 3 = 데뷔 나이**예요.

| 카드 나이 | 데뷔 | |
|---|---|---|
| 18세 | **만 21세** | ❌ 실제 축구의 프로 데뷔(18~20세)보다 늦습니다. 그리고 **balancer의 커리어 측정이 전부 만 20세 기준**이라 값을 다시 재야 해요 |
| **17세** | **만 20세** | ✅ 아카데미 유망주의 나이대이고, 기존 측정이 그대로 섭니다 |

**커리어 길이**: 만 20세 데뷔 ~ 만 38세 = **19시즌**
(은퇴 제안을 받아들이면 조숙 **15** · 보통 **17** · 만성 **18**).

> ⚠️ **`38 − 20 = 18`이 아닙니다.** 만 20세부터 만 38세까지 **양끝을 다 뜁니다** —
> `mustRetire`가 `age >= 38`이고 그 판정은 **그 시즌을 치른 뒤 결산에서** 나거든요.
> 초판의 18은 제가 뺄셈으로 적은 값이고, **실행으로 세어 보니 19**였습니다.
> **`RETIRE_AGE`는 38 그대로입니다** — 37로 내리면 커리어를 실제로 한 시즌 줄이는
> 밸런스 변경이 되고, 통산 기록·전당 점수가 따라 움직여요.
> **틀린 건 코드가 아니라 제 계산입니다.**

##### ✅ ⓑ(정점 기준값 저장 + 곡선 곱)는 **유지합니다**

나이가 같아도 **성장타입이 다르면 그 나이의 곡선이 다릅니다.**
실측 레이더 합 **조숙 163 · 보통 136 · 만성 109 (1.50배)** — 정점 기준값은 셋 다 194입니다.

> **반대급부가 나이 칸에서 성장타입 칸으로 옮겨갈 뿐입니다. 그리고 더 깨끗해집니다** —
> **하나의 칸이 하나의 이야기**를 하니까요.
> `AGE_TRADE`는 여전히 필요 없습니다.

> ### 카드에 적힌 여섯 숫자는 **정점에서의 값**입니다. 레이더는 거기에 곡선을 곱해 그려요.
> ```
> 세이브에 들어가는 값 = 정점 기준값            ← 세 장의 총합이 같습니다
> 화면 레이더           = 정점 기준값 × 곡선(나이, 성장타입)
> ```
>
> **반대급부가 자동으로 생깁니다.** 세 장이 다 만 17세인데도 🌳 만성 카드는 곡선이 낮아
> **지금 실력이 약하게 보이고**, 🌱 조숙은 **0.84**라 강해 보여요.
> **지금 실력이 약하게 보이고**, 🌱 조숙은 강해 보여요 (실측 레이더 합 **조숙 163 ·
> 보통 136 · 만성 109 — 1.50배**, 정점 기준값은 셋 다 194로 같습니다).
> 시간이 지나면서 뒤집힙니다. `AGE_TRADE` 같은 새 손잡이가 **필요 없습니다.**
>
> **그리고 이건 새 규칙이 아닙니다** — §6-4가 *"NPC가 이미 쓰는 자를 플레이어에게 그대로
> 씁니다"*라고 적은 그 모델(`peak × ageCurve`)을 **카드에도 적용**하는 것뿐이에요.
> 원칙 ④(총합 동일)는 **정점 기준값에서** 깨끗하게 지켜집니다.
>
> **네 칸이 하나의 이야기가 됩니다** — 🌳 만성 카드는 **아주 약해 보이지만 정점이 가장
> 늦고 길고**, 🌱 조숙은 **지금 강해 보이지만 먼저 꺾입니다.**
>
> ⚠️ **director** — 레이더 옆에 **성장타입 힌트가 반드시 함께** 보여야 합니다.
> 안 그러면 *"왜 이 카드만 약하지?"*가 노이즈가 돼요 (원칙 ③).
> 힌트는 §6-2의 스카우트 코멘트가 그 자리입니다 —
> `기본기부터 차근히 쌓는 스타일이래요` 옆에 약한 레이더가 있으면 읽힙니다.

**성장타입을 흐릿하게 두는 이유**는 원칙 ⑧입니다. 전부 공개하면 만성이 늘 정답이 되고,
전부 감추면 순수 운이에요. 컴백 컨셉의 **소문 2종**과 같은 해법:

```
"또래보다 몸이 빨리 여물었다는 평이에요"   → 조숙 70% / 보통 30%
"기본기부터 차근히 쌓는 스타일이래요"      → 만성 70% / 보통 30%
"코치들 사이에 평이 갈려요"               → 셋 다 균등
```

**결함 8종 (제안 — balancer 실측 전)**

| 결함 | 내용 | 카드의 반대급부 |
|---|---|---|
| 🩹 유리몸 | 부상 확률 ×1.6 | 초기 배분 한 칸 높음 |
| 😤 욕심쟁이 | 도움 −10% · 골 +14% | (그 자체가 트레이드오프) |
| 🥶 새가슴 | 순간 카드 성공 −8%p | 훈련 상승폭 +10% |
| 🐢 굼벵이 | 스피드 성장 ×0.7 | 체력·수비 성장 ×1.15 |
| 🗣️ 독불장군 | 🤝 신뢰 상승 ×0.5 | 골 +8% |
| 🌧️ 원정 울렁증 | 원정 기여 −7% | 홈 기여 +7% |
| 📉 슬로 스타터 | 시즌 첫 8경기 −12% | 마지막 8경기 +12% |
| 🧊 냉정 | 감정 이벤트 선택지 1개 잠김 | 순간 카드 성공 +5%p |

> **결함은 "약한 카드"를 만드는 장치가 아닙니다.** 세 장의 기대 강도는 같아야 하고,
> 다른 건 **어떤 게임을 하게 되느냐**예요.

**리롤 2회** — 무제한이면 "타고난 것"이 무의미해집니다(현행 `game.js:463-466`은 무제한).
0회는 첫 화면에서 이탈을 불러요.

### 6-3. 성향 3택 — **능력치·기댓값 배수를 일절 건드리지 않습니다**

| 성향 | 시즌 목표 3개 | 칭호 문턱 | 순간 카드 분포 |
|---|---|---|---|
| 🔥 해결사 | 득점·결승골·순간 성공 | 득점 낮고 도움 높음 | ⚽ 결정 카드가 자주 |
| 🎼 지휘자 | 도움·공격포인트·평점 | 도움 낮고 득점 높음 | 🅰️ 전개 카드가 자주 |
| 🧱 심장 | 출전수·수비 성공·무실점 | 수비/평점 낮고 공격 높음 | 🧱 수비 카드가 자주 |

> ⚠️ **여기가 원칙 ④의 함정입니다.** 카드 분포를 바꾸면 기댓값이 움직여요.
> **성향 3종의 "순간 카드 1장당 기대 획득량"이 같아야** 하고,
> 칭호 문턱도 **한쪽을 −15% 하면 다른 쪽을 +15%** 해서 시즌 칭호 개수 기댓값이
> 같아야 합니다. 세부 자리에서 가중 평균 1을 안 맞춰 골든부츠 수상률이
> **43% → 60%**로 튄 사고가 정확히 이 자리입니다.

### 6-4. 나이 축 — NPC가 이미 쓰는 자를 그대로

`S.proYear` 1~15를 버리고 `S.age`로 갑니다.

| | 현행 | v2 |
|---|---|---|
| 시간 | `proYear` 1~15 | `age` 17~38 |
| 성장 | 1~4시즌만, 그 뒤 정체 | 나이곡선 × 성장타입. **정체 구간 없음** |
| 노쇠 | 11시즌부터 일률 `rand(0.6,1.8)` | 나이곡선이 내려가는 만큼 |
| 은퇴 | 15시즌 고정 | 🔄 **`ageMul < 0.78` + `나이 > 정점 나이`** + 2시즌 연속 출전 15경기 미만 → 은퇴 제안 |

`squad.js:131-146`의 `ageCurve`·`AGE_MIN/MAX/PEAK_AGE/DECLINE_K`를 그대로 씁니다.

> ### ✅ 은퇴 제안이 조숙에게만 걸리던 문제 — **곡선을 고쳐서 풉니다. 문턱 0.75는 그대로**
> 초판 곡선에서는 **0.75 밑으로 가는 게 조숙뿐**이었습니다(보통 38세에 0.758 · 만성 0.84).
> 보통·만성은 제안이 영영 안 뜨고 **전원 38세 강제 은퇴**로 끝났어요.
>
> **§7-1의 새 앵커에서는 셋 다 닿습니다** — 조숙 **34세** · 보통 **36세** · 만성 **37세**.
> 커리어 길이가 타입마다 다른 것이 **트레이드오프의 일부**입니다.
>
> ### 🔴 그런데 문턱만으로는 **오르막에서도 걸립니다** — 가드를 함께 넣습니다 (2026-08-29 승인)
> `ageMul < 0.75`는 정점 **앞**에서도 참입니다. 곡선이 뒤집힌 U자라 **양쪽 끝이 낮으니까요.**
> 실측: **🌳 만성이 만 19~22세에 은퇴 권유를 받습니다** —
> 화면에 `🕯️ 만 21세 · 은퇴를 생각해 볼 때입니다`가 나가요.
>
> **제가 「내리막」을 조건에 안 넣었습니다.** balancer가 값까지 냈고 **한 벌**입니다 —
> ```
> RETIRE_CURVE 0.75 → 0.78   +   ageOf(st) > peakAgeOf(st) 가드
> ```
> | 넣는 것 | 결과 |
> |---|---|
> | 가드만 | 만성이 **은퇴 제안을 영영 못 받습니다** (37세 곡선 0.760 > 0.75) |
> | 문턱만 0.78 | 오르막 제안이 **만 23세까지 넓어집니다** |
> | **둘 다** | ✅ 조숙 34 · 보통 36 · 만성 37 — 설계표와 정확히 일치 |
>
> **승인합니다. 따로 넣지 마세요 — 한 벌입니다.**
> `AGE_MAX 38` 강제 은퇴는 **안전망으로 유지**합니다.

#### ✅ `agePen` 폐기 — **승인합니다** (2026-08-29)

engineer 판단: *"`overall()`이 이미 곡선만큼 내려가니 `agePen`은 나이를 두 번 세는 자리."*
**맞습니다.**

```
나이 ↑ → 곡선 ↓ → overall ↓ → 생산량 ↓ → 축 ↓ → hype ↓     ← 이미 다 들어갑니다
                                          − agePen           ← 두 번째
```

> **v1에 `agePen`이 있던 이유**: v1은 `proYear` 기반이고 **곡선이 없었습니다** —
> 노쇠가 `rand(0.6, 1.8)` 일률 감소였어요(`career.js:2364`). 나이 효과를 hype에
> **직접 넣는 것 말고 길이 없었습니다.**
> **v2는 곡선이 그 일을 하므로 필요 없습니다.** §2-10의 「형태로 폐기」에 맞는 판정이에요.

##### ✅ 폐기 유지 — 다만 `agePen`이 **다른 것을 가리고 있었습니다** (2026-08-29)

폐기 후 만 34~38세: 리그MVP 0.1% → **57.5%** · 🥈 베스트11 5.5% → **99.6%**.

> ### 🔴 되살리면 **다시 가려집니다.** 가리는 것은 고치는 게 아닙니다.
> balancer가 진짜 자리를 짚었어요 — **🥈 베스트11이 유효 능력치 90에서 이미 99.9%로
> 포화**입니다. **나이와 무관하게** 그래요. `agePen`이 그걸 덮고 있었을 뿐입니다.
>
> **능력치 90에서 99.9%면 그건 상이 아닙니다.** 자동으로 받는 거예요.
> 그리고 베스트11은 시즌 칭호(`eff: {g:0.05, a:0.05, d:0.05}`)를 주니
> **모두가 항상 다는 버프**가 됩니다 — 칭호 체계의 뜻이 죽어요.
>
> **리그MVP 57.5%는 별개입니다.** 곡선이 다섯 점을 통과했으니 능력치 기준으로는 맞아요 —
> 34~38세에 유효 능력치가 그만큼 높으면 받는 게 맞습니다. **정점을 지나고도 강한 것이
> 만성의 값어치**니까요. 다만 크기는 봐야 합니다.

→ **`agePen`은 폐기 유지. 🥈 베스트11 문턱을 별도 항목으로.**
부문상 눈금(방금 0.7%p로 모은 것)과 **다른 축**이라 안전합니다.

##### ✅ 문턱 확정 — **세 값 한 벌** (2026-08-29 승인)

```
4.5 · (4.2, 6.2)   →   5.8 · (5.5, 8.0)
```

| | 결과 |
|---|---|
| 능력치 90 | 97.1% → **48.9%** ✅ |
| 칭호(110) | 99.9% → **76.4%** — 살아남습니다 |
| 곡선 되먹임 | **≤0.9%p** — `AXIS_OFF` 재조정 불필요 |

> ### 🚫 상한을 **8.5 이상으로 올리면 안 됩니다**
> 능력치 130에서 **🥈 베스트11(89.8%)이 👑 리그MVP(91.4%)와 역전**합니다.
> **볼트의 `else if` 사고와 결과가 같은 형태**예요 —
> *"대상을 받으면 본상 판정을 아예 건너뛰어서 **잘한 시즌이 오히려 상을 덜 받았다**"*
> ([[분석 — 순위 천장과 성장 곡선]], ⚾에서도 같은 모양을 고쳤습니다).
> 거기는 `else if`였고 여기는 **문턱 배치**입니다. 형태가 다르고 결과가 같아요.
>
> **하나만 넣으면 안 됩니다** — 하한만: 94.3%(거의 안 고쳐짐) · 상한만: 67.5%(절반만).
>
> 🔑 **5-h(노장)와 5-j는 같은 손잡이입니다.** 만 38세 베스트11 98.7%도 이 값이 내려요.
> **따로 손대지 마세요.**

#### ⚠️ 커리어가 15 → **19시즌**으로 길어집니다 — **의도입니다**

> 만 20세 데뷔 ~ 만 38세 강제 은퇴(양끝 포함 **19시즌**).
> 은퇴 제안을 받아들이면 조숙 **15** · 보통 **17** · 만성 **18**시즌이에요.
>
> **"오래 뛰는 것이 보상"이 맞습니다.** 나이 축을 도입한 게 §6-4의 핵심이고,
> §3-③이 진단한 *"5~10시즌이 통째로 정체 구간"*을 없앤 결과예요.
>
> **다만 명예의 전당은 `grow-hof-v1`으로 8종이 공유하는 표입니다.**
> MOM 때와 같은 자리라 확인이 필요해요.
>
> ✅ **전당 점수는 통산 골·도움이 아니라 「상」을 셉니다** —
> 발롱도르·리그MVP·베스트11·신인왕·우승·명성·MOM·리그격·월드컵
> (balancer가 §12-4 G-6에서 확인). 그러면 **§7-1의 곡선 면적을 맞추는 한
> 총 상 개수도 비슷해집니다** — 시즌이 길어도 실력이 낮은 구간이 늘어난 것이니까요.
>
> ⚠️ **그래도 v1과 대조해야 합니다.** v1은 15시즌인데 **5~10시즌이 정체**였고,
> v2는 21시즌인데 **정체가 없습니다.** 총 면적이 v2가 클 수 있어요.
> **실측 ④에 항목을 넣습니다** — v1·v2의 **커리어 총 상 개수**를 나란히.
> 크게 벌어지면 전당 점수의 **시즌 수 정규화**를 봐야 합니다.

---

## 7. 성장

### 7-1. 성장타입 3종 — 🔄 **재설계** (2026-08-29 판정 · engineer §2-②)

> ### 🔴 초판이 트레이드오프가 아니라 **우열**이었습니다
> 실측: 곡선 배수(20→37세) **조숙 0.628 · 보통 0.868 · 만성 1.001** ·
> 훈련 총량 **96.4 / 100 / 101.7**. **만성이 두 축 모두에서 이깁니다.**
>
> 제가 §B-2에 *"조숙일수록 빨리 오르고 빨리 꺾인다 — 좋은 타입이 없는 트레이드오프"*라고
> 적었는데, **지금 구조에서는 만성을 고르는 것 말고 선택이 없습니다.**
> 유망주 3택의 다섯 칸 중 하나가 죽는 자리예요.

#### ① 🔄 훈련 배수 — **폐기를 취소합니다. 「중립화 상수」로 되살립니다** (2026-08-29 재판정)

> ### 🔴 제 논리가 틀렸습니다
> 제가 *"둘이 상쇄되니 아무 일도 안 하는 계수"*라고 썼는데, engineer가 정확히 반박했습니다 —
> **"A와 B가 상쇄하면 A를 지운 결과는 0이 아니라 「상쇄되지 않은 B」입니다."**
>
> | 훈련 계단 총량 (20~37세) | 조숙 | 보통 | 만성 |
> |---|---|---|---|
> | **계단만 (= 배수를 지운 상태)** | **81.5** | 100 | **119.2** |
> | 옛 배수(1.18/0.86)를 얹으면 | 96.2 | 100 | 102.5 |
> | 정확한 중립화 값 | **×1.226** | ×1.000 | **×0.839** |
>
> **옛 배수는 계단의 불균형을 지우는 일을 하고 있었습니다.** 지우니
> **만성이 훈련을 46% 더 받는** 게 드러났어요 — 제가 없애려던 "좋은 타입"이
> 다른 축에서 되살아난 겁니다.
>
> 초판 실측을 다시 보면 훈련 총량이 **96.4 / 100 / 101.7**로 이미 거의 중립이었습니다.
> **초판의 문제는 곡선(0.628 vs 1.001)뿐이었고, 훈련은 멀쩡했어요. 멀쩡한 걸 지웠습니다.**

**계단을 정점 기준으로 두는 것은 그대로 옳습니다** — 절대 나이 기준으로 바꾸면
*"성장타입이 훈련 효율의 시점을 옮긴다"*는 뜻이 죽어요. 조숙은 **일찍 잘 크고 일찍
둔해지는 것**이 정의니까요. **문제는 그 계단이 총량에서 중립이 아니라는 것**입니다.

##### 갈래 셋 중 — **② 「배수를 중립화 값으로 재정의」**

| 갈래 | 판정 |
|---|---|
| ① 계단을 타입별로 중립화 | ❌ 계단 표가 **3배**로 늘어납니다. 값이 흩어져 다음 사람이 못 찾아요 |
| **② 배수를 중립화 값으로** | ✅ **계단은 하나, 상수 셋.** 값이 한자리에 모입니다 |
| ③ 곡선으로 상쇄 | ❌ 🔴 **곡선은 곡선의 일(면적 중립)을 해야 합니다.** 훈련 불균형을 곡선으로 메우면 둘이 얽혀 **어느 쪽을 만져도 다른 쪽이 깨집니다** |

```
TRAIN_NEUTRAL = { early: 1.226, normal: 1.000, late: 0.839 }   (제안 — balancer 실측 전)
```

> ### 🔒 이름이 중요합니다 — **「훈련 상승폭」이 아니라 「훈련 계단 보정」입니다**
> 이 값은 **트레이드오프 손잡이가 아니라 중립화 상수**예요.
> balancer가 자유롭게 조정하는 값이 아니라, **계단(1.1 → 1.0 → 0.7 → 0.45)이 바뀌면
> 따라서 재계산되는 종속값**입니다.
>
> **다음 사람이 "만성을 약하게 하려고 ×0.7로" 같은 걸 하면 안 됩니다** —
> 그건 계단 중립을 깨는 것이고, 트레이드오프는 **곡선**이 맡습니다.
> 계단을 만졌으면 **이 셋을 반드시 다시 계산**하세요 (검사 38).

**트레이드오프는 여전히 곡선 하나가 맡습니다.** 훈련 보정은 트레이드오프가 아니라
**계단이 만든 불균형을 0으로 되돌리는 일**이에요 — 세부 자리 가중 평균 1과 같은 성격입니다.

#### ② 곡선 앵커 — **정점을 옮길 때 양끝을 함께 옮깁니다** (제안 — balancer 실측 전)

초판의 잘못은 **정점 나이만 옮기고 양끝을 그대로 둔 것**입니다.
그러면 만성은 상승기가 길고 조숙은 하락기가 길어져 **면적이 통째로 벌어집니다.**

| 타입 | 18 | 22 | 26 | 30 | 34 | 38 | 정점 | 0.75 도달 |
|---|---|---|---|---|---|---|---|---|
| 🌱 **조숙** | **0.84** | **1.00** | 0.97 | 0.88 | 0.76 | 0.62 | 22 | **34세** |
| 🌿 **보통** | 0.70 | 0.88 | **1.00** | 0.93 | 0.82 | 0.70 | 26 | **36세** |
| 🌳 **만성** | **0.56** | 0.74 | 0.90 | **1.00** | 0.88 | 0.72 | 30 | **37세** |

**조숙은 짧고 높게, 만성은 길고 낮게.**

| 타입 | 얻는 것 | 잃는 것 |
|---|---|---|
| 🌱 조숙 | **20대 초반에 이미 최고** — 신인상·조기 이적·긴 전성기 시작 | 30대 초반에 꺾이고 **커리어가 가장 짧습니다**(34세 제안) |
| 🌳 만성 | **30대 중후반까지** — 커리어가 가장 깁니다(37세 제안) | **10대 후반~20대 초반이 아주 약합니다**(18세 0.56). **초반 시즌을 버텨야** 해요 |

#### ④ 🔴 "버틴다"와 "못 뛴다"는 다릅니다 — `YOUTH_BONUS` (2026-08-29 판정)

실측: 🌳 만성 신인이 지금 실력 47.0 vs 클럽 전력 62 →
**출전 9.8/38 · 출전<15가 75.9%.** 그리고 그게 곧 은퇴 권유로 이어집니다.

> ### 이건 트레이드오프가 아니라 **벌**입니다
> 설계는 *"초반 성적이 낮다"*였는데 게임은 **"경기를 안 준다"**로 처리했어요.
> 그리고 더 나쁜 건 **복리**입니다 — 못 뛰면 실전 성장이 없고, 성장이 없으면 다음 시즌도
> 못 뜁니다. **만성 카드를 고른 플레이어가 첫 두세 시즌을 통째로 잃습니다.**

| 갈래 | 판정 |
|---|---|
| (가) 데뷔 리그를 낮춘다 (만성만) | 🟡 자연스럽지만 **만성에게만 주는 특혜**라 원칙 ④ 검증이 늘어납니다. **2차 후보** |
| **(나) 선발 문턱을 나이로 완화** | ✅ **고릅니다** — 아래 |
| (다) 곡선 10대 후반 값을 올린다 | ❌ **면적 중립(1.40%)이 깨집니다.** 마지막 수단 |

```
YOUTH_BONUS(age) — 선발 경쟁 무게에만 (제안 — balancer 실측 전)
  18세 +18% · 19세 +14% · 20세 +9% · 21세 +5% · 22세 이상 0
```

> ### 왜 (나)인가
> 1. **축구적으로 맞습니다.** 유망주는 실력이 조금 부족해도 출전 시간을 받아요 —
>    *"경험을 쌓게 한다"*는 실제 축구의 상식입니다
> 2. **타입이 아니라 나이의 일반 규칙**입니다. 만성만 받는 특혜가 아니라
>    **모든 어린 선수**에게 적용돼요 — 원칙 ④에 안 걸립니다
> 3. **곡선을 안 건드립니다** — 면적 중립이 지켜집니다
> 4. **화면에 이미 있는 개념**이에요 — `squad.js`가 `🌱 21세 이하`를 표시하고 있는데
>    **배정에는 안 쓰고 있었습니다.** 보이는 것과 하는 일을 맞추는 자리예요
>
> 🔒 **이건 어린 선수를 세게 만드는 게 아니라 기회를 주는 것입니다.**
> 생산량은 곡선이 정하니, 못하는 어린 선수가 뛰면 **성적은 나쁩니다.**
> 다만 **경험(실전 성장)은 쌓여요.** 그게 "버틴다"의 뜻입니다.
>
> ⚠️ **NPC에게도 똑같이 겁니다.** 안 그러면 *"나와 경쟁자가 다른 자"*예요.
> 경쟁이 함께 세지니 **내 이득이 얼마나 남는지 재야 합니다** (실측 ④ 5-i).
> (실측: 남는 이득 **68~69%** ✅ · 조숙은 이미 천장이라 아무 일도 안 합니다 — 의도대로예요)

##### 🔴 신인 출전 목표를 **정정합니다** — 15경기는 애초에 다른 선의 값이었습니다

실측 (`rollLineup` 70시즌 × 38R): ⚔️ 공격수 **12.8** · 🏃 윙어 22.2 ·
🎯 미드필더 29.9 · 🛡️ 수비수 28.8. **공격수만 15에 못 미칩니다.**

> ### 🔑 그런데 15경기는 **신인 출전 목표가 아니었습니다.**
> 그 값이 원래 있던 자리는 둘뿐입니다 —
> **§6-2 잠재력 안개**(*"그 시즌 15경기 이상일 때만 폭이 좁혀짐"*)와
> **§6-4 은퇴 조건**(*"2시즌 연속 출전 15경기 미만"*).
> 둘 다 *"안 뛰면 스카우트가 못 본다 / 몸이 예전 같지 않다"*를 재는 선이에요.
>
> **제가 실측 5-i의 기준으로 그 값을 빌려 쓴 게 잘못입니다.** 신인이 첫 시즌부터
> 주전의 40%를 뛰어야 한다는 근거가 어디에도 없어요.

```
신인 데뷔 시즌 출전 목표 = 38라운드의 30% 이상 (11.4경기)   ← "로테이션 자원으로는 뛴다"
```

**전 포지션 통과**: 공격수 12.8 ✅ · 윙어 22.2 ✅ · 미드필더 29.9 ✅ · 수비수 28.8 ✅

> ### ⚔️ 공격수가 가장 낮은 것은 **의도입니다**
> 38라운드의 **34%** — 리그 세 경기 중 한 번은 뜁니다. **"못 뛴다"가 아니라 "버틴다"예요.**
> 만성 신인의 9.8(**75.9%가 15 미만**)과는 성격이 다릅니다.
>
> §8-1이 다룬 *"공격수·윙어가 2자리를 놓고 경쟁하는데 탈출구가 없다"*와 같은 뿌리이고,
> 그 탈출구는 **포지션 전환**(§8-3)입니다. 신인이 아직 전환할 게 없는 건 맞지만,
> **신인이 바로 주전 스트라이커가 되는 게 오히려 이상합니다.**
> 주전 뒤에서 로테이션으로 출전하는 것 — 축구적으로 정확해요.
>
> 갈래를 따져 본 것:
> | 갈래 | 판정 |
> |---|---|
> | `YOUTH_BONUS`를 포지션별로 | ❌ *"나이의 일반 규칙"*이라는 성격이 깨집니다. 그게 이 손잡이를 고른 이유였어요 |
> | 신인 데뷔 클럽을 낮춘다 | 🟡 축구적으로 맞고 일반 규칙이지만 **커리어 전체를 옮깁니다** — 곡선을 다시 재야 해요. **2차 후보** |
> | 목표를 포지션별로 다르게 | ❌ 15는 **안개·은퇴의 선**이라 포지션마다 다르면 그 둘도 갈라집니다. 화면 문구도 복잡해져요 |
> | **목표 자체를 고친다** | ✅ **고릅니다** — 빌려 쓴 값을 제자리로 돌려놓는 것입니다 |
>
##### 🔑 목표는 **평균**입니다 — 분포의 꼬리는 남깁니다 (2026-08-29 확정)

실측: ⚔️ 만성 공격수 **평균 12.3 ± 0.1** ✅.
**다만 전력 62 클럽에서는 8.0경기, 그 칸의 47%가 목표 아래**입니다.

> ### 그 칸은 **세 불리함이 겹친 최악의 조합**입니다
> 🌳 만성(데뷔 곡선 최저) + ⚔️ 공격수(경쟁률 2.0으로 최고) + **강한 데뷔 클럽**.
> **그런 칸이 존재하는 게 정상입니다.** 카드를 고를 때 감수하는 것이고,
> 그게 §7-1 ②의 트레이드오프예요.
>
> **원래 문제였던 복리 고리는 끊겼습니다** —
> *못 뜀 → 실전 성장 없음 → 다음 시즌도 못 뜀 → **은퇴 권유***.
> 마지막 고리를 **정점 가드**(§6-4)가 막았고, 8경기라도 실전 성장은 쌓입니다.
> 처음 진단했던 9.8/38 + **75.9%가 미달** + 은퇴 권유와는 성격이 다릅니다.

##### ❌ `YOUTH_BONUS ×2.2`로 올리지 않습니다 — **손잡이가 축에 안 맞습니다**

| 왜 | |
|---|---|
| 🔴 **다른 축을 다른 축으로 고치는 것** | `YOUTH_BONUS`는 **나이** 손잡이인데 문제는 **포지션 경쟁률**입니다. §2-8b (2)에서 `NPC_SPOT_D`가 무실점(클럽 기록)에 안 닿았던 것과 **같은 자리**예요 |
| 🔴 **이미 통과한 셋을 밀어 올립니다** | 손잡이가 전 포지션에 걸려서 공격수를 올리면 **미드필더가 21.5 → 26**이 됩니다 |
| 🔴 **`20` 칸만 올리는 건 특혜 형태** | 데뷔가 만 20세라 **`20`은 나에게만, 18·19·21은 NPC에게만** 걸립니다. 효과의 대부분이 *"내가 세지는 것"*이 아니라 **"경쟁이 안 세지는 것"**이에요 — engineer가 피했고 **검사 42b가 지키는 자리**입니다 |
| 🟡 **리그가 약해집니다** | 21세 이하 20% → 24% · 선발−명단 +3.78 → +3.38. **다섯 점을 다시 재야 해요** |

**경쟁률과 출전이 단조입니다** — fw 2.0 → 12.3 · wg 1.5 → 17.4 · df 1.33 → 19.8 · mf 1.25 → 21.5.
**공격수가 가장 낮은 건 자리가 둘이기 때문**이고, 그건 `FORMATION`의 성질이에요.

##### 진짜 손잡이가 있는 자리 (2차)

| 손잡이 | 언제 |
|---|---|
| **데뷔 클럽 전력을 낮춘다** (전 타입) | 커리어 전체를 옮기니 곡선 재측정과 함께 |
| **포지션 전환** (§8-3) | 3시즌 뒤 열립니다 — 신인은 못 쓰지만 **탈출구가 있긴 합니다** |
| `FORMATION`의 fw 자리 수 | **20팀 38라운드 개편**(§12-3)과 함께 |

> ⚠️ **하한은 박습니다** — 최악 조합이 **7경기 아래로 내려가면** 그건 다른 문제입니다.
> 8.0이 6.0이 되면 *"버틴다"*가 아니라 *"안 준다"*예요 (검사 41b).

> ⚠️ **남는 것 하나**: 공격수 신인은 첫 시즌에 15를 못 넘어 **잠재력 안개가 한 시즌 늦게
> 좁혀집니다.** 작은 페널티이고 **자리 경쟁이 치열한 포지션의 성격**으로 남깁니다.
>
> ✅ **은퇴 오작동은 이미 막혀 있습니다** — `나이 > 정점 나이` 가드가 있어
> 22세 공격수는 2시즌 연속 15 미만이어도 은퇴 제안을 안 받아요.

#### ③ 🔄 면적 중립은 **「강제 은퇴 나이(38세)까지」**로 잽니다 (2026-08-29 재판정)

engineer 실측이 원인을 갈라 줬습니다 —

| 기준 | 조숙 | 보통 | 만성 | 벌어짐 |
|---|---|---|---|---|
| **은퇴 제안을 받아들이면** | 89.4 | 100 | 102.5 | **13.7%** ❌ |
| **38세까지 뛰면** | — | — | — | **2.8%** ✅ |

**앵커 자체는 거의 완벽하게 중립입니다.** 격차의 거의 전부가
**은퇴 제안이 조숙을 3시즌 일찍 끊는 데서** 나와요.

> ### 🔑 은퇴 제안은 **강제가 아니라 정보**입니다. 그러니 중립 대상이 아닙니다.
> §6-4가 *"은퇴 **제안**"*이라고 적었고 engineer도 *"제안은 강제가 아니에요"*로 구현했습니다.
> **받아들일지는 플레이어가 정합니다.**
>
> **선택의 결과를 중립 대상으로 삼으면 안 됩니다** — 그건
> *"잘 고른 사람과 못 고른 사람이 같아야 한다"*가 되니까요. 원칙 ⑥의 반대 형태예요.
> 34세 조숙 선수가 거절하고 계속 뛰는 건 **실제 축구의 노장**입니다.
> 성적은 떨어지지만 통산은 쌓여요.
>
> **그래서 곡선 앵커는 고치지 않습니다.** 2.8%면 목표(±8%) 안입니다.
>
> ⚠️ **director** — 은퇴 제안 화면이 **"끝"처럼 보이면 안 됩니다.**
> *"이제 몸이 예전 같지 않아요 — 그래도 계속 뛰실래요?"* 처럼,
> **거절이 정상 선택지**로 읽혀야 해요. 한쪽 버튼만 강조하지 마세요.

> ⚠️ **중립 검증은 「곡선 배수」가 아니라 「커리어 총 면적」입니다** —
> **데뷔~38세**의 곡선 적분이에요. 조숙은 앞이 높고 만성은 뒤가 높습니다.
> **은퇴 제안 나이까지로 재면 안 됩니다** — 그건 플레이어의 선택이 섞인 값이에요.

**고정 라벨이 아닌 것은 그대로입니다** — 3시즌 연속 평균 평점 7.0↑이면
**정점 나이 +1** (커리어당 최대 +2).

### 7-2. 특능 — 수평 성장 축

| 티어 | 1차 | 획득 | 한도 |
|---|---|---|---|
| 일반 | **12종** | 조건 달성 자동 해금 · 각성 시도 · 유망주 카드 | 보유 무제한 / **장착 6칸** |
| ⭐ 금특 | **2차** | 이벤트 체인 완주 + 마지막 선택지 분기 | 커리어당 3개 |

**장착 6칸**이 시즌 시작마다 "이번 시즌은 뭘 켤까"라는 결정을 만듭니다.
보유가 무제한이면 후반에 전부 켜져서 파워크립이 나요.

**효과의 절반 이상을 조건부로 둡니다** (원칙 ⑦ — 몰빵 메타에 반대 압력):

```
❌ 「결정력」 골 +8%                          ← 몰빵을 더 세게 만듦
✅ 「늦은 시간의 사나이」 70분 이후 골 +22%
✅ 「추격자」 2점 이상 뒤질 때 모든 기여 +18%
✅ 「빅게임 플레이어」 컵·월드컵에서 순간 카드 +12%p
```

조건부면 **고르게 키운 쪽이 조건을 더 자주 만족합니다.**

### 7-3. 6스탯 등급 F~S — 구간을 불균등하게

내부 수치는 연속으로 두고 **표시만** 등급으로. (제안 — balancer 실측 전)

| 등급 | 구간 |
|---|---|
| S | 128↑ — **상위 3%. "S를 봤다"가 사건이 되도록** |
| A | 108~127 |
| B | 90~107 |
| C | 74~89 |
| D | 58~73 |
| E | 40~57 |
| F | ~39 |

**승급 카드**: `⚽ 슛 C → B 승급!`을 훈련 결과에서 끊어 보여줍니다.
🦶 약발도 같은 문법으로 (`🦶 약발 5 → 6 승급!`).

### 7-4. 성장 경로

| 경로 | v2 |
|---|---|
| 유스 훈련 · 프로 훈련 · 벤치 주 성장 · 1년 특훈 | 유지 |
| 실전 성장 | 유지 · **강화** — 순간 카드에서 한 일이 무게 |
| 시즌 결산 자동 성장/노쇠 | **나이곡선으로 대체** |
| 각성 / 초월 | **각성 시도로 재설계** — 남은 훈련 포인트를 도박성 교환에 (시즌당 2회) |
| — | **신규: 특능 해금** |

---

## 8. 포지션 · 주발 · 전환

### 8-1. 윙어를 **깊게** 합니다 (세게가 아니라)

이름이 "더 윙어 II"인데 윙어가 4개 중 하나입니다. **현행과 같은 구조라 새 문제는
아니지만**, 현행에서 **윙어가 가장 얕은 게 문제**예요 — 자리가 LW/RW 둘뿐인데
미드필더는 넷, 수비수는 셋입니다.

| | 윙어 | 다른 셋 |
|---|---|---|
| **세부 자리** | **4개** — LW · RW · **인버티드 LW** · **인버티드 RW** | 각 +1 (fw 폴스나인 / mf 레지스타 / df 리베로) |
| 전용 미니게임 | 🏃 컷인 돌파 | 각 1종 |
| 자리 전용 금특 (2차) | 2종 | 각 1종 |
| **기대 산출 · 자리 가중 평균** | **같음 · 1** | **같음 · 1** |

**인버티드 자리가 주발과 맞물립니다** — 반대발 윙어(왼발잡이가 RW)는 **골 ↑ 도움 ↓**,
전통 윙어는 **도움 ↑ 골 ↓**. 넷의 평균은 여전히 1이에요.

⚠️ 새 자리를 `POS_SLOTS`에 넣으면 **정규화 IIFE(`squad.js:41-74`)가 자동으로 다시
정규화**합니다. 자리표만 넣으면 평균 1이 지켜져요.

### 8-2. 🦶 주발 — 셋 다 켭니다

| | 무엇 | 값 |
|---|---|---|
| ① 자리 가중 | 왼발잡이가 왼쪽 자리를 받을 확률 | `FOOT_FIT` 0.04 → **0.10** |
| ② 골·도움 배수 | 약발이 높으면 기여가 큼 | `1 + (약발 − 5) × 0.012` |
| ③ 판정 창 | 주발 쪽 코스가 넓음 | 주발 **+25%** / 약발 **−25%** |

**②는 화면에 안 보이는 효과입니다. 그래서 약발을 화면의 1급 시민으로 올립니다** (원칙 ③):

| 어디 | 무엇 |
|---|---|
| 능력치 화면 | 6스탯 아래에 `🦶 약발 6 — 좋음`. **F~S와 같은 라벨 문법** |
| 그 옆 | **효과를 문장으로** — `골·도움 +1.2%`. ⚠️ 퍼센트는 문자열로 박지 말고 계수에서 뽑으세요 |
| 순간 카드 | 약발 쪽 코스로 성공하면 `🦶 약발로 마무리!` 피드 한 줄 |
| 훈련 결과 | `🦶 약발 5 → 6 승급!` 카드 |

**약발 라벨**: 2 못 씀 / 3 서툼 / 4 약간 / 5 보통 / 6 좋음 / 7 **양발**

**손잡이 — 「약발 훈련」을 1차에 넣습니다.** ⚠️ **훈련 총량은 늘리지 않아요** —
새 슬롯이 아니라 **기존 훈련 슬롯 안의 선택지 하나**입니다. 총량이 늘면 성장 곡선이
통째로 움직입니다(백로그의 판단 그대로).

**3중 중립 검증** (§12 실측 4): 약발 5 · 가운데 자리 · 좌우 균등에서 **정확히 1.000**.
최선/최악 격차 **±12% 안**.

### 8-3. 포지션 전환

| | |
|---|---|
| 언제 | **시즌 사이. 감독 제안 이벤트로만** — 내가 신청할 수 없어요 |
| 조건 ⓐ | 프로 3시즌 이상 |
| 조건 ⓑ | 목표 포지션 주스탯이 현 주스탯의 **80% 이상** |
| 조건 ⓒ | **직전 시즌 선발률 60% 미만** |
| 인접만 | `fw ↔ wg` · `wg ↔ mf` · `mf ↔ df`. **`fw ↔ df` 불가** |
| 대가 | 첫 시즌 평점 −0.3 · 새 주스탯 재능 +0.15 · 옛 주스탯 재능 −0.10 |
| 한도 | 커리어당 **2회** |

**조건 ⓒ가 핵심입니다.** 전환은 *"공격수·윙어가 2자리를 놓고 경쟁하는데 탈출구가 없다"*를
푸는 장치예요. 주전에게까지 열면 **최적화 수단**이 되고, 그러면 전원이 전환합니다.

**중립 검증이 조건부입니다** (§12 실측 6):
선발률 60% 미만에서 전환 → **+8% 안팎** · 주전(85%↑)에서 전환 → **−10% 안팎**.

---

## 9. 저장 데이터

### 9-1. 새 필드와 읽는 쪽 기본값

| 필드 | 형 | 기본값 | 무엇 |
|---|---|---|---|
| `S.age` | number | `18` | `proYear` 대체 |
| `S.growthType` | string | `"normal"` | `"early"` / `"normal"` / `"late"` |
| `S.peakShift` | number | `0` | 정점 나이 이동 (0~2) |
| `S.archetype` | string | `"finisher"` | 성향 id |
| `S.peak` | number | (카드가 결정) | 잠재력 실제값 (숨김) |
| `S.peakFog` | number | `4` | 남은 안개 폭. **2차** |
| `S.body` | object | `{h:"mid", b:"mid"}` | 키·체격. **2차** |
| `S.traits` | array | `[]` | 보유 특능 id |
| `S.traitSlots` | array | `[]` | 장착 6칸 (id 또는 null) |
| `S.golds` | array | `[]` | 금특 id (최대 3). **2차** |
| `S.flaw` | string | `""` | 결함 id |
| `S.chain` | object | `{}` | 이벤트 체인 진행. **2차** |
| `S.trust` | number | `50` | 🤝 동료 신뢰. **2차** |
| `S.posSwitches` | number | `0` | 포지션 전환 횟수 |
| `S.rerolls` | number | `0` | 입단 리롤 사용 수 |
| `S.avatar` | object | `{hair:0, skin:0}` | CSS 캐릭터 파츠 |
| `S.seasonGoals` | array | `[]` | 시즌 목표 3개 `[{key, need, got}]` |
| `S.career.gradeUps` | number | `0` | 등급 승급 누적 |
| `S.career.moments` | object | `{g:{t:0,p:0}, a:{t:0,p:0}, d:{t:0,p:0}}` | **카드 3종별** 시도/성공 |

**기존 `legacy` 시스템은 그대로 승계합니다** — `legacyTalentBonus`(재능 +최대 0.25) ·
`legacyMoneyBonus`(자금 +최대 15000) · `legacyGain`이 `game.js:415-427`에 이미 있어요.
키 이름만 `winger2-save-v1-legacy`로 바꿉니다.

### 9-2. 규칙 여섯 — 이게 표보다 중요합니다

1. **마이그레이션 함수를 만들지 않습니다.** 새 필드는 읽는 쪽에서 기본값을.
   ```js
   const rd = (v, d) => (v == null ? d : v);
   ```
2. **`schemaVersion` 필드를 두지 않습니다.** 버전 번호가 있으면 반드시 마이그레이션
   유혹이 생기고, 그 함수가 옛 세이브를 부수는 첫 번째 경로가 됩니다.
3. **`id`는 영원히 안 바꿉니다.** 성장타입·성향·특능·결함 전부 문자열 `id`예요.
   순서가 필요하면 `order`/`tier` 같은 새 필드를 더합니다
   (축구에서 `id`로 순서를 판단해 사고가 났습니다).
4. **가중 카운터는 옛 카운터를 이어받습니다.**
   ```js
   S.career.mvpW = (S.career.mvpW != null ? S.career.mvpW : S.career.mvp) + prestige;
   ```
5. **배열은 `|| []`, 객체는 `|| {}`.**
6. 🔴 **키로 접근하는 표는 없는 키를 반드시 대비합니다.**
   ```js
   const allStat = 평균(존재하는 값들);
   const st = (k) => (S.stats[k] == null ? allStat : S.stats[k]);
   ```
   **`|| 0`이나 `?? 0`을 쓰지 마세요.** 0으로 채우면 종합이 내려가 그 선수가 갑자기
   약해집니다. **나머지 평균으로 채우면 `(합 + 평균) ÷ (n+1) = 평균`이라 종합이
   소수점까지 안 흔들려요** — ⚡ 스피드 추가 때 검증된 방법입니다.
   적용 대상: `S.stats` · `S.talents` · `S.body` · 앞으로 추가될 모든 키-값 표.

---

## 10. 시드 주입 창구 스펙 (engineer · inspector)

**점진 확정은 매 경기 결과가 다릅니다.** 고정 시드가 없으면 회귀 검사가 표본 수로
버텨야 하고, 그러면 느립니다.

### 10-1. 요구사항

```js
// ⚠️ 바깥 스코프에 둡니다. 함수 안에 두면 return { _t: … }에서 안 보여요
// (컴백 컨셉 구현에서 정정한 것 — 계획이 FANDOM_CAP 근처를 지목했는데 그 자리는
//  playShow() 안이라 바깥에서 안 보였습니다)

let _rng = Math.random;
const rnd = () => _rng();                 // 🔒 엔진의 모든 난수가 이 하나를 지나야 합니다
const rand = (a, b) => a + rnd() * (b - a);
const randInt = (a, b) => Math.floor(rand(a, b + 1));
const pick = (arr) => arr[Math.floor(rnd() * arr.length)];
```

**`Math.random`을 전역으로 갈아치우지 마세요.** 다른 모듈(`timing.js`·`cloud.js`)이
영향을 받습니다.

### 10-2. `_t` 창구

```js
return {
  /* … 게임 API … */
  _t: {
    seed(n)   { _rng = mulberry32(n >>> 0); },   // 32bit PRNG. 결정론적
    unseed()  { _rng = Math.random; },

    // 미니게임을 DOM 없이 확률로 판정합니다. balancer·inspector 둘 다 필요해요
    skill: null,               // null이면 실제 조작 · 0~1이면 succ(ability, skill)로 자동 판정

    playMatch(cfg),            // 한 경기를 끝까지. 반환: {myGoals, assists, defense, teamGoals, oppGoals, res, cards:[…]}
    season(cfg),               // 38라운드. 반환: 시즌 집계 + 순위표
    leagueRound(cfg),          // 경쟁자 66명 카드 루프 한 라운드

    K: { SCENE_ATK, BIG_BASE, FAT, URG, FIN, CON, ME_P, SPOT, NPC_SPOT, COND_K, /* … */ },
  },
};
```

### 10-3. 🚨 검사가 `_t.K`에서 문턱을 읽으면 안 됩니다

> `CLAUDE.md`의 함정 목록에 **"문턱을 코드에서 읽어옴 — 상수를 바꿔도 검사가 따라가서
> 안 잡힘"**이 있습니다.
>
> **`_t.K`는 "엔진이 실제로 쓰는 값"과 "소스의 상수"가 같은지 대조하는 용도로만** 씁니다.
> 검사의 기준값은 **검사에 직접 적습니다.**
> 예: `SPOT`을 1로 되돌리면 곡선 검사가 **빨간불이어야** 합니다 (변이 검증).

### 10-3b. ✅ 산식을 정규식으로 뜯어오지 마세요 — **실제 엔진을 굴립니다**

engineer가 엔진을 `S`·`WingerSquad`에서 떼어 **`cfg`로만 받게** 만들었습니다
(`window.WingerEngine` · `beta/winger2/engine.js`). **node에서 화면 없이 그대로 돌아갑니다.**

> ### 이 저장소의 함정 하나가 이 게임에서는 구조적으로 사라집니다
> `CLAUDE.md`의 *"초록불인데 아무것도 안 지키는 검사"* 목록 중 **"픽스처가 다른 모양"**과,
> 제가 §14-2에 올린 **"검사가 떼어 온 조각이 실제 배선에서 한 단계 더 지나가는 것을 못 봤다"**
> (`curve-test.js`가 `splitMine`에 덮이는 `c.g`를 재던 그 결함) — **둘 다 원인이 같습니다.
> 소스에서 조각을 뜯어와 따로 굴렸기 때문이에요.**
>
> **balancer와 inspector는 이제 `engine.js`를 `require`해서 실제 엔진을 돌립니다.**
> 20·21번이 쓴 `extract.js`의 56조각 정규식 추출은 **v2에서는 필요 없습니다.**
> 뜯어온 조각과 배선이 어긋날 수가 없어요.
>
> ⚠️ **엔진 밖에 남은 것은 여전히 대조해야 합니다** — `RATE`·`POS_AXIS`·수상 판정 블록처럼
> `career.js` 쪽에 있는 값들이요. 엔진이 `cfg`로 받는 것만 안전합니다.

### 10-3c. 🔑 **종속값은 값이 아니라 「관계」로 검사합니다**

`CARD_AGE`(만 17세)는 혼자 서는 값이 아닙니다 — **유스 3년이 뒤에 붙어 데뷔 나이를
통째로 정합니다.** 그런 값을 `=== 17`로 검사하면 **상수만 지키고 뜻은 안 지킵니다.**

```
❌  CARD_AGE === 17
✅  CARD_AGE + 유스 기간 === 데뷔 나이(20)     ← 관계
```

engineer가 `CARD_AGE`를 18로 되돌려 보니 검사 42-3·42-4가
*"카드 18세 + 유스 3년 = 데뷔 만 21세"*로 잡았습니다. **값이 아니라 관계로 본 덕이에요.**

같은 성격의 종속값들 — 반드시 관계로 검사하세요:

| 종속값 | 지켜야 할 관계 |
|---|---|
| `CARD_AGE` | `+ 유스 기간 = 데뷔 나이` |
| `TRAIN_NEUTRAL` | 훈련 계단을 얹은 **총량이 세 타입 ±3%** (§7-1 ①) |
| `RETIRE_CURVE` + 정점 가드 | **한 벌** — 하나만 바꾸면 오르막 오작동 또는 영구 미발동 (§6-4) |
| `NPC_SPOT_G/A/D` | 세 부문상의 **수상률이 서로 ≤12%p** (§2-8b) |
| `POS_AXIS.n` | 포지션 곡선이 **같은 밴드** 안 |
| 🥈 베스트11 문턱 `5.8 · (5.5, 8.0)` | **세 값 한 벌** — 하나만 넣으면 거의 안 고쳐지거나(94.3%) 절반만(67.5%). 그리고 **베스트11 ≥ 리그MVP** (§6-4) |

> **상수를 검사에 적어 두는 것과 다릅니다.** 문턱은 검사에 직접 적고(§10-3),
> **종속값은 관계식으로** 봅니다 — 그래야 위 값을 옮겼을 때 **따라 움직여야 할 것이
> 안 움직이면** 빨간불이 떠요.

### 10-4. 실기기 이벤트 순서

미니게임 검사는 **`pointerdown` → `pointerup` → `click` 셋 다** 보내야 합니다.
`pointerdown` 하나만 보내던 검사가 **24개 케이스를 전부 놓친** 전례가 있어요.

⚠️ **재발 조건**: `pointerdown` 핸들러 안에서 화면을 갈아치우고 그 자리에 새 클릭 대상을
그리면 손을 뗄 때 그 대상이 `click`을 받습니다. 미니게임은 **`click`으로 열도록** 하세요.

---

## 11. 1차 순서와 실측을 끊는 지점

```
0  등록 9지점 · 폴더 · SAVE_KEY · CACHE                       (§1)
   │
1  점진 확정 경기 엔진 + 경쟁자 카드 루프                       (§2 · §3)
   │  ├ STEP 1~4 · 산식 정정 4건 · 확정 계수 전체
   │  └ 시드 주입 창구                                          (§10)
   └─▶ 🔴 실측 ① — 곡선 재확인 · MOM 조정            ← 게이트
   │
2  미니게임 4종 + 주발 판정 창 ③                                (§4)
   └─▶ 🔴 실측 ② — 조작 vs 능력치(원칙 ⑥) · 4종 난이도 균등
   │
3  골 연출 · 경기 화면                                          (§5-4 · §5-5)
   │
4  나이 축 + 성장타입                                           (§6-4 · §7-1)
   └─▶ 🔴 실측 ③ — 커리어 곡선 · 20시즌 리그 평균 · **`c*` 확정**
   │
5  유망주 3택 + 결함 + 리롤 2회                                 (§6-2)
6  성향 3택 + 시즌 목표 카드                                    (§6-3)
7  특능 엔진 + 일반 12종                                        (§7-2)
   └─▶ 🔴 실측 ④ — 유망주 3장 · 성장타입 · 성향 · 특능 6칸 중립
   │
8  주발 ①②③ 완성 + 약발 표시·훈련                             (§8-2)
   └─▶ 🔴 실측 ⑤ — 주발 3중 중립 · 최선/최악 격차 ±12%
   │
9  윙어 자리 4종 + 각 포지션 자리 +1                            (§8-1)
10 스탯 등급 F~S · 승급 카드 · CSS 실루엣 3장                   (§7-3)   ← 곡선을 안 흔듦
   │
11 포지션 전환                                                  (§8-3)
   └─▶ 🔴 실측 ⑥ — 전환 손익 조건부 중립
```

**실측을 여섯 번 끊는 이유** — 곡선을 흔드는 변경마다 재야 어디서 틀어졌는지 압니다.
한꺼번에 넣고 마지막에 재면 원인을 못 찾아요. 아이돌 하나로 곡선을 확인하고 나머지
5종에 옮긴 것과 같은 순서입니다.

**11번(전환)이 마지막인 이유** — 전환은 기존 곡선을 바꾸지 않고 **새 분기**를 만듭니다.
먼저 넣으면 모든 실측에 "전환/미전환" 두 갈래가 붙어 측정 비용이 두 배가 돼요.

---

## 12. 남은 위험 · 후속 실측

### 12-1. 🔴 위험

| 위험 | 무슨 일이 | 어떻게 |
|---|---|---|
| **MOM이 현행의 두 배** (K1 6% → **12%**) | 카드 8칸이라 평점 분산이 작아 상위 선수가 라운드 최고를 더 자주 가져갑니다. **MOM은 명예의 전당 점수와 수당에 직접 들어가고**(`game.js:734`), 전당은 **8종이 공유하는 `grow-hof-v1` 표**예요 | ✅ **결정됨 — 6% 수준으로 복원.** `MOM_MIN 8.40` 절대 문턱(§2-8). ✅ 확정 — 능력치 110에서 6% 수준 |
| **조작이 육성을 대체한다** (원칙 ⑥) | 미니게임이 결과를 직접 정함 | 커리어 단위 검증(실측 ② 2-a). 회당 성공률 역전은 의도이고, **20시즌 총량은 역전되면 안 됩니다** |
| **미니게임 4종이 시즌 55회 반복** | 물립니다 | **2차 세트피스 추가가 늦어지면 안 됩니다** |
| **🅰️ 도움 경로를 옮기면 총량이 움직인다** | `ASSIST_P2`가 전개 → 결정 장면으로 자리를 옮겼습니다. 결정 골에 도움이 새로 붙어요 | 실측 ①-D·①-E가 게이트. **`ASSIST_P2`를 0.75에서 조정**해 총량을 되맞춥니다 |
| **`FLOOR_SHARE`가 저연차 곡선을 밀어 올린다** | 바닥이 능력치 70 구간의 카드를 두 배로 만듭니다 | 실측 ①-I ⓓ. **목표(70에서 5%)를 넘으면 값을 낮춥니다** — 현재 1.8%라 가까워지는 방향이지만 확인이 필요해요 |
| **`SPOT` 계단이 화면에 안 보이면 노이즈** | *"우리 팀 에이스가 되는 순간"*에 카드가 1.3배 뜁니다. 안 보이면 원인을 알 수 없어요 | 🌟 배지 + 승격 문구 (§2-4 · director) |
| 🔴 **🛡️ 철벽상을 수비수가 구조적으로 못 받는다** (110에서 **0%**) | 축이 **차단 횟수**라 *"두들겨 맞을수록 유리"*합니다. 능력치를 올려도 리그 1위가 안 따라와요(50.1 → 50.6) | **축을 무실점 경기 수로 바꿉니다** (§2-8b (4) · 게이트 ①-G). **포지션 넷을 대등하게 대접하기로 한 D-(나)와 정면으로 부딪히는 자리**라 미룰 수는 있어도 안 할 수는 없습니다 |
| **🎯 플레이메이커가 자동으로 들어온다** (110에서 **88%**) | `autoMatch`가 전개 장면을 골로 넣어 경쟁자에게 도움이 안 쌓입니다 | ✅ 게이트 ①-G의 G-3 — engineer 구현 완료 (도움÷골 0.747 → 0.881) |
| 🔴 **시즌 칭호·피버 버프가 리그 경기에 안 닿는다** | `buffMul`을 부르는 곳이 `matchContribution` 하나뿐인데 v2 리그는 카드 엔진을 씁니다. **칭호 여덟 개의 `g`/`a`/`d`가 통째로 죽어요** — 개념집이 *"효과가 붙는 자리는 전부 경기 안"*이라고 적은 체계입니다 | §2-7b — **1차 항목**. `engRow`가 버프를 함께 넘기고 `w(i)`에 곱합니다. 실측 ①-L |
| 🔴 **내 클럽만 다른 산식으로 실점했다** | `autoMatch`가 `chance(pf * pc)`로 **한 장면을 두 번 굴렸습니다.** 무실점률 22.6% vs 52.5% | ✅ §2-8b (7) — `chance(pf)`. 검사 30·31 |
| 🟠 **`shareByWeight`만 옛 규칙** — 리그 도움 −2.2% | 그 한 팀이 **언제나 내 상대**라 **계통 오차**입니다. 평균으로 안 씻겨요 | §2-8b (6) — `pBig` 사후 재구성. G-4 안에서 |
| 🟡 **🛡️ 무실점은 팀 기록이라 클럽 안에서 11명이 같은 값** | 실질 판정자가 **차단 타이브레이커**가 됩니다 | 설계대로입니다(클럽 간은 무실점, 클럽 안은 차단). 다만 **`NPC_SPOT_D`(G-2)를 움직이면 수상률이 차단 쪽으로 반응**할 수 있어요 — G-2에서 이 경로를 같이 보세요 |
| 🟡 **🏆 컵이 아직 v1 산식(`deriveOppGoals`)** | 컵 무실점률이 리그의 **+60.5 ~ +198.5%**였습니다 | ✅ 컵 `act.cs`를 뺐습니다 (§2-8b (9)). **컵을 카드 엔진으로 옮기는 것은 §11 순서 1의 미완 항목**으로 남습니다 |
| 🚧 **실점 쪽 비대칭이 남았다** (실점 −14.83 ~ −16.42% · 무실점 +31.6%) | 후보 D가 남기는 것입니다. `CON 1.111` vs `FIN 0.884`의 **25.7% 차** + 내 수비수 `sc`. 득점은 **+0.9%**로 맞았어요 | **의도적 미달로 표시하고 상한을 박습니다** (검사 31b). 2차의 **칸마다 관점 굴리기**로 해소 (§2-8b (7)) |
| 🔴 **31b의 밴드 통과가 시드에 달려 있습니다** | 시드 넷 중 **둘이 −15% 밴드 밖**입니다(−14.83 / −15.21 / −16.42 / …). 단일 시드로 재면 통과했다 실패했다 합니다 | **inspector의 승격 보류 판단이 옳았습니다.** 상한을 관측 최악(−16.42)에 여유를 둔 **−17%**로 잡고, 검사는 **시드 넷의 평균**으로 재세요 — 단일 시드는 신호가 아니라 잡음입니다 |
| 🔴 **31b는 코드가 아니라 플레이어를 재고 있습니다** | 같은 코드가 **조작 실력에 따라 통과도 실패도** 합니다 — `s=0.35` → **−18.2%** · `s=0.50` → −15.89% · `s=0.65` → **−14.0%** | 🚧 **승격 금지의 세 번째 근거**입니다. 검사는 **`_t.skill`을 0.5로 고정**해서 재야 해요 (§10-2). 안 그러면 *"검사가 무엇을 지키는지"*가 사람마다 달라집니다 |
| 🔴 **성장타입이 우열이었다** (만성이 두 축 모두 승) | 정점 나이만 옮기고 **양끝을 안 옮겼습니다** | ✅ §7-1 ② 곡선 앵커 재작성. 38세 기준 면적 벌어짐 **2.8%** |
| 🔴 **훈련 배수를 지운 게 잘못이었다** | *"상쇄되니 아무 일도 안 한다"*로 읽었는데, **배수가 계단의 불균형을 지우고 있었습니다.** 지우니 만성이 훈련을 **46% 더** 받아요 | ✅ §7-1 ① — `TRAIN_NEUTRAL`(1.226/1.000/0.839)로 되살립니다. **중립화 상수이지 손잡이가 아닙니다** (검사 38) |
| ✅ **🥈 베스트11 포화** (90에서 99.9%) | `agePen`이 그걸 **가리고 있었습니다.** 나이와 무관한 문제였어요 | ✅ 문턱 **`5.8 · (5.5, 8.0)`** — 90에서 48.9%. 🚫 **상한 8.5 이상 금지**(리그MVP와 역전) · 검사 44 |
| 🔴 **만성 신인이 데뷔 시즌 벤치** (출전 9.8/38 · 75.9%가 15경기 미만) | 설계는 *"성적이 낮다"*였는데 게임은 **"경기를 안 준다"**로 처리했습니다. 못 뛰면 실전 성장도 없어 **복리로 커집니다** | ✅ `YOUTH_BONUS`(§7-1 ④) — **나이의 일반 규칙**이라 만성 특혜가 아닙니다. NPC에게도 걸어요 |
| 🟡 **⚔️ 공격수 신인의 안개가 한 시즌 늦게 걷힙니다** | 첫 시즌 출전 12.3으로 **15경기(안개 축소 조건)를 못 넘습니다.** 2자리 경쟁이라 구조적이에요 | **의도로 남깁니다** — *"자리 경쟁이 치열한 포지션"*의 성격입니다. 은퇴 오작동은 **정점 가드**가 이미 막아요 (§7-1 ④) |
| 🟡 **최악 조합(만성 ⚔️ · 강한 데뷔 클럽)이 8.0경기** — 그 칸의 47%가 목표 아래 | 세 불리함이 겹친 칸입니다. **목표는 평균이고 꼬리는 남깁니다** | 복리 고리(못 뜀 → 성장 없음 → 은퇴 권유)는 **정점 가드가 끊었습니다.** `YOUTH_BONUS`로 안 올려요 — **나이 손잡이로 포지션 경쟁률을 고치는 것**이라 미드필더가 21.5 → 26이 됩니다. **하한 7경기**를 검사로 (41b) |
| 🟡 **커리어 길이를 뺄셈으로 셌습니다** (18 vs 실제 19) | `38 − 20 = 18`로 적었는데 **양끝을 다 뜁니다.** balancer 측정은 이미 실제 값이라 아무것도 안 움직였어요 | ✅ 문서를 19 / 15·17·18로 정정. **`RETIRE_AGE`는 38 그대로** — 37로 내리면 밸런스 변경이 됩니다. **설계 문서에 기간·개수를 적을 때는 뺄셈이 아니라 실행으로 세세요** |
| 🟡 **「15경기」를 신인 출전 목표로 빌려 썼습니다** | 그 값의 자리는 **안개 축소**(§6-2)와 **은퇴 조건**(§6-4) 둘뿐이에요. 근거 없이 옮겨 쓴 게 공격수 미달의 정체였습니다 | ✅ 신인 목표를 **38라운드의 30%(11.4)**로 정정. 검사 41도 그 기준으로 (§7-1 ④) |
| 🔴 **은퇴 제안이 오르막에서 걸렸다** (만성 19~22세) | 곡선이 뒤집힌 U자인데 **문턱 하나로 판정**했습니다 — §2-10의 다섯 번째 냄새 | ✅ `RETIRE_CURVE 0.78` + **정점 가드 한 벌** (§6-4) |
| 🔴 **카드 나이가 커리어 길이를 정했다** (총 상 12.5% 차) | ⓑ안이 **능력치는 풀었지만 시간을 못 풀었습니다.** 시간은 다른 무엇으로도 못 사요 | ✅ **카드 나이 칸 폐기 — 셋 다 만 17세**(데뷔 만 20세). 반대급부는 성장타입 칸이 맡습니다 (§6-2) |
| 🔴 **나이 칸에 반대급부가 없었다** | 같은 총합이면 17세가 그냥 좋았습니다 | ✅ §6-2 — 카드 스탯을 **정점 기준값**으로. 반대급부가 자동으로 생기고 `AGE_TRADE`가 필요 없어요 |
| 🟡 **커리어가 15 → 21시즌** | 전당이 **8종 공유 표**(`grow-hof-v1`)입니다 | 전당은 **상**을 세므로 곡선 면적을 맞추면 비슷합니다. **다만 v1과 대조**하세요 (실측 ④ 5-g) |
| ✅ **📈 공격포인트왕** (35.6%) | 파생 축이라 **리그 1위가 구조적으로 낮습니다**(1위 골 22.0 + 도움 22.9 = 44.9인데 1위 공격P 32.7). 계수로는 못 풀리는 것이 실측으로 증명됐어요 | **G-7의 기준을 「수상률」에서 「기대 이득」으로** (§2-8b (8c)). 📈는 효과가 `g·a +6%`로 셋(+15%)의 40%라, **자주 받는 게 정합적**입니다 |
| 🟠 **세 갈래 도움÷골의 여유가 줄었다** | 0.750 / 0.750 / 0.768 — 최대 차 **2.36%**(문턱 3%). 여유가 1.13% → 2.36%로 좁아졌습니다 | **`ASSIST_P2`를 더 내리면 문턱에 닿습니다.** G-11에서 셋을 함께 보고, 내려야 하면 `shareByWeight`의 `pBig` 재구성을 먼저 넣으세요 |

### 12-2. 🟡 위험

| 위험 | 어떻게 |
|---|---|
| `condMul` `c*`가 잠정값 | 훈련 루프가 붙은 뒤 실측 ③에서 확정. 그때까지 80 |
| 곡선 오차가 목표 대비 최대 9.1%p (130에서 94.1% vs 85%) | **밴드 안이라 지금은 손대지 않습니다.** `ME_P`를 1.8→1.6으로 낮추면 완화되지만 골 총량이 같이 내려가요 |
| 단판 대회 분산이 리그의 2.8배 | 월드컵 조별 3경기면 능력치 110도 20%↑ 무득점. **연출이나 대회 개인상 문턱에서 받습니다** |
| 카드 종류가 난이도 라벨이 됨 | §4-4 ② 검사 |
| CSS는 기계가 못 봅니다 | 경기 화면·유망주 카드·승급 카드 전부 480px를 아무도 못 봅니다. **실기기 확인 필수** |
| 등록 9지점 누락 | `sync-beta.sh`의 `CONTENT`가 가장 조용한 실패. 교차 비교 `grep` 필수 |
| 초록불인데 아무것도 안 지키는 검사 | 여덟 번 넘게 나왔습니다. **변이 검증**을 반드시 |

### 12-3. 2차 후보

#### (1) 리그 구조 — **20팀 38라운드** ← 2차의 첫 항목 (2026-08-28 확정)

현행 승계(6팀 38라운드)로 1차를 갑니다. **안 하는 게 아니라 뒤로 미루는 것**이에요.

> **왜 1차가 아닌가** — 팀 수를 바꾸면 **시즌 총량·부문상 문턱·승격 확률이 전부 같이
> 움직입니다.** 1차는 이미 경기 엔진이 곡선을 통째로 바꾸는 중이라, 둘을 겹치면
> 어디서 틀어졌는지 못 찾아요. 6종을 한꺼번에 안 바꾸고 아이돌 하나로 확인한 것과
> 같은 이유입니다. 백로그의 *"대진표를 실제로 만들 일이 생기면"* 조건은 충족될
> 소지가 있지만, **순서의 문제**입니다.

**왜 20팀 38라운드인가** — 유럽 리그와 정확히 일치하고, **경기 수가 안 바뀌어
시즌 총량이 유지되는 유일한 조합**입니다. 현행 6팀 38라운드는 한 팀을 시즌에
6~7번씩 만나는 셈이에요(백로그).

**그때 다시 재야 할 것**

| # | 무엇 | 왜 |
|---|---|---|
| 1 | **순위표 경쟁 난이도** | 6팀 중 1위 ≠ 20팀 중 1위. 우승·승격 확률이 통째로 움직입니다 |
| 2 | 🔴 **부문상 난이도** | `leagueBest`의 후보가 **6 → 20으로** 늘어납니다. 골든부츠·최다도움 문턱이 그만큼 올라가요 — **경쟁자 카드 루프도 66명 → 220명** |
| 3 | **명단 생성 비용** | 라운드당 카드 굴림이 24회 → 80회. 시즌 912회 → **3,040회.** 여전히 감당되지만 재야 합니다 |
| 4 | **세이브 크기** | 리그당 클럽 6 → 20. `localStorage` 용량과 클라우드 세이브 페이로드 |
| 5 | 대진표 | 20팀 홈·원정 두 바퀴 = 정확히 38라운드. **무작위 짝짓기(`recordRound`)를 실제 대진표로** 바꿀 수 있게 됩니다 |

#### (2) `LEAD_TILT` (§5-3에서 미룬 것)

리드하는 팀이 내려앉는 건 축구적으로 자명해서 언젠가는 넣을 값어치가 있습니다.
**형태는 이미 정해 뒀습니다** — 곡선을 다시 재야 해서 1차에는 안 넣어요.

```js
lead = clamp(myScore - oppScore, -2, +2);
tilt = 1 - LEAD_TILT * lead * (k / N);      // 앞설수록·후반일수록 우리 공격 장면이 줄어듦
p(우리 공격) ∝ atkW * tilt;
p(상대 공격) ∝ defW / tilt;                 // 총 장면 수(SCENE_ATK)는 고정 — 비율만 이동
```

`LEAD_TILT = 0.12` 제안. **중립(0-0)에서 `tilt = 1`이라 기댓값이 불변**인 게 이 형태의 장점입니다.

### 12-4. 후속 실측 목록 (balancer)

| 실측 | 항목 |
|---|---|
| **①** | 🔴 **`MOM_MIN` 확정** — K1 기준 MOM 비율이 **6% 근처**가 되는 문턱값 (제안 7.6). 문턱 조정이라 곡선 전체를 다시 잡을 필요는 없겠지만 **① 곡선 다섯 점이 밴드에 남는지 · 🔥 물오른 폼 획득률 · 명예의 전당 점수·수당이 현행 눈금인지** 확인 |
| **①-C** | ✅ **①-J에 흡수** — §2-6이 카드의 중심을 `autoP`로 넘겨 도움 대칭이 정의상 성립합니다. `FIN`은 안 움직여요. **다만 🅰️ 전개의 `ok`가 골을 주지 않는지**는 따로 확인하세요 (주면 골 중립이 깨집니다) |
| **①-D** | 🔴 **도움 총량 보존** — `ASSIST_P2`가 결정 장면으로 자리를 옮겼으니 시즌 도움이 (라′) 측정값 **±3%**인지 (능력치 110: fw 12.0 · wg 17.1 · mf 19.7 · df 5.9). 안 맞으면 **`ASSIST_P2`를 0.75에서 조정** |
| **①-E** | ✅ **리그 정합** — **리그 전체 도움 ÷ 리그 전체 골 = 0.70 ~ 0.80.** 지금까지 아무도 안 잰 값입니다. 도움이 두 경로로 나뉘었으니 여기서 한 번 봐야 해요 |
| **①-F** | ✅ **닫힘** — `CLUTCH 1.35`는 시즌 골 **+3.6%**로 밴드를 넘었습니다. **1.25로 확정** (§2-3) |
| **①-G** | 🔴 **별도 게이트로 승격 — 아래 §12-4b** (부문상 눈금 · 철벽상 축). 이번 실측 ①에서는 **손대지 않습니다** |
| **①-I** | 🔴 **`FLOOR_SHARE 0.12` 확정** (§2-4 · inspector F3) — ⓐ 능력치 70 전 포지션이 **≥ 0.6회/경기** ⓑ 동료 str 50/70/80 세 조건에서 **이적 페널티가 사라졌는지** ⓒ 능력치 70→80 계단이 **2.6배 → 1.5배 이하** ⓓ **저연차 곡선**이 목표(70에서 5%)를 넘지 않는지 — 현재 1.8%라 가까워지는 방향이지만 넘으면 값을 낮춥니다 |
| **①-J** | 🔴 **`autoP` 중심 재정의 검증** (§2-6 · inspector F4 · engineer §4-⑤) — 능력치 **70/90/110/130/150** × **4포지션** × **3카드종류** 전 칸에서 `s = 0.5`일 때 `P(사건\|카드) = autoP`. **차 ≤ 1%** (밴드를 ±3%에서 조입니다 — 이제 정의상 같아야 하니 남는 건 몬테카를로 잡음뿐이에요) |
| **①-L** | 🔴 **시즌 칭호·피버 버프를 카드 엔진에 연결한 뒤**(§2-7b) 곡선이 밴드에 남는지 · 생산량 증가폭이 `BUFF_CAP` 안인지 · `moment` 키가 판정 창에 붙는지. **버프 있음/없음 둘 다 재세요** (실측: `g` +40%에서 시즌 골 17.0 → 19.1, **+11.9%**) |
| **①-K** | 🔄 **정정 — clamp가 처음 닿습니다.** 22번의 *"실플레이 구간 밖"*은 **대등한 경기에서만** 참이었어요(`s`가 늘 0.5라). 미니게임이 `s ∈ [0,1]`을 내면서 **리그 최대 전력차(K1 78:52)에서 능력치 130부터 상단이 잘립니다**(150·78:52에서 `s > 0.80` 버려짐). 시즌 영향 **0.2%p 미만**이라 곡선엔 안 닿고, **능력치 110까지는 전 상대 안전**합니다. **기록만 하고 손대지 않습니다** — `half(a)`를 줄이면 조작 폭이 좁아져 `E[s]` 창(§4-4)이 흔들려요 |
| **①-H** | **`momAdj = 0` 확인** — `RATE`의 `b +10`을 재적합할 때 이 항이 0이었는지. 들어 있었다면 `b`를 다시 잡아야 합니다 |
| **②** | 2-a **커리어 단위 원칙 ⑥** (능력치 40+최고조작 < 능력치 120+최악조작, 20시즌) · 2-b 조작 고정 시 능력치 기울기 · 3-a **4종 난이도 ±5%p** · 3-b 결정/전개 성공률 동일 |
| **③** | 커리어 곡선 · **20시즌 리그 평균 ±1.0 재측정**(현행 노화 모델 값이라 나이 축 도입 후 다시) · **`c*` 확정** · `condMul` 제곱 누적 중립 ±0.01 |
| **④** | 5-a 유망주 3장 ±5% (**정점 기준값 총합**으로 재세요 — §6-2) · 5-b 🔄 **성장타입 3종을 「커리어 총 면적」(데뷔~**38세**까지의 곡선 적분)으로 ±8%.** 곡선 배수로도, 은퇴 제안 나이까지로도 재면 안 됩니다 — 뒤는 플레이어의 선택이 섞여요 (§7-1 ③. 현재 **2.8%** ✅) · **5-b3 `TRAIN_NEUTRAL` 확정** — 훈련 계단 총량이 세 타입 **±3%** (지금 배수 없이는 81.5/100/119.2로 **만성이 46% 더**) · **5-h `agePen` 폐기 후 30대 후반 수상률** — 리그MVP 57.5%가 능력치로 설명되는지 · **5-i `YOUTH_BONUS` 확정** — ✅ 전 조합이 **11.4경기 이상**(공격수 12.8이 최저) · NPC에게도 걸었을 때 남는 이득 **68~69%**. **15경기로 재지 마세요** (§7-1 ④) · ✅ **5-j 닫힘** — 베스트11 문턱 **`5.8 · (5.5, 8.0)`** 확정. 능력치 90에서 48.9% · 곡선 되먹임 ≤0.9%p. **5-h와 같은 손잡이라 따로 안 잽니다** (§6-4) · **5-b2 은퇴 제안이 세 타입 전부에 뜨는지** (조숙 34 · 보통 36 · 만성 37) · **5-g v1 vs v2 커리어 총 상 개수** — 전당이 8종 공유 표예요 (§6-4) · 5-c 성향 3종 카드 1장당 기대 획득량·칭호 개수 · 5-d 등급 S 3% · 5-e 특능 6칸 `BUFF_CAP` · 5-f 윙어 4자리 가중 평균 1 |
| **⑤** | 4-a 리그 전체 `footFit` 평균 1.000±0.005 · 4-b 약발 기댓값 5.0 + 훈련 후 상승분 · 4-c **판정 창 ±25%의 성공률 비선형성** · 4-d 셋의 곱 격차 ±12% |
| **⑥** | 6-a 밀린 상태 전환 +8% · 6-b 주전 전환 −10% |

### 12-4b. 🔴 별도 게이트 **①-G** — 부문상 눈금과 철벽상 축 (§2-8b)

**곡선이 V1에서 굳은 뒤에 엽니다.** 여기 손대면 곡선의 전제가 바뀌어요.

| # | 요청 | 통과 기준 |
|---|---|---|
| **G-1** | 🔑 **세 축의 `내 생산량 ÷ 리그 1위`를 같게** — 능력치 70/90/110/130/150 × 골·도움·수비 | 세 비의 차 **≤ 0.08** (골 0.71 기준선) |
| **G-2** | 🔄 **재정의** — 종류별 분할은 폐기(§2-8b (10)). `NPC_SPOT` **하나로 2.90**. **기준선을 17%가 아니라 「넷이 비슷할 것」으로 옮깁니다** — 7.00 고정과 17%가 양립 불가임이 실측으로 확인됐어요(7.00에서 4.6%) | 골든부츠 **12~18%** · G-7의 넷이 서로 ≤12%p |
| **G-3** | 🔴 **`autoMatch`의 전개 장면을 `createMatch`와 같게** (§2-8b (3) — 버그) | 나와 경쟁자가 **같은 규칙**. 고친 뒤 G-1·G-4를 다시 |
| **G-4** | **도움 ÷ 골 재확인** — G-3이 리그 도움을 +18.6% 늘렸습니다 (실측 0.747 → **0.881**). ⚠️ **`shareByWeight`의 `pBig` 사후 재구성**(§2-8b (6))을 **먼저 넣고** 재세요 — 안 넣으면 리그 6팀 중 한 팀이 다른 자를 씁니다 | **0.70 ~ 0.80.** 손잡이는 `ASSIST_P2` |
| **G-5** | 🛡️ **철벽상 축을 무실점 경기 수로** (§2-8b (4)) — 주축 = 내 선발 경기 중 팀 무실점, 타이브레이커 = 차단 | ⓐ 수상률이 능력치에 **단조증가** ⓑ 능력치 110에서 **17% ±5%p** ⓒ **약팀 수비수가 유리해지지 않을 것** (지금은 두들겨 맞을수록 차단이 쌓입니다) |
| **G-6** | 🔄 **재산정이 아니라 회귀 확인입니다** (§2-8b (5) 판정) — `posAxis`는 **차단을 계속 봅니다.** ⓐ **`df.n = 0.816`이 그대로여도 되는지** ⓑ **간접 경로** — 철벽상이 살아나면 df가 「🛡️ 리그 최고 수비수」(`d: 0.15`)를 받고 그게 차단을 늘려 hype에 들어갑니다. 그 크기 | df 곡선이 밴드(0~8 / 8~25 / 45~72 / 78~95 / 90~100%) 안에 남을 것 |
| **G-8** | ✅ **닫힘 — 조건 충족(+60.5 ~ +198.5%).** `cupFinalize`의 `act.cs`를 **뺍니다** (§2-8b (9)). 컵을 카드 엔진으로 옮기면 되살려요 |
| **G-9** | 🔴 **후보 D 한 벌을 한꺼번에 넣고 재측정** — ⚠️ **`S.buffs`를 비운 대조군에서 재세요.** 후보 D 표(23번)는 **칭호 버프가 죽어 있는 세계에서 잰 값**입니다. 계수 조정(1~4)과 **없던 경로가 생긴 것**(5 = 칭호 버프)을 한 판에 섞으면 *"계수가 틀린 건지 버프가 센 건지"*를 못 가려요 (§2-8b (10)) — `chance(pf)` · `ASSIST_P2 0.48` · `NPC_SPOT 2.90` · `AXIS_OFF 2.10` · `POS_AXIS.n` 넷 · `ACE_POOL.goal` · 컵 `act.cs` 제거 | G-1 ✅ · G-4 ✅ · G-5 ✅ · 곡선 5점 밴드 · MOM 5~8% · 평점 7.0~7.2 |
| **G-10** | 🟠 **`ACE_POOL.goal`에 `wg` 추가 효과** (§2-8b (8)) — 리그 1위 공격P가 올라가는지 · **골 축이 안 무너지는지** | 📈 공격포인트왕이 **12~18%** · 골든부츠가 **12~18%를 유지** |
| **G-11** | 🔴 **`shareByWeight`의 `pBig` 사후 재구성**(§2-8b (6))이 들어간 뒤 세 경로의 도움÷골 | 셋의 차 ≤ 3% (현재 **2.36%** — 여유가 좁습니다) |
| **G-12** | ✅ **닫힘** — `ACE_POOL.assist`+`fw`가 노린 대로 작동했습니다(겹침 67% → 74% · 1위 공격P 25.2 → 26.1 · 내 비 1.01 → 0.91). 확정 계수 `NPC_SPOT` **3.05/6.50/4.90** · `POS_AXIS.n` **1.072/0.967/0.873/0.794** |
| **G-13** | ✅ **닫힘 — 유지.** `BUFF_CAP`은 안 물립니다(K1 천장 `g` 합 0.26 = 상한의 65%). 트리거는 부문상이 아니라 **🏅 발롱도르 + 🌏 월드컵 우승이 겹칠 때**예요 (§2-7b) |
| **G-14** | 🟢 **「수상률 × 효과」 재계산** (§2-8b (8c)) — 코드 변경 없음. 넷의 기대 이득만 계산하면 됩니다 | 격차 ≤30% · 📈가 25~45% |
| **G-7** | 🔄 **재정의됨** (§2-8b (8c)) — 세 독립 축은 **수상률 격차 ≤12%p**(현재 **0.7%p** ✅) · 넷 전체는 **「수상률 × 효과」 격차 ≤30%**(현재 **22%** ✅) · 📈 단독 **25~45%**(현재 35.6% ✅) | ✅ **닫힘** |

> **왜 별도인가** — `autoMatch` 한 줄을 고치면 골든부츠 17% → **1%**, 도움÷골 0.777 → **0.847**로
> 세 자리가 함께 움직입니다. *하나만 바꾸고 잰다*를 지키려면 여기서 끊어야 해요.

### 12-5. inspector 검사 목록

> ⚠️ **문턱의 원본은 §3-2 계수표입니다.** inspector 검사가 이 표를 보고 문턱을 잡으니,
> 계수가 확정되면 **표를 먼저 갱신**하세요 — 표가 옛 값이면 검사가 계속 빨간불입니다.
> (검사가 `_t.K`에서 값을 읽어오면 안 되는 것과는 다른 이야기예요 — §10-3.)

| # | 무엇 | 문턱 |
|---|---|---|
| 1 | 리그MVP 곡선 (70/90/110/130/150) | 0~8 / 8~25 / 45~72 / 78~95 / 90~100% |
| 2 | 단조증가 | 70 < 90 < 110 < 130 |
| 3 | 포지션 격차 | ≤ 12%p |
| 4 | `POS_SLOTS` 자리 가중 12칸 | 정확히 1.0000 |
| 5 | **옛 세이브** — `speed` 없는 stats로 `wMe`가 유한 | **NaN 금지** |
| 6 | 골든부츠 단조증가 · 리그 1위 골 | 18~25골 |
| 7 | 시즌 골 꼬리 — 능력치 150 fw 99퍼센타일 | ≤ 35골 |
| 8 | 20시즌 리그 평균 | ±1.0 |
| 9 | **🔒 `AXIS_K` 잠금** — 3.00에서 바뀌면 빨간불 + 이유 문구 | — |
| 10 | **(라′) 중립** — AUTO/CARD 두 모드의 골·도움·차단·실점 기댓값 | 🔴 **±1%** · 능력치 70/90/110/130/150 × 4포지션 × 3카드종류 **전 칸** |
| 11 | **`fatigue`가 골만 센다** — 도움을 세면 결정 카드가 −4% | **변이 검증 필수** |
| 12 | **자동 확률이 능력치를 탄다** — `sc(x)`를 1로 고정하면 수비수 곡선이 무너져야 함 | 격차 > 12%p로 빨간불 |
| 13 | 카드 빈도 — 전 포지션·전 능력치 · **동료 str 50/70/80 세 조건 전부** | ≥ 0.6회/경기 |
| 13b | **이적 페널티** — 같은 능력치에서 동료 str 70 vs 80의 카드 빈도 | 차 ≤ 15% |
| 13c | **`FLOOR_SHARE`가 나에게만** — 경쟁자에게 걸면 리그 1위 골이 무너져야 함 | 변이 검증 |
| 14 | `condMul` 중립 — 시즌 평균 컨디션에서 제곱이 1.000 | ±0.01 |
| 15 | `RATE`의 `b` — 능력치 110 시즌 평균 평점 | 7.0~7.2 |
| 16 | **미니게임 실기기 이벤트** — `pointerdown`→`pointerup`→`click` | 4종 전부 |
| 17 | **🅰️ 도움 두 갈래 대칭** — 내가 전개 주인공일 때 CARD vs AUTO | ±3% |
| 18 | **리그 도움 ÷ 리그 골** | 0.70~0.80 |
| 19 | **`condMul`이 STEP 3에 안 걸린다** — 걸면 컨디션 편차가 세제곱이 됨 | 변이 검증 |
| 20 | **`CLUTCH`가 마지막 카드·1점 차에만** — 카드 수는 안 늘어남 | `N ≤ 8` 불변 |
| 21 | 🧱 **수비 카드의 `miss`가 clamp에 안 잘린다** — `P(miss) = pConcede(me)` | ±1% · 능력치 150 포함 |
| 22 | **`autoP` 중심** — `mid(a)` 표를 되살리면 검사 10번이 빨간불이어야 함 | 변이 검증 |
| 23 | 🔴 **`autoMatch`와 `createMatch`가 전개 장면을 같게 해석** — 주인공은 **만드는 사람**(득점자 아님) | ①-G 게이트 후 |
| 24 | 🔴 **세 부문상의 `내 생산량 ÷ 리그 1위`** | 차 ≤ 0.08 · ①-G 게이트 후 |
| 25 | 🛡️ **철벽상이 약팀 수비수에게 유리하지 않을 것** | 팀 전력 60/70/80에서 수상률 역전 없음 · ①-G 게이트 후 |
| 26 | 🔴 **`raceRank("d")`의 정렬** — 주축 무실점 내림차순 · 동점이면 차단 내림차순. **표시(`RACE_TABS`·`raceValue`)가 판정과 같은 값을 볼 것** | 변이 검증 — `raceValue`를 `r.d`로 되돌리면 빨간불 |
| 27 | 🔴 **리그 6팀이 전부 같은 자** — `createMatch` · `autoMatch` · `shareByWeight` 세 경로의 **도움 ÷ 골** | 셋의 차 ≤ 3% |
| 28 | 🔴 **`posAxis`가 차단을 본다** — `a.defense`를 `a.cs`로 바꾸면 리그MVP 곡선이 밴드를 벗어나야 함 | 변이 검증 |
| 29 | **무실점 ≤ 출전 수** — 나와 경쟁자 전원 | 위반 0명 |
| 30 | 🔴 **한 장면에 한 번만 굴린다** — `autoMatch`를 `chance(pf * pc)`로 되돌리면 **내 클럽과 다른 클럽의 무실점률이 벌어져야** 함 | 변이 검증 · 클럽별 시즌 무실점 편차 ≤ 15% |
| 31 | 🔴 **여섯 클럽이 같은 산식 — 득점만** | 내 클럽이 다른 5팀의 **±15%** 안 (현재 **+0.9%**) |
| 31b | 🚧 **실점·무실점 — 알려진 미달. 현재 값을 상한으로 고정** | ⚠️ **시드 넷으로 재니 −14.83 ~ −16.42%**로 둘이 −15% 밴드 밖입니다. **상한은 −17%로 잡으세요** (관측 최악 −16.42에 여유). 무실점 **+33%**. **`_t.skill = 0.5` 고정 · 시드 넷의 평균**으로 재세요. §2-8b (7) 상자의 2차 항목이 들어가면 31로 합칩니다 |
| 32 | 🟠 **시즌 칭호의 `g`/`a`/`d` 효과가 리그 경기에 닿는다** — 칭호를 달고/떼고 기여가 달라질 것 | 변이 검증 · 실측 `g` +40%에서 시즌 골 **+11.9%** |
| 33 | 🔴 **세 독립 축(🥇🎯🛡️)의 수상률** | 격차 ≤ 12%p |
| 33b | 🔴 **넷의 「수상률 × 효과」** — 📈는 파생 축이라 수상률로 재면 안 됩니다 (§2-8b (8c)) | 격차 ≤ 30% · 📈 단독 25~45% |
| 35 | 🔴 **성장타입 셋의 커리어 총 면적** — 곡선 배수가 아니라 **적분** | ±8% · 셋 다 |
| 36 | 🔴 **은퇴 제안이 세 타입 전부에 뜬다** — 강제 은퇴(38세)로만 끝나는 타입이 없을 것 | 조숙 34 · 보통 36 · 만성 37 근처 |
| 37 | 🔴 **유망주 3장의 총합은 「정점 기준값」에서 같다** — 레이더 표시값은 다를 수 있습니다 | 정확히 동일 |
| 38 | 🔴 **훈련 계단 총량 중립** — `TRAIN_NEUTRAL`을 얹은 뒤 세 타입의 훈련 총량 | ±3%. **변이 검증: 셋을 1.0으로 되돌리면 빨간불**(만성이 46% 더 받아야 함) |
| 39 | 🔴 **면적 중립을 38세까지로 잰다** — 은퇴 제안 나이까지로 재면 안 됩니다 | ±8% (현재 2.8%) |
| 40 | 🔴 **은퇴 제안이 오르막에서 안 뜬다** — 만 19~25세에 은퇴 권유 0건 | **변이 검증: 정점 가드를 빼면 빨간불** |
| 41 | 🔴 **모든 성장타입·포지션 신인이 데뷔 시즌 「38라운드의 30%(11.4경기) 이상」** — **평균으로** 잽니다 | 전 조합의 **평균**. ⚠️ **15경기로 재지 마세요** — 그건 안개·은퇴의 선입니다 (§7-1 ④) |
| 41b | 🔴 **최악 조합의 하한** — 🌳 만성 ⚔️ 공격수 · 강한 데뷔 클럽 | **≥ 7경기.** 8.0이 6.0이 되면 *"버틴다"*가 아니라 *"안 준다"*입니다 |
| 44 | 🔴 **🥈 베스트11이 👑 리그MVP보다 받기 어려워지지 않는다** | 전 능력치에서 베스트11 ≥ 리그MVP. **변이: 상한을 8.5로 올리면 빨간불** |
| 42 | 🟡 **카드 세 장의 나이가 같다**(만 17세) — 시즌 수 편차 0 · **데뷔 만 20세** | 정확히 동일 |
| 43 | 🟡 **🥈 베스트11이 능력치에 단조증가** — 90에서 포화하지 않을 것 | 70 < 90 < 110 < 130 |
| 34 | 🟡 **`BUFF_CAP`이 상한 역할을 한다** — 칭호를 인위적으로 다섯 개 붙이면 합계가 상한에서 잘릴 것 | 잘림 확인 (평소 안 물리는 건 정상) |

**변이 검증**: `SPOT`을 1로 되돌리면 1번이, `sc(x)`를 1로 고정하면 12번이,
`fatigue`가 도움을 세게 하면 11번이 **반드시 빨간불**이어야 합니다.

---

## 13. 폐기 목록 — 읽지 마세요

| 폐기된 것 | 어디 있었나 | 왜 |
|---|---|---|
| **8칸 베르누이 절충안 (다)** | 11번 §C-1 | 범민 님이 (가) 점진 확정으로 확정 |
| **NPC λ 닫힌 형태** + `FAT_ADJ 0.16` | 11번 §1-4 | 분산이 최대 18% 어긋남. 경쟁자도 카드 루프 |
| **balancer (라) 원안의 "다음 카드 `pBig`가 오름"** | 20번 | 원칙 ② — 추상적 수치 관리. 💥 힘 배분·🥵 버티기가 반려된 그 형태 |
| **에이스 계단** `iAmAce ? ACE_W : 1` | `career.js:935` | 축이 70→90에서 2.8배 튀는 주범. `SPOT` 상수로 |
| `splitMine` · `deriveOppGoals` · `holdConceded` | `career.js:886` · `game.js:1832` · `game.js:2114` | 점진 확정이 대체 |
| **12번 §1-3의 카드 빈도 제안표** | 12번 | 전개 카드를 2배 과대평가. §5-2의 실측표가 원본 |
| **12번 §4-1의 `condMul` 세 곳 / 기준 50** | 12번 | 기준점이 80. 거는 곳은 두 곳 |
| **20번 §확정 계수표** | 20번 | 21번 §확정 계수 갱신이 대체 |
| **11번 §1-2의 제안 6계수** | 11번 | `FIN`·`CON`이 역산됨 |
| **`succ`의 `mid(a)` 표** (40→0.35 · 80→0.49 · 120→0.66) | 13번 §2-6 초판 | 중심을 독립으로 두면 카드/자동이 반드시 어긋납니다. **`autoP(me)`가 중심** |
| **"`succ`가 뱉는 확률은 `perfect` 확률"** | 13번 §2-6 초판 | 🧱 수비는 `perfect + ok`(막음)가 그 자리예요 |
| **🅰️ 전개의 `ok` = 동료 골(도움 없음)** | 13번 §2-6 초판 · engineer §2-5 | 골 중립이 깨집니다. `ok`는 무위 |
| **`NPC_SPOT 2.20`** | 12번 §2-2 | 닫힌 형태 기준값. 카드 루프에서는 7.00 |
| **"경기당 개입 2회" 서술** | 10번 §C-3 · 12번 §1-3 | 능력치 130↑에서만 참 |
| **"이기고 있으면 수비 카드가 더 열린다"** | 12번 §1-4 | 산식에 없음. 비율만 이동 |
| **"1-d: `AXIS_K` 재산정"** | 11번 §7 | `AXIS_K`는 리그 격에 곱해집니다. 잠금 |
| **"`matchRating`은 그대로"** | 11번 §1-5 | `RATE`의 `b`에 +10 |
| **(가) 윙어 특화 · `prospect` 이름** | 10번 §D | 범민 님이 (나) 4포지션 · `winger2`로 확정 |

---

## 14. 판단 항목

### 14-1. ✅ 정해진 것 (2026-08-28)

| 항목 | 결정 | 반영된 곳 |
|---|---|---|
| **MOM 비율** | **현행 6% 수준으로 복원.** `MOM_MIN` 절대 문턱을 추가합니다 (제안 7.6) — 전당이 **8종 공유 표**라 v2만 두 배면 같은 순위표를 잡아먹고, 이건 의도한 설계가 아니라 **구조의 부산물**이에요 | §2-8 · §3-2 · §12-1 · 실측 ① |
| **리그 구조** | **1차는 현행 승계(6팀 38라운드). 2차 첫 항목으로 승격 — 20팀 38라운드** | §12-3 (1) |

### 14-2. 남은 판단 — **셋 다 1차 착수를 막지 않습니다**

| # | 항목 | 무엇을 정하나 | 언제 |
|---|---|---|---|
| **1** | **구버전 처리** | v2가 자리 잡으면 ⚽ 더 윙어를 허브에서 내릴지 (🦄 유니콘 전례 — 주소는 살림) | 상용 승격 시점 |
| **2** | 🆕 **리그 부문상이 컵 기록을 봅니다** | `raceTop`이 `act`를 보는데 `cupFinalize`가 `act.goals`·`assists`·`defense`·`apps`를 전부 올립니다(`career.js:2198-2206`) — **"리그 득점왕"이 컵 골로 정해질 수 있어요.** v1(`beta/soccer/`)부터의 구조로 보이고, 네 축을 다 건드리므로 **①-G 밖**입니다. v2에서 고칠지, v1과 함께 별건으로 볼지 | 1차 착수를 막지 않음 |
| **3** | **현행 상용 곡선 수정 시점** | `tests/soccer/curve-test.js`가 **살아 있지 않은 경로**를 재고 있습니다 — 검사는 능력치 130에서 83.3%인데 라이브는 **33.3%**, 포지션 격차는 검사 4.3%p vs 라이브 **51%p**. **현행 게임이 목표 곡선을 한참 벗어난 채 상용에 나가 있습니다.** v2와 별건으로 언제 다룰지 | v2와 무관 |

---

## 관련

- **수치의 원본**: `21_balancer_revised-engine.md` §확정 계수 갱신
- 설계 경위: `10_designer_v2-skeleton.md` → `11_designer_v2-confirmed.md` → `12_designer_v2-revised.md`
- 확정 근거: `00_input/decisions.md` §2차
- 저장소 규칙: `CLAUDE.md` · `.claude/skills/grow-new-game/SKILL.md`
- 볼트: [[분석 — 순위 천장과 성장 곡선]] · [[설계 — 미니게임 확장]] · [[개념집 — 더 윙어]] · [[백로그 — 미룬 결정]]
