# 구현 — ⚽ 더 윙어 II · 🏔️ 계수 한 벌 · 🧒 초1 화면 · 결함 넷 · 🎯 등급 셋

2026-09-02 · grow-engineer · 커밋 `dfeea4c` 기준
입력: `117_designer_school-arc.md` · `118_inspector_one-grid.md` · `119_balancer_floor-arc.md` ·
`121_director_grid-css.md` · `122_designer_hot.md`

> 🔴 **커밋 안 했습니다.**
> 🔴 **`tests/`를 한 줄도 안 건드렸습니다** — 어긋난 자리는 §6에 **적기만** 했어요.
> 🔴 **`beta/winger2/style.css`를 한 글자도 안 건드렸습니다** — director가 잡고 있습니다.
> ⚠️ `beta/winger2/focus.js`는 **추적도 참조도 안 되는 남의 고아 파일**입니다(어디서도 안 싣습니다).
>    손대지도, 커밋에 넣지도 않았어요.

---

## 0. 판정 요약

| | |
|---|---|
| **①** | ✅ **`CELL_FLOOR 0.36` + `ONE_WIN 23`을 한 벌로 넣었습니다.** 🔬 **제 하네스로 다시 재서 balancer 표를 독립 확인**했어요 — 「능숙」이 소수점 셋째 자리까지 일치합니다 (§1) |
| **②** | 🚨 **「보통」 절대값은 계약이 될 수 없습니다 — 감도로 못 박았습니다.** 조작자를 **상용 5칸에서 0.572로 고정**해 놓고 「오차가 어디서 오나」만 바꿨더니 짝지은 차이가 **+0.046 ↔ +0.006으로 8배** 갈립니다. 🔑 **그런데 어느 구석에서도 `0.36/23`이 `0.30/22`를 절반으로 줄입니다** — **모델에 안 흔들리는 것은 「개선폭」**이에요 (§1-3) |
| **③** | ✅ **🧒 초1 화면 하나를 지었습니다.** 초2·초3은 **안 지었습니다.** `CHILD_FOCUS` 2+2+2 · 겹침 0 · 합집합 6칸을 **실측으로 확인**했고, 총합 194 · `overall()` 32.333이 **32,000굴림에서 한 값**입니다 (§2) |
| **④** | 🔴 **⓷ 「↩️ 뒤로 → 다시 고르기」는 `beta/` 결함이 아니었습니다.** 실기기 경로(↩️ 뒤로 → 🏟️ 유스 고르기)는 **6/6 서로 다른 배분**을 굴립니다. 죽어 있던 것은 **검사의 옛 길**(🎯 자리 카드 누르기)이고, 그 길은 **조립대로 안 돌아옵니다** (§3-3) |
| **⑤** | ✅ **⓸ 상대가 득점합니다** — 🅰️ 전개의 끊긴 패스를 역습 실점으로. 되돌려 확인: **30아크 중 0 → 14** (§3-4). 🟡 **연출 판정이라 designer 확인이 필요합니다** |
| **⑥** | 🟢 **♿ `wideOn()`에 결함이 없습니다.** 체크박스 → `localStorage` → `winMul` **×1.3000**까지 끝까지 삽니다. 「환경이 우연히 막아 줌」이 맞았고 코드는 멀쩡했어요 (§4) |
| **⑦** | ✅ **📈 `rise` 제거(묘비 포함) · ✅ `in` 신설 · `TIER_S` 한 주인 · 🪦 `ONE.post` 제거 · 🏷️ `w2m-k-{kind}`** (§5) |
| **⑧** | 🔴 **어긋난 검사 7곳** — 전부 **흐름·상수·변이 정규식이 옛 소스를 가리키는 것**입니다. 고칠 자리를 파일·줄·문장까지 적었습니다 (§6) |
| **⑨** | 🚨 **검사의 빈 자리 둘** — `w2m-cell-in`을 보는 검사가 **0곳**이고, 🧒 초1 화면을 보는 검사도 **0곳**입니다 (§7) |

---

# 1. 🏔️ 계수 — 한 벌로 넣었습니다

```
beta/winger-moment.js   CELL_FLOOR  0.30 → 0.36
beta/winger-moment.js   ONE_WIN     22   → 23
```

🔒 **두 주석을 다시 썼습니다**(원칙 ⑨) — 옛 주석은 *"6칸으로 가면서 `ONE_WIN`이 23 → 22.5로
따라 움직였어요"*라고 **옛 규칙을 말하고** 있었습니다. 지금은 양쪽에 🚨 **「한 벌」 절**이 있고,
`ONE_WIN` 쪽에는 **왜 한 번 22로 내렸다가 돌아왔는지**까지 적었어요.

## 1-1. 🔬 제 하네스 — 다시 지었습니다

engineer 하네스가 사라졌다는 지적이 맞습니다. **처음부터 다시 지었어요.**

| | |
|---|---|
| **판 A** | 커밋 `082018b`의 `beta/winger-moment.js`를 git에서 그대로 — 옛 5칸 · 고정 키퍼 · **상용에 나간 판** |
| **판 B** | 지금 디스크 — 6칸 · 미끄러지는 키퍼 |
| **산식** | 🔒 **베껴 적지 않았습니다.** 두 소스를 통째로 `new Function`에 실어 `_t`(`oneRoll`·`oneAt`·`oneErr`·`sOne`·`winMul`·`K`)를 **그 파일에서** 꺼내 씁니다 |
| **변이** | `[\d.]+`로 뜯습니다(값을 안 박음). 🔒 **디스크와 같은 값이면 변이를 안 걸고**, 대신 **실린 `K`를 되읽어** 요청한 계수인지 대조합니다 — 그게 진짜 검산이에요 |
| **난수원** | 🔒 **셋을 가릅니다** — 무대 `seed ^ 0x85EBCA6B` · 조작자 `^ 0x9E3779B9` · 상황 `^ 0xC2B2AE35` |
| **짝지음** | 🔒 조작자 잡음을 **판마다 고정 개수로 미리** 뽑습니다 — 칸 수(5 ↔ 6)로 소비량이 달라지면 두 판의 난수 열이 밀려 **짝지은 차이가 잡음이 아니라 편향**이 됩니다 |
| **표본** | 시드 3벌 × **20,000판** × 조작자 3벌 |
| **조작자 재적합** | 셋 다 **판 A에서 서툰 0.406 / 보통 0.572 / 능숙 0.677**이 나오게 이분법으로 맞췄습니다 (절대값은 계약이 아니라 **자리표**예요) |

## 1-2. 📐 모델이 필요 없는 자 — **먼저 맞습니다**

```
s 천장 = 1 − (칸폭 × CELL_FLOOR) ÷ ONE_WIN        격자 폭 = 칸폭 × (0.5 − CELL_FLOOR) ÷ ONE_WIN
```

| 판 | s 천장 | 상용 대비 | 격자 폭 |
|---|---|---|---|
| **A 옛 5칸 (상용)** | **0.7391** | — | 0.1739 (100%) |
| 0.30 / 22 (개편 직후) | 0.7727 | +0.0336 | 0.1515 (87%) |
| 0.30 / 23 (창만) | 0.7826 | +0.0435 | 0.1449 (83%) |
| 0.36 / 22 (바닥만) | 0.7273 | **−0.0119** 🔴 | 0.1061 (61%) |
| ⭐ **0.36 / 23 (디스크)** | **0.7391** | **±0.0000** ✅ | 0.1014 (58%) |

`1 − 6/23 = 0.73913`. **시뮬레이션이 필요 없습니다.**

## 1-3. 📊 짝지은 E[s] 차이 (새 − 옛) — 시드 3벌 × 20,000판 × 조작자 3벌

**「능숙」** — 🔑 **balancer 표와 소수점 셋째 자리까지 같습니다**

| floor / win | 모델 A | 모델 B | 모델 C | balancer(119) |
|---|---|---|---|---|
| 0.30 / 22 | +0.0678 | +0.0684 | +0.0678 | +0.067 / +0.066 / +0.064 |
| 0.30 / 23 | +0.0790 🔴 | +0.0795 🔴 | +0.0790 🔴 | +0.078 / +0.077 / +0.075 |
| 0.36 / 22 | +0.0166 | +0.0172 | +0.0166 | +0.015 / +0.017 / +0.013 |
| ⭐ **0.36 / 23** | **+0.0300** | **+0.0306** | **+0.0300** | +0.029 / +0.030 / +0.026 |

**「서툰」**: 0.36/23에서 +0.0017 / +0.0197 / +0.0124 — 셋 다 얌전합니다.

**「보통」**: 0.36/23에서 **+0.0633 / +0.0456 / +0.0701** — 🔴 **balancer의 −0.003 ~ −0.009와 다릅니다.**

### 🚨 그 차이의 정체를 감도로 못 박았습니다 — **「보통」은 계약이 될 수 없습니다**

조작자를 **판 A에서 정확히 0.572**로 붙들어 놓고, **「오차가 어디서 오나」만** 바꿔 봤습니다:

| 「보통」을 어디서 만드나 | 손떨림 σ | 판A | 옛 `0.30/22` | ⭐ `0.36/23` |
|---|---|---|---|---|
| ⏱️ 시각이 흔들림 (손은 정확) | 4.76% | 0.5710 | +0.0751 | **+0.0456** |
| 🤝 반반 | 6.52% | 0.5718 | +0.0442 | **+0.0211** |
| 👆 손이 흔들림 (시각은 능숙) | 7.50% | 0.5715 | +0.0248 | **+0.0055** |
| 👆👆 손만 흔들림 (즉시 누름) | 7.02% | 0.5718 | +0.0338 | **+0.0127** |

| | |
|---|---|
| 🚨 | **같은 「보통」인데 짝지은 차이가 +0.046 ↔ +0.006 — 계약(≤0.005)의 8배 폭으로 갈립니다.** 🔑 **balancer의 진단이 맞았고, 그 크기를 독립으로 확인했습니다** |
| ✅ | 🔑 **그런데 네 구석 전부에서 `0.36/23`이 `0.30/22`를 절반 안팎으로 줄입니다** (0.075→0.046 · 0.044→0.021 · 0.025→0.006 · 0.034→0.013). **모델에 안 흔들리는 것은 「개선폭」**이에요 |
| 🔒 | 그래서 소스 주석에 **절대값을 계약으로 적지 않았습니다.** 적은 것은 **차이의 범위**와 **모델이 필요 없는 천장**입니다 |
| 🟡 | 🔴 **「보통 ≤ 0.005」를 계약으로 유지할지 designer/balancer 판정이 필요합니다.** 제 하네스로는 **어떤 짝도** 세 모델 전부에서 그 안에 못 들어옵니다 |

## 1-4. 🟡 치르는 값 — **격자 폭이 5칸의 58%**

`CELL_FLOOR`를 올리는 건 「어느 칸을 눌러도 비슷해지는」 쪽입니다.
🔴 **주석에 「0.36이 상한이라고 보세요」를 박아 뒀습니다** — 더 올리면 격자가 버튼 하나가 됩니다.
📮 범민 님이 *"아무 칸이나 눌러도 비슷한 것 같다"*고 하시면 **여기가 그 자리**예요(122번 §8).

---

# 2. 🧒 초1 화면 — **하나만** 지었습니다

## 2-1. 흐름

```
✏️ 이름 → 🦶 주발 → 🗺️ 지도 → 🧒 초1 → 🏫 초등부(2장) → 📨 조기 → 🎯 자리 → 🏫 중등(3) → 🏫 고등(3)
                              └── 🆕 여기 하나
```

🔴 **초2·초3은 안 지었습니다.** 소스 주석에 **왜 안 짓는지**를 박았습니다 —
`spread()`가 `focus`를 `indexOf`로 봐서 **같은 걸 또 고르면 값이 정확히 0**이라
화면만 늘고 축은 안 늘어요. 되살리려면 **「같은 걸 두 번 고르면 달라지는」 구조가 먼저**입니다.

### ⚠️ 제가 채운 빈칸 하나 — **designer 확인 부탁드립니다**

조율자님 화면 순서는 `🗺️ 지도 → 🧒 초1 → 🎯 자리 → 🏫 대항전`인데,
**⚽ 동네 카드가 그 사슬에 안 적혀** 있었습니다. 제가 읽은 방식:

| | |
|---|---|
| 🔑 | **지금 「🏫 초등부」 단계가 곧 「동네」입니다.** 그 단계의 문구가 이미 *"공터에 애들이 다 모였어요 · **자리도 없이** 다 같이 공만 쫓아다녀요"*라 **그대로 씁니다** |
| 🔑 | **🎯 자리는 이미 초등부 「뒤」·중등 「앞」**입니다(`town.js` `next: "🎯 자리를 정해요"`). 「대항전 앞으로」가 **이미 그 상태**예요 — `STAGES`를 **한 칸도 안 옮겼습니다** |
| ✅ | 이 읽기라야 *"자리도 없이"*라는 문구가 안 거짓말이 되고, **첫 카드가 초1 바로 뒤**에 와서 ≤ 50초가 섭니다 |
| 🔴 | **다르게 의도하셨다면 여기가 고칠 자리입니다** — `STAGES` 순서와 `goPosition` 호출 위치 둘뿐이에요 |

## 2-2. 무엇을 어디에 넣었나

| 파일 | 무엇 |
|---|---|
| `beta/winger2/prospect.js` | 🆕 `CHILD_FOCUS` (3택 × 2칸) · `childFocus(picks)` · `childOf(who)` |
| ″ | `rollShape(marketId, pos, childPicks)` — 🔒 **`YOUTH_FOCUS`와 「같은 배열」에 concat.** `rollBuild`·`rollPair`도 같이 |
| ″ | 🧬 조립대 머리 한 줄 — *"🧒 ⚽ 공을 많이 찬 아이라 몇 칸이 먼저 자랐어요."* (§5-2 갚는 법 ②) |
| `beta/winger2/intro.js` | 🆕 `openChild(cur, done)` · `CHILD_PICKS`(글) · `STEPS` 한 줄 (5 → **6**) |
| `beta/winger2/index.html` | 🆕 `<section id="screen-child">` |
| `beta/winger2/game.js` | 🆕 `goChild()` · `chosenChild` · `S.childPicks` · `btn-back-child` · `BACK_SAFE` |

🔒 **`engine.js`·`POOL`·`evenStats`·`REROLL_MAX`·`PEER_REF`·`YOUTH_CARD_P`·`BAND_EDGE`·`OFFER`
·`SHAKE`·`FIT` — 한 줄도 안 건드렸습니다.**

## 2-3. 🔒 중립을 **실측으로** 확인했습니다

| 재는 것 | 결과 |
|---|---|
| 합집합 · 겹침 | **6칸 · 겹침 0** · 여섯 칸을 전부 덮음 ✅ |
| `sum(stats) === POOL` | 4갈래 × 5시장 × 4포지션 × 400 = **32,000굴림 중 어긋남 0** ✅ |
| `overall()` (= 총합 ÷ 6) | 서로 다른 값 **1개** — `32.333333` ✅ (실측이 아니라 `POOL` 고정의 따름정리) |
| 눈으로 보이는 차이 | 같은 시드에서 `ball`→dribble 38 · `body`→defense 24→33 · `eye`→speed 48 ✅ |

## 2-4. ⚠️ 제가 내린 판단 둘 — **되돌리기 쉽게 해 뒀습니다**

| | 무엇 | 왜 · 어떻게 되돌리나 |
|---|---|---|
| **①** | **고른 뒤 620ms 머물렀다 넘어갑니다** (`intro.js` `ECHO_MS`) | 🔑 [다음]이 없으면 **「선택 직후 그 자리에 한 줄」**(§5-2 갚는 법 ① · 원칙 ③)을 볼 시간이 **0**입니다. 🔴 초읽기가 아니에요 — 되돌릴 시간이 아니라 **방금 고른 것을 읽는** 시간이고, 움직이는 것도 남은 시간 표시도 없습니다. 값은 미니게임 결과가 머무는 **620ms와 같은 값**으로 맞췄어요. 🔁 되돌리려면 `ECHO_MS = 0` |
| **②** | **`S.childPicks`는 길이 1짜리 「배열」** | 🔑 설계가 초2·초3을 열어 둔 자리라(117번 §9-1) **나중에 해가 늘어도 세이브 스키마가 안 바뀝니다.** 스칼라로 두면 그때 마이그레이션이 필요해져요 |

🔒 **읽는 쪽 기본값은 `[]`**이고, 빈 배열이면 기여가 **정확히 0**이라 **옛 세이브가 개편 전과 똑같이** 굽니다.
🔒 **원칙 ⑥을 지켰습니다** — 화면 교체는 `click`에서만. `gate` 하나로 머무는 620ms 동안 두 번째 탭을 삼킵니다.
🎨 **director께** — 새 클래스는 `.child-place` · `.child-story` · `.child-echo` ·
`#child-list .card`입니다. 카드는 기존 `.card-grid`/`.card`라 **CSS 없이도 그대로 섭니다.**

---

# 3. 🔴 결함 넷

## 3-1. ⓵ 확인 페이지 — **고쳤습니다** (유일한 빨간불이었어요)

`beta/_check.html` — `W2M_LIST` 네 줄 → **두 줄**(🥅 일대일 슈팅 · ⚡ 컷백 연결) ·
「4종」 다섯 자리 → 「2종」. ⚠️ **공유 파일이라 이 일곱 줄로 끝냈습니다.**
✅ `check-w2m-test.js` **빨간불 1건 → 전부 통과** (되돌리면 다시 빨간불인 것을 고치기 전에 봤습니다).

## 3-2. ⓶ 빈 유스 상자 — **방어 겹침을 없앴습니다**

🔴 **`kind === "defend"`를 두 곳에 적는 것 자체가 문제**였습니다 — 한쪽을 지워도 증상이 0장이라
**검사가 통째로 아무것도 못 지킵니다.** 판단의 **주인을 하나**로 세웠어요:

```
beta/winger-moment.js   const opens = (kind) => kind !== "defend";   // 🆕 내보냅니다
beta/winger2/game.js    if (autoMiniOn() || (M.opens && !M.opens(kind))) { … }
```

✅ 확인: `opens` false로 부르면 **상자 조각 0 · 판정 그대로**, 그리고 game.js가 **상자를 비우기 전에** 물어요.

## 3-3. 🔴 ⓷ — **`beta/` 결함이 아니었습니다. 안 고쳤습니다**

실기기 경로를 게임 입구로 실제로 걸어 봤습니다 (↩️ 뒤로 → 🏟️ 유스 고르기 → 🧬 조립대 · 6회):

```
서로 다른 배분 6 / 6 · 서로 다른 잠재력 6 / 6
```

🔑 **`btn-back-prospect` → `showOffers()` → 유스 카드 → `openBench()` → `WingerProspect.open()`**이고,
`open()`은 **`rollTalents`·`rollBuild`를 매번 새로 부릅니다.** 잘 돕니다.

🔴 **죽어 있던 것은 검사의 옛 길입니다** — 옛 `drawProbe`는 뒤로 간 다음
**`[data-pos]`(🎯 자리 카드)**를 눌렀는데, 그 핸들러는 `goMiddle()` → 이미 뛴 단계라
**`showOffers()`로 흘러가 조립대를 다시 안 엽니다.** 굴림이 안 일어나니 `build`가 같은 게 **맞습니다.**
그리고 **사람은 그 버튼을 누를 수 없습니다** — `#position-list`는 그때 감춰진 화면이에요.

> ✅ inspector가 이미 🎲 버튼을 누르게 고쳤고, 그게 **옳은 고침**입니다.
> 🔒 `index.html:163`이 *"「← 자리 다시 고르기」가 없습니다 — 그 경로가 **구조적으로** 없어서예요"*라고
> 적어 둔 그대로예요. **`prospect.js`를 이 건으로 고치지 않았습니다.**

## 3-4. ⓸ 상대가 득점합니다 — 🟡 **연출 판정이라 designer 확인이 필요합니다**

```
beta/winger2/town.js   GOAL_BY.a.miss = "them"      // 🅰️ 끊긴 패스 → 역습 실점
                       CALL.a.miss    = ["concede", "😣 패스가 끊겨 역습… 실점"]
```

| | |
|---|---|
| 🔒 | **새 굴림이 0입니다** — 이미 나온 판정 `r`을 읽기만 합니다. 🔬 확인: 아크 전체 `Math.random` 호출 수 **16 → 16 (불변)** |
| 🔒 | **`PTS`·편차 `d`·`spotMul`에 한 톨도 안 닿습니다** — 스코어는 화면에만 갑니다 |
| 🔑 | 살아 있는 판정 중 **실점으로 읽히는 것은 「패스가 끊겼다」 하나**예요. *"발 끝에 안 걸렸어요"*(슛 실패)는 실점이 아니라 **`g.miss`는 안 열었습니다** |
| ✅ | **되돌려 확인**: 고등부 최종에서 상대가 득점한 아크 **30 중 14** ↔ 되돌리면 **30 중 0** |
| 🔴 | **되돌리는 법은 그 칸 하나를 지우는 것**입니다. 🧱 수비 격자가 돌아오면 `d.miss`가 다시 사니, **그때 여기를 지울지 designer가 판정**하세요 |

---

# 4. 🟢 ♿ `wideOn()` — **결함이 없습니다**

inspector의 진단(「환경이 우연히 막아 줌」)이 맞고, **코드 쪽은 멀쩡합니다.** 끝까지 걸어 봤어요:

| 재는 것 | 결과 |
|---|---|
| `winMul(80,1)` 기본 ↔ ♿ 켬 | 1.0000 → **1.3000** (비 **1.3000** = `WIDE`) ✅ |
| winger2 페이지의 `#wide-judge` | 있음 · `DOMContentLoaded` 뒤 `onchange` **붙음** ✅ |
| 체크 → `localStorage` → `winMul` | `"1"` 저장 · 비 **1.3000** ✅ |

🔑 **`readyState`가 `complete`가 되기 전에 읽으면 `onchange`가 `null`로 보입니다** — 제가 처음
그렇게 재서 «안 붙었다»고 볼 뻔했어요. 📮 **검사를 쓰실 때 한 틱 기다리세요.**

---

# 5. 🎯 등급 셋 — `rise` 묘비 · `in` 신설 (122번)

| 자리 | 무엇 |
|---|---|
| 🔗 `TIER_S = { perfect: 0.75, in: 0 }` | 🆕 **화면 등급의 유일한 주인.** `TIER`·`paint()`·판 끝 문구(`end`) **셋이 전부 여기를 읽습니다** |
| 🎯 `paint()` | `hot` / `in` **배타 3상태**. 🔒 문턱을 새로 안 적었습니다 |
| 🪦 `rise` | **지웠습니다 + 묘비 주석** — *"되살아나는 건 클래스가 아니라 「기다려」라는 거짓말"*. `slide < cov1−cov0` 부등호를 근거로 박았어요 |
| 🪦 `ONE.post` | **상수·기둥 두 줄 다 지웠습니다** (122번 §7-4) |
| 🏷️ `w2m-k-{kind}` | `box(container, "w2m-oneone w2m-k-" + ctx.kind, …)` — 📮 **director의 `[aria-label^="문전"]` 임시 배선을 걷어내셔도 됩니다** |
| 🗣️ 낱말 | `why`·`keys[0].desc`를 **테두리/◎ 기준**으로 (goal · assist 둘 다) |

## 5-1. 🔬 채택 조건 — **배선 검산만** 했습니다 (판정은 balancer)

designer 조건: **0.5초 시점 `in` 칸 중앙값 2~4개**. 소스의 `_t`로 4,000판:

| 초 | `in` 중앙 | `in` 평균 | `hot` 중앙 | `hot` 평균 | 0점 중앙 | `in`이 2~4칸인 판 |
|---|---|---|---|---|---|---|
| 0.00 | 2 | 2.04 | 1 | 0.51 | 3 | 100.0% |
| 0.25 | 2 | 2.00 | 1 | 0.51 | 3 | 99.8% |
| ⭐ **0.50** | **2** | **1.99** | 1 | 0.51 | 3 | **99.4%** |
| 1.00 | 2 | 2.40 | 0 | 0.07 | 4 | 97.2% |
| 1.50 | 2 | 1.97 | 0 | 0.00 | 4 | 93.2% |
| 2.40 | 2 | 1.69 | 0 | 0.00 | 4 | 69.5% |

| | |
|---|---|
| 🟡 | **조건 안이지만 「바닥」에 붙어 있습니다** (중앙 2 · 평균 1.99). designer §2-6의 *"늘 1~2칸이면 사실상 argmax"* 쪽 경계예요 |
| 🟢 | 다만 `hot`이 절반쯤 뜨는 초반에는 **테두리 2 + 링 1**이라 «어느 게 최선인가»가 여전히 남습니다 |
| ✅ | ⏱️ **시간 축이 그림이 됩니다** — `hot`이 0.51 → **0**으로 사라지고 0점 칸이 3 → 4로 늡니다 |
| 🔴 | 🚨 **이건 「제 하네스의 확인」이지 판정이 아닙니다.** **balancer가 판 단위로** 다시 재 주세요 — 조율자님 말씀대로 director의 7.8~17.5%는 **프레임 비율**이었습니다 |

---

# 6. 🔴 어긋난 검사 **일곱 곳** — **고치지 않았습니다. 적기만 합니다**

> 🔑 **일곱 다 「검사가 옛 소스를 가리키는」 것**이고, 문턱이 틀린 곳은 **한 곳도 없습니다.**

| # | 파일 | 자리 | 무엇을 |
|---|---|---|---|
| **1** | `tests/winger2/moment-test.js` | **D-1** 상수표 | `ONE_WIN 22 → 23` · `CELL_FLOOR 0.30 → 0.36` · 🆕 **`"ONE.post": 3.5` 줄을 지우기**(상수가 사라졌습니다) |
| **2** | ″ | **D-2a 변이** | `FLOOR0: /const CELL_FLOOR = 0\.30;/` → `[\d.]+`로 |
| **3** | `tests/winger2/one-grid-test.js` | 💥 **죽습니다** · `M9_LOOK_LONG` | `/look: \d+,/` → **`/look: \d+/`**. 🔑 `ONE.post`가 빠지며 `look`이 **마지막 키가 되어 쉼표가 사라졌습니다.** ⚠️ 이걸 먼저 고쳐야 아래가 보여요 |
| **4** | ″ | `const FLOOR_R = 0.30;` | → **0.36** (inspector 118번 §7이 예고한 자리) |
| **5** | ″ | 상태 클래스 목록 (`:639` · `:897` · `:924`) | `hot` · `soon` · ~~`rise`~~ → **`hot` · `soon` · `in`** |
| **6** | `tests/winger2/school-scene-test.js` | **FLIP 변이 정규식** | `GOAL_BY` 리터럴을 통째로 박고 있어 **안 걸립니다**. `a: \{ perfect: "us"(, miss: "them")? \}` 꼴로 |
| **7** | `tests/winger2/youth-moment-test.js` | **M5_DEFEND_OPENS** | `/if \(autoMiniOn\(\) \|\| kind === "defend"\)/` → 이제 **`M.opens(kind)`**입니다 |
| **8** | `tests/winger2/creation-test.js` | C-0 · C-3 · C-1/C-2 문구 | 만들기 화면 **5 → 6**(`screen-child`가 `screen-origin`과 `screen-position` **사이**). 분모 5 → **6** |
| **9** | ″ | `M_STEPS3` · `M_PICK1` · `M_PICKCOND` · `M_PICKBIG` | `STEPS` 리터럴 · `rollShape(market.id, pos)` · `rollPair(draw.market.id, draw.pos)` 가 **셋째 인자로 바뀌었습니다** |
| **10** | `tests/winger2/foot-next-test.js` | **N-8** *"탭 6번"* | 🧒 초1 탭 하나가 늘어 **7번**입니다 |
| **11** | `tests/winger2/_load.js` | **`passArc`** | 🧒 초1을 지나야 합니다. 🔴 **이게 `school-test`·`town-test` 💥의 뿌리**예요 |
| **12** | `tests/winger2/offer-test.js` | **자체 드라이버** | 🤝 승낙 갈래를 따로 몰고 가는 길이라 여기도 초1에서 멈춥니다 — 출력에 **`→ screen-child`**로 찍혀요 |

**11번에 넣을 것** (`pickOrigin` 바로 뒤 · `passStage` 앞):

```js
  /* 🧒 초1 — [다음]이 없습니다. 탭 하나가 고르기 겸 넘김이고, 고른 뒤 `ECHO_MS`만큼
   *    머물렀다 넘어가요(방금 고른 것을 읽는 시간 · 초읽기가 아닙니다).
   * 🔴 **탭 횟수를 세세요** — 안 누르고도 지나가면 그게 「자가 복구가 실패를 삼킴」입니다. */
  if (cur() === "screen-child") {
    press(D.querySelector(`#child-list .card[data-child="${o.child || "ball"}"]`), "🧒 초1");
    await waitFor(() => cur() !== "screen-child");   // 🔒 시간을 박지 말고 「화면이 바뀔 때까지」
  }
  mark();
```

⚠️ `passArc`가 이미 `async`라 `await`이 들어갑니다. `passTown`(동기)에는 **안 넣어도 됩니다** —
그건 🎯 자리 카드를 곧바로 눌러 앞단을 건너뛰는 길이라 초1을 안 지납니다.

---

# 7. 🚨 검사의 빈 자리 — **제가 만들지 않았습니다** (변이 검증 없는 검사가 곧 초록불 함정이라)

| | 지금 보는 검사 | 무엇이 안 잡히나 |
|---|---|---|
| 🎯 **`w2m-cell-in`** | **0곳** | 배타(`hot`과 겹침) · 문턱이 `TIER_S`에서 오는가 · **0점 칸에 안 붙는가.** 🔑 `in` 토글을 통째로 지워도 **아무도 안 웁니다** (되돌려 봤습니다) |
| 🧒 **초1 화면** | **0곳** | 카드 3장 · **탭 횟수 > 0** · `S.childPicks` 기본값 `[]` · `CHILD_FOCUS` 합집합 6 겹침 0 · 3택 × 5시장 × 4포지션에서 `sum === POOL` |
| 🏟️ **⓸ 실점 경로** | **0곳** | *"학교 경기에서 상대가 득점할 수 있는가"*. 🔑 `school-scene-test`의 FLIP은 **변이**라 이 계약을 안 봅니다 |
| 🔗 **`TIER_S` 한 주인** | **0곳** | `paint()`·`end()`가 **같은 자**를 쓰는가. 둘이 갈라지면 «완벽»이라 그려 놓고 «스쳤어요»라고 말합니다 |

📮 **inspector께** — 117번 §11이 적은 구조 검사(합집합·겹침·`POOL`·`overall()`·단계별 카드 수)를
그대로 세우시면 위 둘째 줄이 닫힙니다. 🔴 **변이 셋**(`CHILD_FOCUS` 한 칸을 겹치게 · `STAGES[0].n` ·
`S.childPicks` 기본값을 `["ball"]`로)이 **전부 빨간불이어야** 합니다.

---

# 8. 📋 지금 상태

| | |
|---|---|
| ✅ 고침 | 계수 한 벌 · 🧒 초1 · ⓵⓶⓸ · `rise`/`in`/`TIER_S`/`ONE.post`/`w2m-k-` |
| 🔴 안 고침 | **⓷ (결함이 아니었습니다 · §3-3)** |
| 🔒 안 건드림 | `engine.js` · `style.css`(director) · `tests/`(inspector) · `focus.js`(남의 고아 파일) |
| 🟢 초록불 유지 | `smoke-test`(9종) · `check-page-test` · `check-w2m-test`(🔴→🟢) |

## 8-1. 📋 `tests/winger2/` 27종 — **종료 코드로 갈랐습니다**

| | 수 | 어느 것 |
|---|---|---|
| ✅ 통과 | **18** | award · bench · check-w2m · engine · foot-map · grade · league · minigame-tap · mutation · neutral · prospect · raf · seed-split · town-neutral · wiring · worldcup · youth-ability · youth-card · youth-clamp |
| ❌ 빨간불 | **7** | creation · foot-next · moment · offer · school-scene · school · youth-moment |
| 💥 죽음 | **2** | one-grid · town |

🔑 **아홉 전부 §6의 열둘로 설명됩니다 — 문턱이 틀린 곳은 한 곳도 없어요.**
🔴 **`one-grid`를 먼저 고치세요** — `M9_LOOK_LONG`이 죽여서 그 뒤(FLOOR_R·상태 클래스)가 **아예 안 보입니다.**
🔴 그리고 **`_load.js`의 `passArc` 한 곳이 `school`·`town`·(간접적으로) `school-scene`을 같이 엽니다.**

