# 설계 — ⚽ 더 윙어 II · 🧱 차단 재설계 (2단 국면)

2026-09-01 · grow-designer
계기: 범민 님 — *"미니게임 별로고"* / *"**유스는 아직 안 해봤어.** 너가 판단해서 젤 최선으로 해놔줘봐"*
입력: `101_designer_feedback.md`(§2 가설·S-2′) · `50_engineer_minigames.md`(숙련도별 `E[s]`) ·
`93_designer_school-arc.md` §18-5(난수 결합) · `beta/winger-moment.js`(읽기만) · `beta/winger2/engine.js`(읽기만)

> ⚠️ **`beta/winger2/`·`beta/winger-moment.js`를 한 줄도 안 건드렸습니다.** 읽기만 했어요.

---

## 0. 판정 요약

| | |
|---|---|
| **①** | ✅ **S-2′(차단만) 동의합니다. 다만 근거를 하나 바꿉니다** — *"싸니까 하나만"*이 아니라 🔑 **"하나만 고쳐야 다음 질문이 성립한다"**입니다 (§6) |
| **②** | 🚨 **진단이 「이산이라 좁다」가 아닙니다.** 🔴 **조작과 결과 사이에 난수가 한 겹 더 있습니다** — **완벽히 읽어도 58%만 맞습니다.** 나머지 셋은 조작→결과가 직결이에요 (§1) |
| **③** | ★ **2단 국면.** 1단계 **읽기(지금 그대로 · 정지 · 무제한)** → 2단계 **몸을 던지는 타이밍**. 🔑 **읽기가 판정 창을 정합니다** — 컷인의 *"주발 쪽 칸이 더 넓어요"*와 **같은 문법** (§2) |
| **④** | 🔒 **3택 읽기를 살립니다.** 겹쳐 읽기(부등호가 양쪽에서 뒤집힘)는 **좋은 설계**예요. 🔴 버리는 건 읽기가 아니라 **「읽기와 결과 사이의 난수 한 겹」**입니다 (§3) |
| **⑤** | 🎯 **숙련도 폭 8~12%p** — 넷 중 **가장 좁은 쪽**으로 겨눕니다. 🧱은 df가 **가장 자주 보는 카드**라 같은 폭이면 포지션 형평이 깨집니다 (§4) |
| **⑥** | 🔑 **실측 하네스 — `E[s]` 표는 다시 안 재도 됩니다.** `winger-moment.js`는 엔진 `_rng`를 **한 번도 안 지납니다**(§5). 🔴 **다시 재야 하는 건 「짝지은 비교(CRN)」이고, 그중 §18-1의 +0.166%p가 가장 위험합니다** |
| **레인** | 🔴 **직렬**(`E[s]` 평균이 곡선에 닿음). 🟢 **다만 ③(`W2Scene`)과는 파일도 축도 안 겹쳐 동시에 가도 됩니다** |

---

## 1. 🚨 진단 — **문제는 이산이 아니라 「난수 한 겹」입니다**

### 1-1. 제가 §101에서 적은 진단을 한 단계 더 파고들었습니다

§101에서 *"`s`가 3단 이산이라 폭이 0.05"*라고 적었는데, **이산은 증상이지 원인이 아닙니다.**

`winger-moment.js:501-585`을 읽고 원인을 찾았습니다:

```
읽기(내 조작)  →  🎲 tell 의 신뢰도  →  맞음/틀림  →  s = 1 / 0.24 / 0
                  ↑ 여기가 문제입니다
```

| 세기 | 최적 전략 | **그 전략이 맞을 확률** |
|---|---|---|
| 확실히 틀었어요 (`hi`) | 몸 방향을 따라감 | `tellHi` = **0.58** |
| 살짝 흔들었어요 (`lo`) | 🦶 주발 쪽 | `favor(1−tellLo)/2` = **0.608** |

> 🔴 **완벽하게 읽어도 60%만 맞습니다. 나머지 40%는 `s = 0.24` 또는 `0`입니다.**
> 🔑 **그래서 「잘 읽은 판」과 「대충 찍은 판」이 결과에서 구분이 안 됩니다.**
> 소스가 적어 둔 *"순수 찍기 0.46 ~ 완벽한 읽기 0.60"*이 정확히 그 뜻이에요 —
> **읽기를 완벽하게 해도 얻는 게 0.14뿐**입니다.

### 1-2. 나머지 셋과 무엇이 다른가 — **난수의 자리**

| | 조작 → 결과 사이 |
|---|---|
| 🏃 컷인 | 갭 위치를 정확히 맞추면 🔑 **`s = 1`이 결정적으로** 나옵니다 |
| 🥅 마무리 | 빈 곳을 정확히 노리면 **결정적** |
| 🎯 킬패스 | 라인을 정확히 맞추면 **결정적** |
| 🧱 **차단** | 🔴 **잘 읽어도 42%는 실패합니다** |

**셋은 「난수가 판을 만들고, 조작이 결과를 만듭니다」.**
**차단은 「난수가 판을 만들고, 조작을 거친 뒤 난수가 결과를 다시 만듭니다」.**

> 🔑 **이건 원칙 ③입니다** — *"통제할 수 없는 효과는 영향이 아니라 노이즈로 읽힌다."*
> 손잡이(읽기)를 돌렸는데 효과가 **40%의 확률로 사라집니다.**
>
> 🔑 **그리고 원칙 ①이기도 합니다** — 화면이 *"읽으면 맞출 수 있다"*고 약속하는데
> 실제로는 **58%**예요. **약속과 메커닉이 어긋납니다.**

### 1-3. 🔴 그런데 그 난수 겹은 **일부러 넣은 것**입니다

`BLK` 주석이 그 이유를 정확히 적어 뒀습니다:

> *"세기가 하나면 사후확률의 크기 순서가 판마다 안 바뀌어서 **늘 같은 쪽을 고르는 게 정답**이
> 됩니다 — 신호가 둘인 척하는 신호 하나짜리 판이 돼요."*

**맞는 말이고, 그래서 `tellHi`/`tellLo` 두 세기가 있어야 합니다.**
🔒 **버릴 것은 「신뢰도가 둘인 것」이 아니라 「맞으면 1, 틀리면 0」이라는 결과 매핑입니다.**

---

## 2. ★ 설계 — **2단 국면**

### 2-1. 흐름

```
┌─ 1단계 · 읽기 ───────────────────────────────┐
│  🧱 어디로 파고들까요?                        │
│                                              │
│  🦶 주발      오른발잡이                      │   🔒 지금 그대로입니다
│  ⬅️ 몸 방향   왼쪽으로 · 확실히 틀었어요       │   🔒 움직이는 것 없음
│                                              │   🔒 시간 무제한
│      [ ⬅️ 왼쪽 ] [ ⬆️ 가운데 ] [ ➡️ 오른쪽 ]   │
└──────────────────────────────────────────────┘
                    ↓  고른 순간
┌─ 2단계 · 손 ─────────────────────────────────┐
│  🧱 지금이에요!                               │
│                                              │
│   🏃‍♂️ ──────────▶            🛡️              │   🔑 상대가 내 앞을 지나갑니다
│   ├────[    판정 창    ]────┤                 │   🔑 창의 폭 = 읽기가 정합니다
│                                              │
│              [ 🛡️ 막기 ]                      │   ⏱️ 1.6초
└──────────────────────────────────────────────┘
```

### 2-2. 🔑 **읽기가 판정 창을 정합니다** — 이게 설계의 전부입니다

```js
s = sBar( 타이밍 오차, BLK_WIN × BLK_READ[읽기결과] × mul )
```

| 읽기 | `BLK_READ` | 읽히는 말 |
|---|---|---|
| **정확** (`pick === truth`) | **1.00** | *"방향을 완벽히 읽고 그 자리에 서 있었어요"* |
| **인접** (한 칸 옆) | **0.45** | *"반 박자 늦었지만 몸을 걸쳤어요"* |
| **반대** | **0.15** | 🔑 *"역동작에 걸렸는데 **발을 뻗어 걷어냈어요**"* |

**셋이 지켜집니다:**

| | | |
|---|---|---|
| ✅ | 🔑 **`s`가 연속이 됩니다** | 타이밍이 연속값이니까요. **이산이 사라집니다** |
| ✅ | 🔑 **읽기가 결과에 직접 실립니다** | 잘 읽으면 창이 **6.7배** 넓습니다 — 난수 한 겹이 **판정 창**으로 바뀌었어요 |
| ✅ | 🔑 **반대로 읽어도 0이 아닙니다** | 손이 완벽하면 건집니다. **원칙 ③** — 손잡이가 항상 뭔가를 합니다 |

> 🔑 **그리고 이건 이미 있는 문법입니다.**
> 🏃 컷인의 준비 화면이 *"🦶 **주발 쪽 칸이 더 넓어요** — 화면에 보이는 폭이 그대로 판정 창"*이라고
> 적어 뒀어요. **차단도 「잘 읽은 쪽이 더 넓다」가 됩니다.** 새 문법이 아니라 **같은 문법의 확장**이에요.

### 2-3. 🔒 **원칙 ①을 지키는 방식 — 두 국면을 「화면」으로 가릅니다**

`runBlock` 주석의 이 문장이 이 설계의 가장 큰 위험입니다:

> *"🚨 **움직이는 것이 하나도 없어요.** (…) 이건 취향이 아니라 이 메커닉의 존재 이유예요 —
> 🧊 볼카운트가 「타이밍처럼 생겼는데 핵심이 안 누르기」라 세 번 고치고도 죽은 자리입니다."*

**그 경고는 여전히 유효합니다. 그래서 국면을 섞지 않습니다:**

| | 1단계 | 2단계 |
|---|---|---|
| 움직이는 것 | 🔒 **없음** | ✅ 상대 한 명 |
| 시간 | 🔒 **무제한** | ⏱️ **1.6초** |
| 화면이 약속하는 것 | *"읽어라"* | *"지금 눌러라"* |
| 재촉하는 연출 | 🔒 **금지** (반짝임·카운트다운·색 전환) | ✅ 있어야 합니다 |

> 🔑 **볼카운트가 죽은 이유는 「타이밍처럼 보이는데 타이밍이 아니어서」였습니다.**
> 여기서는 **타이밍처럼 보이는 국면이 진짜 타이밍**이고, **읽기 국면은 타이밍처럼 안 보입니다.**
> 🔴 **1단계에 초읽기를 붙이는 순간 정확히 볼카운트가 됩니다.** 이 문장을 주석에 남기세요.

### 2-4. ⚠️ 2단계가 「또 하나의 컷인」이 되면 안 됩니다

🏃 컷인·🥅 마무리·🎯 킬패스는 전부 **「움직이는 것을 겨눠 맞추기」**입니다.
2단계도 그러면 **넷이 다 같은 게임**이 되고 H3(반복)이 나빠집니다.

★ **그래서 2단계는 「겨냥」이 아니라 「순간」입니다** — 방향은 이미 1단계에서 정했어요.
**고를 게 없고 「언제」만 남습니다.** 버튼도 **하나**입니다(`🛡️ 막기`).

| | 조작 | 고르는 것 |
|---|---|---|
| 🏃 🥅 🎯 | **어디를** 겨눠 **언제** | 방향 + 시점 |
| 🧱 **2단계** | 🔑 **언제만** | **시점 하나** |

> 🟢 **버튼 하나 · 1.6초**라 **탭이 안 늘어납니다**(1단계 1탭 → 2단계 1탭 = 지금과 같은 2탭 구조).
> ⏱️ 판당 **+1.6~2.0초.** 나머지 셋이 2.5~6초라 **차단이 그 범위 안으로 들어옵니다** —
> 지금은 무제한이라 **혼자만 길이가 정의되지 않았습니다.**

---

## 3. ④ 판정 — **3택 읽기를 살립니다**

조율자님 물음: *"읽기 자체가 재미없는 건지, 읽기인데 손이 안 먹혀서인지"*

★ **후자입니다.** 근거 셋:

| | |
|---|---|
| ① | 🔑 **겹쳐 읽기 설계가 정교합니다.** 부등호가 양쪽에서 뒤집히도록 계수를 맞췄고(`0.58 > 0.336` · `0.24 < 0.608`), *"세기가 하나면 s가 0.540에서 한 걸음도 안 움직인다"*까지 **실측으로 확인**했어요. **이건 좋은 설계이고 버리면 아깝습니다** |
| ② | 🔑 소스가 *"`beta/soccer/cup.js`의 승부차기 문법 — **이 저장소에서 유일하게 성공한 축구 조작**"*이라고 적어 뒀습니다. **문법 자체는 통합니다** |
| ③ | 🔴 **문제는 읽기가 아니라 「읽은 것이 결과에 안 실리는 것」**입니다(§1) |

### 🟡 참고작의 텍스트 3택은 왜 성립하나 — 그리고 왜 우리는 다른가

조율자님이 짚으신 대로 **텍스트 3택도 그것대로 성립합니다.**
🔑 **다만 거기는 「읽기」가 게임의 **전부**이고, 우리는 **넷 중 하나**입니다.**

> 🔴 **한 게임 안에 장르가 둘이면 어긋남으로 읽힙니다** — 그게 §101이 짚은 것이고,
> 범민 님이 *"미니게임 별로"*라고 **통째로** 말씀하신 이유일 수 있어요.
> ★ **그래서 「읽기를 버려서」가 아니라 「읽기에 손을 얹어서」 장르를 맞춥니다.**

---

## 4. 수치와 그 근거

> ⚠️ **아래 값은 balancer의 실측 전까지 전부 「제안」입니다.**

### 4-1. 🔴 계수 둘 — **성격이 다르니 표를 나눕니다** (원칙 ⑨)

```js
/* 📏 **중립화 값입니다. 난이도 손잡이가 아니에요.**
 * 🔗 종속값 — `BLK_READ`·상대 속도·1.6초 중 하나라도 바뀌면 **따라 재계산**하세요.
 * 이 값이 하는 일은 하나뿐입니다: 🔑 **E[s] 평균을 0.537에 붙들어 두는 것.**
 * (넷의 평균이 0.521~0.537로 폭 0.017인 게 원칙 ④를 만듭니다) */
const BLK_WIN = 0.30;        // 상대가 내 앞을 지나는 구간 대비 판정 창 (제안)
```

```js
/* 🎯 **읽기 보상 — 이건 트레이드오프 손잡이입니다.**
 * 「읽기를 얼마나 결과에 실을 것인가 ↔ 손을 얼마나 실을 것인가」를 맞바꿉니다.
 * 🔴 키우면 읽기 게임이 되고, 줄이면 타이밍 게임이 됩니다. **숙련도 폭(§4-2)이 여기서 나와요.** */
const BLK_READ = { exact: 1.00, near: 0.45, opp: 0.15 };
```

> 🔴 **둘을 같은 표에 나란히 두지 마세요.** 하나는 **평균을 붙드는 종속값**이고
> 하나는 **폭을 정하는 손잡이**입니다. 「훈련 계단 보정」에서 데인 자리와 같은 형태예요.

### 4-2. ⑤ **숙련도 폭 목표 — 8~12%p**

| | 🏃 컷인 | 🥅 | 🎯 | 🧱 지금 | 🧱 **목표** |
|---|---|---|---|---|---|
| `E[s]` 폭 | 0.43 | 0.28 | 0.29 | 0.05 | **0.21 ~ 0.32** |
| `Δperfect = 2·half(70)·ΔE[s]` | 16%p | 11%p | 11%p | 🔴 **2%p** | **8 ~ 12%p** |

**★ 넷 중 「가장 좁은 쪽」으로 겨눕니다.** 근거 둘:

| | |
|---|---|
| ① | 🔑 **`defend` 카드는 df가 가장 자주 봅니다.** `MINI` 표에서 네 포지션 모두 `block` 하나뿐이고, df는 그 카드가 오는 빈도가 가장 높아요. **같은 폭이면 df 커리어가 조작에 가장 크게 좌우됩니다** — 🔴 **원칙 ⑦(포지션 형평)의 자리**입니다 |
| ② | 🔑 **🛡️ 철벽상이 「무실점 경기 수」**입니다(§①-G). 수비 카드의 조작 폭이 커지면 **부문상이 조작으로 결정**돼요. 🔴 **원칙 ⑥** — *"덜 키운 쪽은 +0"* |

> ⚠️ **①은 제 추정입니다.** balancer가 **포지션별 `defend` 카드 빈도**를 먼저 재세요 (실측 19).
> **빈도가 고르면 폭을 12%p 쪽으로 올려도 됩니다.**

### 4-3. 🔒 **`E[s]` 평균 0.537은 그대로입니다**

```
지금:  E[s] = P(정확)·1 + P(인접)·0.24 + P(반대)·0        ≈ 0.537
새로:  E[s] = E[ sBar(오차, BLK_WIN · BLK_READ[읽기]) ]   ← 0.537 에 맞춥니다
```

🔴 **평균이 움직이면 그건 미니게임 개선이 아니라 밸런스 변경입니다** (원칙 ④).
`BLK_WIN` **하나로만** 맞추세요 — `BLK_READ`를 만져서 평균을 맞추면 **폭이 같이 움직여
무엇을 고쳤는지 못 가립니다.**

> 🔑 **손잡이를 나눈 이유가 이것입니다: `BLK_WIN`은 평균, `BLK_READ`는 폭.**

---

## 5. ⑥ 🔴 실측 하네스 — **확인했습니다. 다시 잴 것과 안 잴 것이 갈립니다**

inspector가 찾은 것: `engine.js:32`의 `let _rng = Math.random;`이 **로드 시점에 함수를 잡아 둡니다.**

### 5-1. 🔑 **`E[s]` 표는 다시 안 재도 됩니다**

`winger-moment.js`는 **엔진 난수를 한 번도 안 씁니다** — 자기 `Math.random()`을 직접 부릅니다:

```
winger-moment.js:49-50   randIn · pickOne        → Math.random()
winger-moment.js:537-546 rollBlock · footR       → Math.random()
```

| | 하네스가 `W.Math.random`을 갈면 |
|---|---|
| 🎮 **미니게임 내부 굴림** | ✅ **호출 시점에 조회하니 갈린 게 먹힙니다** |
| ⚙️ **엔진 판정**(`outcome`·`cardP`) | 🔴 **로드 시점에 잡아 둬서 안 먹힙니다** ← 이 버그 |

> ★ **`E[s]`는 미니게임 내부값과 조작자 모델만으로 나옵니다 — 엔진 `_rng`를 한 번도 안 지납니다.**
> **§101의 숙련도 표(0.312→0.740 등)는 유효합니다.**
> 그리고 `Δperfect = 2·half·ΔE[s]`는 **산식 환산**이라 하네스와 무관합니다.

### 5-2. 🔴 다시 재야 하는 것 — **「짝지은 비교」입니다**

**엔진 `_rng`가 시드에 안 걸렸다는 건, 그 하네스에서 판정이 매번 새 난수였다는 뜻입니다.**

| | 영향 |
|---|---|
| 🟢 **평균값** (큰 N) | **대체로 살아 있습니다** — 표본이 많으면 평균은 수렴합니다 |
| 🔴 **짝지은 비교 (CRN)** | **깨졌습니다.** CLAUDE.md 2026-08-29 — *"공통 난수(CRN)로 짝지어 300벌 — 안 짝지으면 시드마다 답이 달라집니다"* |

> 🚨 **가장 위험한 것: §18-1의 「승낙 vs 거절 +0.166%p」입니다.**
> **짝지은 비교이고, 차이가 0.166%p로 극히 작습니다.**
> 판정이 안 짝지어졌으면 그 값은 **잡음일 수 있어요** — 목표 창이 `0 < 차이 ≤ 0.5%p`라
> **부호가 뒤집힐 수도** 있습니다.
> 🔴 **`W.WingerEngine._t.seed(n)`로 다시 재세요** (balancer 18).

> 🔑 **그리고 §18-5의 원칙이 여기서 한 번 더 확인됩니다** —
> *"난수는 값을 안 넘겨도 소비량으로 결합한다."* 이번엔 반대 방향이에요:
> **난수원을 갈았는데 한쪽만 안 갈렸습니다.**
> ★ **다음 줄로 적어 둡니다: 「난수원을 갈 때는 「누가 언제 잡았는가」를 확인하세요.**
> **로드 시점에 잡은 참조는 나중에 갈아도 안 따라옵니다.」**

---

## 6. ① 판정 — **S-2′ 동의. 다만 근거를 바꿉니다**

조율자님 근거 셋 중 ①②는 동의합니다. **③(*"넷 다 고치면 79번 정의가 흔들리는데 근거가 없다"*)에
한 줄 보탭니다:**

> ★ **하나만 고치는 이유는 「싸서」가 아니라 「다음 질문이 성립해서」입니다.**
>
> 🔑 **차단을 고치고 나면 「고친 하나」와 「안 고친 셋」이 생깁니다 — 대조군이에요.**
> 그때 여쭐 수 있습니다: *"🧱 막기가 나아졌나요? 나아졌으면 나머지 셋도 같은 식으로 고칠까요?"*
>
> 🔴 **넷을 다 고치면 그 대조가 또 사라집니다 — Q1에서 방금 배운 것입니다.**
> (평가전을 안 해보셔서 대조가 **애초에 없었던** 그 자리요.)

### 🔴 ⑤ — **차단만 고쳐도 안 풀릴 가능성을 어떻게 다루나**

**정직하게 적습니다: 모릅니다.** 제 실측은 **모델**을 잰 값이고, 그건 제가 §101에서 스스로 경고한 자리예요.

**그래서 「안 풀렸을 때의 다음 수」를 미리 정해 둡니다 — 싼 것부터:**

| 순서 | 가설 | 무엇 | 비용 |
|---|---|---|---|
| **1** | **H7 준비 화면** | 학교 아크에서 **둘째 판부터 `▶️ 시작`을 자동으로.** 설명은 그대로 보이되 안 기다림 | 🟢 곡선 무관 |
| **2** | **H5 피드백** | 판정 연출 — director. *"잘했다"*가 화면에서 안 보이는 것 | 🟢 곡선 무관 |
| **3** | **H6 맥락** | ✅ **③(`W2Scene`)이 이미 갑니다** | 🟢 진행 중 |
| **4** | **H4 깊이** | 나머지 셋에도 2단 국면 | 🟡 실측 3벌 |
| **5** | 🔴 **79번 정의** | **Q6** — *"머릿속에 그리셨던 「내가 개입하는 순간」은 어떤 모습인가요?"* | 🔴 로드맵 전체 |

> 🔑 **1~3은 곡선을 안 만지니 차단 작업과 동시에 가도 됩니다.**
> 🔴 **4·5는 차단의 결과를 보고 정합니다** — 먼저 손대면 대조가 사라집니다.

> ⚠️ **범민 님께 다시 여쭐 시점은 「차단 + ③이 둘 다 들어간 뒤」입니다.**
> 그때 질문이 딱 하나예요: *"🧱 막기랑 나머지 셋 중 어느 쪽이 나은가요?"*
> **넷을 한 화면에서 견주실 수 있는 유일한 순간입니다.**

---

## 7. 🗺️ 레인

```
🔴 직렬 — 곡선을 만짐 (E[s] 평균이 판정에 닿습니다)
 ⑤    🧱 차단 2단 국면        ★ 이 문서
 ④    📏 PEER_REF.town = POOL/6                    ← §101 §4. 따로 내보내세요
 ②    🎮 나머지 셋            ← 🚧 차단의 결과를 보고

🟢 병렬 — 곡선 무관. 위와 동시에
 ③    🏫 W2Scene 을 학교에    (engineer 진행 중)   🔑 파일도 축도 안 겹칩니다
 ①-1  🔢 STEPS 5단계
 ①-2  🎯 자리 화면에 「필요한 몸」
 ①-3  🧬 조립대 2택
 H7   ▶️ 준비 화면 자동 진행   ← §6의 1번. 싸고 독립적입니다
```

🟢 **③과 동시에 가도 됩니다** — `winger-moment.js` ↔ `town.js`/`match-scene.js`로 **파일이 안 겹치고**,
③은 **표시만이라 `d`·밴드에 안 닿습니다.**
🔴 **다만 `④ PEER_REF`와는 같이 내보내지 마세요** — 둘 다 곡선입니다.

---

## 8. 🔴 버려지는 것 / 사는 것

| | |
|---|---|
| 🔴 **죽습니다** | `sBlk(pick, truth)` — **3단 이산 매핑**(`1 / 0.24 / 0`). `BLK.part`도 함께 |
| 🔴 **부분적으로 죽습니다** | `runBlock`의 *"움직이는 것이 하나도 없어요"* — 🔒 **1단계는 그대로 정지입니다.** ⚠️ **주석을 지우지 말고 「1단계에 한해」로 고치세요** — 지우면 다음 사람이 **읽기 화면에 초읽기를 붙입니다**(§2-3) |
| ✅ **삽니다** | `BLK.tellHi / tellLo / hiP / favor` — **겹쳐 읽기의 부등호가 그대로** · `rollBlock()` · `DIRS` · 실선/점선 세기 표시 · *"정답을 알려주는 줄은 안 넣는다"* |
| ✅ **삽니다** | `sBar(err, win)` — 🔑 **네 종이 같은 모양을 쓰는 그 자**입니다. 차단이 **드디어 여기에 합류**해요 |
| 🟡 **고칩니다** | 준비 화면 `lines` — 2단계 한 줄 추가. 🔒 **`short`도 함께**(둘째 판부터 그것만 보입니다) |
| 🟡 **고칩니다** | `_t` 창구 — `sBlk`를 보는 검사가 있으면 계약이 바뀝니다 |
| 🔴 **한 줄도 안 고침** | `engine.js` `cardP` · `half` · `outcome` · `judgeAtP` · `BAND_EDGE` · `PEER_REF` · `OFFER`/`SHAKE`/`SIGN_SHAKE` |

---

## 9. 📮 grow-balancer

> 🔴 **⚠️ `W.WingerEngine._t.seed(n)`을 쓰세요.** `W.Math.random`만 갈면 **판정이 안 걸립니다**(§5).
> 🔴 **⚠️ 시드 하나로 재지 마세요.** 🔴 **⚠️ 18번을 먼저** — 그게 기존 결론을 흔듭니다.

| # | 재는 것 | 목표 |
|---|---|---|
| **18** | 🚨 **§18-1의 「승낙 vs 거절」을 CRN으로 다시** | 🔒 **부호 = 승낙 쪽 · `0 < 차이 ≤ 0.5%p`.** 🔴 **부호가 뒤집히면 `SIGN_SHAKE`를 다시 잡습니다** |
| **19** | 🔴 **포지션별 `defend` 카드 빈도** | §4-2 ①의 전제 확인. **고르면 폭을 12%p로** |
| **20** | 🔴 **`BLK_WIN`을 잡아 `E[s]` 평균을 0.537로** | **±0.005** (넷의 폭 0.017 안) |
| **21** | 🔴 **숙련도 폭** — 서툰 손 ~ 능숙 (조작자 **두 벌 이상**) | `ΔE[s]` **0.21~0.32** → `Δperfect` **8~12%p** |
| **22** | 🔴 **원칙 ⑥** — 능력치 70 최고조작 vs 능력치 130 최악조작, **20시즌 총량** | **능력치가 이길 것** |
| **23** | 🔴 **🛡️ 철벽상 수상률**이 개편 전후로 | **±3%p** — 조작 폭이 부문상을 흔들면 안 됩니다 |
| **24** | 🟡 **판당 시간** — 1단계(무제한) 제외한 2단계 | **1.6~2.0초.** 넷의 2.5~6초 안 |

> ⚠️ **21은 「조작자 모델」을 잽니다.** CLAUDE.md 2026-08-29 —
> *"겨냥 전략만 바꾼 조작자 모델에서 E[s]가 0.52 → 0.66"*.
> 🔒 **모델을 두 벌 이상 짜서 둘 다 밴드 안인지 보세요.** 한 벌이면 그 벌을 잰 것입니다.

---

## 10. 📮 grow-engineer / director / inspector

### grow-engineer
- 🔒 **`beta/winger-moment.js`만 고칩니다.** `engine.js`·`town.js`는 **한 줄도** — ③이 그 파일을 만지는 중입니다
- 🔒 **`BLK_WIN`(평균) · `BLK_READ`(폭)를 다른 표로.** 주석에 §4-1의 문장을 그대로
- 🔴 **`runBlock` 주석을 지우지 말고 「1단계에 한해」로 고치세요** (§8)
- 🔴 **2단계 난수는 `Math.random()` 그대로** — 지금과 같은 자리를 쓰면 §18-5에 안 걸립니다
- ⚠️ **이중 탭**: 1단계 버튼 → 2단계 버튼이 **같은 자리에 그려지면 안 됩니다**. `gate` 확인

### grow-director
- 🔒 **1단계에 재촉하는 연출 금지** — 반짝임·카운트다운·색 전환 (§2-3)
- ✅ **2단계는 반대입니다** — 상대가 다가오는 게 **보여야** 합니다
- 🔑 **판정 창을 화면에 그리세요** — 컷인의 *"보이는 폭이 그대로 판정 창"*과 같은 약속. **잘 읽으면 창이 눈에 띄게 넓어야** 합니다(원칙 ③)
- 🎨 실선/점선 세기 표시는 **그대로** — 색으로 가르지 마세요

### grow-inspector
- 🔴 **`sBlk`를 보는 검사가 있으면 계약이 바뀝니다** — 설계가 뒤집힌 자리예요
- 🔴 **`E[s]` 평균 검사** — 문턱은 **검사에 박고** 산식은 **소스에서 정규식으로**
- 🔴 **하네스 넷 + `town-neutral`에 `_t.seed(n)`** (§5). 🔑 **0번 변이로 「시드가 실제로 걸리는가」를 검사**하세요 — 같은 시드 두 번이 **같은 결과**여야 합니다
- 🟡 **1단계에 타이머가 없는지** — 변이 검증으로. 붙으면 볼카운트가 됩니다

---

## 11. 위험

| 위험 | 무엇이 무너지면 실패인가 | 어떻게 막나 |
|---|---|---|
| 🔴 **2단계가 1단계로 스며듦** | 읽기 화면에 초읽기가 붙으면 **정확히 🧊 볼카운트**입니다 | §2-3. 주석을 **고치되 지우지 말 것** |
| 🔴 **`E[s]` 평균이 움직임** | 미니게임 개선이 아니라 **밸런스 변경**입니다 (원칙 ④) | `BLK_WIN` **하나로만** 맞춤 (§4-3) |
| 🔴 **폭이 너무 넓어짐** | *"조작 잘하면 다 이긴다"* — **원칙 ⑥**. 🛡️ 철벽상이 조작으로 결정됩니다 | 실측 22·23. **8~12%p** |
| 🔴 **§18-1의 +0.166%p가 잡음이었음** | **승낙/거절의 부호가 뒤집힙니다** | 실측 18을 **먼저** |
| 🟡 **2단계가 또 하나의 컷인이 됨** | 넷이 다 같은 게임 → H3(반복)이 나빠집니다 | §2-4. **버튼 하나 · 시점만** |
| 🟡 **차단만 고쳐도 안 풀림** | 원인이 넷 공통이었던 것 | §6의 **1~5 순서**를 미리 정해 뒀습니다 |
| 🟡 **넷을 한꺼번에 고치고 싶어짐** | **대조군이 사라집니다** — Q1에서 방금 잃은 것 | §6. *"하나만 고쳐야 다음 질문이 성립합니다"* |

---

# 12. 📮 구현 뒤 판정 (2026-09-01 · `106_engineer_block.md` 회신)

| | |
|---|---|
| **①** | ✅ **engineer가 맞습니다. `0.537`을 계약에서 내립니다.** 🔑 계약은 **절대값이 아니라 「같은 모델로 잰 짝지은 차이」**입니다. `BLK_WIN = 14.2` 확정 (12-1) |
| **②** | 🔴 **제가 원칙 ⑨를 어겼습니다.** `BLK_READ`를 **하는 일이 아니라 하리라 짐작한 일**로 이름 붙였어요. 그리고 **상한은 제가 읽은 소스 안에 이미 있었습니다** (12-2) |
| **③** | ✅ **받아들일 만한 대가입니다.** 🔑 결정적 근거 — **옛 차단은 전략 값어치가 2.75배(0.143)였는데 그게 「별로」라고 불린 것**입니다. **전략 값어치는 애초에 없던 문제였어요** (12-3) |
| **검사** | 🚨 **CLAUDE.md 표의 열 번째 줄입니다 — 「자가 복구가 실패를 삼킴」** (12-4) |

---

## 12-1. ★ 반박 ① — **`0.537`을 계약에서 내립니다**

### (a) 두 값 중 어느 쪽이 맞나 — **물음이 잘못 세워졌습니다**

| | `E[s]` (🧱 옛 매핑) |
|---|---|
| engineer 옛 모델 (`50번`) | **0.537** |
| engineer 새 모델 | **0.5102** |
| inspector 독립 재구성 (`moment-test.js` 머리말) | **0.510** |

**셋 다 맞습니다. 셋 다 「그 모델」을 잰 값이니까요.**
🔑 **그리고 그중 무엇도 「이 게임의 난이도」가 아닙니다.**

이건 제가 §101 §2-5에서 스스로 경고한 자리입니다 —
> ⚠️ *"`E[s]` 실측은 「사람」이 아니라 「모델」을 잰 값입니다."*

**그런데 §104 §4-3에서 제가 그 모델값을 「계약」으로 굳혔습니다.** 경고해 놓고 제가 밟았어요.

### (b) 🔑 원칙 ④가 요구하는 것은 **절대값이 아니라 차이 0**입니다

원칙 ④ — *"새 배수·가중을 도입하면 **중립 상태의 기댓값이 이전과 같은지** 따지세요."*
**「이전과 같은지」이지 「어떤 값인지」가 아닙니다.**

> ★ **engineer가 한 것이 정확히 그것입니다** — 같은 조작자·같은 판으로
> **옛 0.5102 → 새 0.5112 (Δ+0.001)**. **짝지은 비교입니다.**
> 🔑 **모델의 치우침은 차이에서 상쇄되고, 절대값에는 그대로 남습니다.**
>
> 그리고 engineer의 문장이 정확합니다 —
> *"절대 0.537로 가려면 15.69인데, **그건 제 모델의 치우침을 실제 난이도로 굳힙니다.**"*

★ **`BLK_WIN = 14.2` 확정.** 15.69로 밀지 마세요.

### (c) 🔒 새 계약 — **§4-3을 갈아 끼웁니다**

| | |
|---|---|
| 🔴 **옛 계약 (폐기)** | *"`E[s]` 평균 0.537 ±0.005"* |
| ✅ **새 계약** | 🔒 **「같은 조작자 모델·같은 판으로 잰 옛 매핑 ↔ 새 매핑의 `E[s]` 차이 ≤ 0.005」** |

> 🔑 **원칙으로 올립니다 — 이건 이 저장소가 세 번째로 데인 형태입니다:**
>
> 🔒 **모델로 잰 값은 「절대값」을 계약으로 삼지 마세요. 같은 모델로 잰 「차이」만 계약이 됩니다.**
> 모델의 치우침은 **차이에서 상쇄되고 절대값에는 그대로 남습니다.**
> 절대값을 계약으로 굳히면, 다음 사람이 **모델을 고친 순간** 계약을 맞추려고
> **게임의 난이도를 모델 쪽으로 끌고 갑니다.**

### (d) 🔴 그러면 §104 §4-3의 **다른 논거 하나도 같이 무너집니다**

제가 이렇게 적었습니다:
> *"넷의 평균이 0.521~0.537로 **폭 0.017**인 게 원칙 ④를 만듭니다."*

🔴 **그 「폭 0.017」도 한 모델의 성질입니다.** inspector 모델에서는 **0.145**였어요
(0.558 / 0.655 / 0.559 / 0.510). **넷이 고른지 어떤지를 모델로는 못 잽니다.**

> ★ **그럼 미니게임 사이의 형평은 무엇이 지키나 — 구조입니다.**
> 넷이 **같은 `sBar(err, win)` 모양**을 쓰고, **창이 「지나갈 수 있는 구간 대비 같은 비율」**이면
> 조작자 모델과 **무관하게** 대칭입니다.
> 🔑 **§17-6에서 이미 두 번 한 판정이에요** — *"중립을 「값의 문턱」으로만 지키면
> 중립을 깨는 변경의 대부분이 통과합니다. **구조적 성질을 직접 검사하세요.**"*
> 🔴 **세 번째입니다. 이제 기본값으로 삼으세요.**

📮 **inspector — 「넷이 같은 `sBar` 모양을 쓰는가」를 구조 검사로.** 값이 아니라 **형태**를요.

---

## 12-2. ★ 반박 ② — 🔴 **제가 원칙 ⑨를 어겼습니다**

```
읽기만 흔들면  1.2%p        손만 흔들면  12.3%p
BLK_READ 를 끝에서 끝까지 훑어도  14.6 → 11.0%p  (±1.8%p)
```

### (a) 자인합니다 — **하는 일이 아니라 하리라 짐작한 일로 이름 붙였습니다**

§4-1에 제가 이렇게 적었습니다:
> *"`BLK_READ` — **읽기 보상. 숙련도 폭(§4-2)이 여기서 나와요.**"*

**틀렸습니다. `BLK_READ`는 폭을 못 정합니다.**

> 🔴 **이건 「훈련 상승폭」에서 데인 것과 정확히 같은 형태입니다** — 계수의 이름이
> 그 계수의 성격을 말하지 않는 것. 🔑 **다만 그때는 성격을 잘못 읽은 것이었고,
> 이번엔 제가 재 보지도 않고 이름을 붙였습니다. 더 나쁩니다.**

### (b) 🔴 그리고 **상한은 제가 읽은 소스 안에 이미 있었습니다**

engineer가 찾은 구조적 상한 — *"3택 읽기는 베이즈 최적으로 읽어도 정타율 51%(찍기 33%)"* —
이건 `BLK` 세 값(`tellHi 0.58` · `tellLo 0.24` · `favor 1.6`)에서 **바로 계산됩니다.**
**§1에서 제가 그 셋을 표로 옮겨 적으면서도** 상한을 안 셌어요.

> 🔒 **남길 것: 손잡이에 이름을 붙이기 전에 「그 손잡이의 상한」을 먼저 계산하세요.**
> 상한이 목표보다 작으면 **그건 그 축의 손잡이가 아닙니다**(원칙 ⑪).

### (c) ★ 이름과 주석을 고칩니다

```js
/* 🎯 **읽기 지분** — 「읽기 ↔ 손」을 맞바꿉니다. 🔴 **숙련도 폭이 아닙니다.**
 * 실측: 이 표를 끝에서 끝까지 밀어도 폭은 14.6 → 11.0%p뿐이에요(±1.8%p).
 *   읽기만 흔들면 1.2%p · 손만 흔들면 12.3%p — **폭은 손이 만듭니다.**
 * 🔴 상한이 구조적입니다: 3택 읽기는 베이즈 최적으로도 정타율 51%(찍기 33%)예요.
 * 🔑 **폭을 잡으려면 여기가 아니라 `BLK_WIN`과 2단계 시간입니다.** */
const BLK_READ = { exact: 1.00, near: 0.45, opp: 0.15 };
```

| 손잡이 | ✅ 진짜 하는 일 |
|---|---|
| `BLK_WIN` | 🔗 **중립화 값** — 옛↔새 **짝지은 차이를 0으로**. (§12-1) |
| `BLK_READ` | 🎯 **읽기 지분** — 판 하나 안에서 읽기가 결과에 얼마나 실리나 |
| **2단계 시간 · 창 비율** | 🔑 **숙련도 폭** — 여기가 폭의 손잡이입니다 |

**§4-1의 「`BLK_WIN`=평균 · `BLK_READ`=폭」 구분은 폐기합니다.**
🔑 **구분 자체는 살아 있습니다 — 성격이 다른 값을 나란히 두지 않는다는 것.**
**다만 둘째 칸의 이름이 틀렸고, 폭의 손잡이는 셋째 칸이었습니다.**

---

## 12-3. ★ 반박 ③ — **받아들입니다. 그리고 근거가 하나 결정적입니다**

```
읽기 전략 값어치:  옛 0.143  →  새 0.052   (BLK_READ 극단까지 밀어도 4.6%p)
판 하나 안의 읽기:  정확 0.719  vs  반대 0.175   (Δ 0.544)
```

### (a) 🔑 **둘은 모순이 아니라 같은 거래의 양면입니다**

| | 옛 차단 | 새 차단 |
|---|---|---|
| 읽기의 **기댓값** | **0.143** (높음) | 0.052 |
| 읽기가 **판 하나에서 보이나** | 🔴 **안 보임** — 완벽히 읽어도 42%가 실패 | ✅ **0.719 vs 0.175** |

**옛 것은 「기댓값은 큰데 어느 판에서도 안 보이는」 상태였습니다.**
새 것은 **「기댓값은 작은데 매 판 또렷하게 보이는」** 상태입니다.

### (b) ★★ 결정적 근거 — **옛 것이 2.75배였는데, 그게 「별로」라고 불렸습니다**

> 🔑 **범민 님이 「미니게임 별로」라고 하신 그 차단이 전략 값어치 0.143짜리입니다.**
> **전략 값어치가 지금의 2.75배였는데도 별로였어요.**
>
> ★ **그러니 전략 값어치는 애초에 없던 문제입니다.**
> 그걸 3분의 1로 줄이고 **판 안의 가독성을 3.6배(0.544)로 바꾼 것**이
> **정확히 이 개편의 내용**이고, 🔑 **없던 문제를 팔아 있던 문제를 산 것**입니다.

그리고 §1의 제 진단이 그대로였습니다 —
> *"완벽히 읽어도 58%만 맞습니다. 그래서 **잘 읽은 판과 대충 찍은 판이 결과에서 구분이 안 됩니다.**"*

**0.143은 그 구분 안 되는 판들에 얇게 흩어져 있던 값입니다.** 모아서 보이게 한 거예요.

### (c) 🔴 다만 진짜 위험 하나 — **읽기가 「탭하고 넘기는 화면」이 되는 것**

`Δperfect = 2 × half(70) × 0.052` ≈ **2.0%p.**
**읽기를 통째로 무시하고 늘 같은 방향을 눌러도 2.0%p만 손해입니다.**

| | |
|---|---|
| 🟢 **왜 괜찮다고 보나** | 🔑 **플레이어는 기댓값을 계산하지 않습니다. 눈앞의 0.719 ↔ 0.175를 봅니다.** *체감되는* 읽기의 값어치는 판 안의 격차예요. 2.0%p는 **최적화하는 사람만** 계산하는 값이고, 30초짜리 육성 미니게임의 관객이 아닙니다 |
| 🔴 **다만 이건 측정이 아니라 주장입니다** | 그래서 **트립와이어를 겁니다** ↓ |

> 🔒 **트립와이어 — 범민 님께 다음에 여쭐 때 이 한 줄을 넣으세요:**
> *"🧱 막기에서 **몸 방향이랑 주발을 보고 고르셨어요, 아니면 아무거나 누르고 타이밍만 보셨어요?"*
>
> **"아무거나 눌렀다"가 나오면 읽기가 장식이 된 것**이고, 그때 고칠 자리가 §12-3(d)입니다.

### (d) 🔴 되돌릴 때의 처방 — **`BLK_READ` 극단이 아니라 「곱셈」입니다**

engineer가 이미 확인했습니다 — `BLK_READ`를 끝까지 밀어도 **4.6%p까지**입니다.
**그 손잡이로는 못 돌아옵니다**(원칙 ⑪ — 다른 축의 손잡이).

★ **읽기 지분을 되찾아야 하면, 실리는 방식을 바꿉니다:**

```js
지금 (창 배율):  s = sBar( 오차, BLK_WIN × BLK_READ[읽기] )
  → 잘못 읽어도 **손이 완벽하면 s가 1까지** 갑니다. 그래서 읽기의 한계 값어치가 낮아요

되돌릴 때 (곱셈): s = BLK_READ[읽기] × sBar( 오차, BLK_WIN )
  → 🔑 잘못 읽으면 **손이 완벽해도 s가 BLK_READ 위로 못 갑니다**
```

| | 창 배율 (지금) | 곱셈 |
|---|---|---|
| 읽기 전략 값어치 | 0.052 | 🔑 **약 0.09 추정** (옛 0.143과 지금의 중간) |
| *"반대로 읽어도 0이 아니다"* | ✅ 살아 있음 | 🟡 **약해집니다** — 손이 s를 0.15까지만 움직임 |
| 손의 지분 | 82% | 줄어듦 |

> ⚠️ **0.09는 계산이지 실측이 아닙니다.** 바꾸게 되면 balancer가 **먼저** 재세요.
> 🔴 **지금 바꾸지 마세요** — 트립와이어가 울리기 전에는 **없는 문제를 고치는 것**입니다.
> 🔒 **다만 이 문단을 코드 주석에 남기세요.** 없으면 다음 사람이 `BLK_READ`를 밀다가
> **4.6%p 벽에 부딪히고 원인을 못 찾습니다.**

### (e) ★ 그래서 §3(*"3택 읽기를 살립니다"*)이 뜻하는 범위를 확정합니다

> 🔒 **「살린다」는 「판 하나 안에서 읽기가 또렷하게 보인다」는 뜻입니다.**
> **「읽기가 전략의 주축이다」는 뜻이 아닙니다.**
>
> 🔑 겹쳐 읽기(`tellHi`/`tellLo`가 서로 다른 쪽을 가리키는 구조)는 **그대로 삽니다** —
> 그게 없으면 *"늘 같은 쪽이 정답"*이 되어 **판 안의 0.719↔0.175도 의미를 잃습니다.**
> **읽기는 여전히 「매 판 답이 바뀌는 물음」이고, 다만 「커리어를 가르는 축」은 아닙니다.**

---

## 12-4. 🚨 검사 둘 — **CLAUDE.md 표의 열 번째 줄입니다**

### (a) `MOMENT_SEL`에 `.w2m-blk-go`가 없어 2단계를 **한 번도 안 누릅니다**

> **안 눌러도 「2단계 타임아웃」이 대신 진행시켜서 초록불입니다.**

★ **새 실패 유형입니다. 표에 열 번째 줄로:**

| 형태 | 무슨 일이 |
|---|---|
| **자가 복구가 실패를 삼킴** | 검사가 **하기로 한 조작을 안 해도**, 구현의 **타임아웃·기본값·폴백**이 흐름을 끝까지 밀어 줍니다. 도달만 재는 검사는 **아무것도 안 눌러도 통과**합니다 |

> 🔒 **막는 법: 검사가 「무엇을 눌렀는지」를 세게 하세요.**
> 도달(`화면이 끝났다`)만 재면 **자가 복구가 대신 도달합니다.**
> **셀렉터마다 「탭 횟수 > 0」을 단언**하고, **0이면 그 자리에서 빨간불**이어야 합니다.
>
> 🔑 **그리고 이건 원칙 ⑫의 검증판입니다** — *"상쇄되는 둘을 발견하면 지운 뒤에 무엇이
> 남는지 재세요."* 여기서는 **폴백이 조작을 상쇄하고 있었고, 조작을 지워도 폴백이 남았습니다.**

### (b) 이중 탭 — **둘 다 고쳐야 잡혔다**는 것을 기록합니다

> *"기존 `press()`가 세 이벤트를 **같은 요소에** 보내서 브라우저의 **click 재타겟을 재현 못 하고**,
> `cb`가 **620ms 뒤**에 와서 그 시점엔 0회였어요."*

**CLAUDE.md에 이미 「이벤트를 하나만 보냄」 줄이 있는데, 그것만으로는 부족했습니다.**
🔒 **그 줄을 이렇게 넓히세요:**

> **실기기 이벤트는 「개수」만이 아니라 「재타겟」과 「시점」도 맞아야 합니다.**
> ① `pointerdown`에서 화면을 갈아치우면 **`click`은 새로 그려진 요소로 갑니다** —
>    세 이벤트를 같은 요소에 보내면 그 재타겟이 재현되지 않습니다
> ② 콜백이 **수백 ms 뒤**에 오면, 그전에 센 값은 **언제나 0**입니다

### (c) 🔴 그리고 「검사가 없다」는 신고 자체가 값어치 있습니다

> engineer — *"화면을 갈아치우는 `pointerdown` 이중 탭을 재는 검사가 **이 저장소에 하나도 없습니다**.
> 준비 화면 → 본 게임의 `gate`도 같은 이유로 **지금 안 재고 있어요.**"*

🔑 **`gate`는 미니게임 넷 전부가 쓰는 자리입니다.** 차단 2단계 때문에 찾았지만
**문제는 차단만의 것이 아닙니다.**
📮 **inspector — 차단 검사에 끼워 넣지 말고 「미니게임 공통 이중 탭 검사」로 따로 세우세요.**
🔴 한 검사에 묶으면 **차단을 고쳤을 때 나머지 셋의 신호가 같이 사라집니다**(§17-2).

---

## 12-5. 📮 갱신

### grow-balancer — §9 표를 고칩니다

| # | 재는 것 | 목표 |
|---|---|---|
| **20′** | 🔧 ~~`E[s]` 평균 0.537~~ → 🔒 **같은 모델·같은 판으로 옛↔새 짝지은 `E[s]` 차이** | **≤ 0.005** ✅ (Δ+0.001) |
| **21** | 숙련도 폭 — **겨냥 모델 두 벌 이상** | 8~12%p ✅ (9.0~11.1%p · 세 벌) |
| **25** | 🔴 **넷의 `sBar` 창이 「지나갈 수 있는 구간 대비」 같은 비율인가** | **구조 검사** — 값이 아니라 형태 (§12-1d) |
| **26** | 🟡 **읽기 무시 전략의 실제 손해** — *늘 같은 방향* vs *겹쳐 읽기*, 20시즌 총량 | **기록만.** 트립와이어가 울리면 이 값이 근거가 됩니다 |

🔴 **18번(§18-1 CRN 재측정)은 여전히 최우선입니다.**

### grow-inspector
- 🔴 **`moment-test.js` 💥 — 계약이 바뀐 자리입니다**(§10 예고). `sBlk`·`BLK.part`가 죽었어요
- 🚨 **`MOMENT_SEL`에 `.w2m-blk-go` 추가 + 「탭 횟수 > 0」 단언** (§12-4a). 🔴 **셀렉터만 더하면 또 타임아웃이 삼킵니다**
- 🚨 **미니게임 공통 이중 탭 검사를 따로** (§12-4c). 차단 검사에 끼우지 마세요
- 🔴 **`sBar` 형태 구조 검사** (§12-1d) — 세 번째로 같은 교훈입니다
- 🟡 CLAUDE.md 표에 **열 번째 줄**과 **「이벤트」 줄 확장** (§12-4a·b)

### grow-engineer
- ✅ **`BLK_WIN = 14.2` 그대로.** 15.69로 밀지 마세요 (§12-1b)
- 🔴 **`BLK_READ` 주석을 §12-2c로 갈아 끼우세요** — 지금 주석은 제 틀린 이름을 그대로 옮겼습니다
- 🔒 **§12-3d(곱셈 처방)를 주석에 남기세요.** 지금 바꾸지는 말고요
