# 설계 — 🔥 `hot` 갈래 판정 · ⬇️ `soon` · 🧒 A-3 · A-4 (⚽ 더 윙어 II)

**담당** grow-designer · **2026-09-02** · 입력 `124_balancer_in-band.md`(§2·§4·§5·§6) · `122_designer_hot.md`(§3·§4·§8) · `117_designer_school-arc.md`(§3-3·§5-3·§9-1) · `beta/winger-moment.js` · `beta/winger2/{game,intro,style.css}` (**읽기만**)

> 🔒 **파일을 한 줄도 안 고쳤습니다.** inspector가 `tests/`를 잡고 있어요.
> 🔒 이 설계는 **`s`를 한 톨도 안 건드립니다** — `cellS`(`:563`) · `CELL_FLOOR` · `ONE_WIN` · `ONE_CLOSE` 전부 그대로.
> 🔒 **F-1(천장 0.739130) · F-2(격자 폭 0.583)** 를 안 지납니다. 계약 두 줄은 그대로 섭니다.

---

## 0. 한 장 요약

| # | 무엇 |
|---|---|
| **①** | 🔥 **갈래 = ㄴ. 단, ㄴ은 `a`가 아닙니다.** 문턱을 `mul`로 나누는 게 아니라 **「완벽」 등급을 「그 판의 천장」의 종속값**으로 둡니다. ◎는 그 등급의 종속값이라 **따라서** 대칭이 됩니다 (§1-3) |
| **②** | 🔑 **balancer의 두 숫자가 서로 다른 말을 합니다.** `자력` → `＋hot` 초과분 **+0.0013 / +0.0025 / +0.0000**. `＋hot` → `a`(판 내내 argmax) **+0.0922 / +0.0054 / +0.0000.** 🔴 **◎는 형태가 argmax이지 효과가 argmax가 아닙니다 — 1/70입니다** (§1-2) |
| **③** | 🔑 **왜 그런가**: ◎가 뜨는 창(t = 0 ~ 0.52~1.13s)은 **1등과 2등의 `s` 격차가 0.15~0.54인 시간대**입니다. 표시가 없어도 그 칸을 고릅니다. `a`가 +0.0922를 내는 건 **판 뒤쪽, 칸이 어둡고 붙어 있을 때** 정답을 계속 말하기 때문이에요 (§1-2) |
| **④** | 🚨 **문제의 축은 표시가 아니라 등급입니다.** 중립(`mul = 1`)의 천장은 **0.739130**인데 「완벽」 문턱이 **0.75** — **천장 위**예요. 🔑 **「완벽」은 손으로 얻는 게 아니라 🦶 주발 쪽 판을 뽑아서 얻습니다.** 표시(ㄱ·ㄷ)를 만지는 건 원칙 ⑪입니다 (§1-1) |
| **⑤** | 🔑 **소스가 이미 증인입니다** — `:666`에 `W.great + (ctx.weak ? " 🦶 약발로!" : "")`가 있는데 **한 번도 못 뜹니다.** 약발 칸의 천장은 0.652이고 ♿ 확대를 켜도 1.0335 < 1.04348이에요. **설계 의도와 배선이 갈린 자리**가 문자열로 남아 있습니다 (§1-1) |
| **⑥** | 📐 **ㄴ의 값**: 🦶주발 창 0.52~1.13s → **0.81~0.94s** · 🦶약발 **없음 → 0.43~0.52s**. 🔒 **cond 80 주발은 0.881s로 소수점까지 지금과 같습니다**(중립화 상수의 정의). 절벽이 **경사로**가 됩니다 (§1-6) |
| **⑦** | ⬇️ **`soon`(▼)은 지웁니다.** 🔴 `rise`와 **다른 형태**예요 — rise는 **불변식이 금지**했고(구조), soon은 **있는데 뜻이 얇습니다**(가치). 묘비 문구를 복사하면 안 됩니다 (§2) |
| **⑧** | 🚨 **화면에 없는 것 둘을 찾았습니다.** ⭐ **`.w2m-cell-in` CSS 규칙이 아예 없습니다** — JS는 클래스를 붙이고(`:604`) 낱말은 *"테두리 = 빈 곳에 닿는 칸"*이라 말하는데 **테두리를 그리는 줄이 한 줄도 없어요.** 그리고 🪦 **`.w2m-cell-rise` CSS는 아직 살아 있습니다**(`:1209·1223·1384` + `@keyframes w2mRise`) (§3) |
| **⑨** | 🧒 **A-3 — 뒤집지 않습니다.** 🔴 다만 **제 조건이 원칙 ⑦ ②를 못 채우고 있었습니다.** 「+2 아래」를 **몇 회 리롤에서** 재는지 안 적었어요. **「리롤 20회에서 +2 아래」로 다시 씁니다** — 지금 **+4.77**, 여유 2.4배 (§4) |
| **⑩** | 🧒 **A-4 — 뺍니다.** 🔑 **측정이 틀린 게 아니라 축을 만들던 화면 둘이 다른 이유로 빠졌습니다.** 「뾰족 ↔ 평평」은 **세 번 고르기**가 있어야 서는데, `game.js:1777`이 *"같은 걸 또 고르면 값이 정확히 0"*이라 초2·초3을 뺐어요. **그 판단은 맞고, 축은 그때 같이 죽었습니다** (§5) |
| **⑪** | 🧒 **mf +0.43 — §3-2는 안 무너집니다.** 🔑 **행 최대**(각 선택의 최선 자리)가 셋 다 다릅니다(ball→fw · body→df · eye→mf). 무너진 건 *"셋 다 어떤 자리엔 최선"*이 아니라 **「고른 것이 자리를 얼마나 밀어 주나」가 자리마다 다르다**는 것이고, 🔴 **안 고칩니다** — 고치려면 `blendOf`(다른 축)를 만져야 해요 (§5-2) |

---

# 1. 🔥 `hot` — **갈래 ㄴ. 단, ㄴ은 `a`가 아닙니다**

## 1-1. 🔑 먼저 축을 정합니다 (원칙 ⑪)

> ⚽ 더 윙어 II에서 두 번 데인 자리예요 — 철벽상을 `NPC_SPOT_D`로, 공격수 출전을 `YOUTH_BONUS`로.
> **「어느 축의 문제인가」를 먼저 정하고, 그 축의 손잡이를 찾습니다.**

증상은 *"◎가 절반의 판에서 안 뜬다"*입니다. 후보 축이 셋입니다.

| 축 | 손잡이 | 이 증상의 원인인가 |
|---|---|---|
| 🎨 **표시** | `w2m-cell-hot` 토글 | ❌ 종속값입니다 — `TIER_S.perfect`를 읽을 뿐이에요(`:602`) |
| 🎯 **등급** | `TIER_S.perfect = 0.75` | ✅ **여기입니다** |
| 🎲 **무대 굴림** | `oneRoll().side` | ❌ 손잡이가 없습니다(50:50). 그리고 이게 **혼자서는** 증상을 안 만듭니다 |

**🔑 결정적 숫자 하나:**

```
중립(mul = 1) 천장 = 1 − 6/23 = 0.739130      ← F-1이 계약으로 못 박은 값
「완벽」 문턱                = 0.750000      ← TIER_S.perfect
```

> 🚨 **「완벽」 문턱이 천장보다 위에 있습니다.**
> 🫀 컨디션도 ♿ 확대도 이 벽을 못 넘습니다 — 🦶 `STRONG 1.25`만 넘습니다.
> 👉 **「완벽」은 손이 주는 게 아니라 무대가 줍니다.** ◎의 비대칭은 그 사실의 **그림자**예요.

### 🔑 소스가 이미 증인입니다

```
winger-moment.js:666
end(s, s >= TIER_S.perfect ? W.great + (ctx.weak ? " 🦶 약발로!" : "") …
```

`ctx.weak`은 **약발 칸을 눌렀을 때** 켜집니다(`:658`). 그런데 약발 칸의 천장은 **0.652**이고,
♿ 확대(WIDE 1.30) + 🫀 컨디션 100을 다 켜도 `mul` 최대가 **1.03350 < 1.04348**입니다.

> 🚨 **`" 🦶 약발로!"`는 한 번도 못 뜹니다.**
> 🔑 이 문자열을 쓴 사람은 **「약발로도 완벽이 난다」고 믿고 있었습니다.**
> 설계 의도와 배선이 갈린 자리가 **문자열로 남아** 있어요. 그게 이번 판정의 근거입니다.

---

## 1-2. 🔑 balancer의 두 숫자가 서로 다른 말을 합니다

**같은 손 · 같은 판 · 재적합 없음**(124번 §3-3의 자)으로 잰 값입니다.

| 무엇을 켰나 | 서툰 | 보통 | 능숙 | 출처 |
|---|---|---|---|---|
| `자력` → **`＋hot`(◎, 지금 상용)** | **+0.0013** | **+0.0025** | **+0.0000** | 124번 §3-4 (0.0252→0.0265 · 0.0712→0.0737 · 0.0288→0.0288) |
| `＋hot` → **`a`(판 내내 argmax)** | **+0.0922** | +0.0054 | +0.0000 | 124번 §5 |

> 🚨 **◎는 형태가 argmax이지 효과가 argmax가 아닙니다 — 서툰 기준 1/70입니다.**

### 왜 그런가 — 닫힌 식이 이유를 말합니다

◎가 뜨는 창은 `mul · tight ≥ 24/23`, 즉 **판의 앞 0.52 ~ 1.13초**입니다(124번 §2-1).
그 시간대의 성질을 124번이 이미 쟀어요:

| | 실측 |
|---|---|
| 켜진 칸 1등과 2등의 `s` 격차 | **0.15 ~ 0.54** (124번 §3-3) |
| 🦶 약발 쪽 판의 두 칸 격차 | **0.47 ~ 0.54** (124번 §4-2) |
| 능숙의 읽기 잡음 σ | 0.035 |

🔑 **◎가 뜨는 구간은 「정답이 이미 압도적으로 밝은」 구간입니다.** 표시가 없어도 그 칸을 고릅니다.
`a`가 +0.0922를 내는 건 **판 뒤쪽 — 칸이 어둡고 격차가 좁아진 뒤에도 계속 정답을 말하기** 때문이에요.

> 🔴 **그래서 122번 §3-2(*"화면이 `ONE_CLOSE`를 몰래 0:100으로 돌려놓는다"*)는 `a`에는 서고, ◎에는 안 섭니다.**
> 공간 몫이 0이 되려면 **칸 고르기가 공짜**가 돼야 하는데, ◎는 **이미 공짜인 순간에만** 뜹니다.
> 🔴 **다만 이건 「1등−2등 격차」로 추론한 것이지 직접 잰 게 아닙니다** — **측정 H-a**로 확정합니다(§10).

---

## 1-3. ✅ 판정

> # ✅ **ㄴ입니다. 단, 「문턱을 `mul`로 나눈다」가 아닙니다.**
>
> ## **「완벽」 등급을 「그 판에서 손이 낼 수 있는 최선」의 종속값으로 둡니다.**
>
> ```
> boardCeil = max over 칸  cellS(칸, oneAt(board, 0))     ← 판 하나당 한 번, 판 시작에
> perfect  ⟺  s ≥ boardCeil × PERFECT_OF_CEIL             ← 0.9478 (🔗 중립화 상수)
> ```
>
> 🔑 **◎는 이 등급의 종속값이라 아무것도 따로 안 정합니다.** `TIER` 한 자리만 바뀝니다.

### 🔑 왜 이게 ㄴ이면서 `a`가 아닌가 — 세 줄

| | |
|---|---|
| **①** | 🔒 **`s`가 한 톨도 안 바뀝니다.** 골 확률은 `cardP(autoP, ability, s)`가 정하고(`engine.js:468`), 이 변경은 **`s`를 읽어 등급 낱말을 고르는 자**만 바꿉니다. **난이도가 안 내려갑니다** |
| **②** | 🔒 **◎가 뜨는 창의 성질이 안 바뀝니다** — 여전히 **판의 앞 1/4**에만 뜹니다(0.43~0.94s / 3.4s). `a`(판 내내)와 형태가 다릅니다 |
| **③** | 🔒 **표시와 판 끝 문구가 안 갈라집니다.** 🔴 balancer §2-5 ③이 걱정한 자리인데, **`TIER` 자체를 상대화하므로 `:602`(표시)와 `:666`(문구)이 같은 함수를 계속 읽습니다.** 122번 §2-4가 막아 둔 자리를 안 지납니다 |

---

## 1-4. 왜 ㄱ·ㄷ·ㄹ이 아닌가

| 갈래 | 왜 안 하나 |
|---|---|
| **ㄱ — `hot`을 지운다** | 🔴 **증상만 지우고 원인을 남깁니다.** ◎를 지워도 **판 끝의 「완벽!」과 ⚡ 파티클은 여전히 🦶 주발 쪽 판에서만** 납니다. **무대 복권이 그대로 남아요** — 표시 축의 손잡이로 등급 축의 문제를 고치는 것(원칙 ⑪) |
| **ㄷ — 약발 판에 다른 안내** | 🔴 *"뭘?"*에 답이 없는 게 정답입니다. 약발 판은 켜진 칸이 **2개**이고 격차가 **0.47~0.54**예요 — **더 말해 줄 게 없습니다.** 안내를 더 얹으면 원칙 ③(손잡이 없는 표시)이 하나 늘 뿐입니다 |
| **ㄹ — 이대로 둔다** | 🔴 **`" 🦶 약발로!"`가 죽은 채로 남습니다.** 그리고 🦶 태그가 **「＋25% / －25%」라는 경사로**를 말하는데 실제는 **절벽**(최고 등급 있음/없음)이에요 — 원칙 ①의 형태입니다. 🔴 상용 ⚽ 더 윙어에도 같은 형태가 있다는 건 **맞습니다**(082018b:645). 그건 **같이 고칠 목록**이지 이대로 두는 근거가 아닙니다 |

---

## 1-5. 🔒 122번 §3-4의 네 조건 — 정산합니다

`a`를 하려면 치르기로 했던 넷입니다. **ㄴ은 `a`가 아니므로 하나씩 따집니다.**

| # | 조건 | 정산 |
|---|---|---|
| **1** | `ONE_CLOSE` 0.45 → 0.15 | 🔴 **면제를 「실측으로」 받아야 합니다.** 근거는 §1-2(◎는 이미 공짜인 순간에만 뜬다)이고, 확정은 **측정 H-a**입니다. 🚨 **H-a가 「보통에서 칸 선택이 바뀐 판 > 5%」로 나오면 ㄴ을 접고 ㄱ으로 갑니다** |
| **2** | `ONE_WIN` 재측정 | ✅ **필요 없습니다.** `s`가 안 바뀌므로 난이도가 안 움직입니다. **이것이 ㄴ과 `a`의 결정적 차이**예요 |
| **3** | `CELL_FLOOR` 재검토 | ✅ **필요 없습니다.** 천장 식을 안 건드립니다 — F-1 **0.739130**(차 0.000000) · F-2 **0.583** 그대로 |
| **4** | 낱말 교체 | 🟡 **거의 없습니다.** ◎의 뜻(*"지금 누르면 완벽"*)이 그대로예요. 🔑 **바뀌는 건 🦶 태그가 처음으로 참말이 된다는 것** — 「－25%」가 **창 길이 55%**로 보이게 됩니다(§1-6) |

---

## 1-6. 📐 수치 — **◎가 떠 있는 창 길이**

> ⚠️ **아래는 닫힌 식 계산입니다**(모델 없음 · `ONE_WIN 23` · `CELL_FLOOR 0.36` · `cells 6` · `grow 2400` · `ONE_CLOSE 0.45`).
> 🔴 **「완벽 판정이 실제로 몇 %에 나느냐」는 조작이 들어가므로 balancer 측정입니다**(§10 측정 H-b).

| 🫀cond | 판종류 | `mul` | 판 천장 | 상대 문턱 | **지금 창** | **ㄴ 후 창** |
|---|---|---|---|---|---|---|
| 55 | 🦶주발 | 1.15625 | 0.7744 | 0.7340 | 0.520s | **0.810s** |
| 55 | 🦶약발 | 0.69375 | 0.6240 | 0.5914 | ❌ 없음 | **0.425s** |
| 70 | 🦶주발 | 1.21250 | 0.7848 | 0.7439 | 0.743s | **0.853s** |
| 70 | 🦶약발 | 0.72750 | 0.6414 | 0.6079 | ❌ 없음 | **0.455s** |
| **80** | 🦶주발 | **1.25000** | **0.7913** | **0.7500** | **0.881s** | 🔒 **0.881s** |
| 80 | 🦶약발 | 0.75000 | 0.6522 | 0.6181 | ❌ 없음 | **0.475s** |
| 85 | 🦶주발 | 1.26875 | 0.7944 | 0.7529 | 0.947s | **0.895s** |
| 85 | 🦶약발 | 0.76125 | 0.6573 | 0.6230 | ❌ 없음 | **0.485s** |
| 100 | 🦶주발 | 1.32500 | 0.8031 | 0.7612 | 1.133s | **0.936s** |
| 100 | 🦶약발 | 0.79500 | 0.6719 | 0.6368 | ❌ 없음 | **0.515s** |

**🔑 이 표가 말하는 것 넷**

| | |
|---|---|
| 🔒 **①** | **cond 80 · 🦶주발은 0.881s로 소수점까지 지금과 같습니다.** 그게 `PERFECT_OF_CEIL`을 그 값으로 잡은 이유예요 — **바뀌는 건 지금까지 0이던 칸뿐**입니다 |
| ✅ **②** | **절벽 → 경사로.** 약발 창이 주발의 **48~53%**입니다. 🦶 태그의 「－25%」가 **처음으로 화면에서 보이는 값**이 돼요 |
| 🟡 **③** | 🫀 **컨디션이 등급 창에서 거의 빠집니다** (주발 0.52~1.13s의 폭 → 0.81~0.94s). 🔑 **그게 맞습니다** — `:372` 주석이 *"이건 손이 얼마나 정확했나의 등급"*이라고 못 박고 있어요. 컨디션은 `s`와 골 확률에 **그대로** 남습니다 |
| ✅ **④** | ♿ **확대는 여전히 이득입니다** (cond 80 주발 0.881 → **1.144s** · 약발 0.475 → **0.667s**). 🔒 *"성적 페널티 없음"*(`:108`)이 지켜집니다. 🟡 정직하게: 확대의 **이득 폭은 줄어듭니다**(주발 1.909 → 1.144s) |

---

## 1-7. 🔒 이름 — **원칙 ⑨** (⚽ 「훈련 계단 보정」에서 데인 자리)

```js
/* 🔗 **중립화 상수입니다 — 트레이드오프 손잡이가 아닙니다.**
 *    🦶 주발 쪽 판 · 🫀 COND_REF · ♿ 꺼짐에서 **옛 절대문턱 0.75를 소수점까지 그대로 재현**하는 값이에요.
 *    종속: `CELL_FLOOR` · `ONE_WIN` · `ONE.cells` · `STRONG`.
 * 🔴 **넷 중 하나라도 바뀌면 손으로 튜닝하지 말고 다시 계산해서 적으세요** —
 *    0.9478 = 0.75 ÷ (1 − (100/cells)·CELL_FLOOR / (ONE_WIN·STRONG)).
 * 🔴 **난이도를 움직이려면 여기가 아니라 `ONE_WIN`입니다**(한 벌: `CELL_FLOOR`). */
const PERFECT_OF_CEIL = 0.9478;
```

> 🚨 **`PERFECT_OF_CEIL`과 `ONE_WIN`·`CELL_FLOOR`를 같은 표에 나란히 두지 마세요.**
> 저 둘은 🎚️ 손잡이고 이건 🔗 종속값입니다. 나란히 두면 다음 사람이 **이걸 만져서 밸런스를 잡으려** 합니다.
>
> 🔒 그리고 **`TIER_S.in = 0`은 상대화하지 않습니다.** *"빈 곳에 닿았는가"*는 물리이지 등급이 아니에요 —
> **성격이 다른 둘이 한 객체에 있게 되므로 주석으로 갈라 적습니다**(§11).

---

## 1-8. 🔴 그래서 **「무대 복권」은 어떻게 되나** — 정직하게

**🔑 먼저 하나 확정합니다: 🦶 주발 선택은 이 판에서 정확히 대칭이라 손잡이가 아닙니다.**
어느 발을 골라도 **3칸 : 3칸**이고(`:479` `right = ctx.foot !== "L"`), `side`는 **50:50**입니다(`:448`).
👉 **「어느 발을 골랐나」가 미니게임 난이도를 한 톨도 안 바꿉니다.** 굴림이 전부예요.

**그럼 굴림은 ㄴ 뒤에도 남습니다.** 남는 것과 없어지는 것을 갈라 적습니다.

| | 지금 | **ㄴ 후** |
|---|---|---|
| 🎲 굴림이 정하는 것 | **최고 등급의 있음/없음** | **창의 길이** (0.88s ↔ 0.47s) |
| 사람이 할 수 있는 일 | 🔴 **없음** — 뽑기 | ✅ **빨리 누른다** — 짧은 창에 맞추는 일이 남습니다 |
| 화면이 그걸 말하나 | 🔴 「＋25% / －25%」로 **경사로**라 말합니다 (거짓) | ✅ 창 길이가 실제로 **55%**입니다 (참) |
| 누적되나 | ❌ 판마다 다시 굴립니다 | ❌ 같음 |

> 🔑 **원칙 ③이 금지하는 것은 「통제 못 하는 효과」가 아니라 「통제 못 하는데 손잡이가 있는 척하는 효과」입니다.**
> 세부 자리가 걸린 이유는 골·도움에 **실리는데 고를 수가 없어서**였어요.
> 여기서는 **굴림이 난이도만 정하고, 그 난이도 안에서 손이 할 일이 남습니다.**
> 🔴 **「어떤 판은 어렵다」는 무대이고, 「어떤 판은 최고 등급이 아예 없다」가 복권이었습니다.**
> 👉 **ㄴ은 복권을 무대로 되돌립니다. 굴림 자체는 안 없앱니다 — 그건 이 판의 무대예요.**

---

## 1-9. 🔴 접는 조건 — 미리 못 박습니다

| 무엇 | 그러면 |
|---|---|
| **H-a**: 「보통」의 **칸 선택이 바뀐 판** > **5%** | 🔴 **ㄴ을 접고 ㄱ으로.** ◎가 실제로 공간 축을 먹고 있다는 뜻이고, 그럼 122번 §3-2가 ◎에도 섭니다 |
| **H-b**: 「완벽」 판정 비율이 **지금의 2.2배** 초과 | 🟡 **ㄴ은 유지 · ⚡ 파티클을 director가 갚습니다**(원칙 ⑪ — 빈도는 연출 축입니다). 🔴 `PERFECT_OF_CEIL`을 올려서 갚지 마세요 |
| **H-c**: 한 판에 `hot` 칸이 **2개 이상** 나옴 | 🔴 **배선 버그입니다.** 닫힌 식으로는 불가능해요 — 2등 칸은 `best`에서 **10.67 이상** 떨어져 있는데 문턱을 넘으려면 **7.19 이내**여야 합니다 |

---

# 2. ⬇️ `soon`(▼) — **지웁니다**

## 2-1. 🔴 먼저 제 자가 틀렸다는 것부터

122번 §4-3의 자는 *"중앙값 1~3 → 삽니다 · 0에 가까움 → 지웁니다"*였습니다.
실측은 **주발 1 / 약발 0** — **8칸 중 4칸이 「삽니다」, 4칸이 「지웁니다」**.

> 🚨 **자가 판정을 못 냈습니다. 자가 틀렸어요.**
> 🔴 **격자를 한 칸으로 보고 세운 자**였습니다 (CLAUDE.md 검증 6 — *"격자 한 칸으로도 재지 않기"*).
> 👉 다시 세웁니다: **① 대칭인가 ② `in`이 안 하는 말을 하는가.**

## 2-2. 판정

> ## ✅ **`w2m-cell-soon`을 지웁니다** — JS `:607` + CSS `:1209 · :1217` + `@keyframes w2mSoon`

| # | 근거 |
|---|---|
| **①** | 🔴 **ㄴ을 하면 ▼가 화면에서 유일하게 비대칭인 표시로 남습니다.** ◎는 대칭이 되고(§1-6), 테두리(`in`)는 원래 밴드 2로 대칭이에요. **▼만 판마다 있고 없습니다** — 원칙 ③ |
| **②** | 🔴 **▼는 `in`이 450ms 뒤에 할 말을 미리 합니다.** ▼가 붙는 칸은 **`in`인 칸 중 가장 어두운 칸**이에요 — `s = 1 − err/(W·mul·tight)`에서 `tight`에 대한 감소율이 **`err`에 비례**하니까요. **가장 밝은 칸(err이 바닥 6.00에 걸린 칸)이 가장 천천히 어두워집니다** |
| **③** | 🔴 **예고 적중률 6.6 ~ 14.9%** (124번 §4-1). ▼가 *"곧 닫혀요"*로 읽히는데 실제로 닫히는 건 **1/10**입니다 — 원칙 ① |
| **④** | ✅ **시간 축이 안 비어 있습니다.** 테두리가 3.00 → 2.00으로 줄어드는 것과 `urgent` 한 줄(`:632`)이 남습니다 |

## 2-3. 🔴 `rise`를 지운 것과 **같은 형태인가** — **아닙니다**

| | 🪦 `rise`(▲) | ⬇️ `soon`(▼) |
|---|---|---|
| 왜 없나 | 🔒 **불변식이 금지합니다** — `slide < cov1 − cov0`(16 < 24). **구조적으로 존재할 수 없어요** | 🟡 **존재합니다.** 판의 89.8~100%에 뜨고 거짓말도 안 해요(100% `in` 칸에만) |
| 지우는 이유 | **없는 것을 그리고 있어서** | **있는데 `in`과 같은 말이라서** |
| 되살리려면 | 🔴 부등호를 깨야 하고, 그러면 **「빨리 차라」가 죽습니다** | 🟡 **되살려도 판은 안 깨집니다.** 판단이 바뀌면 되살릴 수 있어요 |

> 🚨 **묘비 문구를 `rise`에서 복사하지 마세요.**
> 복사하면 다음 사람이 *"불변식 때문에 ▼도 없다"*로 읽고, **되살릴 수 있는 것을 못 되살립니다.**
> 🔒 묘비에 적을 것: **「구조가 아니라 중복이라서 지웠다. 조건이 바뀌면 되살려도 된다.」**

## 2-4. 🔄 판정이 뒤집힐 조건 — **측정 S-1**

| | |
|---|---|
| **무엇** | ▼가 붙은 칸이 **그 순간의 argmax 칸인 비율** |
| **① 무엇과 견주는가** | 같은 시각에 `in` 테두리가 이미 말하는 것 |
| **② 어느 칸에서 재는가** | 🦶 주발/약발 × 🫀 55·70·85·100 × t = 0.3 / 0.5 / 1.0 |
| 🔄 **뒤집기** | **주발 쪽 판에서 ≥ 70%면 남깁니다.** 그러면 ▼는 *"저 칸 버려라"*가 아니라 **"지금 여기 차라"**이고, **그건 `in`이 못 하는 말**입니다 |
| 🔴 **왜 70%인가** | 한 표시가 **두 뜻**을 섞으면 채널이 아닙니다. 70% 아래면 사람이 ▼를 볼 때마다 *"이게 지금 차라는 건가 버리라는 건가"*를 다시 읽어야 해요 |
| 📐 **제 예상** | **낮게 나옵니다**(§2-2 ②의 감소율 논증). 🔒 **그래도 재세요 — 원칙 ⑫는 「지우기 전에 재라」입니다** |

---

# 3. 🚨 화면에 없는 것 둘 — **이번 판정보다 급합니다**

## 3-1. ⭐ **`.w2m-cell-in` CSS 규칙이 없습니다**

```
winger-moment.js:604   c.el.classList.toggle("w2m-cell-in", !hot && v > TIER_S.in);
winger2/style.css      → `cell-in`을 찾는 규칙이 **0건**
낱말 :679              "테두리 = 빈 곳에 닿는 칸 · ◎ = 완벽"
안내 :677              "**테두리가 있는 칸** 중 가장 밝은 칸으로 차요"
```

> 🚨 **화면이 «테두리를 보라»고 두 번 말하는데 테두리를 그리는 줄이 한 줄도 없습니다.**
> 🔑 **124번이 «채택»이라고 판정한 그것이 지금 안 보입니다** — 서툰 +0.020~+0.088이 **한 톨도 안 들어가고 있어요.**
> 🔴 **원칙 ①의 정확한 형태입니다** — 화면이 약속한 것이 없습니다. **hot 갈래보다 이게 먼저입니다.**
> 📮 **director** — §12.

## 3-2. 🪦 `.w2m-cell-rise` CSS가 아직 살아 있습니다

```
winger2/style.css:1209   .w2m-cell-soon::after, .w2m-cell-rise::after {
winger2/style.css:1223   .w2m-cell-rise::after { content: "▲"; … }
winger2/style.css:1384   .w2m-cell-soon::after, .w2m-cell-rise::after { animation: none; }
                         @keyframes w2mRise
```

JS 토글은 이미 없어졌는데 CSS가 남았습니다. 🔴 **inspector의 R-1(`beta/` 전체 `cell-rise` 0건)이 지금 빨간불입니다.**
🔑 §2가 `soon`도 지우므로 **저 블록 전체가 한 번에 나갑니다** — 붙이신 director가 지우는 게 맞습니다(122번 §7-1).

---

# 4. 🧒 A-3 재검토 — **뒤집지 않습니다. 🔴 제 조건을 다시 씁니다**

## 4-1. 판정

> ## 🔒 **A-3(♾️ 리롤이 편중을 안 씻는다)을 유지합니다.**
> 🔴 **뒤집지 않지만, 「+2」 조건 자체가 원칙 ⑦ ②를 못 채우고 있었습니다.**

`117번 §9-1`에 제가 적은 폐기 조건은 이랬습니다:

> *「`CHILD_FOCUS`를 굴림 뒤 고정 이동으로」 — 실측 44에서 **리롤 후 차이가 +2 아래로** 나오면*

🚨 **「리롤 후」가 몇 회인지 안 적혀 있습니다.** 그래서 balancer가 1,000회까지 가서 **+2.21**을 냈고,
*"조건 코앞"*이 됐어요. 🔴 **조건이 코앞인 게 아니라 조건이 안 닫혀 있었습니다.**

## 4-2. 🔒 조건을 다시 씁니다

| | 옛 조건 | **다시 쓴 조건** |
|---|---|---|
| ① 무엇과 견주는가 | 「+2」 (그냥) | 🔒 **`focus`에 dribble이 있는 쪽 − 없는 쪽의 `max(dribble)` 차이가 +2 아래** |
| ② 어느 칸에서 재는가 | 🔴 **없음** | 🔒 **리롤 20회 지점**. 🔑 `REROLL_MAX = Infinity`지만 **사람이 실제로 굴리는 구간**입니다 |
| 지금 값 | — | **+4.77** (시드 3벌 4.80 / 4.74 / 4.77) — **여유 2.4배** |

**왜 20회인가**

| | |
|---|---|
| 🔑 | **1,000회를 굴리는 사람은 게임을 하고 있지 않습니다.** 그 지점의 값은 「이 설계가 사람에게 무엇을 하나」를 안 말해요 |
| 🔑 | 🔴 **그리고 1,000회에도 +2.21이면 오히려 근거가 강해집니다** — *"씻으려면 게임을 그만두는 만큼 굴려야 한다"* |
| 🔒 | **근거만 정정합니다**: 「리롤이 편중을 **벌린다**」 ❌ → **「좁히지만, 사람이 굴리는 구간에서는 안 씻긴다」** ✅. 117번 §5-3의 표(1회 +6.5 → 50회 +8.1)를 **124번 §6-1 표로 갈아 끼웁니다** |

---

# 5. 🧒 A-4 · mf

## 5-1. ✅ A-4 — **설계 문서에서 뺍니다. 🔑 다만 이유가 balancer가 적은 것과 다릅니다**

balancer는 *"실제 배선에서 SD 폭이 ±2%이고 대조가 한가운데라 축이 아니다"*라고 했습니다.
🔴 **결론은 같지만 이유가 다릅니다 — 그리고 그 차이가 다음 사람에게 중요합니다.**

```
game.js:1777
🔴 **초2·초3은 없습니다** (실측 119번 §2) — `spread()`가 `focus`를 `indexOf`로 봐서
   같은 걸 또 고르면 값이 **정확히 0**이라, 화면만 늘고 축은 안 늘어요.
game.js:1785   chosenChild = key ? [key] : [];        ← 한 번만 고릅니다
```

> 🔑 **「뾰족 ↔ 평평」 축은 「세 번 고르기」가 있어야만 섭니다.**
> ⚽⚽⚽ = 2칸 · ⚽⚽🛡️ = 4칸 · ⚽🛡️👀 = 6칸 — 그게 축의 전부였어요(117번 §3-3).
> **초2·초3을 뺀 순간 축도 같이 죽었습니다.** 그리고 **뺀 판단은 맞습니다** —
> 같은 걸 또 고르는 갈래가 정확히 0이면 그건 **고르는 척하는 화면**이니까요.
>
> 👉 **A-4는 「제가 잘못 쟀다」가 아니라 「축을 떠받치던 화면 둘이 다른 이유로 빠졌다」입니다.**

**🪦 117번 §3-3에 묘비를 답니다** (「폐기는 이름이 아니라 형태」):

> 🪦 **「뾰족함 ↔ 평평함」 축은 없습니다.** 세 번 고르기(초1·초2·초3)가 있어야 서는 축인데,
> 초2·초3을 뺐습니다(`game.js:1777` — 같은 걸 또 고르면 `indexOf` 때문에 값이 **정확히 0**).
> 🚨 **되살리려면 화면 둘을 되살려야 하고, 그러면 「고르는 척하는 화면」이 둘 생깁니다.**
> **살아 있는 축은 「어느 자리에 맞추느냐」 하나입니다**(§3-2).

## 5-2. 🟡 mf +0.43 — **§3-2는 안 무너집니다. 무너지는 건 다른 것입니다**

`117번 §3-2`가 세운 것은 ***"셋 다 어떤 자리엔 최선입니다"*** 였습니다. **행으로 읽는 자**예요.

| 선택 | fw | wg | mf | df | **행 최대 = 그 선택의 최선 자리** |
|---|---|---|---|---|---|
| ⚽ ball | **115.41** | 114.99 | 114.10 | 110.47 | ✅ **fw** |
| 🛡️ body | 110.33 | 111.65 | 113.63 | **116.07** | ✅ **df** |
| 👀 eye | 112.19 | 113.99 | **115.39** | 111.72 | ✅ **mf** |

> ✅ **행 최대가 셋 다 다릅니다. §3-2는 섭니다** — 지배되는 선택지가 없어요.
> 🔑 그리고 **자리는 초1 뒤에 고릅니다**(`game.js` goChild → … → goPosition · 117번 §2-4).
> 👉 **초1 선택이 자리를 「추천」하는 구조**이고, 셋이 서로 다른 자리를 추천합니다.

**🔴 그럼 무엇이 무너지나 — 「자리마다 폭이 다르다」입니다.** 열로 읽는 자예요.

| | fw | wg | mf | df |
|---|---|---|---|---|
| 선택 간 폭 | 5.08 | 3.33 | 🔴 **1.76** | 5.60 |
| 대조 대비 최선 | +3.14 | +1.28 | 🔴 **+0.43** | +3.62 |

| | |
|---|---|
| 🔑 **왜 mf만 좁나** | mf는 **여섯 칸이 다 조금씩 필요한 자리**입니다. `CHILD_FOCUS`는 **2칸**만 미는데, 그 2칸이 mf 가중에서 큰 몫이 아니면 폭이 안 납니다. 🔑 **mf의 성질이지 초1의 결함이 아닙니다** |
| 🟡 **wg도 봐 주세요** | balancer는 mf만 짚었는데 **wg가 +1.28 · 폭 3.33**입니다 — 게임 이름이 「윙어」인 자리예요. fw·df의 **60%**입니다 |

## 5-3. 🔴 판정 — **고치지 않습니다. 「알려진 한계」로 못 박습니다**

| | |
|---|---|
| 🔴 **왜 안 고치나** | mf의 좁은 폭은 **`blendOf`(포지션 적합) 가중의 그림자**입니다. 고치려면 `blendOf`를 만져야 하는데 **그건 프로 특화 축**이에요 — **다른 축의 손잡이로 이 축을 고치는 일**(원칙 ⑪) |
| 🔴 **`CHILD_FOCUS` 칸 배정을 mf 쪽으로 기울이면** | 🚨 **`2+2+2 · 겹침 0`이 깨집니다.** 그게 117번 §5-2의 「판정에 닿는 양 = 정확히 0」을 만드는 **잠금장치**입니다 |
| ✅ **문구는 안 고칩니다** | 지금 문구가 이미 정직해요 — *"발에 붙는 감각이 **먼저** 자라요"*. **자리를 약속하지 않습니다**(`intro.js:378`) |
| 🔒 **적을 것** | 117번 §3-2에 **위 열 표를 그대로** 넣습니다. 🚨 **다음 사람이 *"mf가 약하네"*로 `CHILD_FOCUS`를 기울이지 않게** |
| 🔄 **판단이 바뀔 조건** | **자리 고르기가 초1보다 앞으로 옮겨질 때.** 그러면 mf를 이미 고른 사람이 초1 화면에서 **「내 자리엔 아무거나 같다」를 알게 되고**, 그때는 원칙 ③입니다 |

---

# 6. 수치와 그 근거

> ⚠️ **balancer 실측 전까지 전부 「제안」입니다.** (§1-6 표는 닫힌 식이라 예외 — 모델이 안 들어갑니다)

| 값 | 제안 | 성격 | 근거 |
|---|---|---|---|
| `PERFECT_OF_CEIL` | **0.9478** | 🔗 **중립화 상수** (종속: `CELL_FLOOR`·`ONE_WIN`·`cells`·`STRONG`) | cond 80 · 🦶주발에서 옛 0.75를 **소수점까지** 재현 (§1-6) |
| `TIER_S.in` | **0 (그대로)** | 🔗 종속값 — 「빈 곳에 닿는가」 | 🔴 **상대화하지 않습니다.** 물리이지 등급이 아니에요 |
| `soon` 문턱 0.08 | 🪦 **없음** | — | §2. 중복 + 비대칭 + 적중률 6.6~14.9% |
| `rise` | 🪦 **없음** (JS ✅ / **CSS 아직 있음** 🔴) | — | §3-2 |
| `ONE_WIN` / `CELL_FLOOR` | **23 / 0.36 (그대로 · 한 벌)** | 🎚️ 난이도 / 🏔️ 천장 | F-1 · F-2가 지금 값으로 통과 |
| `ONE_CLOSE` | **0.45 (그대로)** | ⚖️ 몫 손잡이 | 🔴 **H-a가 면제하면** 그대로. 아니면 **ㄱ으로 접습니다**(§1-9) |
| `FOOT_WIN` | **0.25 (그대로)** | 🎚️ 🦶 축 | 🔑 **안 만집니다.** 124번 §9가 「hot + FOOT_WIN 한 벌」이라 했는데, **ㄴ은 등급 쪽에서 풀어서 `FOOT_WIN`을 안 지납니다** |
| `ONE.look` 450 | **그대로** | 🎚️ 읽기 축 | `soon`이 없어져도 🔮 예고 띠(`w2m-cell-next`)가 씁니다 |

---

# 7. 저장 데이터

**새 필드 0개.** `boardCeil`은 **판 하나 안에서만 사는 지역 변수**이고 세이브에 안 닿습니다.
`S.childPicks`도 그대로예요 — A-3·A-4 판정은 **문서만** 바꿉니다(코드 변경 없음).
🔒 옛 세이브의 `childPicks === undefined` ≡ `[]`가 **4,000/4,000 일치**로 확인됐습니다(124번 §6).

---

# 8. 하지 않은 것

| 안 한 것 | 왜 | 🔄 판단이 바뀔 조건 |
|---|---|---|
| **ㄱ — ◎ 지우기** | 증상만 지우고 **원인(등급 비대칭)을 남깁니다** — 원칙 ⑪ | 🔴 **H-a에서 「보통」 칸 선택이 5% 넘게 바뀌면** — 그럼 ◎가 진짜 argmax고 ㄱ이 맞습니다 |
| **ㄷ — 약발 판에 다른 안내** | 켜진 칸이 2개, 격차 0.47~0.54. **더 말해 줄 게 없습니다** | 약발 판의 밴드가 3 이상으로 넓어질 때 |
| **ㄹ — 이대로** | `" 🦶 약발로!"`가 죽어 있고, 🦶 태그가 **경사로라 말하는데 절벽**입니다 | 범민 님이 *"주발 판이 쉬운 게 주발을 고른 보람"*이라 하실 때. 🔴 **다만 주발 선택은 대칭이라 「보람」이 안 생깁니다**(§1-8) |
| **`FOOT_WIN` 내리기** | 🔴 `WEAK < 1`인 이상 **어떤 값으로도 절대문턱 0.75에 못 닿습니다**(중립 천장이 0.739라서). **이 축의 손잡이가 아니에요** | `CELL_FLOOR`·`ONE_WIN`이 바뀌어 중립 천장이 0.75 위로 올라갈 때 |
| **`ONE_WIN` 올려서 천장 띄우기** | F-1(`0.739130 ± 0.005`)을 깹니다. 그리고 **읽기 축 문제를 난이도 손잡이로** 고치는 것 | F-1 계약 자체를 다시 열 때 |
| **`soon` 문턱 상대화** | 손잡이를 하나 더 만듭니다. 그리고 **적중률 6.6~14.9%는 상대화로 안 고쳐집니다** | S-1이 ≥70%로 나와 `soon`을 살릴 때 |
| **`CHILD_FOCUS`를 mf 쪽으로** | `2+2+2 · 겹침 0`이 깨지고, 그게 「판정에 닿는 양 = 0」의 잠금장치입니다 | 자리 고르기가 초1 앞으로 갈 때 (§5-3) |
| **초2·초3 되살리기** | `indexOf` 때문에 같은 걸 고르면 **정확히 0** — 고르는 척하는 화면이 둘 늡니다 | `spread()`가 `focus`를 **개수로** 세게 바뀔 때 (🔴 그럼 `POOL 194` 중립이 깨집니다) |
| **④(고르는 게임 ↔ 타이밍 게임) 열기** | 122번 §9 ④ 그대로 — **백로그입니다** | 범민 님이 *"칸을 고르는 건지 타이밍인지 모르겠다"*고 하실 때 |

---

# 9. 위험 — **무엇이 무너지면 이 설계가 실패인가**

| # | 무엇 | 어떻게 압니까 |
|---|---|---|
| 🔴 **1** | **◎가 실제로 공간 축을 먹고 있으면** ㄴ은 `a`를 전면화하는 것이고, 제가 §3에서 거부한 것을 스스로 합니다 | **H-a** (보통 칸 선택 변경 ≤ 5%) |
| 🔴 **2** | **「완벽」이 흔해져 ⚡ 파티클이 골 세리머니와 뭉치면** `:360`의 위계가 무너집니다 | **H-b** + 🖐️ director 실기기 |
| 🔴 **3** | 🫀 **컨디션이 등급 창에서 빠지는 것을 범민 님이 「컨디션이 의미 없어졌다」로 읽으면** 판정이 틀린 겁니다 | 🖐️ **범민 님** — 🔴 `s`·골 확률에는 그대로 남는다는 걸 §12의 문구가 말해야 합니다 |
| 🔴 **4** | **`.w2m-cell-in`이 계속 안 그려지면** 이 문서 전체가 헛돕니다 — 124번이 채택한 것이 화면에 없어요 | **§3-1** · inspector 검사 하나 |
| 🟡 **5** | **`PERFECT_OF_CEIL`을 다음 사람이 난이도 손잡이로 만지면** — ⚽ 「훈련 계단 보정」과 **같은 사고** | §1-7 주석 + inspector 종속 관계 검사 |
| 🟡 **6** | **`soon`을 지운 뒤 시간 축이 비면** *"언제 눌러야 하는지 모르겠다"*가 돌아옵니다 | 🖐️ 범민 님 (122번 §8 세 번째 줄) |
| 🔴 **7** | **A-4를 「측정이 틀렸다」로 요약하면** 다음 사람이 초2·초3을 되살립니다 | §5-1 묘비 문구를 그대로 옮겼는지 |

---

# 10. 📮 grow-balancer — 재야 할 것

🔒 **원칙 ⑦의 두 줄을 채워 적었습니다.** 산식은 `_t`에서 뽑으세요(베껴 적지 말 것).
🔒 **난수원을 가르세요** (`seed ^ 0x9E3779B9`). 🔒 **모델로 잰 절대값을 계약으로 삼지 마세요 — 차이만.**

### 🔴 H-a — **◎가 칸 선택을 바꾸나** (§1-5 조건 1의 면제 여부 · **가장 급합니다**)

| | |
|---|---|
| **무엇** | ⓐ `자력`(밝기만 읽음) ↔ ⓑ `＋hot`(◎ 있으면 그 칸)에서 **칸 선택이 바뀐 판%**. 🔑 124번 §3-3이 `in`에 쓴 **그 자 그대로** |
| **① 견줌** | `in`의 같은 값(서툰 3.5~28.9% / 보통 ≤0.3% / 능숙 0.0%) |
| **② 격자** | 🫀 55·70·85·100 × 🦶 주발/약발 × 실력 3 |
| 🎯 **문턱** | **보통 ≤ 5%면 ㄴ 채택** · **> 5%면 ㄱ으로 접습니다** |
| **덧** | ◎가 뜬 프레임에서 **1등−2등 `s` 격차**의 분포도 같이. 🔑 격차가 서툰 읽기 σ의 **3배 이상**이면 표시가 정보를 안 더한다는 직접 증거입니다 |

### 🔴 H-b — **「완벽」 판정이 얼마나 흔해지나**

| | |
|---|---|
| **무엇** | 한 판에서 판정이 `perfect`로 끝나는 비율. **지금(절대 0.75)** ↔ **ㄴ(`s ≥ boardCeil × 0.9478`)** |
| **① 견줌** | 🔑 **지금 값의 배수.** **2.2배 이하면 그대로** · 넘으면 director가 ⚡로 갚습니다(§1-9) |
| **② 격자** | 🫀 4 × 🦶 2 × 실력 3 (서툰·보통·능숙) |
| 🔒 **주의** | `boardCeil`은 **`t = 0`의 판 천장**입니다. **매 프레임 다시 계산하면 안 됩니다** — 그러면 argmax를 고르기만 해도 늘 완벽이라 **시간 축이 통째로 죽습니다** |

### 🔴 H-c — **`hot` 칸이 늘 ≤ 1인가**

| | |
|---|---|
| **무엇** | ㄴ 배선에서 한 프레임에 `hot`인 칸 수 |
| **① 견줌** | **0 또는 1.** 2 이상은 배선 버그 |
| **② 격자** | 격자 8칸 전부 · 프레임 스캔 |
| 📐 **닫힌 식** | 2등 칸은 `best`에서 **≥ 10.67**(칸 간격 16.67 − 바닥 6.00), 문턱을 넘으려면 **≤ 7.19**. 겹칠 수 없습니다 |

### ⬇️ S-1 — **▼가 argmax 칸에 붙나** (§2-4 · 지우기 전에 재는 것 · 원칙 ⑫)

| | |
|---|---|
| **무엇** | ▼가 붙은 칸이 그 순간의 argmax인 비율 |
| **① 견줌** | **주발 쪽 판 ≥ 70% → `soon`을 살립니다** |
| **② 격자** | 🦶 2 × 🫀 4 × t = 0.3 / 0.5 / 1.0 |

### 🟡 H-d — ♿ **확대의 이득이 남는가** (접근성은 축약 대상이 아닙니다)

| | |
|---|---|
| **무엇** | 확대 ON/OFF의 **「완벽」 비율 차** — 지금 ↔ ㄴ |
| **① 견줌** | 🔒 **ㄴ에서도 ON이 OFF보다 높아야 합니다.** 닫힌 식 예상: 창 0.881 → **1.144s**(주발) · 0.475 → **0.667s**(약발) |
| **② 격자** | 🫀 80 × 🦶 2 |

---

# 11. 📮 grow-engineer — 스펙 (`beta/winger-moment.js`)

🔴 **H-a가 나온 뒤에** 손대세요. 🔴 **inspector가 `tests/`에서 손을 뗀 뒤에** — 움직이는 표적을 재게 됩니다.

## 11-1. `TIER` — `:384` 근처

🚨 **성격이 다른 둘이 한 객체에 들어갑니다. 표를 갈라 적으세요**(원칙 ⑨).

```js
/* 🔗 **「빈 곳에 닿았는가」 — 절대 경계입니다.** 물리이지 등급이 아니에요.
 *    🔴 상대화하지 마세요. `s > 0`은 «창 안에 들어왔다»와 같은 말입니다. */
const TIER_IN = 0;

/* 🔗 **중립화 상수 — 트레이드오프 손잡이가 아닙니다.**
 *    🦶 주발 쪽 판 · 🫀 COND_REF · ♿ 꺼짐에서 **옛 절대문턱 0.75를 소수점까지 재현**하는 값.
 *    종속: `CELL_FLOOR` · `ONE_WIN` · `ONE.cells` · `STRONG`.
 *    0.9478 = 0.75 ÷ (1 − (100/cells)·CELL_FLOOR / (ONE_WIN·STRONG))
 * 🔴 넷 중 하나라도 바뀌면 **손으로 튜닝하지 말고 다시 계산해서** 적으세요.
 * 🔴 난이도를 움직이려면 여기가 아니라 `ONE_WIN`입니다(한 벌: `CELL_FLOOR`).
 * 🚨 **`ONE_WIN`·`CELL_FLOOR`와 같은 표에 두지 마세요** — 저 둘은 손잡이입니다. */
const PERFECT_OF_CEIL = 0.9478;

/* 🔑 **「완벽」은 «그 판에서 손이 낼 수 있는 최선을 냈나»입니다** (설계 125번 §1-3).
 * 🔒 `ceil`은 **판 시작(t = 0)의 천장 한 값**이에요 — 🔴 **매 프레임 다시 계산하면
 *    argmax를 고르기만 해도 늘 완벽이라 「빨리 차라」가 통째로 죽습니다.**
 * 🪦 옛 절대문턱 0.75는 **중립 천장(0.739130)보다 위**였습니다 — 그래서 「완벽」이
 *    🦶 주발 쪽 판에서만 났어요(실측 100.0% / 0.0%). 되돌리지 마세요. */
const TIER = (s, ceil) => (s >= ceil * PERFECT_OF_CEIL ? "perfect" : s > TIER_IN ? "ok" : "miss");
```

## 11-2. `runShotGrid` — `boardCeil`을 **판 시작에 한 번**

```js
// cells 만든 직후 (:487 근처)
const a0 = oneAt(board, 0);
const boardCeil = Math.max.apply(null, cells.map((c) => cellS(c, a0)));
```
🔴 `cellS`(`:563`)가 그 위에 있어야 합니다 — **선언 순서를 확인하세요.**

## 11-3. `paint` — `:602-604`

```js
const hot = TIER(v, boardCeil) === "perfect";
c.el.classList.toggle("w2m-cell-hot", hot);
c.el.classList.toggle("w2m-cell-in", !hot && v > TIER_IN);
```
🔒 **문턱을 여기 다시 적지 마세요.** `TIER`가 유일한 주인입니다.

## 11-4. 🪦 `soon` 제거 — `:607`

```js
c.el.classList.toggle("w2m-cell-soon", v2 < v - 0.08);   // ← 지웁니다
```
👉 **묘비 주석** (🔴 `rise`의 것을 복사하지 마세요 — §2-3):

> 🪦 **`soon`(▼ 곧 어두워질 칸)은 없습니다.** ▼가 붙던 칸은 **`in`인 칸 중 가장 어두운 칸**이었고
> (`s`의 `tight` 감소율이 `err`에 비례해서요), 그건 **테두리가 450ms 뒤에 할 말**이었습니다.
> 실측: `in` 칸에만 붙음 100% 🟢 · **예고 적중률 6.6~14.9%** 🔴 · 🦶 약발 쪽 판에서 **중앙값 0**(124번 §4).
> 🔑 **`rise`와 다릅니다** — rise는 **불변식이 금지**해서 없고(되살리면 「빨리 차라」가 죽음),
> ▼는 **있는데 중복이라** 지웠습니다. **조건이 바뀌면 되살려도 판은 안 깨집니다.**

## 11-5. `end` — `:666`

```js
end(s, TIER(s, boardCeil) === "perfect" ? W.great + (ctx.weak ? " 🦶 약발로!" : "")
  : s > TIER_IN ? W.ok : onKeeper ? W.onKeeper : W.far);
```
> ✅ **`" 🦶 약발로!"`가 이 변경으로 처음 뜹니다.** 죽은 문자열이 아니라 **의도였습니다**(§1-1).

## 11-6. 🔍 `_t` — `:807`

`PERFECT_OF_CEIL`을 **`K`에 넣으세요.** 🔴 다만 inspector에게: **문턱은 검사에 직접 적고,
`K.PERFECT_OF_CEIL`은 「종속 관계 검사」에만** 쓰게 하세요(§13).

## 11-7. 🔒 안 건드리는 것

`cellS` · `oneErr` · `cellFloorErr` · `CELL_FLOOR` · `ONE_WIN` · `ONE_CLOSE` · `FOOT_WIN` · `ONE.look` · `condMul` · `engine.js` **전부.**

---

# 12. 📮 grow-director

| # | 무엇 | 왜 |
|---|---|---|
| 🔴 **1** | ⭐ **`.w2m-cell-in` 규칙을 만들어 주세요** — **지금 한 줄도 없습니다**(§3-1) | 🚨 낱말이 *"테두리가 있는 칸 중"*이라 **두 번** 말하는데 테두리가 없어요. 124번이 «채택»한 것이 화면에 안 나옵니다. **이 문서에서 가장 급합니다** |
| 🔒 **1-a** | 위계: **◎(링 + 조준점) > 테두리 > 없음.** 🔴 **셋이 한 덩어리로 뭉치면 실패**입니다 | 122번 위험 5. `.w2m-cell-hot`이 `inset 0 0 0 2px`를 쓰니 테두리는 **더 얇고 어둡게** |
| 🔴 **2** | 🪦 **`.w2m-cell-rise` · `.w2m-cell-soon` 블록을 통째로 지워 주세요** — `:1209 · :1217 · :1223 · :1384` + `@keyframes w2mSoon` · `w2mRise` | §2 · §3-2. **붙이신 분이 지우는 게 맞습니다.** 🔴 `:1188-1190`의 *"한 칸이 hot이면서 soon일 수 있어요"* 주석도 같이 |
| 🟡 **3** | **◎ 빈도가 올라갑니다** (판의 절반 × 0.88s → 모든 판 × 0.43~0.94s) | H-b가 2.2배를 넘으면 **⚡ 파티클 8개를 줄이거나 밝기를 낮춰** 갚아 주세요 — 🔴 `PERFECT_OF_CEIL`로 갚지 마세요(원칙 ⑪) |
| 🟡 **4** | 🖐️ **실기기**: 🦶 약발 쪽 판에서 ◎가 **0.43초만** 떠 있습니다. **그 안에 눈에 들어오는지** | 너무 짧으면 «깜빡였다»로 읽혀 노이즈가 됩니다 |
| 🟡 **5** | 🦶 태그 문구 — **「＋25% / －25%」가 이제 참말입니다**(창 길이 비 ≈ 55%). 🔴 **바꾸지 마세요**, 다만 위계에서 **약발 태그가 안 묻히게** | §1-8 |

---

# 13. 📮 grow-inspector — 검사가 뒤집힐 자리

| 검사 | 무슨 일이 |
|---|---|
| 🔴 **문턱 검사 `0.75`** | 🔄 **뒤집힙니다.** 절대 0.75가 **판 천장의 94.78%**가 됩니다. 🔒 **검사에는 `0.9478`과 `0.739130`(중립 천장)을 직접 적으세요** — 소스에서 읽으면 안 잡힙니다 |
| 🆕 **종속 관계 검사** ⭐ | 🔑 **`CELL_FLOOR`나 `ONE_WIN`을 변이시키면 「완벽이 되는 최소 `s`」가 따라 움직여야** 합니다. **안 움직이면 빨간불** — 「산식은 소스에서 · 문턱은 검사에 박고 · **종속값은 관계식으로**」의 세 번째 축 |
| 🆕 **대칭 검사** ⭐ | 🚨 **`t = 0`에 `hot`인 칸이 🦶 주발 쪽 판과 약발 쪽 판 **양쪽에서** 정확히 1개.** 이게 이 설계의 전부입니다 |
| **H-1 배타** | 그대로 — 한 칸에 `hot`과 `in`이 동시에 안 붙음 |
| 🔄 **H-2** | **식이 바뀝니다.** `mul·tight ≥ 24/23`(절대) → `tight ≥ u/(1−k+k·u)`, `u = 6/(23·mul)`. 🔑 **여전히 조준이 안 들어갑니다** — 그 성질을 지키세요 |
| 🚨 **H-3** | 🔴 **넣지 마세요. 그리고 이미 있으면 지우세요** — 「🦶 약발 쪽 판에서 `hot`이 0%」는 **이 설계가 없애는 것**입니다. 124번 §8이 *"designer 판정 전까지는 넣지 마세요"*라 했고, **판정이 났습니다** |
| 🆕 **`in` 그림 검사** 🔴 | **`beta/`에 `cell-in`을 그리는 CSS 규칙이 ≥ 1건.** 🚨 지금 **0건**입니다(§3-1). 🔑 **클래스가 붙는지만 보는 검사는 이걸 못 잡습니다** — 「초록불인데 아무것도 안 지키는」 열다섯 번째 모양이에요 |
| 🆕 **묘비 검사** | `beta/` 전체에 **`cell-rise` 0건** · **`cell-soon` 0건** (JS·CSS·keyframes 전부). 🔴 R-1이 **지금 빨간불**입니다 |
| 🆕 **죽은 문자열 검사** | `" 🦶 약발로!"`가 **실제로 도달 가능한지.** 🔑 지금은 **불가능**하고 이 설계가 **가능하게** 만듭니다. **되돌리면 빨간불**이어야 합니다 |
| **F-0 · F-1 · F-2** | 🟢 **그대로.** 이 설계는 천장 식을 안 지납니다 (F-1 차 0.000000 · F-2 0.583) |
| **C-1~4** | 🟢 **그대로.** A-3·A-4는 **문서만** 바꿉니다 |
| 🔴 **먼저 할 것** | `moment-test.js` **D-1 · D-2a**가 빨간불이면 **먼저 초록불로.** 이미 빨간불인 검사는 새 변이 신호를 먹습니다 |

---

# 14. 🔴 범민 님께 — 한 줄

> **ㄴ으로 갑니다. 하지만 「◎를 양쪽에 켠다」가 아니라 「완벽의 뜻을 고친다」예요.**
>
> 지금 「완벽」은 **잘 눌러서** 나오는 게 아니라 **🦶 주발 쪽으로 열린 판을 뽑아서** 납니다.
> 중립 상태의 천장이 **0.739**인데 문턱이 **0.75**라, **손으로는 못 닿는 자리**에 걸려 있었어요.
> 소스에도 증거가 있습니다 — *"🦶 약발로!"*라는 축하 문구가 있는데 **한 번도 못 떴습니다.**
>
> 고친 뒤에는 **어느 판이든 처음 0.4~0.9초 안에 맞는 칸을 누르면 「완벽」**입니다.
> 🦶 주발 쪽 판은 창이 **0.88초**, 약발 쪽 판은 **0.47초** — 「－25%」가 **처음으로 눈에 보이는 값**이 돼요.
>
> 🔴 **그리고 급한 게 하나 더 있습니다.** 화면이 *"테두리가 있는 칸 중에서 고르세요"*라고
> **두 군데서** 말하는데, **테두리를 그리는 CSS가 한 줄도 없습니다.** 지금 안 보이실 거예요.
