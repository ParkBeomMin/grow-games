# 이 저장소에서 일할 때

빌드·의존성 없는 정적 웹게임 8종. 배포는 GitHub Pages.
배포 흐름(`beta/` → `promote.sh` → `release.sh`)은 `README.md`에 있어요.

---

## ⚠️ 여러 세션이 동시에 작업합니다

**이 저장소는 여러 Claude 세션이 같은 워킹트리·같은 `main`을 씁니다.**
아래는 실제로 사고가 난 뒤에 만든 규칙이에요. 추상적인 주의가 아닙니다.

### 1. `git add`는 반드시 경로를 명시

```bash
git add beta/rookie/ tests/rookie/     # ✅
git add -A                             # ❌
git commit -a                          # ❌
```

**실제 사고**: 한 세션의 커밋이 다른 세션이 편집 중이던 `beta/_check.html` 380줄을
통째로 담아갔어요. 남의 커밋은 다시 쓸 수 없어서 그대로 둘 수밖에 없었습니다.

`git status`에 **내가 안 건드린 파일**이 떠 있으면 다른 세션이 작업 중입니다.
**손대지도, 커밋에 넣지도 마세요.**

### 2. `promote.sh --all` 쓰지 않기

```bash
scripts/promote.sh rookie/ post-stage.js   # ✅ 내가 검증한 것만
scripts/promote.sh --all                   # ❌
```

`--all`은 **검증 안 끝난 남의 베타 작업까지 상용으로 내보냅니다.**
스크립트가 원래 "대상을 반드시 적게" 만들어진 이유예요 —
주석에 *"실제로 한 번 나갔고, 두 번은 손으로 막았습니다"* 라고 적혀 있습니다.

### 3. 게임 단위로 영역을 나눕니다

게임 폴더(`rookie/` · `soccer/` · `idol/` …)는 서로 독립이라 안 부딪혀요.
**문제는 공유 파일입니다.**

| 공유 (조심) | 전용 (안전) |
|---|---|
| `timing.js` · `base.css` · `cloud.js` · `match.js` · `help.js` | `beta/tour-stage.js` (아이돌만) |
| `beta/_check.html` · `beta/_fixtures.js` · `scripts/` | `beta/post-stage.js` (야구만) |

**공유 파일을 건드릴 땐 짧게 끝내세요.** 오래 잡고 있으면 부딪힙니다.

새 기능은 **전용 파일로 빼는 게 최선**이에요. 미니게임 작업이 한 번도 안 부딪힌 게
그 설계 덕분입니다. `timing.js`는 8개 게임이 전부 내려받으니 거기 넣지 마세요.

### 4. 릴리스는 한 번에 한 세션만

`VERSION`·`CHANGELOG.md`가 유일한 진짜 경합 지점입니다.

```bash
git fetch origin && git log --oneline HEAD..origin/main
```

**비어 있지 않으면 남이 뭔가 올린 겁니다.** 배포 전에 확인하고, 배포는 짧게 끝내세요.

### 작업 시작할 때 30초

```bash
git fetch origin
git log --oneline -5        # 누가 뭘 하고 있나
git status --short          # 남이 작업 중인 파일이 있나
```

---

## 검증

테스트 러너가 없고 **CSS는 기계가 못 봅니다.** 자세한 건 옵시디언 `Grow Games/검증 수단`에 있어요.

```bash
node tests/smoke-test.js beta    # 8종이 실제로 로드되는지 (root도 가능)
node tests/check-page-test.js    # 확인 페이지 시나리오 도달
for t in tests/rookie/*.js; do node "$t"; done
```

`tests/rookie/`(5) · `tests/soccer/`(16) · `tests/idol/`(15+) · `tests/cloud/`(12)

### 초록불인데 아무것도 안 지키는 검사 — 여덟 번 넘게 나왔습니다

매번 모양이 달랐어요.

| 형태 | 무슨 일이 |
|---|---|
| 직접 `eval` | `eval("const x = …")`은 선언이 eval 스코프에 갇혀 **값이 항상 `undefined`**. 산식을 뭘로 바꾸든 통과 |
| 문자열 매칭 | 배선이 죽었는데 소스에 낱말이 있으면 통과 |
| 픽스처가 다른 모양 | 실제 디스크 형태와 달라 아무것도 안 건드림 |
| 버그를 정답으로 단언 | 없애기로 한 동작을 검사가 지키고 있었음 |
| 이벤트를 하나만 보냄 | 실기기는 `pointerdown`→`pointerup`→`click` 셋 다 옵니다 |
| 문턱을 코드에서 읽어옴 | 상수를 바꿔도 검사가 따라가서 안 잡힘 |
| 자기 자신과 비교 | 함수 출력을 정답으로 삼아 대조 |

**그래서 지키는 것:**

1. **`new Function(...)` + `return`.** 직접 `eval` 금지
2. **산식은 소스에서 정규식으로 추출.** 값을 복사해 적지 않기
3. **게임 입구를 통해** 실제 버튼을 눌러 도달하기
4. **변이 검증** — 고치기 전에 빨간불이 뜨는지 반드시 확인. 안 잡히면 검사를 고칠 것
5. **실기기 이벤트 순서**를 그대로 보내기

---

## 저장 데이터

**마이그레이션하지 않습니다.** 새 필드는 읽는 쪽에서 기본값을 줘요.

```js
const lg = LEAGUES.find((l) => l.id === ((st && st.league) || 1)) || LEAGUES[0];
```

**리그·티어 같은 `id`는 옛 세이브가 가리키는 값이라 안 바꿉니다.**
순서가 필요하면 `tier` 같은 새 필드를 더하세요 —
축구에서 `id`로 순서를 판단해 사고가 났습니다.

가중 카운터를 더할 땐 **옛 카운터를 이어받으세요.**

```js
// ❌ 옛 세이브가 새 상을 받는 순간 지난 상이 전부 사라져요
S.career.mvpW = (S.career.mvpW || 0) + prestige;
// ✅
S.career.mvpW = (S.career.mvpW != null ? S.career.mvpW : S.career.mvp) + prestige;
```

---

## 그 밖에

- **실제 상표명을 쓰지 않습니다.** 광고가 붙은 서비스라 "비영리 팬 제작물" 항변이 약해요.
  구단·방송·브랜드 이름은 전부 가상입니다 (시장 지수명은 예외)
- **`beta/_*`는 베타 전용.** `promote.sh`가 밑줄로 시작하는 최상위 파일을 승격에서 막아요.
  **GitHub Pages는 Jekyll이 밑줄 파일을 404로 만들어서 `.nojekyll`이 필요합니다**
- 주석·사용자 문구는 **한국어 존댓말(`~해요`)**, 기존 파일 어투를 따릅니다
- 밸런스 수치는 **감이 아니라 몬테카를로 실측**으로 잡습니다.
  바꿀 땐 곡선을 다시 재고 근거를 남기세요

---

## 하네스: 육성 게임 빌드

**목표:** 설계 → 몬테카를로 실측 → 구현 → 검증을 한 흐름으로 끌고 가서,
"초록불인데 아무것도 안 지키는" 상태로 배포되지 않게 합니다.

**트리거:** 게임을 새로 만들거나 시스템을 개편·확장하는 요청에는
`grow-game-build` 스킬을 사용하세요. 단순 버그 수정이나 한 줄 질문은 직접 처리해도 됩니다.

에이전트 5종은 `.claude/agents/`, 스킬 6종은 `.claude/skills/`에 있어요.
`grow-game-build`가 나머지를 조율합니다.

**⚠️ 이 환경에는 `TeamCreate`/`TaskCreate`가 없습니다.** `Agent`(백그라운드 병렬)와
`SendMessage`/`ListAgents`만 있어서, 하네스는 **서브 에이전트 + 파일 기반 전달**로 돕니다.
팀 도구를 전제로 오케스트레이터를 고쳐 쓰면 첫 실행에서 죽어요.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-08-28 | 초기 구성 (에이전트 5 · 스킬 6) | 전체 | ⚽ 더 윙어 v2 준비 |
| 2026-08-28 | 커스텀 에이전트 폴백 규칙 추가 | skills/grow-game-build | 방금 만든 `.claude/agents/`는 잠시 동안 `subagent_type`으로 안 잡힘 (몇 분 뒤 자동 로드 확인) |
| 2026-08-28 | 등록 체크리스트에 게임 키 표·`tour-mech-test` 추가 | skills/grow-new-game | ⚽ 더 윙어 II 신설 중 발견 — `SAVE_KEY`만 바꾸면 `Match.register`·HOF·fever가 상용 데이터를 오염시킴 |
| 2026-08-28 | Phase 3에 경계면 계약 선확정 절차 추가 | skills/grow-game-build | 서브 에이전트끼리는 서로의 `agentId`를 몰라 `SendMessage`가 안 닿음 — 오케스트레이터가 계약과 파일 담당을 미리 나눠야 함 |
| 2026-08-28 | "폐기는 이름이 아니라 형태" 원칙 추가 | agents/grow-engineer | `ACE_W` 폐기 근거를 읽고도 같은 형태의 계단을 다른 이름으로 되살려, 능력치 70 수상률이 0.0%가 됨 |
| 2026-08-28 | "고친 뒤 되돌려 빨간불 확인" 원칙 추가 | agents/grow-engineer | G-3 버그를 되돌려도 검사 4종이 전부 초록불 — 그 영역에 검사가 없었음 |
| 2026-08-28 | 구현자·검증자 동시 실행 금지 | skills/grow-game-build | engineer가 고치는 파일로 inspector가 검사를 돌려 20/20 실패 — 움직이는 표적을 잼 |
| 2026-08-28 | "빨간불인데 그냥 두는 검사" 절 추가 | agents/grow-inspector | 버그와 알려진 설계 한계를 한 검사에 묶으면, 고친 뒤에도 빨간불이라 신호를 잃음 |
| 2026-08-28 | 문턱·기준선 여유 절 추가 | agents/grow-inspector | `fatigue` 문턱 여유가 0.4%까지 좁아져 계수 한 번만 더 움직이면 우연으로 빨간불 |
| 2026-08-28 | "한 벌 계수를 요약에서 줄이지 마라" 추가 | agents/grow-balancer | `NPC_SPOT` 세 값을 "하나를 내리는 것과 같다"로 요약해 분할이 폐기되고 곡선이 무너짐 |
| 2026-08-28 | 종료 코드 3갈래·0번 변이 등록·픽스처 건강 검사 | skills/grow-test-writing | 변이 정규식이 구현 변경에 두 번 죽었는데 "실패 1건"으로만 보임 |
| 2026-08-29 | "시드 하나로 재지 마라" 추가 | skills/grow-test-writing | 한 세션에 두 번 — 시드에 따라 밴드를 넘나들거나, 고정 시드에서만 통과 |
